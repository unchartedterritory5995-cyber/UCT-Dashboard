{
  "_comment": [
    "Scope for the breadth history reader programme. Enforced by tools/git_scope.py.",
    "A branch matching any prefix in 'branches' may only stage paths under 'allow'.",
    "An UNMATCHED branch is not a violation - this tool only enforces scopes that were",
    "written down, because a check that refuses everybody gets bypassed within a day.",
    "Written after a breadth commit used `git add -A` and swept in two joystick docs,",
    "silently normalising another programme's deliberate raw control bytes.",
    "SD-1 (2026-09-15) authorised S1 (.gitattributes) and a second test file, so both are added to 'allow'. Widening the scope is a deliberate, reviewable act - the point of the declaration is that it says what this programme may touch, not that it never changes."
  ],
  "branches": [
    "breadth/",
    "docs/session9-record",
    "docs/session10-record",
    "docs/session11-record",
    "docs/session12-record",
    "repo/git-scope"
  ],
  "allow": [
    ".git-scope/",
    ".gitattributes",
    "api/routers/breadth_monitor.py",
    "api/services/breadth_",
    "docs/breadth-history-reader/",
    "docs/breadth/",
    "docs/feature_flags.json",
    "logs/",
    "tests/test_breadth_",
    "tests/test_git_scope.py",
    "tests/test_gitattributes_eol.py",
    "tools/breadth_",
    "tools/git_scope.py"
  ]
}
