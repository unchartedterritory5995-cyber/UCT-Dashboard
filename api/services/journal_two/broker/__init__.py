"""Broker-sync subpackage (SnapTrade).

Auto-imports brokerage trades, positions, and balances into Journal 2.0.
All SnapTrade-specific surface lives in `snaptrade_client`; everything
else (connections, reconstruction, balances, sync) is provider-agnostic.
"""
