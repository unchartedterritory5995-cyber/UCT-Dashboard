"""Structure detectors.

These surface the invisible scaffolding of the chart - swing pivots,
support/resistance levels, trendlines, stage analysis - rather than
discrete trade setups. They emit a SINGLE Detection per symbol/timeframe
that contains the structural map, used by the chart overlay to render
reference levels and by other detectors as supporting context.
"""
