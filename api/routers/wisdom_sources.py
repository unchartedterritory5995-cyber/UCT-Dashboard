"""Wisdom Loop sources admin routes (stream S-C). Skeleton: no routes yet; every route added must Depends(require_admin)."""
from fastapi import APIRouter

router = APIRouter(prefix="/api/admin/wisdom/sources", tags=["wisdom"])
