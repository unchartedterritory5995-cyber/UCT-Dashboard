"""Wisdom Loop extraction admin routes (stream S-D). Skeleton: no routes yet; every route added must Depends(require_admin)."""
from fastapi import APIRouter

router = APIRouter(prefix="/api/admin/wisdom/extract", tags=["wisdom"])
