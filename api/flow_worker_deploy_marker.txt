# flow-worker deploy marker
#
# Read by NOTHING. Its only purpose is to sit on flow-worker's Railway watch
# list so that appending a line here forces flow-worker to rebuild from
# master's tip and pick up ADDITIVE strands it would otherwise skip.
#
# Why this exists: `railway redeploy --service flow-worker` re-runs the LAST
# ACTUAL deployment, not master's tip, so it cannot discharge a strand. Proved
# 2026-09-12 04:07 UTC: a redeploy came back SUCCESS on the same commit it
# started from, having dropped the OPRA websocket for nothing.
#
# Append one dated line per deploy. Never rewrite history here.

2026-09-12 23:15 CDT - marker created; pending watch-list registration. Strands awaiting discharge: 1667fc64d (S3 admin routes), 1a3668eaf (D1 adapter gaps G2/G4), both ADDITIVE.
2026-09-12 23:27 CDT - marker bump #1. Discharges ADDITIVE strands 1667fc64d (S3 admin routes: entity_master/store.py status_counts) and 1a3668eaf (D1 adapter gaps G2/G4: 12 new fmp_client functions). Both add functions flow-worker never calls. Market closed.
2026-09-12 00:28 CDT (Sat) - marker bump #2. Weekend, market closed. DISCHARGES NOTHING, deliberately: S7 price-level CP1 is ADDITIVE and api/services/alert_taxonomy/price_level.py is NOT in flow-worker's reachable set -- CP1 never calls register(), so nothing imports it. Measured with the rail's own reachable_paths(): of the 29 files master gained since fd735513b, ZERO are reachable-but-unwatched. This bump establishes a KNOWN-CURRENT baseline before CP2, which WILL strand -- api/services/alerts.py (reachable) imports the taxonomy modules, so wiring register() makes price_level.py reachable and unwatched. NOTE ON THE TWO LINES ABOVE: both are stamped 2026-09-12 but were written 2026-09-11 ~23:15/23:27 CDT; the real order is created -> bump #1 -> bump #2. Not corrected in place because this file is APPEND-ONLY by owner rule -- rewriting a past line is exactly what the rule forbids.
2026-09-12 10:30 CDT (Sat) - marker bump #3. Weekend, market closed. DISCHARGES A REAL STRAND, unlike #2. D1 gap G1 (owner-authorized 2026-09-12) touches FOUR files in flow-worker's reachable closure that its watch list does not cover: api/services/fmp_client.py, api/services/earnings_estimates.py, api/services/earnings_history_fmp.py, api/services/fmp_transcripts.py. Measured with the rail's own reachable_paths() -- tools/flow_worker_watch_coverage.py exited 1 and named exactly those four. Without this bump flow-worker would keep running the OLD copies with every test green, which is the entire failure class this marker exists for. WHAT IT CARRIES: a keyword-only timeout parameter on 36 typed fmp_client functions (13 internal defaults preserved as `timeout if timeout is not None else N`), and 19 legacy _fmp_get call sites pinned to 10s EXPLICITLY -- the number they already had, made a decision rather than an accident. Zero intended behaviour change; a timeout regression would show as SLOWNESS, never as an error, which is why it is pinned and railed rather than trusted. CORRECTION TO BUMP #2 ABOVE: it predicted that wiring register() would make price_level.py reachable. It did not -- alerts.py imports receipts and document_arrival BY NAME, not the package, and register() is wired in api/main.py, which is the WEB entry and is not in flow-worker's closure. Measured after CP3 landed. Not corrected in place: this file is APPEND-ONLY by owner rule.
2026-09-12 11:55 CDT (Sat) - marker bump #4. Weekend, market closed. DISCHARGES A REAL STRAND. G1 TRANCHE 1 (owner error-semantics ruling, 2026-09-12) migrates the twelve originally-explicit _fmp_get call sites onto the typed adapter. TWO of them are in flow-worker's reachable closure and unwatched: api/services/industry_map.py and api/services/ticker_meta.py. Measured with the rail's own reachable_paths(); tools/flow_worker_watch_coverage.py exited 1 and named exactly those two. WHY THIS IS NOT COSMETIC: earnings_estimates._fmp_get catches Exception and returns None for EVERYTHING -- network error, 5xx, auth failure, rate limit, malformed JSON. The typed adapter RAISES. So every migrated site changes failure behaviour, and flow-worker running the OLD copy of a module whose new copy raises is exactly the split-brain this marker exists to prevent. Both modules keep their docstring promise of never raising via a local catch, so the contract is unchanged -- but the CODE is, and flow-worker must have it. Also in this push: the S8 provenance envelope (api/services/provider_degraded.py) that the two member-request-path sites attach on a provider failure, and the G5 quarantine entry for news/adapters/fmp_news.py, which clears a standing census red.
2026-09-12 17:13 CDT (Sat) - marker bump #5. Weekend, market closed. DISCHARGES A REAL STRAND. The bar_provenance D/W/M fix touches api/services/bars_disk_cache.py, which is in flow-worker's reachable closure and is NOT on its watch list. Measured with the rail's own reachable_paths()/verdict(), not assumed: tools/flow_worker_watch_coverage.py names exactly that one file, and the same verdict() returns ok=True once this marker rides along. WHAT IT CARRIES: one call site, `bar_provenance.record(ticker, tf, int(bar.get("t") or 0), source)` becomes `_pt = bar_quarantine.norm_bar_time(bar.get("t"))` + a `if _pt is not None:` skip. This is the SECOND instance of the bug 7500777a2 fixed one line below and deliberately left: int() on an ISO daily date raises, the bare except swallows it, and no D/W/M provenance row has ever been written. Key-ADDITIVE - norm_bar_time is identity for ints, so every existing epoch-keyed row keeps its key and nothing is re-keyed. Blast radius is OBSERVABILITY, not correctness: nothing on a member's chart reads bar_provenance. WHY FLOW-WORKER MUST HAVE IT ANYWAY: it runs bars_disk_cache.put(), so a stale copy there would keep writing nothing for daily bars while every test on master is green - the exact split-brain this marker exists for.
2026-09-12 18:08 CDT (Sat) - marker bump #6. Weekend, market closed. DISCHARGES A REAL STRAND. api/services/ticker_meta.py is in flow-worker's reachable closure and is NOT on its watch list; measured with the rail's own reachable_paths()/verdict(), which returns ok=False naming exactly that file and ok=True once this marker rides along. WHAT IT CARRIES: ONE line. `_log.warning(...)` -> `_logger.warning(...)` at ticker_meta.py:145. `_log` is defined nowhere in that module; every other line uses `_logger`. It shipped at 12:01 today in 553f6b68b (D1 G1 tranche 1) and has been raising NameError on master since. It sits INSIDE the except handler that commit added to keep the function's docstring promise "Never raises" - so the handler written to preserve the contract was the thing breaking it. WHY FLOW-WORKER MUST HAVE IT: it runs ticker_meta, and a stale copy keeps destroying the real FMP error on every profile failure while master is green.
2026-09-12 22:30 CDT (Sat) - marker bump #7. Weekend, market closed, no OPRA tape to gap. DISCHARGES A REAL STRAND. api/services/alerts.py is in flow-worker's reachable closure and is NOT on its watch list; measured with the rail's own reachable_paths()/verdict(), which exited 1 naming exactly that one file. THE EDGE, TRACED RATHER THAN ASSUMED: flow_worker -> watchlist_alert_service -> `from api.services.alerts import add_alert, CHANNEL_*`, so flow-worker genuinely runs this module's delivery path. WHAT IT CARRIES: DISCORD_ALERT_WEBHOOK stops being captured at module import. `_DISCORD_WEBHOOK = os.environ.get(...)` at :64 is replaced by DISCORD_WEBHOOK_ENV + discord_webhook(), read at CALL time, at both use sites (the fire decision and the post). WHY FLOW-WORKER MUST HAVE IT, and the direction is the point: BLANKING the webhook is how this estate turns the channel OFF - a kill switch is a variable, never a delete - and against an import-time capture the operator blanks it, reads it back empty, sees --kv agree, and the process keeps posting to a channel everyone believes is silent. A stale copy here leaves flow-worker's kill switch inert while master is green, which is the exact split-brain this marker exists for. Six existing test suites monkeypatched the removed constant and were converted to set the env var - patching that constant was only ever possible BECAUSE it was the defect.
2026-09-13 12:15 CDT (Sun) - marker bump #8. WEEKEND, market closed, no OPRA tape to gap. DISCHARGES A REAL STRAND, and the classification was PRE-DECLARED rather than discovered: GATE-D4 CP3's approval line (fingerprint 40caca541) says BEHAVIOUR-CHANGING in the scope itself, because the measurement was taken the same day it was signed. Both adopters are in flow-worker's reachable closure and neither is watched: api/services/theme_performance.py and api/services/groups.py. Measured again here with the rail's own reachable_paths(), and a TEST asserts the membership so a future reader can see the bump was not guessed. WHAT IT CARRIES: each module gains a PER-ENTITY cache tier under its existing request-set key, which is KEPT as a fast path (SPEC-D4 2.4 rule 2: a set key is fine OVER a per-entity cache and wrong AS the cache). theme_performance's theme_ts_extra:: joined every sym into one key around a loop already iterating per symbol; groups' _TODAY_CACHE did the same thing in a BESPOKE MODULE DICT OUTSIDE TTLCache entirely, with a hand-checked TTL and hand-rolled eviction at >256 entries. WHY FLOW-WORKER MUST HAVE IT: it RUNS both modules, so a stale copy would keep the old single-key behaviour while master is green - the exact split-brain this marker exists for. api/services/cache.py is UNTOUCHED, per D4-D: flow-worker runs it and does not redeploy for it, so a TTLCache API change would be a stop-and-re-gate condition rather than a scope stretch. ONE BEHAVIOUR PRESERVED DELIBERATELY: groups caches only symbols the provider actually answered for. A symbol it silently omits keeps NO row, so the next call retries it instead of inheriting a pinned gap - mutation-proved.
2026-09-19 12:56 CDT (Sat) - marker bump #9. Weekend, market closed, no OPRA tape to gap. DISCHARGES A REAL STRAND, BEHAVIOUR-CHANGING. api/services/alert_taxonomy/indicator_condition.py is in flow-worker's reachable closure (via GATE-S7-INDICATOR-CONDITION CP3's indicator_condition_projection.py, which run_dark_sweep schedules and which imports this module) and is NOT on its watch list; measured with the rail's own reachable_paths()/verdict() against origin/master, which exited 1 naming exactly that one file (indicator_condition_compare.py, the other file this push touches, is not independently reachable -- it is reached only through indicator_condition.py, which already names the strand). WHAT IT CARRIES: registration_refusal/cadence_for now ask D2 CP4's own indicator_axis.declarations() for a metric's rename target (a new _canonical_metric_name() helper, lazily importing indicator_axis so the 2,630-line indicator_alert_evaluator strand stays out of this module's own import closure) before looking the metric up in the canonical address book -- so the metric "close" now resolves through the SAME close -> ohlcv.c rename indicator_condition_projection.comparability() (CP3) already asks, instead of disagreeing with it. WHY THIS IS BEHAVIOUR-CHANGING, NOT ADDITIVE: it changes registration_refusal's RETURN VALUE for the metric "close" specifically (REFUSAL_ADDRESS_UNRESOLVED -> REFUSAL_CADENCE_UNDECLARED), a function flow-worker's already-running dark sweep actually calls via observe()/would_fire() on every tick for the rollout:s7-dark cohort. WHY FLOW-WORKER MUST HAVE IT: a stale copy would keep disagreeing with CP3's comparability() on "close" while master is green, which is the exact split-brain this marker exists for. THE OUTCOME DOES NOT CHANGE for the live sweep's tallied comparison-spans counts (agreed/legacy_only/new_only/not_comparable): classify() only branches on whether the dark side FIRED, and the dark side was already refused before this change (for a different, wrong reason) and is still refused after it (bars-store cadence remains undeclared -- F-D2-2). Only the internal, never-persisted refusal-reason TEXT differs. Signed: docs/terminal-research/12-decisions/gates/s7-indicator-condition-pre-implementation-gate.md, fingerprint 12855d501 (a correctness fix to CP1's already-approved cadence gate, not a new checkpoint).
