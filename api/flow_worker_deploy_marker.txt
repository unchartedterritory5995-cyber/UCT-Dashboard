# flow-worker deploy marker
#
# Read by NOTHING. Its only purpose is to sit on flow-worker's Railway watch
# list so that appending a line here forces flow-worker to rebuild from
# master's tip and pick up ADDITIVE strands it would otherwise skip.
#
# Why this exists: `railway redeploy --service flow-worker` re-runs the LAST
# ACTUAL deployment, not master's tip, so it cannot discharge a strand. Proved
# 2026-09-12 04:07 UTC: a redeploy came back SUCCESS on the same commit it
# started from, having dropped the OPRA websocket for nothing.
#
# Append one dated line per deploy. Never rewrite history here.

2026-09-12 23:15 CDT - marker created; pending watch-list registration. Strands awaiting discharge: 1667fc64d (S3 admin routes), 1a3668eaf (D1 adapter gaps G2/G4), both ADDITIVE.
2026-09-12 23:27 CDT - marker bump #1. Discharges ADDITIVE strands 1667fc64d (S3 admin routes: entity_master/store.py status_counts) and 1a3668eaf (D1 adapter gaps G2/G4: 12 new fmp_client functions). Both add functions flow-worker never calls. Market closed.
2026-09-12 00:28 CDT (Sat) - marker bump #2. Weekend, market closed. DISCHARGES NOTHING, deliberately: S7 price-level CP1 is ADDITIVE and api/services/alert_taxonomy/price_level.py is NOT in flow-worker's reachable set -- CP1 never calls register(), so nothing imports it. Measured with the rail's own reachable_paths(): of the 29 files master gained since fd735513b, ZERO are reachable-but-unwatched. This bump establishes a KNOWN-CURRENT baseline before CP2, which WILL strand -- api/services/alerts.py (reachable) imports the taxonomy modules, so wiring register() makes price_level.py reachable and unwatched. NOTE ON THE TWO LINES ABOVE: both are stamped 2026-09-12 but were written 2026-09-11 ~23:15/23:27 CDT; the real order is created -> bump #1 -> bump #2. Not corrected in place because this file is APPEND-ONLY by owner rule -- rewriting a past line is exactly what the rule forbids.
