D4 CP5 -- ticker_logos.py miss-retry provider-skip, per docs/terminal-research/12-decisions/gates/d4-cp5-ticker-logos-cache-scoped-proposal.md section 4 MUST BUILD only (the packet's own recommendation: sign MUST as CP5, leave the SHOULD item and CP5-B/F-D4-2 as separate, smaller decisions). Entirely inside api/services/ticker_logos.py, zero new files/schema/module: .miss files become JSON ({transient, failed, ts}) instead of a bare string, written at the existing resolve_and_cache write site; _recent_miss parses JSON with a legacy-format fallback so every .miss file already on the production volume keeps behaving exactly as today until naturally re-resolved -- no backfill, no migration script; _fetch_sources/_fetch_sources_with_clearbit gain an optional skip parameter over one new shared, ordered provider-name tuple; the failed list for a non-transient miss is derived statically from Python's own "or" short-circuit semantics (no per-provider return-tracking added anywhere); a transient miss writes failed=[] so a retry after a hiccup still re-tries everything, unchanged from today; run_miss_retry's _retry_one passes skip=failed into the extended fetch chain, and Clearbit -- never a member of the base chain's failed list -- is always still attempted, so a retry never degenerates into a no-op; on a resolved hit, a sibling {SYM}.source plain-text file records which provider won, requiring the three named call sites (resolve_and_cache, _retry_one, _upgrade_one) to unpack a (bytes, name) tuple instead of a bare bytes value. EXPLICITLY DEFERRED, NOT AUTHORIZED BY THIS LINE: the SHOULD item (folding .source/.miss breakdown into GET /api/logos/status); the in-memory addressed-tier cache.py entry that SPEC-D4/GATE-D4's own CP5 row originally named (filed as F-D4-2, a distinct, larger, differently-motivated piece of work, not silently substituted by this line); any change to _MISS_TTL/_MISS_TRANSIENT_TTL, _DOMAIN_OVERRIDES, provider priority, or which providers exist; backfilling .source for tickers already cached before this ships. No member-visible change. No schema change. Revertible by reverting one commit.
