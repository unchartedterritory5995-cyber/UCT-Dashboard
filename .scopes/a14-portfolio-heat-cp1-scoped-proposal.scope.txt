A14 CP1 -- a real member door on the already-shipped portfolio_heat.py engine, per docs/terminal-research/12-decisions/gates/a14-portfolio-heat-cp1-scoped-proposal.md section 2. MUST BUILD: one new read-only route, GET /api/portfolio/heat, calling portfolio_heat.portfolio_heat(user_id, account_id=account_id, account_size=account_size) exactly as the two existing assistant-tool callers (voice_tool_impls.py, coach_chat_tools.py) already do -- no new parameters, no change to the function's signature or return shape -- gated with the existing require_paid dependency, the same idiom ai_search.py/analyst.py/backtest.py already use; one new page rendering the response's own existing fields as-is (risk_heat_pct against caps.aggregate_pct, notional_exposure_pct, the per_position table including the placeholder_stop flag rendered visibly not hidden, by_sector/concentration_flags, room_to_add_pct) -- no new computation, no new field invented; one nav entry added the normal way, not added to FREE_PAGES. SHOULD BUILD: none, matching the house convention (S5 CP1, S6 CP1, A12 CP1) that a first checkpoint on a surface with no PRD is a thin door, not a feature bundle. EXPLICITLY DEFERRED, NOT AUTHORIZED BY THIS LINE: any corp-actions event data (splits/dividends/M&A/spinoffs/etc) on this page -- that is DEC-08's separate, still-unscoped work, gated on F-09's own open provider gap, not this checkpoint's; any change to portfolio_heat.py itself, which this checkpoint only reads; a dedicated A14 PRD/spec; historical/trend views of portfolio heat over time. No schema change. No new computation.
