{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && cd app && npm install && NODE_OPTIONS='--max-old-space-size=4096' npm run build"
  },
  "deploy": {
    "startCommand": "if [ \"${WORKER_ENABLED:-0}\" = \"1\" ]; then python -m api.worker_main; else uvicorn api.main:app --host 0.0.0.0 --port $PORT; fi",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 600
  }
}
