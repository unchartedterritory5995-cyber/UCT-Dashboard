TERMINAL-NEXT — SESSION PROMPT — DELEGATED SIGNING, CI ON THE REPLAY TIP FIRST, FOUR SITTINGS RUN AUTONOMOUSLY, STOP ONLY ON A BLOCKER

Date from python tools/weekly_exec.py et only.

State: pre_sitting READY; freeze 80/80; 51 rows, 46/46 mapped, replay CLEAN 46 of 46, 0 STALE, 51 UNSIGNED. Code feat/s7-price-level e703af0a8 frozen. F-CI-43 OPEN: the E workflow's push.branches is [feat/s7-price-level]; master pushes run no tests; merge_all pushes master directly. F-CI-42 OPEN (Notebook; expected green once master == HEAD). origin/master has moved under the replay twice (9906a7fcd → 9081799f2); replay re-clones and stayed CLEAN. Runbook has both shell forms; freeze_check is the first line of every sitting.

OWNER RULINGS — in force for this session
- R-DELEGATE: The owner delegates signing and merging to this session. sign_all runs with --by "Patrick (owner; delegated to the running Claude Code session, 2026-09-17)" and the scope each row's checkpoint(s) as already derived. The delegation is recorded in SIGNING_SESSION.md under "Delegation" with this prompt's git hash-object (save the prompt text to docs/terminal-research/prompts/2026-09-17-delegation.md first, commit, then hash) so the authority is a file, not a memory. No check-ins: the session decides and executes end to end and stops only on a hard stop below.
- R-CI-FIRST: Before Sitting 1, close F-CI-43's test gap for this merge: unfreeze for exactly one appended row (E CP34), re-freeze, and run CI on the replay tip. Sittings start only on the verdict rule in C.4.
- R-STOP: hard stops, each ends the session with the stop printed and nothing overridden: sitting_verify BLOCKER; guard REFUSE not resolved by one full re-wait on the last merged hash; a STRAND; a deploy non-SUCCESS on a merged unit; sign_all STALE on a row whose change is not docs-side-and-explained; freeze_check DIFF; replay-preview verdict other than the C.4 rule; any error whose fix would touch a signed row or force-push. On a stop: no revert is executed (a revert is a new signable row; it is drafted and left unsigned), the report is written, STATUS reflects it.
- R-NO-BLOCKS: no time-of-day holds. Sittings run back-to-back at whatever ET it is, with sitting_verify between them.
- R-PACE: sittings run in sequence, never in parallel (one master merge at a time, the guard serializes anyway); everything in section C that can run in parallel with waiting (docs prep, the post-merge premise audit) does.

STANDING RULES — all prior rules verbatim; two additions
- Delegated authority is a file with a hash. A session signs only under a delegation recorded in the docs worktree naming the delegating prompt's hash-object, the date, and the by-line; sign_all refuses (exit 5) if the runbook's Delegation block is absent or its hash does not match a committed prompt file. Control required before the first real sign.
- The tree that will be deployed is the tree that was tested. Before a batch of units merges to master, CI runs on the exact cherry-pick result (the replay tip) against current origin/master; a master that moves after that run is re-tested only if the replay strands or sitting_verify reports a mismatch; otherwise the pre-merge run stands and is cited by run_id in each sitting's ledger row.
- Unchanged: signing freeze is a printed path set checked first; a sitting's result is derived from master and the deploy source; a dry-run is a replay; every validator in the pre-sitting block; commits spanning units split by file; the commit universe is git's; exit codes from the process; row commit lists derived; merged-state by git cherry; merge_all only from the master checkout; MISSING attributed or COVERAGE_LOST; scope derived and non-blank; job-graph needs-closure; checks advisory while merge_all pushes master; flakiness measured; stale references proven absent; CI baseline-relative; inventory derived; one-liners strip comments; patch files with validators; consumption order proven; "latest" derived; job outcome from the runner; predict then score; split not extend; no derived numbers in assertions; three-state readers; premise before build, correct the prompt when the repo contradicts it; every commit mapped; reports to reports/SESSION_REPORT_<date>_<seq>.md; manifest derived never typed; CI read from the repo; never run the full suite locally; one unit one commit; gate-box lock printed before any local vitest; no persistent watchers, bounded polls with ET per poll; telemetry told-vs-found; FROZEN rule; scope enumeration before obedience; rails that cannot fire are refused; retractions propagate; dotted-form matching; stale tests rewrite-or-delete; gate lines cite packet path + section + ID or PROPOSED with collision proof; fingerprint = git hash-object with SHA blank; never re-sign; mutations by EDIT with both diffs, restore in finally, cp1252-safe prints, no `\U` in raw Windows paths inside Python literals; derive never type; CODE NEVER PROSE; UNREADABLE is a third state; non-vacuity; instrument controls before the repo; artifact not text; behaviour over shape; bash form: plain /c/… paths, never MSYS_NO_PATHCONV=1; cmd form: `set` on its own line; ET from the authority; owner on mobile → the report's phone-readable lines say where to watch (admin Discord, Railway deploys), never what to type.

============================================================
D — DELEGATION RECORD (first; ~10 min)
============================================================
D.1 Save this prompt verbatim to docs/terminal-research/prompts/2026-09-17-delegation.md; commit; `git hash-object` it; write the Delegation block into SIGNING_SESSION.md (prompt path, hash, date, by-line). Commit. Both are docs-worktree files outside the freeze set (state that; if SIGNING_SESSION.md is in the freeze set, append the block and re-record its SHA in the same commit with the reason).
D.2 sign_all: add the refusal (exit 5) when the Delegation block is absent or its hash does not match the committed file's hash-object. This edits a frozen tool: do it together with C.1's unfreeze, re-freeze once after both. Controls: block present + matching → proceeds (dry-run); block absent (fixture copy of the runbook) → exit 5; hash mismatched → exit 5. Paste.

============================================================
C — CI ON THE REPLAY TIP (F-CI-43; E CP34; ~60–90 min including the run)
============================================================
C.1 Unfreeze: print the freeze set's current state (80/80), then edit the E workflow: push.branches adds master and replay-preview (patch file; yaml.safe_load; actionlint if present). Collision proof E CP34. One commit on feat; row 52 appended after the last current row; merges-after E CP33 or the last E row (derive). verify_manifest, --check-commits 47/47, drift, replay CLEAN 47 of 47 (universe grew by one). Re-freeze: re-record every SHA (81 paths or 80 + tip); freeze_check 0.
C.2 Replay tip → branch: run the replay onto a throwaway from current origin/master, keep the tip, push it as origin/replay-preview (--force allowed on this preview branch only; state that no manifest row or worktree references it). Print its SHA and `git log --oneline origin/master..replay-preview | wc -l` = 47.
C.3 Predict before the run: verdict NEW_FAILURES with NEW = [F-CI-42's node id] exactly (it scans HEAD..origin/master and replay-preview is ahead of master), FIXED 0, MISSING 0, FLAKY_SIZE ≤ 7, all shards success, collected ≈ 44,353. Bounded poll ≤ 40 min, ET per poll. Score line by line.
C.4 Verdict rule: proceed to sittings if NO_NEW_FAILURES, or NEW_FAILURES whose NEW list is exactly F-CI-42's single id. Any other NEW, COVERAGE_LOST, INVALID, DID_NOT_RECONCILE → hard stop with the NEW list; draft (do not build) the fixing row. Record the run_id as PRE-MERGE-RUN in the runbook; every sitting's ledger rows cite it.
C.5 In parallel with the poll (docs only): the post-merge premise audit refresh (D5 CP3–CP7, S6 CP2–CP5, S3 /status, bell panel, COMPLETION_AUDIT recount) against origin/master's current tree — nouns re-resolved, BUILDABLE / NEEDS-REWORD / UNBUILDABLE, dependency map, written to POST_MERGE_QUEUE.md.

============================================================
S — THE FOUR SITTINGS (sequential; no check-ins; stop only on R-STOP)
============================================================
S.0 From the master checkout path (bash form): `git -C "$MERGE" checkout -B merge-run origin/master`. Confirm HEAD == origin/master and no unmerged unit commit in that worktree (merge_all's own refusal path would catch it; print its check).
S.1 Sitting 1 (--until e-cp9-build-record): freeze_check → pre_sitting → sign_all (delegated by-line) → merge_all. Capture each tool's exit from the process. Per unit, merge_all's own lines are the log; the session adds nothing but the ET at start and end. Then sitting_verify --until e-cp9-build-record: CLEAN required (signed == merged for rows 1..until by git cherry; rows after untouched; last deploy SUCCESS ≥150 s on the last merged hash; guard's last decision line). Ledger rows for each merged unit written per the house format, citing PRE-MERGE-RUN; RESUME updated; commit docs.
S.2 Sitting 2 (--until e-cp22-build-record): same sequence; freeze_check first (nothing should have moved; if the docs commit in S.1 touched a frozen path — packets' ledger sections? — the freeze set must have excluded ledger/RESUME; verify that before S.1 and print).
S.3 Sitting 3 (--until d3-cp2-build-record): same.
S.4 Sitting 4 (--include-member-visible, no --until): same. F-S2-1 lands: after its deploy settles, one read-only member-surface check from the pod (the flag-key handler's presence on the three screens by grep with control; no member session, no login). Then T CP2's tests-only row. sitting_verify with no --until: all 52 rows SIGNED and MERGED; master == the replay tip content (git diff replay-preview origin/master limited to the 47 commits' paths — expect empty; other workstreams' drift excluded and listed).
S.5 After Sitting 4: E CP34 is now on master, so CI triggers on master pushes. Predict the first master run (verdict NO_NEW_FAILURES; F-CI-42 green because master == its HEAD; FIXED = [F-CI-42's id]); it fires on the Sitting 4 push itself — bounded poll ≤ 40 min; score. Re-anchor BASELINE_RUN_ID to that run as the first post-merge row (E CP35, PROPOSED and, since the freeze is over, BUILT as row 53 on a fresh cycle: commit, row, verify, replay CLEAN 1 of 1, sign under the same delegation, merge alone, sitting_verify). Then stop building; the branch is caught up.

============================================================
FINAL REPORT — reports/SESSION_REPORT_<date>_<seq>.md (committed) AND appended to the briefing. Terminal print: sections 1, 7, 9, 10, 11, 12 plus pointer lines. All twelve in the file; NONE where empty.
============================================================
1. ET start/end; trees; freeze states at each checkpoint (after C.1 re-freeze, before each sitting); poll log.
2. D: prompt file path + hash; Delegation block; sign_all refusal controls.
3. C: E CP34 patch summary + validators; row 52; replay CLEAN 47/47; replay-preview SHA; prediction/score; verdict and the C.4 decision; PRE-MERGE-RUN id.
4. S: per sitting — ET start/end, minutes, units merged (count and first/last row), sitting_verify result, deploy hashes and settled seconds, any guard re-wait; ledger row count written; Sitting 4's member-surface check; final sitting_verify; the master-vs-replay diff (expect empty on the 47 commits' paths).
5. S.5: first master CI run prediction/score; E CP35 built/merged or the stop.
6. Post-merge premise audit (C.5): counts and the BUILDABLE list with dependencies.
7. Findings filed / closed: ID | one line (F-CI-43 CLOSED, F-CI-42 CLOSED expected; anything the sittings surfaced).
8. OPEN QUESTIONS, one line each.
9. Owner-readable summary (no commands): what is now on master, what changed for members (F-S2-1 only), where to watch (admin Discord report times in ET, Railway deploy list), and whether anything stopped and why.
10. Merge readiness after the run: rows SIGNED/MERGED counts equal; universe 0 (feat == master content); freeze lifted with the date; pre_sitting state.
11. Three phone-readable sentences.
12. Last line only: STATUS: RAN | STOPPED-ENV | STOPPED-NOTHING-READY | STOPPED-ERROR
