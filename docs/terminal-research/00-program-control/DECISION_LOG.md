# DECISION LOG

Every important decision with rationale; every change made under extreme ownership; every recorded instruction conflict (Document B §0, §5; C Part CLXVII). Newest at the bottom.

| ID | Date (UTC) | Decision | Rationale / evidence | Reversibility |
|---|---|---|---|---|
| DL-001 | 2026-09-02 05:39 | Step Zero VERIFIED rather than re-created. The pasted execution command said to create the worktree fresh from current origin/master; the on-disk `06-execution-command.md` (owner-approved 9/1, applied 9/2) records that the worktree already exists at `9c3df14b9` with the charter committed at `a4ef6f240`. After `git fetch origin`, origin/master is still `9c3df14b9`, so the existing worktree is exactly "a fresh worktree from current origin/master". | Recreating would have destroyed an identical, already-pushed state. Instruction conflict resolved toward the on-disk (approved) text; both agree on branch, base, and SHA. | n/a |
| DL-002 | 2026-09-02 05:50 | Protection-rail definitions fixed for the program: "application source paths" = everything except `docs/terminal-research/`; check (2) = the named vitest and pytest command sets; check (3) = HTTP liveness (`/api/health` 200 ok, `/calendar` 200 with SPA root) plus an authenticated browser assertion of header "UCT Terminal" + week strip + roster line. The browser assertion was refined after the first run showed the roster is filter-dependent ("0 reporting · 145 hidden" under the owner's saved scope), so "at least one earnings row" is NOT a valid assertion. | Document B §14A requires the exact commands and assertion to be named on Day 1a. | rail can be tightened later; never loosened |
| DL-003 | 2026-09-02 05:55 | Model classes: Opus 5 for leaf-depth (internal archaeology, licensing, Bloomberg, dossier authors, domain pods); Sonnet 5 for leaf-breadth (verifiers, workflow reconstructors, evidence collectors); Fable 5.1 for synthesis, council, red team, planning, architecture proposals; Haiku 4.5 for probes/mechanical tasks. | OWNER_SEED_FACTS §5: strongest available for synthesis/council/red team; a faster class allowed for leaf research. Probe confirmed all four classes run as delegated agents. | per-wave |
| DL-004 | 2026-09-02 05:58 | Role map changes under extreme ownership (Part X, CLXXXI): added B-VAL-01 (benchmark universe validator) so validation is an explicit task, not an assumption; added ARCH-01..08 as Day 4 competing-proposal authors (three target architectures A/B/C run as separate contracts so the fixed/modular/hybrid comparison has independent inputs); split F-03 into F-03a (system map + capability ledger + tech debt) and F-03b (provider ledger) because they have different inputs; pod-synthesis tasks per product named B-POD-<P>; E-02 deferred to Wave 1b because it consumes E-01's evidence. Coverage map remains complete (every Document C part and appendix line has an owning role in `AGENT_REGISTRY.md` §3). | Fewer overlapping roles, explicit validation, independent architecture inputs. | any time |
| DL-005 | 2026-09-02 06:00 | Wave 1 dispatch at measured concurrency: batch 1 = D-01..D-10 (10 tasks); batch 2 = D-11..D-14, E-01, E-03, E-04 (7) dispatched as batch-1 tasks complete, keeping ≤10 in flight. | Capability probe measured 10 concurrent tasks completing cleanly; Document A "recycle agent capacity as assignments finish". | per-wave |
| DL-006 | 2026-09-02 06:00 | Contracts are persisted at `00-program-control/contracts/<ID>.md` with a shared preamble; the dispatch prompt points at both files and repeats the SOURCE HANDLING / SECRETS / DO NOT clauses verbatim. | Survives compaction and usage pauses; allows exact re-dispatch; keeps the orchestrator's context small (B §35, §36, §37). | n/a |
| DL-007 | 2026-09-02 06:00 | Evidence index is generated by `00-program-control/scripts/build_evidence_index.py` from research-file frontmatter (Part CCLVI); the script lives under the research tree, not under the application's `tools/`, so the protection rail stays clean. | B §5, §14A. | n/a |
| DL-008 | 2026-09-02 06:20 | Wave 1 batch 2 (D-11..D-14, E-01, E-03, E-04) dispatched while batch 1 is still running: 17 tasks in flight as a MEASURED STEP-UP from the probe's 10. If any dispatch fails or the seat pauses, the working ceiling returns to 10 with top-up on completion; contracts on disk make re-dispatch exact. | Deadline compression (Document A: parallelism is the primary mechanism); the probe measured 10 as safe but not as a limit; Wave 1b (28 tasks) would otherwise compete with batch 2 for slots after approval. | per-wave |
| DL-009 | 2026-09-02 06:50 | Protection rail TIGHTENED after D-07: the frontend check names all 31 test files explicitly, pre-checks their existence, and asserts `Test Files 31 passed (31)`; the backend check adds `test_econ_calendar_fmp.py`, `test_ipo_calendar.py`, `test_market_calendar_router.py` (baseline 374 passed). | D-07 showed vitest positional args are substring filters, so a rename or deletion shrinks the rail silently (a gate that cannot fail); the backend set missed suites that feed Terminal-Current. Rails may tighten, never loosen. | tighten only |
| DL-010 | 2026-09-02 06:50 | INSTRUCTION CONFLICT recorded: OWNER_SEED_FACTS §6 says the paid tier's paywalled item is the Morning Wire; the repository says the opposite — `app/src/components/AuthGuard.jsx:112` `FREE_PAGES = ['/morning-wire']` (mirrored in `NavBar.jsx:39`, `MoreSheet.jsx:70`), i.e. the Morning Wire is the ONLY free page and every other route is paid-gated, with server-side `require_paid` dependencies in 47 backend files (D-10). Per Document B §2 the repository wins for current-system facts; the program proceeds on the code reading and asks the owner to confirm the intended commercial model (OI-12). GOVERNING_PRINCIPLES §13 updated. | Level 1 seed fact vs actual code; the audit rule says a seed fact marked VERIFY is confirmed or corrected during discovery. | owner may overrule the intent, not the code fact |
| DL-011 | 2026-09-02 07:10 | MASTER DRIFT POLICY: `origin/master` moved from the start SHA `9c3df14b9` to `c9ae85fb6` (six commits, 11 files: buzz digest board, indicator-endzone manifest, a runbook, a formula doc) via another session at ~06:04 UTC. The research branch is NOT rebased mid-wave: leaf citations carry line numbers at `9c3df14b9`, and the rail proof compares against the recorded start SHA. Drift is logged in `protection-rail.md`; a re-sync (merge of master INTO `terminal-research`, never the reverse) is considered only at a program-day boundary, logged, with citations re-verified for touched files. | Archaeology accuracy and rail stability outrank freshness during discovery; the touched files are outside Terminal-Current. | day boundary |
| DL-012 | 2026-09-02 07:10 | Orchestrator-only production reads: `railway status --json` and `railway variables --service <svc> --json` from the linked CLI directory (output filtered to key NAMES plus values of flag-shaped keys, secret-shaped names excluded), one authenticated browser GET each of `/api/admin/api-health` and `/api/admin/provider-coverage`, and one HEAD of `/api/flow/data` for `cf-cache-status`. All read-only; nothing run on the pod. Recorded in `02-data-providers/railway-flag-state.md`. Delegated agents remain forbidden from these reads. | Converts KEY-PRESENT/CODE-REFERENCED rows toward OBSERVED-CALLED cheaply (D-03 request); closes D-04's link gap and the D-09/D-10/D-12 flag-state questions. | n/a |
| DL-013 | 2026-09-02 07:30 | D-13's request for a read-only production COUNT sweep over the Railway volume is REFUSED for this program (B §14A: nothing runs on the production pod or against its volume). Production volumes stay 🔴 and are recorded as bounded unknowns with the exact queries the owner could run in a normal operations session. | The rail is absolute; six asset classes' volumes are useful but not decision-critical for Phase Zero. | owner may run the counts themselves |
| DL-014 | 2026-09-02 10:41 | USAGE-LIMIT PAUSE observed: at ~08:05 UTC the seat's session limit (reset 04:20 America/Chicago) terminated E-02, F-03a, F-03b before they wrote (one nested sub-agent spawned by a leaf was also killed). All three were re-dispatched verbatim from their contracts at 10:41 UTC with an instruction to write the core artifact first; the two partial files they left are overwritten by the re-runs. Contracts now say "do not spawn sub-agents" (a leaf spawning its own agents doubles concurrency invisibly). | Document B §8: pauses are expected; contracts on disk make re-dispatch exact. | n/a |
| DL-015 | 2026-09-02 10:41 | OWNER PROCEED received ("Proceed now that the rate limit is reset for the session") and treated as the Day 1b proceed instruction per the execution command and RESUME.md. External research begins. Wave 1b runs in batches of ten (three synthesis tasks count toward the ten), topped up on completion, to stay inside the measured concurrency and the seat's window. | Owner instruction; Document A Day 1b. | n/a |
| DL-016 | 2026-09-02 10:45 | External contracts are organized as one shared `_EXTERNAL_PREAMBLE.md` plus family files (`B-BBG.md` with eight role sections, `B-DOSSIER.md` with a template and eleven product appendices, `B-DESK.md`, `C-WAVE1B.md`) rather than 28 separate files; a dispatch names the family file and the role/appendix ID. | Keeps the evidence standard identical across roles and the orchestrator's context small; re-dispatch stays exact. | n/a |
| DL-017 | 2026-09-02 11:20 | BENCHMARK UNIVERSE VALIDATED (extreme ownership, Part VII/X): accept B-VAL-01's recommendation. Deep: Bloomberg (8 workflow roles), Gödel, Unusual Whales (ADDED: the candidate list had zero options-native products for an equities-and-options desk). Standard: TradingView, Koyfin, Benzinga Pro, AlphaSense, Fiscal.ai (FinChat renamed), Quartr, FactSet, LSEG Workspace (Eikon sunset 2025-06-30), SpotGamma (ADDED). Light merged note: TIKR + YCharts + S&P Capital IQ Pro (redundant with Koyfin/Fiscal.ai/FactSet). Desk tools: thinkorswim, TradingView-as-used, Finviz, Market Chameleon (fourth slot, provisional pending OI-19). Fey excluded (acquired by Wealthsimple, closed). Coverage map stays complete: every original candidate is covered at deep, standard, or light depth. | `03-competitive-research/benchmark-universe.md` (71 cited URLs); Document C Part VII asks for 10–12 validated products plus desk tools. | any time |
| DL-018 | 2026-09-02 11:45 | SEARCH-BUDGET WORKAROUND: the session's shared `WebSearch` cap (200) was exhausted by the Bloomberg roles. `_EXTERNAL_PREAMBLE.md` now orders channels: WebFetch on known documentation URLs, then browser search in one tab (Google/Bing in the owner's Chrome, read-only, closed afterwards), then Bing via WebFetch with quoted product names. Practitioner-tier sources (Reddit, WSO) are largely unreachable (UA blocks, 403, paywalls), so the practitioner tier will be thin across dossiers and must be named as a ceiling, not inferred. | Document A: replace a stalled channel, do not stall the wave; Part XII ceilings. | session-scoped |
| DL-019 | 2026-09-02 16:06 | SECOND USAGE-LIMIT PAUSE (~14:30 UTC, reset 10:40 America/Chicago) killed eleven in-flight tasks. Five had written files: B-SG-01, B-LSEG-01, C4-01 complete (accepted after QC); B-POD-BBG and F-04 partial (no GAPS/SOURCES) — re-dispatched as COMPLETION runs that read the partial file, keep what is sound, and finish the remaining sections. Six left nothing (C5-01, C7-01, F-06, F-08, E-05, B-GDL-02) and are re-dispatched verbatim. Owner said proceed at 16:05 UTC. Concurrency stays ≤10; throughput per seat window is the binding cap, so pauses will recur roughly every four hours of full-parallel work; RESUME.md remains the continuation mechanism. | Document B §8; Document A (replace stalled agents, keep the wave moving). | n/a |
| DL-020 | 2026-09-02 (recovery checkpoint) | THIRD USAGE-LIMIT PAUSE and COMPUTE-TIER POLICY adopted on owner instruction. Three session-limit pauses have now occurred this session (resets observed at 4:20am, 10:40am, and 4pm America/Chicago — each a fresh ~5-hour rolling window, not calendar-aligned), each after roughly 2-4 hours of sustained ~10-parallel Opus/Fable-class dispatch. Going forward, delegated-agent model class follows a four-tier policy: **Tier 1 (routine/mechanical)** — Sonnet 5 Medium/High: repository archaeology, file inventory, provider mapping, evidence gathering, table/ledger/register maintenance. **Tier 2 (important analysis, the DEFAULT for most of the program)** — Sonnet 5 High: dossier synthesis, workflow comparison, domain synthesis, gap analysis, cross-product matrices. **Tier 3 (high-consequence reasoning)** — Fable 5.1 High or Opus 5 High: product-strategy synthesis, consequential architecture decisions, licensing/economic interpretation, Executive Product Council review — used only when stronger reasoning has a realistic chance of changing the decision, not by default. **Tier 4 (exceptional/maximum-quality)** — Fable 5.1 xhigh: final adversarial red team, final architecture challenge, final master-plan synthesis — not a default operating mode. Wave sizing now weighs decision value, dependency, and overlapping evidence, not just measured concurrency; premium-model (Opus/Fable) concurrency is deliberately limited relative to Sonnet. | Owner instruction (compute-budget reset); Document A's parallelism principle is preserved, but "maximum safe parallelism" is redefined as useful work per usage window, not raw concurrency. Every prior finding in this program was produced correctly; nothing is retracted by this policy change. | governs future waves; does not reopen accepted artifacts |
| DL-021 | 2026-09-02 (recovery checkpoint) | LICENSING FACT-CHECK before escalating OI-03: searched all five UCT repositories for Massive plan-tier and FMP-agreement evidence before asking the owner, per instruction to establish independently-discoverable facts first. FOUND: five independent code comments (`polygon_extras.py`, `polygon_options.py`, `polygon_news.py`, `voice_tool_impls.py`, `voice_agents.py`) consistently name "Polygon Advanced, $200/mo" as UCT's Massive plan -- Polygon's legacy individual-tier naming. NOT FOUND: any contract, invoice, order form, or DDLA file for Massive or FMP anywhere in any repository. Conclusion: OI-03(a) (Massive tier) is now a high-confidence CLAIM, not owner-only-knowledge -- recommend the owner CONFIRM rather than supply from scratch. OI-03(b) (FMP DDLA) remains genuinely owner-only (a legal/billing fact the codebase cannot express). Neither blocks any research; the licensing register's default scenario already matches the evidence found. | Owner instruction: establish discoverable facts before asking; do not block unrelated work. | n/a |
| DL-022 | 2026-09-02 (mid-recovery-wave) | GOVERNING PROVIDER-DISCOVERY REQUIREMENT adopted on owner instruction, via /btw: the provider/data audit must be exhaustive across the full UCT ecosystem, not centered on Massive/FMP. A new canonical artifact, the PROVIDER MASTER LEDGER (role F-09, superseding `provider-ledger.md` as gate item 4's home), will restructure the existing 48-row ledger (F-03b, accepted) into a full per-asset-class capability matrix per provider with the owner's A-G status taxonomy, and will explicitly separate technical access from contractual redistribution/storage/derivation/AI-processing rights at the per-capability level. F-03b's existing ledger, D-03's and D-14's archaeology, and the orchestrator's Railway variable-name read (ORCH-RAILWAY-01) are F-09's primary INPUTS, not superseded raw evidence -- F-09 restructures and extends, it does not re-discover from zero. Contract drafted (`contracts/F-09.md`); scheduled as the first item of the wave immediately following the current six-worker recovery wave and Day-1 synthesis, per the owner's explicit instruction not to stop the in-flight wave. | Owner instruction (/btw): do not purchase or architect around a new vendor without first confirming the existing stack cannot already serve the need; the B/C-vs-G distinction is the mechanism. | governs all future vendor recommendations |
| DL-023 | 2026-09-02 (Day 1 close) | RESEARCH-TO-EXECUTION READINESS REVIEW conducted on owner instruction as a formal checkpoint before authorizing further research or any implementation. Repository/workflow integrity verified from source-of-truth (clean, all pushed, zero blocking items found across a full RESEARCH_GAPS/CRITICAL_PATH/OPEN_QUESTIONS review); the Day 1 synthesis independently audited for genuine integration (confirmed) and fact-checked against source files (confirmed high reliability, 2 drifts corrected); a capability-to-infrastructure map found roughly two-thirds of needed terminal capabilities already substantially supported by existing UCT infrastructure; a 23-system proposed architecture and a 9-item architectural decision register were produced. Final recommendation: CONDITIONAL GO. Artifact: `00-program-control/READINESS_REVIEW_DAY1.md`. | Owner instruction: a formal checkpoint before Phase 2, not an ad hoc go-ahead. | governs the Phase 2 authorization that followed (DL-024) |
| DL-024 | 2026-09-02 (Phase 2 close) | PHASE 2 (ARCHITECTURE/DESIGN, NOT IMPLEMENTATION) authorized by the owner as a CONDITIONAL GO, explicitly scoped to architecture/design work with owner-bound decisions kept open. Three workstreams (Product Architecture, Information/Interaction Architecture, Provider/Data Architecture) plus F-09 (exactly as scoped under DL-022, not expanded) ran in parallel, followed by F-09 integration into the data architecture, a capability-to-infrastructure matrix, and an independent adversarial validation pass checking nine specific failure modes (unsupported assumptions, contradictions, duplicated responsibilities, missing boundaries, unsupported provider claims, accidental Bloomberg/Gödel cloning, silently-decided owner items, assumed-nonexistent infrastructure, unnecessary rebuilds). The validator found six genuine issues (1 high, 3 medium, 2 low severity); all six were corrected directly by the orchestrator, including one case (a Gödel `TREND`/strikethrough citation flagged as unsupported) where investigation found the underlying claim was true and sourced to an accepted VERIFIED-tier leaf file, just mis-cited -- the citation was corrected rather than the true claim deleted. Four of the original nine architectural decisions (D3 Entity Master, D4 Provider Abstraction, D6 Provenance Component, D7 Alert Taxonomy) are now LOCKED with no counter-evidence found; two remain genuinely owner-bound (D5 licensing, D9/D-003 decisiveness-for-two-audiences, newly escalated to `OWNER_DECISIONS.md`); one new open technical question surfaced (D13, which of UCT's two existing regime classifiers is the authority for Breadth/Regime). Final recommendation: CONDITIONAL GO for Phase 3 (technical validation + PRD/technical specification), explicitly NOT for implementation. Artifacts: `05-product-strategy/product-architecture.md`, `06-ux-and-information-architecture/information-architecture.md`, `07-technical-architecture/data-architecture.md`, `05-product-strategy/capability-infrastructure-matrix.md`, `02-data-providers/provider-master-ledger.md` (F-09), `12-decisions/ARCHITECTURAL_DECISION_REGISTER.md`, `13-executive-synthesis/PHASE_2_INTEGRATION_SYNTHESIS.md`. | Owner instruction: architecture/design authorization only, explicit implementation gate before Phase 3. | governs Phase 3 scoping; no application code touched |
| DL-025 | 2026-09-02 (Phase 3 close) | PHASE 3 (technical validation + PRD/technical specification, NOT implementation) authorized by the owner explicitly re-anchored to the program's north-star objective (a differentiated UCT Terminal combining the strongest competitor ideas with UCT's existing product/data/AI estate -- not a Bloomberg clone) and an explicit anti-drift rule. Two technical-discovery tasks ran first: D13 (regime-classifier authority) resolved -- `voice_regime_classifier.get_current_regime()` is the single live authority; the engine's `market_regimes` table has one dashboard reader with zero frontend callers (dead code); a genuine, currently-shipping, previously-unknown bug was found and reported outside program scope (RG-32: Compass can show two different "regime" words in one conversation, since `journal_two/regime.py` is an unrelated exposure-tier bucketing under the same name) -- and three narrow RESEARCH_GAPS items (RG-16, RG-24, RG-25) closed by direct codebase investigation. Then PRD+technical-spec pairs were produced for the four LOCKED systems (Entity Master, Provider Abstraction Layer, Provenance & Freshness, Alerts & Monitoring), each required to open with an explicit north-star traceability chain and each technical spec required to ground every reuse claim in the actual UCT codebase, not the architecture documents alone. An adversarial validator checked all eight documents against seven specific failure modes (scope drift, vague Bloomberg-like language, unverified reuse claims, unnecessary parallel platforms, missing traceability, silently-decided owner items, contradictions with the Phase 2 architecture) and found two genuine issues (1 medium, 1 low); both corrected directly -- an untracked boundary-matrix exception was named and time-boxed (new register items D14/D15, both self-expiring), and an interim job was retroactively authorized by its own PRD with a sunset condition, rather than stripped. No scope drift from the north star was found. Artifacts: `05-product-strategy/prds/{entity-master,provider-abstraction,provenance-freshness,alerts-monitoring}-prd.md`, `07-technical-architecture/specs/{entity-master,provider-abstraction,provenance-freshness,alerts-monitoring}-spec.md`. | Owner instruction: Phase 3 authorization explicitly scoped to specification, not implementation, with a mandatory north-star traceability requirement. | governs the Phase 3 exit gate; no application code touched |
