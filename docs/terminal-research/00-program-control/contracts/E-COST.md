# CONTRACTS E-05 and E-06 — Cost models (Document C Parts XLII, CCXC, CCCXXVII)

Read `_SHARED_PREAMBLE.md` first; it binds. Both roles produce LABELED-ASSUMPTION models: every number carries its source (a leaf report, a public price page with date, or "ASSUMPTION: owner input OI-10 pending"). Never present an estimate as a fact. MODEL: Fable. WAVE: 2.

KNOWN FACTS (read in full): `02-data-providers/provider-ledger.md` (F-03b), `09-security-licensing-cost/data-use-classification.md` (E-02), `realtime-and-exchange-classification.md` (E-03, per-user fee evidence), `vendor-terms-evidence.md` (E-01, public prices), `02-data-providers/railway-flag-state.md` (five services), `01-existing-system/database-and-infrastructure.md` (D-04, resources), `08-ai/existing-ai-systems.md` (D-12, lanes, guards, price tables), the Unusual Whales / Koyfin / TradingView / Benzinga dossiers §L (comparable retail price points), `OWNER_INPUTS_REQUESTED.md` OI-01/OI-10 (member count and spend baseline unknown; defaults).

## E-05 — Data and infrastructure cost model, six scenarios → `09-security-licensing-cost/cost-model-data.md` (BUDGET 100 calls / 70 min)
Scenarios: internal-only (2–5 users) · 100 · 500 · 1,000 · 5,000 · 10,000 members. Lines: current fixed vendor cost (public list prices for the plans the ledger implies; unknown contract terms labeled), incremental data cost for the terminal's likely data classes (real-time quotes per user under E-03's fee evidence; options/OPRA; fundamentals/estimates upgrades; transcripts; news), Railway compute per service (public pricing × D-04's resource claims), storage/R2, streaming fan-out (from D-05's envelope), observability. Show which lines scale with member count (escalation trigger under the seed facts) and which are fixed. Deliver: a scenario table, a per-member cost curve, the three largest sensitivities, and the owner questions that bound the ranges.

## E-06 — AI inference, infrastructure, and feature cost attribution → `09-security-licensing-cost/cost-model-ai-infra.md` (BUDGET 100 calls / 70 min)
Per-feature unit economics for the AI features the program is likely to propose (contextual "why is it moving", earnings prep brief, watchlist morning brief, natural-language screen, company-page summaries, alerts explanations): tokens per call (assumption ranges), calls per user per day (assumption), model class per lane (never downgraded for cost; caching and batching as levers per the seed facts), current Anthropic/OpenAI/Perplexity public prices with dates, cache hit assumptions, population reserve for scheduled lanes, the existing budget-guard mechanics (D-12) and the mis-priced table (RG-12). Deliver: a per-feature cost table at the six scenarios, the point where per-member AI cost exceeds plausible ARPU (from OI-12 default and comparable price points), and the guard design constraints ARCH-05 must respect.

RETURN SUMMARY ≤150 words each: path, the headline number range with its biggest assumption, ≤3 open questions. OUTPUT STRUCTURE, CONFIDENCE, SOURCE HANDLING, SECRETS, DO NOT per `_SHARED_PREAMBLE.md` (binding). Public price pages may be fetched (WebFetch); no vendor API calls. Do not spawn sub-agents.
