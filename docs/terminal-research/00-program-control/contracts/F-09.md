# CONTRACT F-09 — Provider Master Ledger (exhaustive, owner directive DL-022)

Read `_SHARED_PREAMBLE.md` first; it binds. You are the SINGLE WRITER of the canonical `02-data-providers/provider-master-ledger.md`, which SUPERSEDES `provider-ledger.md` as the artifact satisfying Document B §49 gate item 4. This is a RESTRUCTURING and EXTENSION task, not a from-zero rediscovery: your primary inputs are the already-accepted provider inventory, and your job is to reorganize it into a materially more granular matrix, close any repo-coverage gaps those inputs left NOT INSPECTED, and separate technical access from contractual right at the per-capability level, per the owner's explicit directive.

MODEL: Sonnet 5 High. WAVE: 3 (immediately following the recovery wave). BUDGET: 180 tool calls or 120 minutes — this is a large synthesis; write incrementally (ledger table first, by provider, in the order KNOWN FACTS lists them) so a budget stop or session-limit pause leaves a usable partial.

KNOWN FACTS (read every one of these in full before writing anything; use offset/limit for files over 2000 lines):
* `02-data-providers/provider-ledger.md` (F-03b, accepted) — the existing 48-row ledger; your starting inventory.
* `02-data-providers/provider-inventory-dashboard.md` (D-03, accepted) — the dashboard-repo provider archaeology with call sites.
* `01-existing-system/ecosystem-cartography.md` (D-14, accepted) — providers referenced across the engine, bot, wire, scans, and chart-renderer repos, plus its own NOT INSPECTED list.
* `02-data-providers/railway-flag-state.md` (orchestrator, ORCH-RAILWAY-01) — every environment-variable NAME set on each of the five Railway services (web, worker, flow-worker, bars-api, chart-renderer), plus the two admin-health reads. Names only; no values are in this file and none may appear in yours.
* `09-security-licensing-cost/vendor-terms-evidence.md` (E-01), `realtime-and-exchange-classification.md` (E-03), `derived-data-rights.md` (E-04), `data-use-classification.md` (E-02), `licensing-register.md` (F-04, 118 rows) — every licensing classification already established per provider and data class.
* `00-program-control/DECISION_LOG.md` DL-021 (Massive plan-tier evidence: five corroborating code comments naming "Polygon Advanced, $200/mo") and DL-022 (this directive).
* `00-program-control/GOVERNING_PRINCIPLES.md` §14A (the owner's exact requirement; reread it, it is your specification).

DELIVERABLE — one file, `02-data-providers/provider-master-ledger.md`, with these sections:

1. **Coverage confirmation.** Before anything else: read D-03's and D-14's own NOT INSPECTED sections and confirm whether any repository, service, or scheduled job was left unexamined for provider references. If you find a gap (a repo, a config file, a job script not yet grepped for provider imports/env-var reads), close it yourself with Read/Grep/Bash — this is the one place genuinely new discovery is expected, not just restructuring. Record what you closed and what remains genuinely out of reach (e.g., a machine or account you cannot access) as NOT INSPECTED.

2. **The ledger.** One row per PROVIDER (not per provider-per-repo — consolidate). Columns, exactly as the owner specified: provider · internal service/repository using it (list all, comma-separated) · environment/config variable NAMES involved (never values) · API/client location (path:line for the primary integration) · capabilities currently consumed · capabilities apparently available but unused · then ONE SUB-TABLE OR ROW-GROUP per asset-class column set: quotes/market data · historical prices · streaming · fundamentals · financial statements · estimates · earnings · transcripts · filings · news · macro/economic data · options · ownership/insiders · corporate actions · calendars/events · alternative data · search/reference/symbol data — mark each cell yes / no / partial, with a one-clause note where non-obvious · update frequency · historical depth · rate limits (if known, cite the code) · current production usage (status per the KEY-PRESENT → CODE-REFERENCED → OBSERVED-CALLED → CONTRACT-ACTIVE vocabulary) · redundancy/overlap with other providers (name them) · source-of-truth role if any · reliability observations (cite D-03/D-14 findings) · cost/tier IF ACTUALLY EVIDENCED (cite the code comment or public price page; never invent a number) · display/redistribution rights · storage/cache rights · derived-data rights · AI/model-processing rights · licensing confidence (cite the E-file/F-04 row) · opportunities for expanded usage.

3. **A–G classification.** For every provider AND, where they diverge, for individual capabilities within a provider, classify: **A** currently used · **B** configured but underutilized (a key exists, code calls it, but a data class it could serve is not consumed) · **C** available through a current provider but not yet used (the vendor's API supports it; nothing in the codebase calls it) · **D** duplicative/overlapping (name the overlapping provider) · **E** legacy/dormant · **F** licensing unknown · **G** missing from the current stack entirely (no provider covers this data class at all). Produce a summary table: class → count → the providers/capabilities in it.

4. **Technical access vs. contractual right — explicit split.** A short section restating, per provider, that a working key or an observed call is evidence of ACCESS only, never of REDISTRIBUTION/STORAGE/DERIVATION/AI-PROCESSING rights; cross-reference the licensing register (F-04) row for each. Do not re-derive licensing classifications — cite F-04.

5. **The "before we buy anything" checklist**, per Document B §15's ten questions, answered for every class-G gap you found: do we already have this data (cite the row); is it underutilized (cite B/C rows that could serve it instead); could an existing provider expose more endpoints (name the provider and the unused endpoint); could it be derived internally; are redistribution rights compatible; are storage/caching rights compatible; can it be passed to AI; what would a new provider uniquely add; what new cost; what new vendor dependency.

6. **Build-vs-buy feed.** For every genuine class-G gap: a one-line note for Part XLIII (Reuse / Extend / Integrate / Build / Defer) as a hypothesis, not a decision.

7. GAPS, NOT INSPECTED (the union of D-03's, D-14's, and your own), and a pointer note: "`provider-ledger.md` (F-03b) is retained as the raw Wave-1 synthesis; this file is now the canonical gate-4 artifact."

RULES: Never print, retrieve, or infer a secret VALUE — variable names only, exactly as GOVERNING_PRINCIPLES §14A requires. Never invent a cost, a rate limit, or a licensing classification not already evidenced somewhere in the KNOWN FACTS or found by your own read-only grep of the code. If Railway needs re-reading for a variable-name gap, that is an ORCHESTRATOR-ONLY action (per DL-012) — you do not have Railway CLI access; flag the gap instead and it will be closed by the orchestrator between waves.

TOOLS AVAILABLE: Read, Grep, Glob, Bash (read-only) across all repositories named in `_SHARED_PREAMBLE.md`'s location table. No web research, no production calls, no provider API calls.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\02-data-providers\provider-master-ledger.md`.
RETURN SUMMARY ≤150 words: path, provider count, A-G class counts, the single most consequential class-G gap found, ≤3 open questions.
OUTPUT STRUCTURE, CONFIDENCE, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding — SECRETS is especially load-bearing for this contract). Do not spawn sub-agents.
