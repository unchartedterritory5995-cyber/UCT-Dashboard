# CONTRACT D-10 — Feature Flags and Entitlements (current state)

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-10
ROLE: Feature flags and entitlements specialist (Group D)
MISSION: Document exactly how features are gated (flags), how users are classified and entitled (auth tiers, paywall, admin), how routes and APIs enforce those rules server-side and client-side, and what a dark Terminal-Next beta (flag → internal users → selected members) would need that does not exist today.
SCOPE: dashboard worktree: `docs/feature_flags.json`, `api/services/feature_flag_index.py`, flag readers across `api/` and `app/src/`, auth modules, `AuthGuard`, tier/paywall code, admin pages/routers, Stripe plan handling. Read-only.

QUESTIONS:
1. Flag system: how a flag is defined and read (env var on Railway? settings table? per-user?), naming conventions, defaults, the ledger's status vocabulary (armed / dark / undecided) and its full current content (reproduce the table: flag, status, note), the AST index that derives the gate list, and the test that fails by name. Client-side flags: how the SPA learns flag state (endpoint, build-time `VITE_*`, settings payload).
2. Kill switches and rollback flags in force (e.g., `LLM_BATCH_ENABLED`, `SCAN_LIVE_SWEEP_ENABLED`, `PATTERN_VISION_ENABLED`, `DESK_PUBLIC_SHOWS`, `WIRE_SUBSTACK_GATE_MODE`), what each gates, and whether "off-and-unset" is distinguishable from "off-on-purpose" today.
3. User classes: visitor / free member / paid member / annual / trial / admin / partner — how each is represented (auth DB fields, Stripe price ids, `ADMIN_EMAILS`, TOTP, email verification), how `tier` reaches the client and what it gates (the "tier is a badge" claim from project memory — verify: is enforcement server-side?), what the paywall covers (Morning Wire only?) and how (`Traders.paywall.test.jsx`, `Pricing`, `Subscribe`).
4. Route and API gating: `AuthGuard` behavior, admin-only routes ("admins see every route"), backend dependencies enforcing member/admin, endpoints reachable unauthenticated by design (render routes for the chart renderer; public pages), the Coming-soon public gate.
5. Per-user targeting: does any mechanism let a feature be enabled for a specific user or cohort (allowlists, `DESK_PUBLIC_SHOWS="*"` style, admin toggles in `Admin.jsx`)? If not, say what the smallest reliable enhancement would be (Part XXXIX) — as a recommendation with code references, not a decision.
6. Admin control surface: what staff can toggle at runtime today (Admin page, admin routers), audit logging of such changes (appendix CDXVIII).
7. Rate limits and abuse protection tied to entitlement (search/AI caps per user; budget guard per tier).
8. Requirements table for a Terminal-Next dark beta: flag name proposal convention, route gating, nav door visibility, API gating, telemetry opt-in, kill switch — each marked exists / partial / absent with the file that would change.

OUT OF SCOPE: coexistence option ranking (D-08); AI budget internals (D-12); payments beyond entitlement (Stripe webhooks etc. one line).

KNOWN FACTS: `docs/feature_flags.json` README says gates are derived by AST (`api/services/feature_flag_index.py`) and `tests/test_feature_flag_ledger.py` fails by name; statuses armed/dark; `.env.example` includes `ADMIN_EMAILS`, `J2_TRIAL_ENABLED`, `STRIPE_PRICE_ID_PRO`, `STRIPE_PRICE_ID_ANNUAL`; project memory claims `tier` is a badge and the paywall covers the Morning Wire only. Verify.

REQUIRED SOURCES: code; `docs/feature_flags.json`; `CLAUDE.md` "Auth & User System" as CLAIMS.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only).
BUDGET: 120 tool calls or 60 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\01-existing-system\flags-and-entitlements.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
