# CONTRACT D-08 — Migration / Coexistence Architect (current mechanisms)

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-08
ROLE: Migration / Coexistence Architect (Group D)
MISSION: Establish, from code and history, how new surfaces have been introduced alongside existing ones in this application (dark, beta, flag-gated, parallel rebuilds, cutovers, retirements), what the exact touch points are for adding a Terminal-Next surface with zero impact on Terminal-Current, what would break if `/calendar` were replaced, and the candidate coexistence options with their code reality — as options, not a decision.
SCOPE: dashboard worktree (`app/src/`, `api/`, `docs/decisions/`, `docs/runbooks/`, `docs/plans/`, `CLAUDE.md`), plus read-only `git log` on the worktree. Other repos only to find inbound links to `/calendar` (grep in `morning-wire`, `uct_intelligence`, `uct-intelligence`, `uct-sunday-scan`).

QUESTIONS:
1. Precedents, each with commit references and files: Journal 2.0 parallel rebuild (beta alongside the old journal), Compass Brain Bridge (dark, flag-gated), Awareness Engine (dark), Live Flow → Live Massive cutover (P5, deploy survival), Patterns page retirement, live-scan retirement, the `/calendar` display rename (`88b87a32b`: what changed, what deliberately stayed), Coming-soon gating of the public site. For each: mechanism (route, door, flag, entitlement, admin-only), how users were exposed in stages, rollback path, what went wrong if documented.
2. Touch points for a new surface: the exact list of files/registries that must change to add a route + nav door + flag + entitlement (App.jsx route table, door-prefix registry, NavBar/MobileNav/MoreSheet, feature flag ledger, AuthGuard, backend router mount, CSS module, tests that enumerate routes/doors and would fail by name). Which of these are "high-conflict" shared files (Part CCIV).
3. Persisted-key hazards: locate the persisted preference and layout keys tied to the calendar (`calendar_view_v3`, `calendar_filters_v2`, `calendar_mystocks_sources`, the `calendar` widget type key inside `charts_workspace_layout`) and explain, with code, why renaming any of them would wipe saved views; note any read-fallback shim pattern that exists elsewhere.
4. Inbound dependencies on `/calendar`: deep links (`/calendar?earnings=SYM&esection=…`, `/calendar/mystocks`, `/r/calendar` render route used by the chart renderer / Discord posts / wire), embeds (`CalendarWidget` in `/charts`, `CalendarEmbed` in the notebook, `journal-2-0` CalendarTab — is it the same data path?), links from the Morning Wire HTML, Discord bot, Substack, emails, YouTube descriptions. Produce the "what breaks if replaced" list.
5. Coexistence options with code reality (Document C Part XXXVIII and the mission list: Terminal Labs / Beta / Next, internal flag, secondary route, workspace toggle, alternate mode, a tab within Terminal-Current, another): for each — files touched, blast radius on Terminal-Current, flag/entitlement needs, rollback, URL/back-button behavior, member-visible naming implications (Part CCXXXII), and a risk note. Rank tentatively; mark the ranking PROVISIONAL.
6. Staged rollout mechanics available today: admin-only routes, `ADMIN_EMAILS`, tier gating, per-user flags (do they exist?), Railway env flags, "armed vs dark" ledger; what a Stage 1–3 rollout (flag → internal → selected members) would require that does not exist.
7. Migration gates evidence: the legacy parity matrix concept (appendix CDLXII) — list Terminal-Current capabilities at the level D-09 will detail (headers only) to seed the matrix.

OUT OF SCOPE: the full calendar feature map (D-09); flag system internals (D-10); UI primitives (D-06).

KNOWN FACTS: `App.jsx` registers `/calendar` and `/calendar/mystocks` via `lazyPage`, plus `/r/calendar` → `CalendarRender`; the header label lives in `app/src/pages/calendar/CalendarHeader.jsx:613`; the production page today shows tabs Wire / Board / Table / Month, scopes My Stocks / Watchlist / Positions / UCT20 / All, cap filters, Hub, week strip. Seed facts: never rename persisted keys; no destructive change to Terminal-Current.

REQUIRED SOURCES: code; `git log --oneline -- app/src/pages/Calendar.jsx app/src/pages/calendar` and the named commits; `docs/decisions/*`, `docs/plans/*`, `docs/runbooks/cutover-watch.md`, `docs/operations/phase-7-launch-checklist.md`; `CLAUDE.md` sections "Journal 2.0 — parallel rebuild", "Compass Brain Bridge", "Awareness Engine", "Live Options Flow — Deploy Survival", "Phase E" as CLAIMS.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only), `git log`/`git show` on the research worktree only.
BUDGET: 140 tool calls or 70 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\10-roadmap\coexistence-current-mechanisms.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none (D-09 runs in parallel; cite it by path for later merge).
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
