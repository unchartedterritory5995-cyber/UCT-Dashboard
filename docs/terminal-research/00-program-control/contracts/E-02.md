# CONTRACT E-02 — Storage, Caching, and AI-Use Classifier

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: E-02
ROLE: Storage, caching, and AI-use classifier (Group E, licensing pod)
MISSION: Turn the licensing pod's evidence into ONE classification table: for every provider and data class UCT uses, classify each USE (desk display · member display · storage/history · caching · derived analytics · AI processing · external publication to Discord/Substack/YouTube) as Allowed / Likely Allowed (verify contract) / Restricted / Unknown / Unsuitable, with the driving clause and the owner fact that would move it. This table is the input to the licensing register (gate item 5).
SCOPE: read-only synthesis over the Wave 1 files below; no new web research except to re-read a clause a sibling cited. Dashboard code only to confirm a use exists.

KNOWN FACTS (read these in full; do not re-derive):
* `09-security-licensing-cost/vendor-terms-evidence.md` (E-01): verbatim clauses for 9 vendors; Massive Business ToS permits "Edge Users"; retail tiers individual-use; FMP requires a Data Display and Licensing Agreement for multi-user display; Finviz has no public terms; yfinance Unsuitable; Anthropic makes UCT warrant input rights.
* `09-security-licensing-cost/realtime-and-exchange-classification.md` (E-03): real-time surfaces, exchange rules, the UTP delayed-plus-volume escape route, unauthenticated endpoints (verification by the orchestrator in progress).
* `09-security-licensing-cost/derived-data-rights.md` (E-04): 29 derived products; the Massive tier re-classifies twenty of them; five clause-vs-code collisions.
* `02-data-providers/provider-inventory-dashboard.md` (D-03): 30 providers, call sites, data classes.
* `02-data-providers/railway-flag-state.md` (orchestrator): which keys are set on which service; FRED/Reddit/TheFly absent; Bullflow/UW/Polygon-direct present.
* `08-ai/existing-ai-systems.md` (D-12): which lanes send which vendor data to which LLM.
* `05-product-strategy/proprietary-asset-inventory-raw.md` (D-13): which external publications carry vendor-derived content (charts to Discord, wire to Substack, desk to YouTube).
* Contract facts come only from the owner. The Massive tier is UNKNOWN (a code comment in `api/services/polygon_extras.py:4` claims "the $200/mo MASSIVE_API_KEY tier", which names an individual product plan — a CLAIM). Escalation D-002 and owner inputs OI-03/OI-09 are open. Nothing may be classified "Allowed" (confirmed).

QUESTIONS:
1. Build the master table: rows = (provider, data class); columns = the seven uses above; cell = class + the clause id from E-01/E-03/E-04 (cite file + section) + what owner fact flips it. Cover: Massive (equities trades/quotes/aggregates, options trades/OPRA, dark pool, snapshots, reference), FMP (statements, estimates, earnings calendar/history, econ calendar, price targets, news), Finviz (screens, sessions, charts), Finnhub, Alpha Vantage (news, transcripts), FRED, twitterapi.io/X, Reddit, TheFly, yfinance/Yahoo, Schwab (chains, account data), CFTC COT (public), SEC EDGAR (public), Discord content (own community), and the LLM providers (Anthropic, OpenAI, Perplexity) as processors.
2. Two scenario columns for the tier question: "if Massive Individual tier" and "if Massive Business tier" (and likewise "with / without an FMP DDLA"), so the owner sees exactly what each answer buys.
3. Storage and history: which stored datasets (bars.db, R2 snapshots, catalysts.db tweets, transcripts, earnings history) are covered, time-limited, or restricted; note the 7-day tweet window collision E-04 found.
4. AI processing: which lanes send restricted inputs to an LLM today; classify under the vendor's ML/automated-processing clauses and the LLM provider's retention terms.
5. External publication: charts, wire, index-close posts, Sunday scans, desk videos — classify the vendor-derived content in each.
6. The escape routes: delayed data + real-time volume (UTP), aggregated/derived-only member surfaces, desk-only raw display; state for each Terminal-Next candidate surface (security page quote, watchlist quotes, options flow, fundamentals, calendar, news) the safest design under the Individual-tier assumption.
7. List every owner question this table cannot resolve, as OI-style entries with defaults.

OUT OF SCOPE: cost modeling (E-05/E-06); new web research beyond re-reading cited clauses; recommendations on product scope beyond the "safest design" column.

REQUIRED SOURCES: the files above; re-fetch a clause only if the sibling report's quote is ambiguous.
TOOLS AVAILABLE: Read, Grep, Glob; WebFetch only for re-reading a cited URL.
BUDGET: 100 tool calls or 60 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\09-security-licensing-cost\data-use-classification.md`.
RESEARCH WAVE: 1b (internal-only synthesis; permitted before external research). DEPENDENCIES: E-01, E-03, E-04, D-03, D-12, D-13 (all accepted).
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
