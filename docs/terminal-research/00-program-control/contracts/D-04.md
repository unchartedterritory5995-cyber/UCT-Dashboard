# CONTRACT D-04 — Existing Database & Infrastructure Archaeologist

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-04
ROLE: Existing Database & Infrastructure Archaeologist (Group D)
MISSION: Map every datastore, its schema and access pattern, and the deployment/infrastructure topology of the dashboard (Railway services, volume, R2, secrets handling, backups, CI), so data and coexistence architecture rest on the real constraints (single replica, SQLite on one volume, deploy-on-push).
SCOPE: dashboard worktree: `api/`, `railway.json`, `nixpacks.toml`, `Procfile`, `runtime.txt`, `.github/`, `docs/runbooks/`, `docs/operations/`, `scripts/`, `tools/` (read-only). Railway CLI read-only if available.

QUESTIONS:
1. Datastores: every SQLite database the code opens (grep `sqlite3.connect`, `aiosqlite`, path constants, `*_DB_PATH` env names): logical name, path pattern, owning module, WAL/pragma settings, tables (from `CREATE TABLE` statements or model definitions) with key columns, indexes, approximate purpose, which routers/services read and write it, retention/purge logic. Produce one table per database. Note any Postgres references (`docs/runbooks/postgres-migration-trigger.md`) and whether a migration is planned (CLAIM).
2. Migrations: how schema changes ship today (ad hoc `ALTER`, migration scripts, `apply_*_patches.py`), versioning, backfill scripts.
3. Volume and paths: the data directory layout on the Railway volume as implied by code (env names such as `AUTH_DB_PATH`), what `C:\data` mirrors locally, the conftest pins concept (name only).
4. Object storage: R2 usage (buckets, prefixes, what is snapshotted, cadence; `boto3` checksum parameters).
5. Deployment: `railway.json` (shared by three services; startCommand branches), `nixpacks.toml`, build command (pip + npm build), healthcheck path and timeout, draining, restart policy; `.github/workflows` (what runs, whether anything gates deploys); the "deploy on push to master" fact (CONFIRM from config or note CLAIM); Railway services list if `railway status` works read-only from the worktree (if the CLI reports "not linked", record NOT INSPECTED; never run `railway link`, `railway ssh`, `railway run`, `railway up`, or any `--set`).
6. Environment variables: full list of NAMES read by code (`os.environ`, `os.getenv`, settings modules) grouped by purpose; which are secrets (name only), which are flags, which are paths. If `railway variables --json` works read-only, print ONLY the key names via a pipe (e.g., `python -c "import json,sys;print(sorted(json.load(sys.stdin)))"`), never values, and diff against the code's list to find KEY-PRESENT-only names.
7. Backups and recovery: `flow_backup.py`, `j2_attachments_backup.py`, snapshot/delta settings, disk budget (spool), anything that restores state after a deploy swap.
8. Resource constraints: single replica; memory (RSS ~1.3 GB at health probe), thread count, arena settings; the OOM history as CLAIM; what this implies for adding terminal workloads.
9. Security of secrets: are any provider keys used client-side; any key logged; `.env` handling in scripts.
10. Local development: `run-local.ps1`, how a local backend is started safely (heavy jobs off), the port-8077 stale-backend hazard.

OUT OF SCOPE: provider call sites (D-03); performance behavior (D-05); test infrastructure (D-07); other repositories (D-14).

KNOWN FACTS: `railway.json` build = `pip install -r requirements.txt && cd app && npm install && npm run build`; startCommand branches on `BARS_API_ENABLED`, `FLOW_WORKER_ENABLED`, `WORKER_ENABLED`; healthcheckPath `/api/health` timeout 600; drainingSeconds 30; restart ALWAYS. Seed facts: 20+ SQLite DBs on the web service's volume so jobs cannot move off web; single replica; a healthcheck path was once claimed wired in five files but never was — never point healthcheck at `/api/ready`. `docs/runbooks/` includes `postgres-migration-trigger.md`, `cutover-watch.md`, `rest-backfill-arming.md`.

REQUIRED SOURCES: code and config; `CLAUDE.md` "Environment Variables", "Running Locally", "`C:\data` IS REAL" as CLAIMS.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only); `railway status`, `railway variables --json | <keys only>` read-only if linked.
BUDGET: 150 tool calls or 75 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\01-existing-system\database-and-infrastructure.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
