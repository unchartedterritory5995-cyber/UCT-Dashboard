# CONTRACT D-14 — Multi-Repository Cartographer and Scheduled-Jobs Mapper (both machines)

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-14
ROLE: Multi-repository cartographer and scheduled-jobs mapper (Group D)
MISSION: Map the ecosystem beyond the dashboard — the engine, bot, wire, scans, and chart-renderer codebases; the Windows Task Scheduler pipeline on the owner's PC (CONFIRMED by scheduler entries); the Railway services; and the external surfaces — including every provider each non-dashboard repo references and every connection between components, so the system map (gate item 2) covers all repositories and both machines.
SCOPE: `C:\Users\Patrick\uct-intelligence`, `C:\Users\Patrick\uct_intelligence`, `C:\Users\Patrick\morning-wire`, `C:\Users\Patrick\uct-sunday-scan`, `services/chart_renderer` in the dashboard worktree, the dashboard's `.gitmodules`/`external/`, Windows Task Scheduler (read-only PowerShell), Railway CLI read-only if linked. Read-only everywhere.

QUESTIONS:
1. Per repository (engine, bot, wire, scans, chart renderer): purpose; top-level structure; entry points and CLIs; databases (paths, tables at a glance); providers referenced (env var NAMES, modules, endpoints; status CODE-REFERENCED with path); outputs (files, DB writes, Discord posts, Substack, YouTube, dashboard API calls, R2 uploads); how it reaches the dashboard (submodule import? HTTP to `uctintelligence.com/api/*`? shared files? shared DB?); git state (branch, last commit date, remote; the bot is NOT a git repo — record that as a risk); tests present; CLAUDE.md claims worth flagging.
2. Task Scheduler (this PC), CONFIRMED evidence: with PowerShell, for every task whose name starts with "UCT" or "UCT20": name, state, actions (executable, arguments, working directory), triggers (schedule, days, local time = Central Time), last run time, last result, next run time. Use `Get-ScheduledTask`, `Get-ScheduledTaskInfo`, and `($task.Actions | Select Execute, Arguments, WorkingDirectory)`, `($task.Triggers | ...)`. Map each task to a repository script. Reproduce the full table in an appendix. Compare against OWNER_SEED_FACTS §2 (which lists ~10 jobs) — there are ~36 tasks; report the delta. Note tasks that look like watchdogs/checks vs producers.
3. Railway topology (read-only): from `railway.json` startCommand branches and any docs, the three services and their roles (web, worker, flow worker; bars API?); which PC jobs call which Railway endpoints; if `railway status`/`railway service` work from the research worktree without linking, record service names; otherwise NOT INSPECTED. Never `railway link`, `ssh`, `run`, `up`, `redeploy`, or `--set`.
4. External surfaces and integrations: Discord (bot app, webhooks, channels, slash commands incl. `/buzz`, `/chart`), Substack (wire publishing; the "no public wire" rule), YouTube (desk uploads; quota 6/day), Buffer, R2 (buckets), Zoom webhook, email (Resend), Stripe — where each is driven from (repo/path) and its status evidence.
5. The chart renderer: what it renders (`/r/*` routes consumed?), how it is deployed (claim: `railway up` from its subdirectory, not git-connected), who calls it (Discord posts, index close posts, calendar week posts).
6. Dependency graph (Part CCCIX): UCT components → providers, AI, auth, storage, deployment, both machines; for each edge: criticality, replaceability, failure mode, cost-bearing (yes/no/unknown). ASCII or mermaid diagram plus a table. Identify single points of failure (Part CCCX), including the owner's PC as a scheduler host.
7. Which desk tools and external sites the pipelines depend on or link to (Finviz, TradingView links, thinkorswim, Yahoo, X) — evidence for Executive Q8 and the fourth desk-tool benchmark slot.

OUT OF SCOPE: dashboard internals (D-01..D-12); proprietary asset value judgments (D-13); licensing (E-*).

KNOWN FACTS: Task Scheduler listing on 2026-09-02 shows these UCT tasks: Brain Midday/Open/Pack Export/Post-Market/Pre-Close/Pre-Market; Breadth Collector; Breadth Live Open/PreOpen/Session/Visual Check; Breadth PostShip Check OPEN and OPEN+20; Buyout Sweep; Clips approval poll and daily build; Desk Creative Watch; Desk Insights Polish; EOD Updater; Flow Corpus Snapshot (+AM); Live Recap; Market Ingest; Market Monitor; Morning Wire; NAAIM Settle; Post-Close Check; Sunday Scan (+Watchdog); Warm Bars Universe; Wire Critic; X News Feed; UCT20 Book Bars Sync; UCT20 Portfolio EOD Refresh. Seed facts list scanner 7:00 CT, morning wire 6:35 CT, wire critic 5:00 CT, breadth collector 3:15p CT, UCT20 EOD 3:20p, brain pre-close 3:30p, EOD updater 4:05p, market ingest 8:05p — verify each from the scheduler. `uct-intelligence` remote: github unchartedterritory5995-cyber/uct-intelligence; `morning-wire` remote likewise; `uct-sunday-scan` has `register_sunday_scan_task.ps1` and `run_sunday_scan.bat`.

REQUIRED SOURCES: the repositories; PowerShell scheduler queries; `railway.json`; each repo's `CLAUDE.md`/README as CLAIMS.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only), PowerShell (read-only `Get-ScheduledTask*` only), `git -C <repo> log -3` read-only.
BUDGET: 160 tool calls or 80 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\01-existing-system\ecosystem-cartography.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
