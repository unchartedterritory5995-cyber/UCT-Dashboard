# CONTRACT D-02 — Existing Backend Archaeologist

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-02
ROLE: Existing Backend Archaeologist (Group D)
MISSION: Produce the authoritative inventory of the FastAPI backend (`api/`) as it actually runs: app composition, routers, services, middleware, auth, in-process workers and schedulers, caching, streaming endpoints, rate limiting, health, and code health, so that architecture roles can reuse what exists and avoid what is dead.
SCOPE: `C:\Users\Patrick\uct-worktrees\terminal-research\api\` (and `services/chart_renderer` only to note its boundary). Read-only.

QUESTIONS (cite path:line):
1. App composition: `api/main.py` — every router mounted (prefix, tags, auth dependency), middleware stack (`api/middleware/`), startup/shutdown hooks, background threads or tasks started in-process, the SPA catch-all (which paths fall through to `index.html`; the production `/api/calendar/week` GET returned the SPA shell — explain why), health endpoints and what `/api/health` reports (`status`, `wire_date`, `uptime_seconds`, `thread_count`, `rss_mb`).
2. Process topology: `railway.json` startCommand branches (`BARS_API_ENABLED`, `FLOW_WORKER_ENABLED`, `WORKER_ENABLED`, default uvicorn). What each of `api/bars_api_main.py`, `api/flow_worker_main.py`, `api/worker_main.py` runs. Which jobs run inside the web process (CLAIM vs CONFIRMED — the health endpoint's thread count is one confirmation signal).
3. Routers inventory: `api/routers/*` and every top-level `*_router.py`: purpose, prefix, auth, data source, caching, notable endpoints. Group by domain (calendar/earnings, flow/options/darkpool/gex, breadth, wire, journal, watchlists, screener, desk, admin, auth, ai). For `/api/calendar/*` list endpoints only (D-09 owns depth).
4. Services inventory: `api/services/*` by domain; identify shared infrastructure services (serve_stale, limiter, feature flags, provider coverage monitor, wire coverage, budget guards) versus domain services.
5. Auth and permissions: the auth model (sessions/JWT/TOTP), admin promotion (`ADMIN_EMAILS`), tier checks as dependencies, where server-side permission checks live, any endpoints that look unauthenticated but return member data. Report variable names only.
6. Caching and staleness: every caching layer you can find (in-memory dicts, disk caches, SQLite caches, `serve_stale`, HTTP cache headers, Cloudflare hints), with TTLs where stated.
7. Streaming: SSE and WebSocket endpoints (`stream_bars`, `chat_stream`, massive streams, flow websocket) — name and purpose only; D-05 owns behavior.
8. Rate limiting, retries, circuit breakers (`api/limiter.py`, provider-specific budgets like the Finnhub budget), watchdogs (`event_loop_watchdog.py`, `flow_watchdog.py`).
9. Observability hooks: Sentry, deploy_log, structured logging, monitors that post to Discord.
10. Code health: dead/duplicate modules (e.g., `flow_router_RESTORE.py`, `main_py_additions.py`, `liveflow_worker_threaded.py` vs `liveflow_worker.py`, the `fill-*.csv` and patch scripts at the top level), experimental modules, largest modules, obvious seams for a terminal service layer.
11. Partner-owned files: confirm existence and mounting of `schwab_router.py`, `live_massive_router.py`, `massive_ws_worker.py`, `massive_processor.py`; describe their boundary in two lines each, no deeper.

OUT OF SCOPE: database schemas (D-04); provider endpoints and status (D-03); performance/real-time behavior (D-05); AI internals (D-12); calendar internals (D-09); flags semantics (D-10).

KNOWN FACTS: `api/` holds ~1,000 Python files; subdirs `data`, `middleware`, `routers`, `services`; `pytest.ini` testpaths are `tests` and `api`; production `/api/health` at 05:41 UTC returned `{"status":"ok","wire_date":"2026-09-01","uptime_seconds":776,"thread_count":67,"rss_mb":1306.6}`; single replica; `MALLOC_ARENA_MAX=2` was set for glibc arena fragmentation (claim from project memory).

REQUIRED SOURCES: the code; `CLAUDE.md` sections "Project Structure", "Auth & User System", "Performance & Scale", "Live Options Flow — Deploy Survival" as CLAIMS.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only). One read-only `GET https://uctintelligence.com/api/health` is permitted.
BUDGET: 150 tool calls or 75 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\01-existing-system\backend-archaeology.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
