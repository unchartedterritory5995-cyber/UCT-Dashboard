# CONTRACT F-03b — Internal Synthesizer: Provider Ledger

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: F-03b
ROLE: Internal synthesizer (Group F). SINGLE WRITER of the canonical `02-data-providers/provider-ledger.md` (gate item 4).
MISSION: Produce the provider ledger: every external data, AI, broker, and infrastructure provider across ALL repositories and services, with the strongest status evidence available (KEY-PRESENT / CODE-REFERENCED / OBSERVED-CALLED; CONTRACT-ACTIVE only when the owner has confirmed — none yet), data classes, call sites, limits, overlap, retirement candidates, and a licensing classification per data class drawn from the pod's evidence (never above "Likely Allowed / verify contract").
SCOPE: read-only synthesis; code only to settle a contradiction.

KNOWN FACTS (read in full):
* `02-data-providers/provider-inventory-dashboard.md` (D-03): 30 providers, call sites, data-class coverage table, retired/dormant, FMP helper duplication.
* `01-existing-system/ecosystem-cartography.md` (D-14): providers referenced by the engine, bot, wire, scans, chart renderer; scheduled jobs that call them.
* `02-data-providers/railway-flag-state.md` (orchestrator): keys set per Railway service (KEY-PRESENT evidence); `/api/admin/api-health` presence read; `/api/admin/provider-coverage` OBSERVED-CALLED evidence for the FMP/Finnhub enrichment fields; FRED, Reddit, TheFly absent on every service; Bullflow, UW, Polygon-direct present on web; MASSIVE_SECRET_KEY unset on web but set on flow-worker.
* `09-security-licensing-cost/vendor-terms-evidence.md` (E-01), `realtime-and-exchange-classification.md` (E-03), `derived-data-rights.md` (E-04); `data-use-classification.md` (E-02) if it exists when you run — cite its rows rather than re-deriving.
* `08-ai/existing-ai-systems.md` (D-12) for LLM providers and lanes.
* Owner facts (OWNER_SEED_FACTS §6, GOVERNING_PRINCIPLES §13): FMP Premium; Massive (ex-Polygon) bars via S3-compatible API; Finviz Elite; Schwab partner-owned; yfinance fallback; R2; Anthropic with budget guard; retired pre-Massive live-flow provider (Bullflow, per D-03) whose 403 reads like billing. Contract terms DEFAULT unknown. Any key with no OBSERVED-CALLED evidence in 30 days is treated as retired (OI-04 default).

DELIVERABLES (one file):
1. Ledger table, one row per provider (Massive/Polygon, FMP, Finviz, Finnhub, Alpha Vantage, FRED, TheFly, twitterapi.io/X, Reddit, yfinance/Yahoo, Schwab, SnapTrade, Bullflow, Unusual Whales, Polygon-direct, CFTC, SEC EDGAR, logo.dev and other logo sources, Anthropic, OpenAI, Perplexity, Picovoice, Stripe, Resend, Sentry, Discord, Zoom, YouTube, Buffer, Cloudflare R2, Railway, any others the leaves found): status + evidence (which artifact) · services/repos where the key is set · data classes · main call sites · refresh/caching · rate-limit handling · known limits · overlap with other providers · licensing class per data class (with the E-file citation) · cost (unknown unless a leaf cites a number; label) · verdict (core / secondary / retired-candidate / duplicate / dormant).
2. Data-class coverage matrix (rows = terminal data classes from the D-03 table; columns = providers in use, status, gaps): where UCT has no provider at all, say so.
3. "Already paid for but underutilized" vs "not available" vs "available but not redistributable" vs "available but uneconomic" (Part VI) — a short list under each with evidence.
4. Retirement and consolidation candidates (Bullflow, Polygon-direct, UW, six FMP helpers with no shared budget) with the owner question each depends on (OI-04).
5. Ten-question checklist (Document B §15) pre-answered for the data classes a terminal needs and UCT lacks (e.g., transcripts, analyst-level estimates, corporate actions, ownership) — as evidence for later build-vs-buy, not a recommendation to buy.
6. NOT INSPECTED (union of the leaves) and the owner inputs that would raise rows to CONTRACT-ACTIVE.

OUT OF SCOPE: licensing register (F-04) — you supply the per-data-class classes it will consume; cost modeling (E-05/E-06).

TOOLS AVAILABLE: Read, Grep, Glob, Bash (read-only). No provider or production calls.
BUDGET: 140 tool calls or 90 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\02-data-providers\provider-ledger.md`. Frontmatter (id F-03b, category synthesis).
RESEARCH WAVE: 2 (internal synthesis; permitted before external research). DEPENDENCIES: D-03, D-14, E-01, E-03, E-04, ORCH-RAILWAY-01 (all accepted); E-02 optional.
RETURN SUMMARY: ≤150 words: path, the ledger's headline (how many core providers, how many retired candidates, which data classes have no provider), overall confidence, ≤3 open questions.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
