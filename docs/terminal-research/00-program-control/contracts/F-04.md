# CONTRACT F-04 — Licensing / Cost Synthesizer: the Licensing Register (gate item 5)

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: F-04. ROLE: Licensing/cost synthesizer (Group F). SINGLE WRITER of `09-security-licensing-cost/licensing-register.md`. WAVE: 2. MODEL: Fable. BUDGET: 120 tool calls or 80 minutes.
MISSION: Produce the licensing register: every data use UCT makes today and every data use Terminal-Next is likely to recommend, classified Allowed / Likely Allowed (verify contract) / Restricted / Unknown / Unsuitable, with the driving clause, the owner fact that would settle it, and the design alternative that avoids the restriction. Unknowns are escalated (they already are: D-002, OI-03, OI-09, OI-17) — the register names them, it never buries them.

KNOWN FACTS (read in full; do not re-derive):
* `09-security-licensing-cost/data-use-classification.md` (E-02): the master table (provider x data class x seven uses) with Individual-vs-Business Massive scenario columns and FMP with/without DDLA.
* `09-security-licensing-cost/vendor-terms-evidence.md` (E-01), `realtime-and-exchange-classification.md` (E-03), `derived-data-rights.md` (E-04).
* `02-data-providers/provider-ledger.md` (F-03b) if present; otherwise `02-data-providers/provider-inventory-dashboard.md` (D-03) and `railway-flag-state.md`.
* `00-program-control/OWNER_DECISIONS.md` D-002 (recommendation in force: desk-first; member-facing raw vendor displays Restricted-pending-contract) and `OWNER_INPUTS_REQUESTED.md` OI-03/04/09/12/15/17; `RISK_REGISTER.md` R-14, R-17.
* Contract facts come only from the owner; nothing is "Allowed" (confirmed) yet. The Massive tier and an FMP DDLA are the two facts that flip most rows.

DELIVERABLES (one file):
1. Register table: one row per (use, provider, data class, audience): today's uses (from E-02 §1/§2) and Terminal-Next candidate uses (security page quote and chart; watchlist quotes; options flow panel; fundamentals and estimates panels; earnings/economic events; news; AI answers over vendor data; alerts; external publication) — columns: class · driver clause (file § cite) · owner fact that settles it · scenario outcomes (Massive Individual / Business; FMP with / without DDLA) · safest design alternative (delayed + real-time volume; aggregated/derived only; desk-only; attribution; retention limit) · cost bearing (per-user? yes/no/unknown).
2. Escalation ledger: the unknowns, each mapped to its OI/D entry and what changes when answered.
3. Publication and AI addenda: the five clause-vs-code collisions (E-04), the unauthenticated endpoints (E-03/R-17), and the LLM input-rights warranty (E-01), as register rows with a "production today" flag distinct from "Terminal-Next plan".
4. Rules for architects (Part XLI as design constraints): what ARCH-04/05/06 must assume until the owner answers; the attribution and retention requirements that hold under every scenario.
5. NOT INSPECTED and the questions that only a contract can answer.

TOOLS: Read, Grep, Glob. No web research (cite the E-files; re-fetch only if a quote is ambiguous), no production calls.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\09-security-licensing-cost\licensing-register.md` (frontmatter; id F-04; category synthesis). Write the register table first.
RETURN SUMMARY ≤150 words: path, how many rows in each class, the two facts that flip the most rows, ≤3 open questions.
OUTPUT STRUCTURE, CONFIDENCE, SOURCE HANDLING, SECRETS, DO NOT: per `_SHARED_PREAMBLE.md` (binding). Do not spawn sub-agents.
