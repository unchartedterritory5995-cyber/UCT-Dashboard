# CONTRACT D-05 — Performance & Real-Time Systems Engineer (current state)

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-05
ROLE: Performance & Real-Time Systems Engineer (Group D)
MISSION: Describe how real-time and near-real-time data actually flows through the dashboard today (vendor streams → workers → stores → SSE/WebSocket/polling → client), every caching and staleness mechanism, the documented performance baselines and incidents, and the capacity envelope of the single-replica web pod, so the real-time and performance architecture for Terminal-Next is designed against measured reality.
SCOPE: dashboard worktree `api/`, `app/src/`, `docs/perf-baseline.md`, `docs/perf-investigation.md`, `docs/runbooks/`, `docs/operations/`. Read-only. No measurement against production.

QUESTIONS:
1. Real-time paths, end to end, with path:line at each hop: (a) Massive WebSocket worker → processor → flow DB → API/WebSocket to `useFlowWebSocket.js`/LiveFlowMassive; (b) live pricing for quotes (which provider, cadence, which pages); (c) breadth live intraday; (d) `stream_bars` SSE and chart live updates; (e) any other SSE (`chat_stream`, curated stream). For each: transport, fan-out model, reconnection/backoff, dedupe of subscriptions across widgets, what happens on disconnect, freshness indicators shown to users.
2. Polling: every client polling interval (grep `setInterval`, `refetchInterval`, hooks) and its endpoint; server-side warmers/prewarm (`Warm Bars Universe` task exists on the PC — cross-reference name only; web-pod warmers like calendar enrichment warmer, prewarm heartbeat).
3. Caching layers with TTLs: `serve_stale`, in-memory caches, disk/SQLite caches, HTTP cache headers, Cloudflare cache rules (runbook `options-flow-cloudflare-cache.md`), client caches; the "bump both cache layers" hazard for the earnings modal.
4. Documented baselines: `docs/perf-baseline.md`, `perf-investigation.md`, `CLAUDE.md` "Performance & Scale — 2026-07-01 launch-hardening" and "Live Options Flow — Deploy Survival": extract every number with its date and method; label CLAIM vs measured artifact.
5. Bundle and load: Vite chunking, lazy pages, the auth-gate serialization of lazy chunks (project memory claims an auth gate serialized every lazy chunk behind it; find the code), initial-load waterfall for `/calendar` as far as code reveals (D-09 owns calendar features; you own its load characteristics).
6. Capacity envelope: uvicorn worker model, thread count (67 at 05:41 UTC), RSS (~1.3 GB), event-loop watchdog thresholds, known OOM incidents (CLAIM from memory: heavy scripts on the pod), rate limiters; what a long-lived multi-panel client with N concurrent subscriptions would stress first.
7. Market-open and event spikes: any code that anticipates spikes (backpressure, queue sizes, spool disk budgets, self-heal leases).
8. Propose (do not run) the Terminal-Current performance baseline protocol for Part CXX: what to measure, with a local backend or the browser only, market hours vs after close, and the exact commands/tools available in the repo (`tools/mobile_audit.py`, Lighthouse via browser, vitest timing) — a protocol for a later role, not a measurement.

OUT OF SCOPE: DB schema (D-04); provider inventory (D-03); UI component structure (D-06); flags (D-10).

KNOWN FACTS: production `/api/health` at 05:41 UTC: uptime 776 s, thread_count 67, rss_mb 1306.6; single replica; `MALLOC_ARENA_MAX=2` claim; partner-owned real-time files (`live_massive_router.py`, `massive_ws_worker.py`, `massive_processor.py`) may be read but not deep-described; `/live-flow` was replaced by `/live-massive`.

REQUIRED SOURCES: code; the docs named above as CLAIMS.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only). One read-only `GET https://uctintelligence.com/api/health` is permitted. No load generation of any kind.
BUDGET: 140 tool calls or 70 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\07-technical-architecture\current-performance-and-realtime.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
