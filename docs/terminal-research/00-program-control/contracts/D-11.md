# CONTRACT D-11 — State, Persistence, and the Existing Workspace/Widget System

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-11
ROLE: State, persistence, and existing workspace/widget system specialist (Group D)
MISSION: Inventory everything the application persists per user (preferences, layouts, widget settings, watchlists, alerts, saved objects, notebook/journal state), where and how (settings API, tables, keys, versioning, client storage), and the state model a terminal workspace would need versus what exists, so Terminal-Next never builds a second, drifting state system.
SCOPE: dashboard worktree `app/src/` (hooks such as `usePreferences`, contexts, `localStorage`/`sessionStorage` use), `api/` settings/preferences routers and tables, watchlist/alert/journal persistence modules. Read-only.

QUESTIONS:
1. Preference system: the settings/preferences API (endpoints, storage table, schema: key/value? JSON blobs? per-user rows?), the client hook(s), defaults, and the full list of persisted preference KEYS with their owning surface and shape (theme, density, calendar keys `calendar_view_v3` / `calendar_filters_v2` / `calendar_mystocks_sources`, chart settings, watchlist prefs, flow board prefs, journal prefs, others).
2. Workspace/layout persistence: `charts_workspace_layout` — full schema (versions, widgets[], `opts.settings`, grid positions, breakpoints), the `chart_settings` seed relationship, migrations between versions, recovery when corrupt, the uuid hotfix history (`fix/charts-layouts-uuid`), cross-device behavior; multi-chart grid state; any other layout persistence (dashboard cockpit, flow board grid).
3. Saved objects: watchlists (schema, columns, flags, sharing), alerts (live alerts DB), journal 2.0 / notebook (DBs, attachments, connectors), saved screens/structures/formulas (`/formulas/shared/`), chart presets/templates, indicator definitions, model book, UCT20 book — what is user-owned vs staff-published vs system.
4. Client storage: every `localStorage`/`sessionStorage` key and what it caches; URL state conventions (query params for calendar, charts deep links); what survives refresh and what does not.
5. Server state caching per user: any per-user caches (cache safety, Part CXCIX), auth session storage.
6. Versioning and migration practice: how a persisted schema change has been shipped before (read-fallback shims, v2→v3 keys), tests that pin persisted shapes.
7. Gap analysis for a terminal workspace state model (Part XXXII, CXXIV, CCCXXXV–CCCXXXVIII): global / workspace / server / persisted-user / transient state — what exists, what is missing (versioned workspace schema, instance-based panel identity, link groups, autosave, undo, recovery), and which existing module would be the natural seed for each. Recommendations only, no decisions.

OUT OF SCOPE: UI component evaluation (D-06); flags/entitlements (D-10); calendar feature semantics (D-09 — you cover only its persisted keys).

KNOWN FACTS: project memory claims: `chart_settings` is only a seed, the real layout is `charts_workspace_layout` → `widgets[].opts.settings`; the calendar widget key inside that layout is `calendar`; renaming persisted keys wipes saved views; `calendar_view_v3` implies a v2 migration. Verify each in code.

REQUIRED SOURCES: code; `CLAUDE.md` "Charts Hub V2", "Watchlists Page", "Journal 2.0", "Auth & User System" as CLAIMS.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only), `git log` on the worktree for the uuid hotfix and key migrations.
BUDGET: 130 tool calls or 65 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\01-existing-system\state-persistence-and-workspaces.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
