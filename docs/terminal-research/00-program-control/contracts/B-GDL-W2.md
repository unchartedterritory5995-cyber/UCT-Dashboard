# CONTRACTS B-GDL-02 (capability verifier) and B-GDL-03 (idea extractor) — Gödel Terminal, Wave 2 (Document C Part IX)

Read `_EXTERNAL_PREAMBLE.md` first; it binds. KNOWN FACTS: read `03-competitive-research/godel/01-evidence.md` (B-GDL-01) in full first: it is the fixed evidence base (DL Software Inc., 2024, Shkreli; $996/yr or $118/mo + $30/mo FINRA surcharge; public beta; browser-based command-driven; no confirmed public API; thin social/video tier). Do not re-collect what it holds; extend it only where your questions require.

## B-GDL-02 — Capability verifier → `godel/02-verification.md` (MODEL Opus; BUDGET 110 calls / 75 min)
For every capability B-GDL-01 catalogued (command line and mnemonics, functions, charting, fundamentals, news, screening, options, AI/natural language, Excel/API, community, pricing), determine the strongest class it reaches: VERIFIED (official docs/product page describing it as shipped) · DEMONSTRATED (official video/stream transcript or dated screenshot page showing it) · CLAIMED (founder/marketing statement) · REPORTED (third party) · SPECULATED. Use YouTube transcripts of official demos/streams (never infer from thumbnails), the changelog if any, docs pages, and the product's public pages in a browser tab (the site may be JS-rendered). Deliver a capability × class table with the artifact per cell, a "what is real today" narrative, a "what is promised" narrative, the design principles the product actually embodies (from docs and demos, not tweets), and the ceiling: what a trial seat (OI-18) would settle.

## B-GDL-03 — Idea extractor → `godel/03-ideas.md` (MODEL Sonnet; BUDGET 80 calls / 60 min; runs AFTER B-GDL-02 when possible — read it if present)
From the verified and demonstrated capabilities only, extract transferable product ideas for a small equities-and-options desk: AI-native workflows, speed of iteration, command grammar choices, density, natural-language interaction, market-data aggregation, personalization, idea discovery. For each idea: what Gödel does (class + artifact), why it might matter to UCT (persona/workflow), what it would cost to try (order of magnitude), and the risk of cargo-culting it (Part CLXX). Also list what Gödel gets wrong or that practitioners complain about (reported tier, labeled). No recommendations beyond hypotheses.

RETURN SUMMARY ≤150 words each. OUTPUT STRUCTURE, CONFIDENCE, SOURCE HANDLING, DO NOT per `_EXTERNAL_PREAMBLE.md` (binding). Do not spawn sub-agents.
