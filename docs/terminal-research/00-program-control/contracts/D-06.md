# CONTRACT D-06 — Terminal UI Architecture Engineer (current UI primitives)

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-06
ROLE: Terminal UI Architecture Engineer (Group D)
MISSION: Evaluate the dashboard's existing UI building blocks against what a terminal shell needs — the `/charts` customizable workspace, widget registry, grid/dock behavior, shell/navigation, tables, chart components, search/command surfaces, modals, density/theme — and state, with code evidence, which could become Terminal-Next primitives (shell, panel, grid, table, chart, search, security header, freshness badge) and which are single-purpose.
SCOPE: `app/src/` in the dashboard worktree. Read-only.

QUESTIONS:
1. The `/charts` Charts Hub V2 workspace: widget registry (types, ids, default sizes, settings schema), `react-grid-layout` configuration (breakpoints, drag/resize, persistence hook — name the persistence keys but D-11 owns their schema), the multi-chart grid mode, widget-level settings, linked-symbol or shared-context behavior if any, empty/error states per widget, performance guards (render limits). Which widgets exist (e.g., `CalendarWidget.jsx`) and how they embed other pages' data.
2. Shell and navigation: `Layout`, `NavBar`, nav tab registry / door keys (CLAUDE.md "Nav Tabs"), icon glyph system (`UIcon`), mobile nav/`MoreSheet`, the dashboard "session cockpit"; how a new surface is registered in the shell (list the touch points).
3. Tables: every table implementation used for dense data (watchlists, screener, options flow board, UCT20, breadth); shared vs bespoke; virtualization; column customization; sorting/filtering; keyboard navigation; live-update behavior (row jumping, flicker guards); copy/export.
4. Charts as components: the chart pane API (`ChartRender`, lightweight-charts wrapper, indicator engine, presets, compare mode, share/export), how charts are embedded elsewhere, multi-instance support.
5. Search and command surfaces: any global search, ticker input components, symbol autocomplete, `AiSearchPage` UI, keyboard shortcuts (react-hotkeys-hook bindings; the Shift+F tool collision fix; `event.code` latching), command-palette-like affordances.
6. Modals and overlays: the calendar earnings modal system (tokens `--glass-*` vs `--menu-*`; fixed 720px; tabs), other modal frameworks, focus management.
7. Density, theme, tokens: theme switching (oled/light), spacing/typography scales, number formatting components, status/freshness indicators (LIVE, delayed, as-of), color semantics for gain/loss, accessibility posture (focus states, contrast).
8. Reusability verdict table: for each candidate primitive (TerminalShell, Panel, Grid/Dock, Tabs, Table, Chart, Search/Command, SecurityHeader, Metric/Sparkline, FreshnessBadge, Empty/Error/Loading, ContextMenu): exists / exists-but-limited / reusable / needs-extension / absent, with the file that would be the seed and the main limitation.
9. Anti-patterns and debt relevant to a terminal: giant components, duplicated fetch logic in components, provider calls from UI, unversioned layout state, global state abuse, CSS-module id hashing hazards.

OUT OF SCOPE: full page inventory (D-01); persisted state schema (D-11); calendar feature semantics (D-09); performance measurement (D-05).

KNOWN FACTS: deps present: react-grid-layout, @dnd-kit/*, @tanstack/react-virtual, lightweight-charts, echarts, chart.js, recharts, react-hotkeys-hook. Project memory claims: `chart_settings` is only a seed and the real layout lives in `charts_workspace_layout` → `widgets[].opts.settings`; the calendar modal is the only surface on `--glass-*` tokens; the touch tier is ≤1024; CSS Modules hash bare `#id` selectors. Verify each in code.

REQUIRED SOURCES: code; `CLAUDE.md` "Charts Hub V2", "Multi-Chart Grid Mode", "Charts — Lightweight Charts v5", "Nav Tabs", "Watchlists Page" as CLAIMS; `docs/ui-consistency-audit.md`, `docs/brand-design-system.md`.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only).
BUDGET: 140 tool calls or 70 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\07-technical-architecture\current-ui-architecture.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
