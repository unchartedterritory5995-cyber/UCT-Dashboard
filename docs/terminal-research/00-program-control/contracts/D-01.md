# CONTRACT D-01 — Existing Front-End Archaeologist

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-01
ROLE: Existing Front-End Archaeologist (Group D, internal system team)
MISSION: Produce the authoritative inventory of the dashboard front end (`app/`) as it actually is: tooling, routing, pages, components, state and data-fetching patterns, design system, tables, charts, keyboard, responsive system, and code health, so that architecture roles can ground every Terminal-Next recommendation in real code.
SCOPE: `C:\Users\Patrick\uct-worktrees\terminal-research\app\` only (Vite + React SPA). Read-only.

QUESTIONS (answer each; cite path:line):
1. Tooling: `package.json` scripts and every dependency with its actual use site (grep for imports); Vite config (chunking, aliases, the parity-bands config); ESLint/stylelint rules that constrain code; `test-setup.js`, `test-stubs`, `test-utils.jsx`.
2. Routing: `src/App.jsx` and `src/routes/`: the full route table (path → page → auth requirement → lazy chunk), the `lazyPage` / door-prefix registry ("longest registered prefix wins"), the `/r/*` render routes (who consumes them), `AuthGuard`, redirects, 404. State explicitly how `/calendar` and `/calendar/mystocks` are registered (do not analyze the calendar internals; D-09 owns them).
3. Pages inventory: every file in `src/pages/` and subfolders: route, purpose (one line), auth/tier gating if visible, status (active / beta / dark / dead / duplicate). Flag pairs that look like parallel rebuilds (e.g., `LiveFlow.jsx` vs `LiveFlowMassive.jsx`, `journal-2-0/` vs older journal, `ComingSoon.jsx`).
4. State and data fetching: what libraries or patterns carry server state (fetch wrappers in `src/lib/`, hooks in `src/hooks/`, any SWR/react-query/zustand/redux — say "none" if none), `src/context/` providers, how auth state and preferences reach components, polling vs websocket hooks (`useFlowWebSocket.js` — name only; D-05 owns real-time).
5. Shared components: `src/components/` and `src/widgets/` inventory with purpose; the shell (`Layout`, `NavBar`, `MobileNav`, `MoreSheet`, `UIcon`), modals, error boundaries, empty/loading/error states.
6. Design system: `src/styles/`, `index.css`, `App.css`, theme tokens (`data-theme` oled/light), CSS Modules conventions (note the hashed `#id` selector hazard), `docs/brand-design-system.md` (claims), typography/spacing/density tokens, number-format utilities in `src/utils/` or `src/lib/`.
7. Tables and grids: how large tables are rendered today (`@tanstack/react-virtual` use sites; any shared table component; sorting/filtering/column-preference patterns; CSV export if any).
8. Charts: where `lightweight-charts`, `echarts`, `chart.js`/`react-chartjs-2`, `recharts` are each used and why four charting libraries coexist; the chart component boundary (`ChartRender.jsx`, `pages/charts/`).
9. Keyboard and interaction: `react-hotkeys-hook` bindings and any global shortcut registry; `@dnd-kit` and `react-grid-layout` use sites (name them; D-06 evaluates the workspace).
10. Responsive/mobile: the touch tier (≤1024) system, mobile CSS files, mobile nav; what breaks density.
11. Code health: dead code, duplicated components, experimental flags in the client, TODO/FIXME density, largest files, and anything "surprisingly sophisticated" or "clearly built for expansion".
12. Test coverage shape: count test files per area; how components are tested; what is untested.

OUT OF SCOPE: calendar internals (D-09); the `/charts` workspace evaluation and UI primitives judgment (D-06); persisted state schema (D-11); flags/entitlements semantics (D-10); AI pages internals (D-12); performance measurement (D-05); backend.

KNOWN FACTS: `app/package.json` has 44 deps / 16 devDeps; scripts: dev, build (vite build + two facts scripts), lint, lint:css, test (vitest run). Key deps present: react-router-dom, react-grid-layout, @dnd-kit/*, @tanstack/react-virtual, lightweight-charts, echarts, chart.js, recharts, react-hotkeys-hook, @tiptap/*. `src/pages/` holds ~150 files plus subfolders (admin, breadth, calendar, charts, community, cot, dashboard, desk, education, formulas, journal-2-0, modelbook, optionsFlow, parityBars, research, screener, theme-tracker, watchlist). The production site title is "UCT Intelligence — Coming soon" and `/calendar` is served by the SPA shell. Partner-owned: `OptionsFlow.jsx` (read, do not deep-dive).

REQUIRED SOURCES: the files themselves; `CLAUDE.md` sections "Nav Tabs", "Mobile Navigation", "Responsive / Mobile System", "Charts Hub V2", "Charts — Lightweight Charts v5", "UI Icons", "Project Structure" as CLAIMS to verify.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only listing/grep/wc), `git log`/`git blame` on the worktree for history questions only.
BUDGET: 150 tool calls or 75 minutes. Then write with GAPS.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\01-existing-system\frontend-archaeology.md` (write nowhere else).
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
