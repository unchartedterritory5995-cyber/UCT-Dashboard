# CONTRACT D-07 — Testing / Reliability / Observability Engineer (current state)

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-07
ROLE: Testing / Reliability / Observability Engineer (Group D)
MISSION: Document how the dashboard is tested, gated, monitored, and kept alive today — and confirm, by running them, the exact commands that make protection-rail check (2) reproducible in the research worktree.
SCOPE: dashboard worktree: `tests/`, `api/**/*_test.py`, `app/src/**/*.test.*`, `conftest.py`, `pytest.ini`, `app/vite.config.js`, `app/src/test-setup.js`, `.github/`, `docs/runbooks/`, `docs/operations/`, `docs/feature_flags.json`, monitors in `api/services/`. Read-only except running tests.

QUESTIONS:
1. Test infrastructure: vitest config and setup (stubs, JSDOM, timeouts, reporters), how components are tested; pytest config (testpaths `tests` + `api`, the repo-wide timeout rationale, `-p no:cacheprovider` conventions), the repo-root `conftest.py` shared-data pins and the `C:\data` tripwire (explain precisely what it protects and how a local run stays safe), chunked backend runs (~14 chunks claimed), mutation gauntlets, AST conformance gates (`docs/runbooks/ast-conformance-gate.md`), `tests/test_test_discovery_coverage.py`, `tests/test_feature_flag_ledger.py`, `tests/test_requirements_pins.py`. Counts: frontend test files, backend test files, api-embedded tests.
2. Protection-rail check (2), executed: run these and record the exact summary lines:
   * `cd C:/Users/Patrick/uct-worktrees/terminal-research/app && npx vitest run src/pages/calendar src/pages/charts/widgets/CalendarWidget src/components/AuthGuard.calendarDeepLink.test.jsx src/pages/journal-2-0/tabs/CalendarTab.test.jsx src/pages/journal-2-0/hooks/useJ2Calendar.test.jsx` (node_modules already installed; the orchestrator's baseline reported 31 files passed).
   * `cd C:/Users/Patrick/uct-worktrees/terminal-research && python -m pytest tests/test_calendar_*.py tests/test_dividends_calendar.py tests/test_catalyst_market_calendar.py -q -p no:cacheprovider` — if Python dependencies are missing on this box, do NOT install anything globally; report what is missing, whether a project venv exists anywhere the repo documents, and the exact command the owner uses (CLAUDE.md, `run-local.ps1`). If the run works, record passed/failed/skipped counts and wall time.
   State whether the selected test set actually covers the calendar router, services, and frontend pages (list what each file covers), and propose additions only if a calendar-critical path is untested.
3. CI: `.github/workflows/*` — what runs on push/PR, whether any check gates a deploy (deploy is on push to master; CONFIRM from config).
4. Observability: Sentry setup (server and client), `/api/health` fields and their producers, `deploy_log`, watchdogs (`event_loop_watchdog.py`, `flow_watchdog.py`), monitors that post to Discord (provider coverage monitor, wire coverage monitor, post-ship checks), structured logging conventions, where errors surface for staff.
5. Reliability patterns: `serve_stale`, self-heal lease for the flow worker, deploy-survival invariants, warm-aware healthcheck, "serves last SUCCESS on failure", the healthcheck-path lore (never `/api/ready`); rollback mechanisms (flags, `railway redeploy`).
6. Feature-flag ledger as a rail: how `docs/feature_flags.json` + `feature_flag_index.py` + tests enforce flag hygiene (D-10 owns semantics; you own the enforcement mechanism).
7. Runbooks index: one line per file in `docs/runbooks/` and `docs/operations/`.
8. Gaps a terminal would expose: untested areas, missing contract tests for providers, no visual regression, no load tests, etc. — with evidence.

OUT OF SCOPE: flag semantics (D-10); infrastructure topology (D-04); performance numbers (D-05).

KNOWN FACTS: frontend rail run at 05:4x UTC passed 31 test files; Python on this box is 3.14.0 and `sqlalchemy` is not installed (it may not be needed); `pytest.ini` explains the timeout policy and testpaths; 33 backend files match `tests/*calendar*`.

REQUIRED SOURCES: the files; `CLAUDE.md` "`C:\data` IS REAL", "Running Locally" as CLAIMS.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only plus the two test commands above). Never run the full suite. Never start a backend server.
BUDGET: 120 tool calls or 60 minutes (test runs included).
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\01-existing-system\testing-reliability-observability.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
