# CONTRACT D-03 — Existing Data/API Archaeologist (dashboard providers)

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-03
ROLE: Existing Data/API Archaeologist (Group D)
MISSION: Inventory every external data, AI, and infrastructure provider referenced by the dashboard repository, how each is actually used, and the strongest status evidence available for each (KEY-PRESENT / CODE-REFERENCED / OBSERVED-CALLED), so the provider ledger (gate item 4) can be built without guessing and no duplicate vendor is ever recommended.
SCOPE: `C:\Users\Patrick\uct-worktrees\terminal-research\` — `api/`, `app/`, `services/`, `tools/`, `scripts/`, `.env.example`, `docs/`. Read-only. Dashboard repo only; D-14 covers the other repositories.

QUESTIONS:
1. Provider roster: for every external provider (start from `.env.example` variable names and grep for hosts/SDKs), record: name; category (bars/quotes, options/flow, fundamentals, estimates, earnings, economic, news, social, filings, AI/LLM, broker, email, payments, error monitoring, storage, messaging); env var NAMES; modules and functions that call it (path:line); endpoints or SDK methods consumed; data classes obtained; refresh cadence and caching around it; rate-limit handling; fallback ordering among providers for the same data class (e.g., prev-day OHLC from Massive with yfinance fallback).
2. Status evidence per provider: CODE-REFERENCED with the strongest call site; OBSERVED-CALLED only if a repository artifact shows recent production calls (coverage monitors, `docs/perf-baseline.md`, deploy logs, monitor reports, dated JSON facts under `app/src` build scripts, cached data files with timestamps). Say exactly which artifact. If nothing in the repo shows production calls, the status stays CODE-REFERENCED. Do not call any provider.
3. Retired or dormant providers: keys present with no live call path (the pre-Massive live-flow provider whose `403 "API subscription inactive"` reads like billing; anything else). Classify KEY-PRESENT-only. Note `/live-flow` vs `/live-massive`.
4. AI providers: Anthropic, OpenAI, Perplexity — which lanes call which (name the lane modules; D-12 owns behavior), `DEEP_RESEARCH_MODEL`, batch/caching flags (`LLM_BATCH_ENABLED`).
5. Browser-side keys: any `VITE_*` variables that ship to the client (`VITE_PICOVOICE_ACCESS_KEY`) and why.
6. Infrastructure providers: Stripe, Resend, Sentry, Discord webhooks, Cloudflare R2, YouTube/Buffer if referenced, Railway-specific env.
7. Data-class coverage table: rows = data classes a terminal needs (quotes, bars intraday/daily, options chain/flow, dark pool, fundamentals/statements, estimates, earnings calendar/history, economic calendar, news, social, filings/transcripts, corporate actions, ownership/insiders, short interest, analyst actions, breadth, futures/COT); columns = provider(s) used today, status, evidence, known limits (FMP 4,000-row truncation; 404 endpoints even on Premium: upgrades/downgrades, earnings surprises, general news; Finviz header case; Massive boto3 checksum params). Mark "no provider" honestly.
8. Provider-facing code quality: is there a provider abstraction or are vendor schemas used directly in routers/components? Where do vendor ticker formats leak?

OUT OF SCOPE: licensing terms (E-01..E-04); non-dashboard repos (D-14); DB schema (D-04).

KNOWN FACTS (from `.env.example` variable names): STRIPE_*, RESEND_API_KEY, SENTRY_DSN, MASSIVE_API_KEY/MASSIVE_SECRET_KEY, FINNHUB_API_KEY, ANTHROPIC_API_KEY, OPENAI_API_KEY, DISCORD_WEBHOOK_URL, FINVIZ_API_KEY, VITE_PICOVOICE_ACCESS_KEY, PERPLEXITY_API_KEY, FRED_API_KEY, FMP_API_KEY, ALPHAVANTAGE_API_KEY, DEEP_RESEARCH_MODEL, TWITTERAPI_IO_API_KEY/ENABLED, REDDIT_CLIENT_ID/SECRET, THEFLY_API_KEY/BASE_URL, WORKER_ENABLED, RECONCILE_ENABLED, SNAPSHOT_*, DELTA_*. Seed facts: FMP Premium; Massive.com bars via S3-compatible API (formerly Polygon.io — verify by code/comments); Finviz screener with "Small+ (over $300mln)" floor; Schwab (partner-owned router); yfinance fallback; R2 bars snapshots bucket. `docs/feature_flags.json` is a flag ledger.

REQUIRED SOURCES: the code; `docs/feature_flags.json`; `docs/runbooks/*`; `CLAUDE.md` "Data Sources", "Data Pipeline", "Live Pricing" as CLAIMS.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only). No network calls.
BUDGET: 150 tool calls or 75 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\02-data-providers\provider-inventory-dashboard.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
