# CONTRACT D-13 — Proprietary Content and Intelligence Inventory (raw)

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-13
ROLE: Proprietary content and intelligence inventory specialist (Group D). Your raw inventory feeds the canonical `proprietary-advantage-inventory.md` written later by synthesis (gate item 11).
MISSION: Discover, across all repositories and both machines, every proprietary or differentiated asset UCT already has — content, signals, scores, rulings, process, community data, education — with location, volume/history, quality, permissions, and how each is consumed today, and map the "UCT way" (OWNER_SEED_FACTS §6) to concrete code. Do not assume; inspect.
SCOPE: dashboard worktree (`api/`, `app/src/`, `docs/`, `themes_taxonomy.json`), `C:\Users\Patrick\uct-intelligence` (engine, `Setups/`, `analysis/`, `data/`, `uct_intelligence.db` read-only via `sqlite3` SELECTs on the LOCAL copy only), `C:\Users\Patrick\morning-wire` (archive, state, docs), `C:\Users\Patrick\uct_intelligence` (bot `data/`, `memory/`, brain outputs), `C:\Users\Patrick\uct-sunday-scan`. Read-only.

QUESTIONS — for each asset class below: what it is; where (repo/path/DB/table); volume and history depth (row counts or file counts you can read locally; production DBs on the Railway volume are NOT reachable — say so); structure; quality signals; who may see it (member/internal); how it is consumed today (page, post, Discord, Substack, YouTube); how it could combine with public market data (one line, 🟡 interpretation):
1. Morning Wire: archive of issues (how far back; HTML/JSON; segments; scores — exposure score, regime, distribution days, FTD rulings, semis tilt), the critic, the Board segment, autosend gate.
2. UCT20 harness and Book: record, positions, stops, rulings (`UCT20_INCEPTION`, `BOOK_RECORD_START`), leadership floor, bars sync.
3. Model Book, Setup Library, Base-structure library (30 structures, gates), Candle library, pattern/indicator engine (pine parity), screener (finviz-based; implied move; deep work), Theme Tracker taxonomy (`themes_taxonomy.json`), Catalysts (multi-tier), Twitter/X news ingestion, Buzz (Discord ticker mentions; 30-day backfill), COT positioning rail, Breadth system (collectors, lenses, presets, daily tab, live), options flow / dark pool / GEX / dealer positioning aggregates and records, index close posts, Sunday scans (archive, public), Desk sessions (YouTube uploads, titles, insights, clips), education videos / curriculum (`docs/curriculum`), formulas, notebook/journal (member-generated; privacy), brain outputs (bot `brain/`, pack export), knowledge base (engine `uct_intelligence.db`: tables and row counts).
4. The UCT way in code: position sizing formula and regime-adjusted caps; market-cap floors ($300M scanner, $500M leadership); Top 5 picks discipline and the four entry types; exposure owned by the wire score; distribution-day and follow-through-day rulings; any scoring (`Opportunity`, grades A+/B) — path:line for each rule.
5. Community/trading-room data: what Discord data exists (buzz counts, channels, activity), what is stored, what is private, what is derivable (ticker mention timelines, staff commentary).
6. Historical calls and commentary: is there a searchable history of what the team said about a ticker (wire archive by ticker, desk transcripts, journal notes)? Volume and accessibility.
7. Uniqueness ranking (🟡): which assets no competitor can replicate, which are merely convenient, with the evidence for volume/quality.

OUT OF SCOPE: AI lane internals (D-12); provider inventory (D-03); structural repo mapping (D-14) — cite D-14's future file path for structure.

KNOWN FACTS: seed facts §6 "UCT way" list; project memory: base-structure library has 30 structures with 3 published; candle library filter reads `candle_matches`; `/buzz` live counting #main-chat; UCT20 record restart authorities; `DESK_PUBLIC_SHOWS="*"`; wire is the paywalled item.

REQUIRED SOURCES: code and data files; local SQLite via read-only `sqlite3 <db> "SELECT name FROM sqlite_master"` and `SELECT COUNT(*)` only (never on any file under `C:\data` and never on production); `CLAUDE.md` files in each repo as CLAIMS.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only; `sqlite3` or `python -c` SELECTs on local repo DB copies only).
BUDGET: 160 tool calls or 80 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\05-product-strategy\proprietary-asset-inventory-raw.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
