# CONTRACT D-09 — Terminal-Current Surface Specialist (gate item 1)

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-09
ROLE: Terminal-Current surface specialist (Group D). You write the canonical `terminal-current-map.md` (Document B §49 item 1); you are its single writer.
MISSION: Map TERMINAL-CURRENT (the `/calendar` surface) completely — views, event types, filters, scopes, persisted preferences, modal, deep links, APIs, data sources, caches, jobs, embeds, member workflows, tests, history, known defects — and state what users would lose if it were removed. Study it; do not judge its future.
SCOPE: dashboard worktree: `app/src/pages/Calendar.jsx`, `app/src/pages/calendar/**`, `app/src/components/calendar/**`, `app/src/hooks/useMarketCalendar.js`, `app/src/pages/CalendarRender.jsx`, `app/src/pages/charts/widgets/CalendarWidget*`, notebook/journal calendar embeds, `api/routers/calendar.py`, `api/services/calendar_alerts.py`, `api/services/earnings_estimates.py`, `api/earnings_router.py`, catalysts/wire services it calls, related tests. Read-only, plus `git log` on these paths.

QUESTIONS:
1. Surface anatomy: every view (Wire, Board, Table, Month, Hub, day detail), every scope (My Stocks, Watchlist, Positions, UCT20, All), filters (market-cap tiers, "Filters · N", hidden/show-all behavior, sectors), week navigation and anchoring, session/time handling (`calendarTime.js`; timezone model), the header (`CalendarHeader.jsx`), the day table, the earnings modal (tabs; instant brief; analysts; ask-AI; charts; fixed 720px; glass tokens), the "reported name not shown yet" logic, ICS export, "seen" state, alerts, personalization, mobile behavior. For each: component file, hook, API call, persisted key.
2. Event types and data: earnings (before/after/TBD handling), economic calendar (sources), dividends, catalysts, sessions (Finviz sessions), past-day backfill, forward-week coverage ("one placement per symbol per week"), enrichment (batch, throttle, warmer), day metrics (avg volume), expected move / implied move (denominator rule), sector read, today supplement, live status. For each: provider and status (CODE-REFERENCED / OBSERVED-CALLED evidence), cache policy (two layers?), freshness.
3. API surface: every endpoint under `/api/calendar/*` and the earnings/catalyst endpoints the page calls: method, path, params, auth, response shape summary, cache headers/TTL, provider chain, error/stale behavior. Explain why an unauthenticated GET of `/api/calendar/week` returns the SPA shell (route shape or auth fall-through).
4. Persistence: every per-user preference the surface reads/writes (`calendar_view_v3`, `calendar_filters_v2`, `calendar_mystocks_sources`, seen state, others), where stored (settings API/table), defaults, migration history (v2 → v3), and the `calendar` widget-type key in `charts_workspace_layout`.
5. Jobs and warmers: server-side warmers, throttles, enrichment schedules, and any PC Task Scheduler jobs the calendar depends on (name only; D-14 confirms the scheduler entries). Label CLAIM vs CONFIRMED.
6. Integrations and inbound links: `CalendarWidget` in `/charts`, `CalendarEmbed`/`CalendarTab` in notebook/journal (same data path or separate?), `/r/calendar` render route → chart renderer → Discord week posts, Morning Wire links, the `?earnings=SYM&esection=` deep link, `/calendar/mystocks` hub, Discord bot commands referencing the calendar (grep in `C:\Users\Patrick\uct_intelligence`).
7. Member workflows: from the UI, docs, and wire integration, describe the concrete daily workflows the surface serves (e.g., "which of my names report this week", "what does the earnings modal give before the print", "week post"), and who uses which scope. Mark inference as 🟡.
8. Tests: list every frontend and backend test file covering the surface with one line on what each pins; note untested critical paths.
9. History: `git log` of the calendar surface — origin, the research-calendar redesign (2026-08-03), instant brief, modal speed, forward-week coverage, modal rebuild (2026-08-31), display rename (2026-09-01, `88b87a32b`); what the owner asked to keep (route, keys).
10. Known defects and debt: TODOs, open issues in docs, mobile audit findings, performance notes — terminal-relevant only.
11. WHAT USERS WOULD LOSE: an explicit list, capability by capability, if `/calendar` disappeared — the seed of the legacy parity matrix (appendix CDLXII).

OUT OF SCOPE: coexistence options (D-08); UI primitive reuse verdicts (D-06); performance measurement (D-05).

KNOWN FACTS: production render on 2026-09-02 05:46 UTC (authenticated) showed header "UCT Terminal", tabs Wire/Board/Table/Month, scopes My Stocks/Watchlist/Positions/UCT20/All, "Filters · 1", Hub, MON 31 … FRI 4 with counts 0, cap chips All/$1B+/$10B+/$100B+, "0 reporting · 145 hidden — Show all", "Week of Aug 31 – Sep 4, 2026", "1 reported name not shown yet: PANW", "No reporters scheduled". 12 frontend and 33 backend test files match "calendar". Seed facts: FMP earnings/economic calendars work; earnings history joins on `acceptedDate`; FMP truncates at 4,000 rows silently; forward-week rule "one placement per symbol per week"; expected-move blank-in-prod lesson ("ask the denominator").

REQUIRED SOURCES: code; tests; `git log`; `CLAUDE.md` sections mentioning calendar as CLAIMS; `docs/plans/*calendar*` if present.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only), `git log`/`git show` on the worktree. No production calls.
BUDGET: 160 tool calls or 80 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\01-existing-system\terminal-current-map.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
