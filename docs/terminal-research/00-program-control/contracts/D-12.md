# CONTRACT D-12 — Existing AI Systems

Read `_SHARED_PREAMBLE.md` first; it is part of this contract.

ID: D-12
ROLE: Existing AI systems specialist (Group D)
MISSION: Map every AI/LLM capability the ecosystem already runs — surfaces, lanes, models, retrieval, grounding, budget guards, caching/batching, telemetry, evaluation harnesses, and which are member-facing — so the AI architecture for Terminal-Next extends real infrastructure and respects the cost doctrine.
SCOPE: dashboard worktree (`api/` AI lanes and routers, `app/src/pages/AiSearchPage*`, earnings modal ask-AI, support chat, `chat_stream.py`, catalyst brain, desk insights), plus READ-ONLY cross-references into `C:\Users\Patrick\uct_intelligence` (bot: `brain/`, RAG in `bot/`, `memory/`), `C:\Users\Patrick\morning-wire` (wire brain / critic), and `C:\Users\Patrick\uct-intelligence` (any AI use). Two-line boundary descriptions for the non-dashboard repos; D-14 maps them structurally.

QUESTIONS:
1. Surfaces: every AI feature a user or staff member can invoke or receive: name, page/route or command, user class that sees it, what it does, latency class, whether output is cached.
2. Lanes and models: for each lane — module path, provider (Anthropic/OpenAI/Perplexity), model id or env (`DEEP_RESEARCH_MODEL`), prompt location, tool use / structured outputs, streaming, Batch API use (`LLM_BATCH_ENABLED` rollback flag), prompt caching, fallbacks.
3. Retrieval and grounding: what data reaches the model (structured tools vs stuffed context), any vector DB / embeddings / FTS5 (notebook FTS5; bot RAG), citation and provenance practices, timestamps/market-status context (appendix CDXXXIX), the `--grounding-audit` harness ("measures retrieval free" per project memory) and any evaluation corpus; the ai-search "fast lane" A/B notes (scores move with live data).
4. Cost control: budget guard modules (per-user caps, population reserve for scheduled lanes, per-lane ceilings), how caching interacts with the guard (the claim that caching without teaching the guard loosens the cap), cost telemetry, the doctrine that member traffic never uses the owner's Max seat — verify member-facing lanes use API keys, and note any lane that runs on a local Claude Code session (e.g., the desk YouTube description "local script ($0)").
5. Safety and trust: hallucination controls, disclaimers, fact/interpretation separation, `_safe() → ""` never-raise pattern in the wire brain, how failures render (the `.catch(() => null)` renders-failure-as-fact lesson).
6. Scheduled AI: the PC brain tasks (pre-market/open/midday/pre-close/post-market/pack export — names only; D-14 confirms scheduler entries), wire brain, wire critic, desk insights polish — what each produces and where it lands.
7. Gap table for Terminal-Next AI (Parts XXV, LXXXIII–LXXXVI, CCLXXXV–CCXCII): context-aware actions, entity resolution, tool permissions, context visibility, internal-source citation, caching policy, model routing — exists / partial / absent with the seed module.

OUT OF SCOPE: provider key inventory (D-03); licensing of AI processing (E-02); product recommendations beyond the gap table.

KNOWN FACTS: env names present: ANTHROPIC_API_KEY, OPENAI_API_KEY, PERPLEXITY_API_KEY, DEEP_RESEARCH_MODEL; project memory: `LLM_BATCH_ENABLED=0` is the rollback; models are never downgraded for cost; the earnings modal has an ask-AI feature; the AI search ecosystem has a grounding audit and a fast lane.

REQUIRED SOURCES: code in all four locations (read-only); `CLAUDE.md` sections mentioning AI, brain, search as CLAIMS; bot repo `CLAUDE.md`/`README.md`.
TOOLS AVAILABLE: Read, Glob, Grep, Bash (read-only). No model calls, no network.
BUDGET: 140 tool calls or 70 minutes.
FILE DESTINATION: `C:\Users\Patrick\uct-worktrees\terminal-research\docs\terminal-research\08-ai\existing-ai-systems.md`.
RESEARCH WAVE: 1. DEPENDENCIES: none.
OUTPUT STRUCTURE, CONFIDENCE FORMAT, RETURN SUMMARY, SOURCE HANDLING, SECRETS, DO NOT: as in `_SHARED_PREAMBLE.md` (binding).
