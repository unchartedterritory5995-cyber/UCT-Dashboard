# CRITICAL PATH — the questions that block major decisions

Format (Document A): question · decision it blocks · owning role · current confidence · what evidence would close it. Tier 1 = must know before product decisions begin (Document B §27A gate). Updated at every checkpoint.

| ID | Tier | Question | Blocks | Owner | Confidence | Closing evidence |
|---|---|---|---|---|---|---|
| CP-01 | 1 | What exactly is Terminal-Current — every view, API, persisted key, embed, inbound link, job — and what would users lose if it changed? | coexistence architecture, legacy parity matrix, first slice | D-09, D-08 | 🟡 (both maps accepted: 31 routes, 9 readers of `/api/calendar`, 32-item what-users-would-lose list, four coexistence precedents; ✅ **the `/r/calendar` consumer note is RECONCILED, 2026-09-25** — the two maps never disagreed on the negative claim (neither says the chart-renderer uses it; D-08 proves it by grep of `services/chart_renderer/`, which references `/r/chart` only). They disagreed on the positive one: D-08 §4.4 named **two** headless screenshot consumers, `morning-wire/substack/panelshot.py` and `uct-sunday-scan/sunday_scan/panels.py`, while D-09 §6.1 row 9 named only the first. Both now name both. ⚠️ Checked and NOT a defect while there: §6.1's "nine consumers" beside the summary table's "8 non-`/calendar` consumers" is correct arithmetic, because row 1 of the nine IS `/calendar` — recorded so nobody re-opens it as a count drift) | `01-existing-system/terminal-current-map.md` accepted; `10-roadmap/coexistence-current-mechanisms.md` accepted |
| CP-02 | 1 | Which providers do we already license and actually call (status ≥ OBSERVED-CALLED), per data class, and which keys are dead? | data architecture, cost model, every "we need vendor X" claim, Q16–18 | D-03, D-14 → F-03b | 🟡 (ledger DRAFTED: 48 rows, 20 core, 7 retirement candidates, gaps named; **OI-04 is now the only path to 🟢 — corrected 2026-09-25**: this cell named OI-03 as a blocker while CP-03 two rows down has recorded since 2026-09-23 that OI-03's answer arrived and was owner-confirmed. Same propagation failure CP-03's own cell was corrected for, one row apart — a resolved input has to be pushed to every row that cites it, not only to the row that owns it) | `02-data-providers/provider-ledger.md` with evidence per row; OWNER_INPUTS on contract status |
| CP-03 | 1 | Can member-facing display, storage, derived use, and AI processing of FMP / Massive / Finviz / news data be classified above Unknown? | MVP member scope, licensing register, cost model, Q19 | E-01, E-03, E-04 → E-02 → F-04 | 🟢 **RESOLVED 2026-09-19/20, corrected here 2026-09-23** — this row was stale; `OWNER_INPUTS_REQUESTED.md`'s own OI-03 entry has carried the real answer for days and nobody propagated it here, the exact second-authority-drift defect this program repeatedly names. Owner confirmed directly: Massive held at **Business/Enterprise** (not Individual); FMP **DDLA confirmed to exist**; Finviz/Finnhub/AlphaVantage/Schwab all confirmed (ESC-07/08/09/10). Net register effect: of 118 rows, 18 stay Restricted (was 81), 76 Likely Allowed (was 7), 12 Unknown (was 18). Re-confirmed directly with the owner again 2026-09-23. Remaining named exceptions: FRED (a structural per-series compliance task, not a provider-confirmation question) and yfinance (an explicit, recorded risk-acceptance per `OWNER_DECISIONS.md` D-004, not a licence). | resolved — `OWNER_INPUTS_REQUESTED.md` OI-03, re-confirmed 2026-09-23 |
| CP-04 | 1 | Does the existing `/charts` workspace (react-grid-layout, `charts_workspace_layout`, widget registry) constitute a reusable workspace primitive, and what state model exists? | fixed / modular / hybrid decision (Part XXI), Tier S eligibility of any workspace primitive, Q21–25 | D-06, D-11 → C5-01 → C5-03 | 🟡 (both internal inputs in: transplantable panel layer, no error boundaries, unversioned non-atomic persistence with a data-loss path) → **the gated deliverable is WRITTEN 2026-09-25**: `06-ux-and-information-architecture/fixed-modular-hybrid.md`, C5-03, **provisional lock HYBRID**, three commitments, and it RESOLVES the D-06/D-11 contradiction on the store by sequencing version-in-place as the bridge and an own store as the destination. It also closes C5-01's 🔴 customisation axis with a production aggregate (17 of 29 accounts hold a board, none empty, modal 5 widgets, 0 versioned, 0 unparseable — cohort is staff under `COMING_SOON_MODE`, stated as a ceiling). ⛔ Still 🟡, not 🟢: the closing evidence requires it **red-teamed**, and the remaining confirming input is **a desk-observed morning**, NOT OI-06 — ⚰️ the deliverable's first draft called OI-06 the confirming input, quoting `READINESS_REVIEW_DAY1` L337-338, a document written BEFORE the owner answered OI-06 on 2026-09-19; corrected same day inside the deliverable's §7. OI-06's real answer (four external tools held open by hand daily) supports hybrid without separating it from modular | `current-ui-architecture.md`, `state-persistence-and-workspaces.md`, then `fixed-modular-hybrid.md` red-teamed |
| CP-05 | 1 | What real-time and load envelope can the single-replica web pod sustain for a multi-panel, long-lived terminal client? | real-time architecture, performance budget, scope of live features | D-05 → C7-01 → ARCH-07 | 🟡 (single-process pooled SSE mapped; bars-api and flow-worker tiers already exist; deploy cold window ~10 min end-to-end as of 2026-09-23 — corrected from a stale "~3-min" figure written against the direct-push model; the pipeline now runs a `master deploy gate` workflow and a separate `promote to production` workflow ahead of Railway, per `LEDGER.md`'s own measurement on the R/F/Z/S ship; no load model yet) → **§8's SIX-PART PROTOCOL WAS EXECUTED FOR THE FIRST TIME 2026-09-26 00:59Z** (`evidence/2026-09-26-cp05-protocol-execution/`). ⛔ The cell's "no load model yet" is accurate but reads as "nothing exists": §8 has carried a complete six-protocol baseline plan, read-only, existing tools, since it was written — the gap was EXECUTION, not authorship. **Protocol D settles §3.3: the documented Cloudflare rule is NOT in effect on `/api/flow/data` — `BYPASS` on all three requests, not the `MISS→HIT` §8 defined as success**, so the days=20-shares-days=1 hazard cannot arise (⚠️ `BYPASS` does not separate a CF rule from the origin's own `cache-control`; that is one more curl). **Protocol A was run and is VOID by §8's own Governing Rule 1** — pod uptime was 112 s against its "under ~300 s invalidates a warm measurement", so its 0 % warm ratio measures a just-booted pod, not the serving layer; a re-run at ≥ 900 s is in flight and only that number may be quoted. ⭐ Protocol E is partly observed from five deploys watched tonight (record→SUCCESS 3m40s–6m40s, uptime resets immediately, `/api/*` answered throughout, fresh-pod page loads 3.1–3.7 s) with SSE-pool reconnection and warm-ratio recovery still unmeasured. ⛔⛔ **THE REAL REMAINING BLOCKER IS A DECISION, NOT A MEASUREMENT:** §8 deliberately generates no load, and roadmap Rule 4 bars running load against production, so a load model needs a NON-PRODUCTION TARGET that does not exist yet | `current-performance-and-realtime.md` with measured baselines; a protocol for Part CXX — ✅ both exist. **§8's named deliverable is now WRITTEN: `docs/perf-baseline-2026-09-26.md`** (one table per protocol, uptime context per row, explicit §4.3 deltas). Executed: B (0 FAIL/11), D (settles §3.3), F (the headline), E (partial). ⭐⭐ **Protocol F answers the question `api/main.py` calls "the prerequisite for any further memory work": the web pod LEAKS — 76 samples over the longest available 104-min deployment, quartile medians 2,429→2,746→2,956→3,028 MB, +7.9 MB/min monotonic. That REFUTES §4.3's 2.2 MB/s by ~17× and CORROBORATES its 11,665 MB long-lived endpoint (7.9 × 1,440 ≈ 11.4 GB).** Threads maxed 178, under the 200 burst line. ✅ **Protocol A got a VALID run at 01:16Z (uptime 942 s) and its headline number is a DEFINITION problem:** intraday **98 % `sqlite`, p50 65 ms** (the Aug-19 fix holds, was 0 % warm / p50 366 ms), while daily reads **0 % warm AND p50 104 ms at the same time** because §8 buckets `stale-swr` under "the user waited". ⛔ **The ≥99 % mem/sqlite definition of done is therefore unusable as a gate until it rules on stale-while-revalidate** — daily has been 100 % `stale-swr` since 2026-08-19, so this is the documented steady state, not a regression. The first attempt was void by §8's own uptime rule and a valid window took ~17 min of waiting through other workstreams' deploys — 14 deploys in 6.5 h, median pod life 26 min. Not run: C and H (need a visible tab), G (an env-var change that redeploys — left for the owner, and it is the cheap next step) |
| CP-06 | 1 | Which workflows does the desk run daily, and which external tools does it still open (Q1, Q6–Q10)? | MVP workflow selection, desk-tool benchmark slots, first slice | D-13, D-14, B-DESK-01..04, owner input OI-06 | 🟡 **CORRECTED 2026-09-23 — OI-06 was answered by the owner directly on 2026-09-19** (`DECISION_CARDS_2026-09-18.md` §7d, recorded in `OWNER_INPUTS_REQUESTED.md`): thinkorswim, TradingView, Finviz, and Unusual Whales are all opened by hand on a trading day, plus unitemized others; TradingView alerts are part of the workflow. This row previously said "still needs D-14 and the owner" — the owner half closed four days before this correction and was never propagated here, same drift class as CP-03. Not itemized by rank/time-spent (owner declined that level of detail as unnecessary) — sufficient to close this question at 🟡, matching how every other non-exhaustive 🟡 answer in this table is treated. | resolved — `OWNER_INPUTS_REQUESTED.md` OI-06 |
| CP-07 | 1 | Which proprietary assets have enough volume and quality to be the differentiator (H4)? | product philosophy, Tier S, moat (Q3, Q35, Q40) | D-13 → F-05 | 🟡 (raw inventory accepted: decision provenance and first-party narrative, with counts; production volumes remain a bounded unknown, DL-013) | `proprietary-advantage-inventory.md` with counts and code refs |
| CP-08 | 1 | Is the flag/entitlement mechanism sufficient for a dark Terminal-Next beta with per-user targeting? | coexistence Stage 1–3, rollout plan | D-10, D-08 | 🟡 (flags are env vars with an AST ledger; NO per-user cohort store, so Stage 3 needs one; `entitlements.py` is the natural seat) | `flags-and-entitlements.md` requirements table |
| CP-09 | 2 | How deep can Bloomberg workflow research go from accessible evidence, and where is the ceiling? | gate item 7, benchmark credibility | B-BBG-01..08, B-POD-BBG, B-BBG-DEEPEN (complete) | 🟡 (dossier complete incl. a multi-asset deepening pass — 33 of 36 owner-named topics covered with cited evidence, 3 honest ceilings remain: preferred-securities depth, cross-company relationship mapping, cross-screen regime-chaining) | `bloomberg/dossier.md` + `bloomberg/09-multi-asset-analytics.md`; remaining ceiling closes only via OI-08 (a seat, screenshot, or practitioner account) |
| CP-10 | 1 | What AI grounding infrastructure exists (retrieval, budget guard, lanes) and what member-facing AI is permitted under the cost doctrine? | AI architecture, Q31–35, cost model | D-12 → C6 → ARCH-05 | 🟡 → **CANDIDATE 🟢, corrected 2026-09-25** (a full AI platform exists to extend; member AI lanes run on API keys). ⛔ This cell's "licensing of AI inputs is the remaining unknown" **cites a row that is resolved**: its own closing evidence is "licensing (CP-03)", and CP-03 has read 🟢 RESOLVED since 2026-09-19/20 — and CP-03's question text explicitly covers *"derived use, and **AI processing** of FMP / Massive / Finviz / news data"*, which is precisely this unknown. So the stated blocker is answered. ⚠️ **The glyph is left at 🟡 deliberately rather than flipped by an agent**: the row's other half asks what member-facing AI is *permitted under the cost doctrine*, and that is an owner call, not a licensing fact. One read of the cost doctrine closes it either way | `existing-ai-systems.md` + licensing (CP-03) |
| CP-11 | 2 | Which of the ~36 PC-scheduled jobs feed surfaces a terminal would depend on, and what is the single-point-of-failure exposure of the owner's PC? | reliability architecture, dependency graph, risk register | D-14 | 🟡 (34 tasks confirmed with last-run results; six code locations; **seven** Railway services as of 2026-09-23 (`railway status --json`: web, worker, bars-api, flow-worker, chart-renderer, terminal-next-monitor, breadth-v2-runner) — corrected from a stale "five," the same hand-typed-count-beside-its-own-source defect this program repeatedly names elsewhere, and a second, previously-uncorrected copy of the identical drift already fixed once in `CLAUDE.md` this session; four silent failures found) | `ecosystem-cartography.md` scheduler appendix |
| CP-12 | 2 | Is there a benchmark product materially more relevant than the candidate list (options-flow terminals, AI-native tools) given UCT's options-heavy desk? | benchmark universe (gate 6) | B-VAL-01 (complete) | 🟡 (universe validated with 71 cited URLs; zero options-native products found in the candidate list — Unusual Whales/SpotGamma/Benzinga are the closest analogues and are already in the researched set) | `benchmark-universe.md` (accepted) |

Gate status (B §27A), **corrected 2026-09-23, twice in the same pass**: CP-03 moved to 🟢
on 2026-09-19/20 (owner-confirmed licensing) and CP-06 moved to 🟡 the same week
(owner-answered desk workflows, OI-06) — this line previously said "NOT OPEN" and "CP-06
still needs... the owner" on the strength of two stale citations nobody had propagated
resolutions into, both closed for days before anyone reread the source docs that actually
carry the answers. **Tier-1 count: 10 of 10 questions at 🟡 or better — the gate has been
open since 2026-09-19 and this file simply never said so.** No owner-input item remains
blocking Document B §27A's gate; whatever this program does next with that fact is a
product decision, not a research one.
