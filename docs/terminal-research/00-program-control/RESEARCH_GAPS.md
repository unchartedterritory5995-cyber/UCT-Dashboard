# RESEARCH GAPS (Document C Part CCLIII)

Questions requiring second-pass or targeted research. Populated as Wave 1 leaf reports land (their GAPS and NOT INSPECTED sections feed this file) and at every checkpoint.

| ID | Gap | Source report | Proposed follow-up (role, wave) | Status |
|---|---|---|---|---|
| RG-01 | GitHub branch-protection / required-checks state for `master` is not observable from this box. | D-07 §3, NOT INSPECTED | Owner input OI-13; or a read-only GitHub API call if a token becomes available. | owner-answerable |
| RG-02 | Rail selection was unguarded (substring filters) and missed three calendar-adjacent backend suites. | D-07 §1, gap 6 | FIXED on Day 1a: DL-009 tightened the rail. | closed |
| RG-03 | Live flag VALUES on Railway (e.g. whether `PATTERN_VISION_ENABLED=0` is actually set, which `*_ENABLED` gates are armed) are unknown; D-04/D-10 were restricted to key NAMES. | D-10 open q1; D-04 pending | Orchestrator-only read: `railway variables --service web --json` filtered to `*_ENABLED` / `*_MODE` keys (non-secret by construction), written to `02-data-providers/railway-flag-state.md`. Wave 2. | planned |
| RG-04 | Per-user cohort targeting for a dark beta does not exist; is `user_tags` a viable durable store, or is an env allowlist acceptable for Stage 1–2? | D-10 open q2, q3 | Fold into D-08/H-07 coexistence design; ask nothing of the owner yet. | planned |
| RG-05 | `StockChart.jsx` (15,500 lines, ~120 props) sits under the reusable `ChartPane`; whether decomposition is in scope changes the UI architecture estimate. | D-06 open q2 | ARCH-01..03 must state the assumption; red team G-02 challenges it. | planned |
| RG-06 | The four-colour link-group ceiling in `/charts` (multi-chart grid bypasses `ChartWidget` because of it). Does a typed-channel link model generalise? | D-06 open q3 | C5-01 survey + C5-03 comparison; prototype only inside the envelope if still uncertain. | planned |
| RG-08 | `/r/calendar` consumers reconciled by F-03a (R1): morning-wire's own Playwright and the Sunday Scan server PNG; the chart renderer never touches it. | D-08, F-03a | — | closed |
| RG-09 | `pages/Confluence.jsx` is a complete page on a live `/api/confluence` with no route and no importer (not in the reachability allow-list). Route, widget, or delete is a product decision for later; for the ledger it is DORMANT. | D-01 open q1 | Capability ledger (F-03a) marks it dormant; no dispatch. | planned |
| RG-10 | Density vs the 44px tap floor: compact rows are 30px and `DensitySwitcher` is built but unmounted. Whether Terminal-Next is a phone product decides the density model. | D-01 open q3; C Part LII | Personas/JTBD (F-07) + C5-02 personalization + ARCH-08 IA; owner default: desktop-first, phone = monitoring only (record in non-goals). | planned |
| RG-11 | Real size distribution of `charts_workspace_layout` / `chart_settings` in `user_preferences` and whether any member has hit the `parseLayout`→null→empty-board→autosave loss path; needs production data, which this program cannot read. | D-11 open q1, q2 | Owner-answerable only via a read-only query on the volume (NOT during this program); record as a bounded unknown for the state-model ADR. | owner-answerable / unknowable-this-week |
| RG-12 | Production cost-guard defect: five LLM price tables with one rail; `api/services/catalyst/cost_guard.py:33` still prices Sonnet 5 at the old rate after the 2026-08-30 fix landed only in `narrative_cost_guard`. ⚰️ This row said *"under-pricing loosens the daily cap"* — corrected 2026-09-22, re-verified against both files directly: `cost_guard.py`'s stale rate ($3.00/$15.00) is HIGHER than the corrected rate ($2.00/$10.00), so the defect OVER-prices every Sonnet 5 catalyst call and trips the $8/$15 daily caps EARLIER than real spend justifies — the opposite direction from what this row originally claimed. The code defect itself is unchanged and confirmed still live (three lanes affected: `curator.py`, `hunter.py`, `rule_learner.py`, all defaulting to `claude-sonnet-5`). | D-12 | Draft gate packet exists, unsigned: `12-decisions/gates/packet-r-catalyst-cost-guard-mispricing-gate.md` (2026-09-22) — single checkpoint, corrects both citations to $2.00/$10.00 plus the four dependent test assertions currently pinning the stale numbers. | packet drafted, awaiting signature |
| RG-13 | `CATALYST_OPUS_MODEL=claude-sonnet-4-6` in production: an Opus-named variable runs a Sonnet-class model. Intent unknown (doctrine: never downgrade for cost). | ORCH-RAILWAY-01 | Owner input OI-14. | owner-answerable |
| RG-14 | Master drift: six commits landed on `origin/master` during Wave 1; touched files are outside Terminal-Current; leaf citations remain valid at the start SHA. | ORCH-RAILWAY-01, DL-011 | Re-sync decision at a program-day boundary; F-03a re-verifies any citation into the buzz board, buzz services, or the formula manifest. | planned |
| RG-15 | The `transcript` and `calendar_forward_multisource` coverage fields had zero samples in the one observed monitor cycle; whether transcripts are live in production is unconfirmed. | ORCH-RAILWAY-01 | Read `/api/admin/provider-coverage` again after a market-hours cycle (Day 2 checkpoint). | planned |
| RG-16 | `MASSIVE_SECRET_KEY` is unset on web (seed facts say it mirrors `MASSIVE_API_KEY`); which code paths require it, and are they only on flow-worker where it IS set? | ORCH-RAILWAY-01, D-03 | **RESOLVED 2026-09-02 (Phase 3)**: all 4 `api/`-tree call sites traced; every one is shadowed by a present fallback key (MASSIVE_API_KEY or MASSIVE_S3_SECRET) on every service that reaches it — leaving it unset on web changes no observed behavior. The one no-fallback consumer (`build_intraday_cache.py`) is a documented local-only script. Genuinely undetermined: why flow-worker has it set at all, since its likely real consumer (`live_massive_router.py`/`massive_ws_worker.py`) is partner-owned and outside this program's read scope. | **RESOLVED** (one sub-question genuinely open, partner-file-bound) |
| RG-17 | Production volumes of the proprietary assets (45 dashboard DBs on the Railway volume) cannot be counted by this program (DL-013). | D-13 open q1 | Bounded unknown; D-13 lists the exact read-only queries for the owner's own operations session. | owner-answerable |
| RG-18 | Base-structure library: D-13 counts 25 structures in the lift ledger; project memory says 30. | D-13 open q2 | F-05 reconciles from the ledger file (the artifact wins over memory). | planned |
| RG-19 | Does the Discord bot (`uct_intelligence`, no git, no scheduled task, no process observed on this PC) still run anywhere, and where? | D-14 open q1 | Owner input (fold into OI-16); provider ledger marks its integrations UNCONFIRMED-RUNTIME. | owner-answerable |
| RG-20 | Is the breadth 'Data Charts never mounted a chart' result (30 monitor runs) a real defect or selector drift in the monitor? | D-14 open q3 | Reported to the owner; not a program task. | reported |
| RG-21 | Five clause-vs-code collisions E-04 found (FRED attribution notice missing; FRED caching; X display requirements in `TapeFeed.jsx`; no tweet deletion-sync; `catalysts.db` retains tweets past the 7-day window). Production defects, fixable in a normal session; not touched by this program. **Re-verified 2026-09-22, all five still hold exactly as described.** New: (a)/(b) FRED's gap is currently DORMANT — `FRED_API_KEY` is unset on `web`, so the module short-circuits before ever fetching, though the code gap is real and free to close. (c) confirmed frontend-only — the author name/handle fields are already in the API payload, just not rendered. | E-04 | Draft gate packet exists, unsigned: `12-decisions/gates/packet-s-licensing-clause-code-collisions-gate.md` (2026-09-22) — 5 independently-approvable checkpoints, one per sub-finding. | packet drafted, awaiting signature |
| RG-22 | The Massive tier -- STRENGTHENED (recovery checkpoint grep sweep): FIVE independently-authored code comments across `api/services/polygon_extras.py:4`, `polygon_options.py:3`, `polygon_news.py:7`, `voice_tool_impls.py:2351`, and `voice_agents.py:410-411` all name the SAME specific plan -- "Polygon Advanced, $200/mo" -- not just a bare dollar figure. "Advanced" is Polygon.io's legacy individual/personal-use tier naming (pre-Massive rebrand), distinct from the "Business"/"Enterprise" tier that grants Edge User redistribution rights (E-01's finding). No contract, invoice, order form, or DDLA file exists anywhere in any of the five UCT repositories (confirmed by a targeted file-type sweep: no `*invoice*`, `*contract*`, `*agreement*`, `*license*.pdf`, or `*billing*` file found). This remains a CLAIM (developer comments, not a verified account page), but it is now five-times corroborated and specific, which is materially stronger than the single-comment reading E-04 first surfaced. | E-04 open q1; orchestrator grep (recovery checkpoint) | Owner input OI-03(a) still formally decides (a comment is not CONTRACT-ACTIVE evidence); recommend the owner CONFIRM rather than research from scratch -- the internal evidence already points strongly at Individual/Advanced. The licensing register's default scenario (Massive Individual, no DDLA) already matches this evidence, so no artifact needs to change; work continues provisionally on that default either way. | owner-answerable, high-confidence default |
| RG-23 | Whether UCT's calendar/earnings pipeline stores per-ticker implied-vs-actual move calibration history (Market Chameleon's differentiator), and whether the screener computes a market-wide relative options-volume ratio. The B-DESK-04 contract asserted D-13 cites implied-move code; D-13 does not, so the fact must be settled from the capability ledger (F-03a) or a targeted code read in Wave 2. | B-DESK-04 | F-03a capability ledger rows for implied move / expected move / screener; if absent, a 30-minute D-09-style code read. | planned |
| RG-24 | Which ticker-mentions door is live (capability ledger reconciliation L-6 lists two candidates). | F-03a | **RESOLVED 2026-09-02 (Phase 3)**: backend is live and mounted (`GET /api/education/tickers/{sym}/mentions`); the frontend hook (`useTickerMentions.js`) is written but has zero non-test importers anywhere — the intended TickerPopup "Desk" tab and chart-marker integration were never wired. Capability ledger L-6 corrected to "backend LIVE, frontend NOT BUILT." | **RESOLVED** |
| RG-25 | Whether the edge caches anything on the flow endpoints and whether UCT's SSE streams emit `id:` and honour `Last-Event-ID` (reconnect without gaps). Three read-only `curl -I` calls plus one grep settle it. | C7-01 open q2, q3 | **RESOLVED 2026-09-02 (Phase 3)**: edge caching is deliberately ON (with documented incident history) for the buffered flow JSON/CSV endpoints and deliberately OFF for SSE/mutating endpoints — live-verified (401/BYPASS on flow, 200/DYNAMIC on the SSE stream). SSE streams are confirmed gapless-reconnect-blind: zero `id:` field emission anywhere in the `api/` tree, zero `Last-Event-ID` handling — a cold resubscribe on every drop. One sub-question genuinely open: the flow 200-success-path's actual Cloudflare Cache Rule (vs. origin-header intent) could not be verified without valid session credentials, which were not attempted. | **RESOLVED** (one live-credential sub-question deferred) |
| RG-26 | Panels per user: every inherited capacity number assumes users, not panels; the load model needs a panels-per-session assumption. | C7-01 open q1 | ARCH-07 states it as a labeled assumption; dogfood telemetry later. | planned |
| RG-27 | Whether a dock library's popout (dockview/FlexLayout) preserves the opener's React tree, i.e. keeps UCT's one-SSE-pool-per-browser property; and which FDC3 standard Context types would fit UCT's payloads (symbol, symbol-set, list-ref, timeframe, range). | C5-01 open q1, q2 | Candidate prototype inside the envelope (one afternoon) only if the fixed/modular/hybrid comparison (C5-03) makes it decision-relevant. | planned |
| RG-07 | Is Terminal-Next a route inside the existing `Layout` shell or its own shell? Every reuse estimate turns on it. | D-06 open q1 | D-08 coexistence options + ARCH proposals answer it as a decision with code reality; not a research gap per se. | planned |
| RG-29 | The existing provider ledger (F-03b, 48 rows) does not yet break each provider into the owner's requested per-asset-class capability matrix or A-G status taxonomy, nor consistently separate technical access from contractual redistribution/storage/derivation/AI-processing rights at the per-capability level. | Owner instruction (/btw), DL-022 | **RESOLVED 2026-09-02**: `02-data-providers/provider-master-ledger.md` (F-09) delivers the full 17-category, A-G-classified matrix with per-capability rights separation, integrated into `07-technical-architecture/data-architecture.md`. | **RESOLVED** |
| RG-31 | Which of UCT's two existing regime classifiers (the engine's `market_regimes` table vs. the dashboard's own classifier, H6) is the single authority for the Breadth/Regime/Positioning system (A11) and the Intelligence Layer's verdict gate. Surfaced by Phase 2's product-architecture.md design work, not previously known as a gap. | Phase 2, `product-architecture.md` A11 | **RESOLVED 2026-09-02 (Phase 3)**: `voice_regime_classifier.get_current_regime()` is the single live authority — wired into `grade_ticker`'s verdict gate, the Awareness Engine's regime-flip rule, and `brain_service`'s fallback. The engine's `market_regimes` table has exactly one dashboard reader (`/api/risk-summary`) with zero frontend callers — dead code, correctly excluded from scope rather than reconciled. See RG-32 for a genuine, related finding this investigation surfaced. | **RESOLVED** |
| RG-32 | **Production finding, outside program scope, reported for a normal operations session.** A THIRD, unrelated "regime" surface exists: `journal_two/regime.py::classify_regime()` is a 4-tier bucketing of the UCT Exposure Rating score alone (not an independent price/breadth read), exposed at `/api/j2/regime` and consumed by Journal 2.0's `RegimeSection`/`CompassOverview`. It is also what `coach_chat.py` injects as ambient "today's regime is X" system-prompt context in Compass **text chat** — while that same chat's `get_regime` **tool** call returns `voice_regime_classifier`'s different 5-way label. A member can see two different regime words in one Compass conversation today. **Re-verified 2026-09-22 against current source (all line numbers re-located, not restated) — still holds exactly, and the real surface is bigger than originally filed**: a persisted `j2_trades.regime` DB column (stamped at trade creation, back-filled historically), a member-configurable position-sizing feature (`regimeSizeMultipliers`) keyed on the same 4 words, a second independent ambient-injection site in `pre_trade_verdict.py`, and a *5th* colliding vocabulary already shipping (`quote_of_the_day.py`'s Aggressive/Constructive/Neutral/Caution/Defensive) — ruling out this row's own suggested rename target ("exposure tier," verbatim) as a fresh collision. | Phase 3, D13 investigation (`api/services/journal_two/coach_chat.py:155-165`, `coach_chat_tools.py:1788-1792`) | Draft gate packet exists, unsigned: `12-decisions/gates/packet-w-journal-two-regime-vocabulary-gate.md` (2026-09-22) — presents rename-vs-reroute with a grounded recommendation for **rename** (rerouting would silently break the sizing feature and fragment `j2_trades.regime`'s historical vocabulary mid-series). | packet drafted, awaiting the owner's CHOOSE |
| RG-28 | THIRD PAUSE (recovery checkpoint) left seven Wave-2 assignments incomplete. | Recovery checkpoint, DL-020 | RESOLVED 2026-09-02: six of seven re-dispatched and accepted this session (B-POD-BBG incl. a multi-asset deepening pass, B-POD-GDL, C2-01, C5-02, C7-02, C7-03 — see `AGENT_REGISTRY.md`); F-06's Day-1-synthesis deliverable also completed, fact-checked, and corrected. The seventh, C2-02 (events intelligence), was never re-dispatched — its 635-byte stub still sits at `05-product-strategy/domain-events-intelligence.md`, untracked, discarded per this row's original instruction. Tracked forward as RG-30. | **RESOLVED** (six of seven); one (C2-02) rolled forward to RG-30 |
| RG-30 | C2-02 (events intelligence — unified catalyst timelines: earnings/economic/corporate-actions/conferences/FDA/investor-days/index-rebalances; time-of-day and TBD handling; expected-move integration; live-call workflows) was never completed. Its only artifact is a 635-byte frontmatter-only stub, explicitly discarded (not a completion base). | RG-28 rollover | Full re-dispatch from the original contract, Tier 2 (Sonnet High) per DL-020, when the next research wave is authorized. | planned, not yet dispatched (owner-directed research pause in effect) |
| RG-35 | `api/routers/education.py` has a 4-route category-management cluster (`GET /categories`, `POST /reorder`, `POST /categories/rename`, `PATCH /categories/{name}`) — three admin-write routes plus one paid-read route, all genuinely implemented (`api/services/education_service.py`) — with **zero frontend callers anywhere in `app/src`** (fresh case-insensitive grep, three query forms, all empty). `app/src/pages/desk/VideosSection.jsx`, the sole admin surface for the Desk video library, only ever calls the per-video `POST`/`PATCH`/`DELETE /api/education/videos` trio; its category field is a free-text `<datalist>` sourced from the `/videos` payload's embedded `categories[]`, never from the dedicated `/categories` endpoint. No drag-to-reorder-within-a-category control, no rename-a-category control, no category-meta (`kind`/`sort_order`/`blurb`) editor exists anywhere. Re-verification also found `POST /reorder` has no HTTP-level test — only a direct service-layer call in `tests/test_education.py:80` — so "covered by backend HTTP tests" holds for three of the four routes, not all four. | Fresh independent re-verification pass, 2026-09-22 | Draft gate packet exists, unsigned: `12-decisions/gates/packet-u-desk-category-management-wiring-gate.md` (2026-09-22) — CP1 proposes one admin-only "Manage Categories" panel in `VideosSection.jsx`, modeled on `Watchlists.jsx`'s `dragColRef` drag-reorder idiom and `useChartLayouts.js`'s optimistic-rename idiom, wired to all four existing routes with no backend change. | packet drafted, awaiting signature |
| RG-33 | **CORRECTED 2026-09-02 (Checkpoint 7) — the original diagnosis below was WRONG about which file is stale; kept struck-through rather than deleted, per this program's own "⚰️ this said X" correction convention, so nobody re-derives the wrong version.** Direct verification against Massive's own live API (`ticker=AL&active=false`) shows `AL` genuinely `delisted_utc: "2026-04-09"` — the EXACT date `delisted_tickers_bulk.json` already had. Systematic re-check of 101 of the 123 originally-flagged tickers found **99 exact date matches and 2 one-day (timezone-rounding) differences against Massive's own live data — zero were actually still active.** `delisted_tickers_bulk.json` was accurate all along. **The real stale file is `api/data/cap_universe.json`** (git history: last touched 2026-07-20, additively — "add 32 live-but-uncharted tickers," not a full membership prune — and AL delisted 2026-04-09, before that touch, so an additive-only update never dropped it). Entity Master's seed script (Checkpoint 4) trusted `cap_universe.symbols()` membership as "active" without cross-checking Massive's own `active` flag for cap_universe-only symbols, inheriting `cap_universe.json`'s staleness into ~101 wrongly-active entities. **`delisted_registry.is_delisted()` excluding these from `theme_index.py`/`theme_performance.py`/`engine.py`'s holdings filters is CORRECT behavior, not a defect** — those tickers are genuinely delisted. Confirmed further: Entity Master's Checkpoint-7 reconciliation job (live-Massive-only, never reads either static file) independently proposed delisting all 101 PLUS 30 more tickers delisted since `delisted_tickers_bulk.json`'s 2026-08-09 generation that neither static file knows about yet — reconciliation is demonstrably more current than both. ~~Original (WRONG) diagnosis: "`delisted_tickers_bulk.json` is a stale, never-refreshed snapshot... 123 of its entries now describe tickers that are actively trading today... AL... is live on NYSE right now."~~ That specific claim (AL being currently live) was not independently verified against Massive before being written — it was inferred from `cap_universe.json` membership alone. Lesson for this program's own record: a symbol's presence in a membership list is not itself evidence of current trading status; check the provider's own status field. | Entity Master implementation, Checkpoint 4 real seed run + Checkpoint 6/7 investigation (`entity-master` worktree, `docs/entity-master-implementation-log.md`) | **Re-verified 2026-09-22**: zero commits to `cap_universe.json` or any related file since 2026-09-02 — still fully current. Fresh measurement finds **102** already-known-delisted tickers still in the file (one more than the original 101). No automated writer of `cap_universe.json` exists anywhere in the repo (every change is a hand-compiled data commit), which rules out "add a delisted-check to the writer" as originally framed — a standing test is the practical equivalent. Draft gate packet exists, unsigned: `12-decisions/gates/packet-z-cap-universe-delisted-prune-gate.md` (2026-09-22) — CP1 one-time prune, CP2 a standing regression test. The `enumerate_delisted.py` scheduling idea is explicitly recommended AGAINST bundling in: its output is a git-tracked repo file, not a `/data` volume file, so a naive Railway cron would write to an ephemeral container and never reach git — a separate infra decision, not this packet's. | packet drafted, awaiting signature |
| RG-34 | **Production finding, outside program scope, reported for a normal operations session.** `api/routers/modelbook.py`'s `GET /debug-index-drawings` and `GET /debug-desc/{sym}` carry ZERO authentication (no `Depends(...)` of any kind), unlike every other route in that file (all `require_paid` or `require_admin`) — a genuine anomaly re-verified fresh against current source, not the file's norm. Mounted at guessable URLs under `/api/modelbook/*`, a prefix members already hit daily. `debug_desc` traces into `_generate_descriptions` — the SAME LLM description-generation code path real stock descriptions use — per request, plus its own direct, ungated `client.messages.create()` call that bypasses even the feature's own `MODELBOOK_DESC_ENABLED` kill switch; up to five billed Anthropic calls per unauthenticated hit. No `@limiter.limit(...)` anywhere in the file despite the app having a wired slowapi limiter. Zero frontend callers anywhere in the repo (grepped) — genuinely dev-only debug tools. Both routes added 2026-06-06, before the file's own documented 2026-08-09 auth-hardening pass that fixed a different-shaped defect (wrong-tier `get_current_user` vs. paid) on twelve other routes — the most likely reason these two zero-tier holes survived that pass. | Normal operations session, `feat/s7-price-level` (`api/routers/modelbook.py:790-791, 811-860`) | Draft gate packet exists, unsigned: `12-decisions/gates/packet-x-modelbook-debug-endpoint-auth-gap-gate.md` (2026-09-22) — single CP1 checkpoint, gates both routes behind `require_admin` (matching this file's own convention for admin-only diagnostic tooling; deliberately not `require_paid` — these are dev tools, not member features), plus first-ever test coverage of either route. | packet drafted, awaiting signature |
| RG-37 | `POST /api/auth/apply-referral` (`api/routers/auth.py:1441-1447`, `apply_referral_endpoint`, login-required) is fully built and correct — it wraps the already-correct `apply_referral()` service function (`api/services/auth_service.py:1221-1254`). Fresh grep confirms **zero frontend callers anywhere** (`apply-referral`/`applyReferral`/`apply_referral`, case-insensitive, whole `s7-price-level` checkout): the only live caller of `apply_referral()` is the inline signup flow (`SignupRequest.referral_code`, `auth.py:95-99, 291-296`, wired from `Signup.jsx`'s `?ref=` param through `AuthContext.jsx:189-191`). `Settings.jsx`'s `ReferralSection` (`:778-821`) only ever DISPLAYS the member's own referral code/link/stats via `GET /my-referral` — no input field exists anywhere for a member to type in someone else's code after signing up without one. Re-verification also found the backend does not model "already has a referrer" as a field anywhere (`get_referral_stats` describes only the member's own OUTBOUND code), so the fix cannot conditionally hide on a field that does not exist. | Fresh independent re-verification pass, 2026-09-22 | Draft gate packet exists, unsigned: `12-decisions/gates/packet-ac-post-signup-referral-apply-gate.md` (2026-09-22) — single CP1 checkpoint, adds a "have a referral code?" input + submit control to `ReferralSection`, wired to the existing endpoint. No backend change. | packet drafted, awaiting signature |
| RG-36 | A cluster of backend admin/ops health-monitor endpoints in `s7-price-level` — twelve deliberately no-auth, read-only, self-documented status routes (`fundamentals-health`, `reconciliation-status`, `bars-stream-status`, `provider-coverage`, `warm-universe-status`, four `calendar-*` routes, `call-recap-status`, `transcript-index-status`, `yfinance-guard`) plus two `require_admin`-gated routes (`admin/patterns/health`, `theme-engine/status`) — are fully built and correct, but have **zero frontend callers anywhere in `app/src`** (fresh grep, all fourteen, zero matches). The owner's only way to see any of them today is curl or `railway ssh`. One of the fourteen, `admin/patterns/health`, is the cheapest fix in the set: `app/src/pages/admin/PatternAdmin.jsx` already exists, is already mounted at `/admin/patterns`, and already calls two SIBLING routes on the same router (`/recent`, `/{id}/review`) but never `/health`. Re-verification also corrected a stale assumption: Packet M ("Compass Health admin panel", a separate, non-overlapping `/api/j2/compass-health` route) reads as `status: PROPOSED, unsigned` in its own doc, but is actually SIGNED (`fa9d509ed`, fingerprint `3f28cd944`) and BUILT (`s7-price-level` commit `e8bd7f161`, `CompassHealthPanel.jsx` mounted in `Admin.jsx`) — that doc's own prose was never updated after signing. | Fresh independent re-verification pass, 2026-09-22 | Draft gate packet exists, unsigned: `12-decisions/gates/packet-y-admin-ops-health-visibility-gate.md` (2026-09-22) — three independently-approvable checkpoints, cheapest first: CP1 adds a `/health` stat strip to the already-mounted `PatternAdmin.jsx` (zero new files); CP2 adds one new "Data Pipeline Health" panel covering the twelve no-auth monitors as compact rows (name / last run / headline number / flagged-or-clean badge, each field mapped from the route's actual verified response shape); CP3 adds one new "Theme Engine" panel surfacing its run ledger + day-cost against the $5/day cap. No backend change in any checkpoint. | packet drafted, awaiting signature |
| RG-38 | `api/flow_explain.py` implements a complete, production-grade "AI Print Explainer" for Options Flow — a deterministic facts pipeline (vol/OI verdict, moneyness, DTE bucket, premium bucket, aggression, earnings proximity, optional GEX context) feeds Claude to narrate any options-flow print in plain English, never inventing a number the deterministic pass didn't hand it, never 500ing on an LLM failure (degrades to a deterministic explanation instead). Paid-gated (`require_paid`), a $5/day global Anthropic cost cap and a 50/day per-user cap (both read as live constants: `FLOW_EXPLAIN_DAILY_CAP_USD` default 5.0, `FLOW_EXPLAIN_USER_DAILY_CAP` default 50), a SQLite explanation cache (`/data/flow_explain.db`), mounted unconditionally at `POST /api/flow-explain` (+ `/api/flow-explain/`) with a real backend test suite (`tests/test_flow_explain.py`) — and **zero frontend callers anywhere in `app/src`** (fresh case-insensitive grep, four query forms — `flow-explain`/`flowExplain`/`FlowExplain`/bare `explain` — all empty or false-positive prose). No "Explain" button, tooltip, or modal exists on `OptionsFlow.jsx` or anywhere under `pages/optionsFlow/`. **`OptionsFlow.jsx` is partner-owned** (Ravi co-edits it, plus `schwab_router.py` and `live_massive_router.py` — see this file's own "OptionsFlow mobile" section), which is why the fix below is two checkpoints rather than one. | Fresh independent re-verification pass, 2026-09-22 | Draft gate packet exists, unsigned: `12-decisions/gates/packet-aa-flow-explain-wiring-gate.md` (2026-09-22) — CP1 builds a standalone `FlowExplainButton`/`FlowExplainModal` under `app/src/pages/optionsFlow/`, calling the existing endpoint, with zero edits to `OptionsFlow.jsx` and no backend change; CP2 is the one-line additive `className` hook that mounts CP1 on each flow-print row, and **explicitly requires the owner's coordination with Ravi before it lands**, even though the diff is additive-only and never touches an existing inline `style={{}}` object. | packet drafted, awaiting signature |
| RG-39 | `POST /api/screener/count` (`api/routers/screener.py:217-229`, `require_paid`) calls `query.preview_count()` — a fully-built competitive-parity feature whose own docstring cites real competitor precedent (Zacks disables its Run button at zero matches; Trade Ideas shows a per-filter histogram; thinkorswim shows pre-scan match counts) and deliberately calls the SAME `build_scan_sql()` the real `/scan` endpoint uses (confirmed by reading both call sites, `query.py:1225` and `:1255`, not the docstring's claim alone), so a preview count can never disagree with the actual run. Fresh exhaustive grep (`screener/count`/`previewCount`/`preview_count`/`matchCount`, case-insensitive, whole `app/src`) confirms **zero frontend callers**. Re-verification corrects the premise this finding was handed under: `/screener` has no 'Run' button to disable — `Screener.jsx`'s own header comment says the old tabbed Scanner Hub retired 2026-08-29, and `ScannerShell.jsx` already auto-runs a debounced (300ms) FULL `/api/screener/scan` on every filter edit, showing a live 'N matches'/'Scanning…' line (`ShellToolbar.jsx:173-175`) sourced entirely from that heavier call. The real, narrower gap: that line sits in the toolbar, apart from where a member edits filters (`FilterRail.jsx` — confirmed at `app/src/pages/screener/shell/FilterRail.jsx`, NOT under `components/chart/builder/` as the task's file list assumed), and it never uses the cheaper, purpose-built count endpoint. A related, smaller finding: the mobile `FiltersSheet`'s 'Show results (N)' button shows the ACTIVE FILTER count, not a match count. | Fresh independent re-verification pass, 2026-09-22 | Draft gate packet exists, unsigned: `12-decisions/gates/packet-ab-screener-match-count-preview-gate.md` (2026-09-22) — single CP1 checkpoint, adds a live match-count badge to `FilterRail.jsx` (both desktop-rail and mobile-sheet variants) fed by a new debounced hook calling the existing `/api/screener/count`, decoupled from `ShellToolbar`'s existing full-scan-derived status line (left untouched). No backend change. | packet drafted, awaiting signature |
| RG-41 | `api/routers/voice.py` has a cluster of computed-and-stored Compass/voice observability data, all gated by `requires_voice_access` (paid/trial/admin, a much wider audience than an admin-only monitor), with **zero frontend callers anywhere** for six of the eight routes touched: `GET /api/voice/hallucinations` + `POST /hallucinations/audit/{session_id}` (`:593-615` — an automatic hallucination-audit pass already runs after every voice session via `_audit_session_background`, `:1273`, but nothing displays the flags it writes or offers a manual re-run); `GET /api/voice/learning/gaps` + `GET /learning/ticker-obsessions` + `POST /learning/consolidate` (`:568-590`, the third route found during re-verification, not in the original list, but zero-caller and tightly coupled to the other two); `GET /api/voice/reward/variants` (`:635-642` — the prompt-variant CATALOG; `VoiceTelemetryPanel.jsx` already shows the reward SCOREBOARD, usage stats only, never the catalog of declared variants). Re-verification also found a closely-related but differently-audienced sibling: `GET /api/admin/voice-hallucinations` (`api/routers/admin_api_health.py:113-119`, `require_admin`, cross-user aggregate, also zero-caller, its own docstring naming a dashboard that does not exist) — left out of this packet's scope as Packet-Y-shaped, not this packet's shape. `POST /api/voice/explain` (`:674-689`, "why did you say that") is also zero-caller but judged a materially different kind of change (a per-turn, in-chat member affordance rather than a Settings-page visibility read) and deliberately excluded from the proposed checkpoints. | Fresh independent re-verification pass, 2026-09-22 | Draft gate packet exists, unsigned: `12-decisions/gates/packet-ad-voice-compass-observability-visibility-gate.md` (2026-09-22) — three independently-approvable checkpoints extending the existing member-facing Settings → "Compass & Voice" section (not a new admin page): CP1 adds the reward-variants catalog to the already-mounted `VoiceTelemetryPanel.jsx`; CP2 adds a new "Hallucination Audit" panel (flags list + manual re-audit trigger); CP3 adds a new "Active Learning" panel (knowledge gaps + ticker obsessions + a "run consolidation now" trigger). No backend change in any checkpoint; `/explain` and the admin aggregate route are explicitly out of scope, reasoning recorded in the packet's §5/§1a. | packet drafted, awaiting signature |
| RG-40 | `GET /api/voice/risk-dashboard` (`api/routers/voice.py:346-351`) calls `voice_position_sizing.get_risk_dashboard`, whose own docstring says it is "Used by the Risk Dashboard UI panel." That panel does not exist: `app/src/pages/RiskDashboard.jsx` was added 2026-05-12 (`c2da811c1`), its route/nav entry was deliberately dropped 2026-05-25 (`709f4407a`, "restrict free tier to 6 pages, drop Risk tab") -- an owner product decision, not an accident -- leaving the file unreachable, then the orphaned file itself was deleted in the 2026-08-09 dead-code sweep (`d26cee0c0`). The backend endpoint and its computation were never cleaned up alongside the frontend retirement -- it still runs against real open positions, is still gated `requires_voice_access` (any paid/trial/admin member can hit it directly), and has zero other callers anywhere in the repo. Comparison against `api/services/portfolio_heat.py` (the already-shipped, already-live Rung-4/5 mentor risk-heat read) found it covers the same ground more completely (notional exposure vs. regime ceiling, actual concentration-breach flags) and more safely (the orphan's own heat math has NO placeholder-stop detection, so a broker-imported no-stop position silently reads as zero risk -- the exact bug class `portfolio_heat.py` was built to close). `portfolio_heat.py` also already has a real, live, paid-gated member door -- `/portfolio-heat` (A14 CP1, `417b6b853`, shipped 2026-09-21, one day before this finding). One feature is genuinely unique to the orphan and not duplicated anywhere: a `recent_refusals` history of the member's own past `validate_trade` refusals, sourced from the real, currently-populated `voice_tool_calls` table. | Fresh independent re-verification pass, 2026-09-22 | Draft gate packet exists, unsigned: `12-decisions/gates/packet-ae-voice-risk-dashboard-orphan-decision-gate.md` (2026-09-22) -- CHOOSE ONE: A) Retire the orphaned endpoint/function/test (recommended -- portfolio_heat.py + /portfolio-heat already covers the ground, more safely) / B) Revive narrowly -- relocate only the recent_refusals read onto the already-live /portfolio-heat page, still retiring the orphan's own unsafe heat computation either way. | packet drafted, awaiting the owner's CHOOSE |
