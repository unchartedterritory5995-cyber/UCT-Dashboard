# RESEARCH GAPS (Document C Part CCLIII)

Questions requiring second-pass or targeted research. Populated as Wave 1 leaf reports land (their GAPS and NOT INSPECTED sections feed this file) and at every checkpoint.

| ID | Gap | Source report | Proposed follow-up (role, wave) | Status |
|---|---|---|---|---|
| RG-01 | GitHub branch-protection / required-checks state for `master` is not observable from this box. | D-07 §3, NOT INSPECTED | Owner input OI-13; or a read-only GitHub API call if a token becomes available. | owner-answerable |
| RG-02 | Rail selection was unguarded (substring filters) and missed three calendar-adjacent backend suites. | D-07 §1, gap 6 | FIXED on Day 1a: DL-009 tightened the rail. | closed |
| RG-03 | Live flag VALUES on Railway (e.g. whether `PATTERN_VISION_ENABLED=0` is actually set, which `*_ENABLED` gates are armed) are unknown; D-04/D-10 were restricted to key NAMES. | D-10 open q1; D-04 pending | Orchestrator-only read: `railway variables --service web --json` filtered to `*_ENABLED` / `*_MODE` keys (non-secret by construction), written to `02-data-providers/railway-flag-state.md`. Wave 2. | planned |
| RG-04 | Per-user cohort targeting for a dark beta does not exist; is `user_tags` a viable durable store, or is an env allowlist acceptable for Stage 1–2? | D-10 open q2, q3 | Fold into D-08/H-07 coexistence design; ask nothing of the owner yet. | planned |
| RG-05 | `StockChart.jsx` (15,500 lines, ~120 props) sits under the reusable `ChartPane`; whether decomposition is in scope changes the UI architecture estimate. | D-06 open q2 | ARCH-01..03 must state the assumption; red team G-02 challenges it. | planned |
| RG-06 | The four-colour link-group ceiling in `/charts` (multi-chart grid bypasses `ChartWidget` because of it). Does a typed-channel link model generalise? | D-06 open q3 | C5-01 survey + C5-03 comparison; prototype only inside the envelope if still uncertain. | planned |
| RG-08 | `/r/calendar` consumers reconciled by F-03a (R1): morning-wire's own Playwright and the Sunday Scan server PNG; the chart renderer never touches it. | D-08, F-03a | — | closed |
| RG-09 | `pages/Confluence.jsx` is a complete page on a live `/api/confluence` with no route and no importer (not in the reachability allow-list). Route, widget, or delete is a product decision for later; for the ledger it is DORMANT. | D-01 open q1 | Capability ledger (F-03a) marks it dormant; no dispatch. | planned |
| RG-10 | Density vs the 44px tap floor: compact rows are 30px and `DensitySwitcher` is built but unmounted. Whether Terminal-Next is a phone product decides the density model. | D-01 open q3; C Part LII | Personas/JTBD (F-07) + C5-02 personalization + ARCH-08 IA; owner default: desktop-first, phone = monitoring only (record in non-goals). | planned |
| RG-11 | Real size distribution of `charts_workspace_layout` / `chart_settings` in `user_preferences` and whether any member has hit the `parseLayout`→null→empty-board→autosave loss path; needs production data, which this program cannot read. | D-11 open q1, q2 | Owner-answerable only via a read-only query on the volume (NOT during this program); record as a bounded unknown for the state-model ADR. | owner-answerable / unknowable-this-week |
| RG-12 | Production cost-guard defect: five LLM price tables with one rail; `api/services/catalyst/cost_guard.py:33` still prices Sonnet 5 at the old rate after the 2026-08-30 fix landed only in `narrative_cost_guard`. Under-pricing loosens the daily cap. | D-12 | Not fixed in Phase Zero (no application changes). Reported to the owner in the checkpoint; tech-debt register entry. | reported |
| RG-13 | `CATALYST_OPUS_MODEL=claude-sonnet-4-6` in production: an Opus-named variable runs a Sonnet-class model. Intent unknown (doctrine: never downgrade for cost). | ORCH-RAILWAY-01 | Owner input OI-14. | owner-answerable |
| RG-14 | Master drift: six commits landed on `origin/master` during Wave 1; touched files are outside Terminal-Current; leaf citations remain valid at the start SHA. | ORCH-RAILWAY-01, DL-011 | Re-sync decision at a program-day boundary; F-03a re-verifies any citation into the buzz board, buzz services, or the formula manifest. | planned |
| RG-15 | The `transcript` and `calendar_forward_multisource` coverage fields had zero samples in the one observed monitor cycle; whether transcripts are live in production is unconfirmed. | ORCH-RAILWAY-01 | Read `/api/admin/provider-coverage` again after a market-hours cycle (Day 2 checkpoint). | planned |
| RG-16 | `MASSIVE_SECRET_KEY` is unset on web (seed facts say it mirrors `MASSIVE_API_KEY`); which code paths require it, and are they only on flow-worker where it IS set? | ORCH-RAILWAY-01, D-03 | F-03b resolves from D-03's call-site table. | planned |
| RG-17 | Production volumes of the proprietary assets (45 dashboard DBs on the Railway volume) cannot be counted by this program (DL-013). | D-13 open q1 | Bounded unknown; D-13 lists the exact read-only queries for the owner's own operations session. | owner-answerable |
| RG-18 | Base-structure library: D-13 counts 25 structures in the lift ledger; project memory says 30. | D-13 open q2 | F-05 reconciles from the ledger file (the artifact wins over memory). | planned |
| RG-19 | Does the Discord bot (`uct_intelligence`, no git, no scheduled task, no process observed on this PC) still run anywhere, and where? | D-14 open q1 | Owner input (fold into OI-16); provider ledger marks its integrations UNCONFIRMED-RUNTIME. | owner-answerable |
| RG-20 | Is the breadth 'Data Charts never mounted a chart' result (30 monitor runs) a real defect or selector drift in the monitor? | D-14 open q3 | Reported to the owner; not a program task. | reported |
| RG-21 | Five clause-vs-code collisions E-04 found (FRED attribution notice missing; FRED caching; X display requirements in `TapeFeed.jsx`; no tweet deletion-sync; `catalysts.db` retains tweets past the 7-day window). Production defects, fixable in a normal session; not touched by this program. | E-04 | Reported to the owner; tech-debt register (F-03a) carries them as Increases Risk. | reported |
| RG-22 | The Massive tier -- STRENGTHENED (recovery checkpoint grep sweep): FIVE independently-authored code comments across `api/services/polygon_extras.py:4`, `polygon_options.py:3`, `polygon_news.py:7`, `voice_tool_impls.py:2351`, and `voice_agents.py:410-411` all name the SAME specific plan -- "Polygon Advanced, $200/mo" -- not just a bare dollar figure. "Advanced" is Polygon.io's legacy individual/personal-use tier naming (pre-Massive rebrand), distinct from the "Business"/"Enterprise" tier that grants Edge User redistribution rights (E-01's finding). No contract, invoice, order form, or DDLA file exists anywhere in any of the five UCT repositories (confirmed by a targeted file-type sweep: no `*invoice*`, `*contract*`, `*agreement*`, `*license*.pdf`, or `*billing*` file found). This remains a CLAIM (developer comments, not a verified account page), but it is now five-times corroborated and specific, which is materially stronger than the single-comment reading E-04 first surfaced. | E-04 open q1; orchestrator grep (recovery checkpoint) | Owner input OI-03(a) still formally decides (a comment is not CONTRACT-ACTIVE evidence); recommend the owner CONFIRM rather than research from scratch -- the internal evidence already points strongly at Individual/Advanced. The licensing register's default scenario (Massive Individual, no DDLA) already matches this evidence, so no artifact needs to change; work continues provisionally on that default either way. | owner-answerable, high-confidence default |
| RG-23 | Whether UCT's calendar/earnings pipeline stores per-ticker implied-vs-actual move calibration history (Market Chameleon's differentiator), and whether the screener computes a market-wide relative options-volume ratio. The B-DESK-04 contract asserted D-13 cites implied-move code; D-13 does not, so the fact must be settled from the capability ledger (F-03a) or a targeted code read in Wave 2. | B-DESK-04 | F-03a capability ledger rows for implied move / expected move / screener; if absent, a 30-minute D-09-style code read. | planned |
| RG-24 | Which ticker-mentions door is live (capability ledger reconciliation L-6 lists two candidates). | F-03a | One targeted read of the buzz router mount in Wave 2 (D-02-style, 15 minutes). | planned |
| RG-25 | Whether the edge caches anything on the flow endpoints and whether UCT's SSE streams emit `id:` and honour `Last-Event-ID` (reconnect without gaps). Three read-only `curl -I` calls plus one grep settle it. | C7-01 open q2, q3 | Orchestrator read-only probe at the Day 2 checkpoint (D-05's finding that the documented Cloudflare rule is unapplied is consistent with C7-01: JSON is not edge-cached by default). | planned |
| RG-26 | Panels per user: every inherited capacity number assumes users, not panels; the load model needs a panels-per-session assumption. | C7-01 open q1 | ARCH-07 states it as a labeled assumption; dogfood telemetry later. | planned |
| RG-27 | Whether a dock library's popout (dockview/FlexLayout) preserves the opener's React tree, i.e. keeps UCT's one-SSE-pool-per-browser property; and which FDC3 standard Context types would fit UCT's payloads (symbol, symbol-set, list-ref, timeframe, range). | C5-01 open q1, q2 | Candidate prototype inside the envelope (one afternoon) only if the fixed/modular/hybrid comparison (C5-03) makes it decision-relevant. | planned |
| RG-07 | Is Terminal-Next a route inside the existing `Layout` shell or its own shell? Every reuse estimate turns on it. | D-06 open q1 | D-08 coexistence options + ARCH proposals answer it as a decision with code reality; not a research gap per se. | planned |
| RG-29 | The existing provider ledger (F-03b, 48 rows) does not yet break each provider into the owner's requested per-asset-class capability matrix or A-G status taxonomy, nor consistently separate technical access from contractual redistribution/storage/derivation/AI-processing rights at the per-capability level. | Owner instruction (/btw), DL-022 | F-09 Provider Master Ledger, scheduled next wave; contract ready (`contracts/F-09.md`). | planned |
| RG-28 | THIRD PAUSE (recovery checkpoint) left seven Wave-2 assignments incomplete: two failed with zero usable output (C2-01 news architecture, C7-03 vendor abstraction), three left durable partials needing a completion run (B-POD-BBG Bloomberg dossier §M-Q+addendum, C7-02 §2+, C5-02 external research), one lost its stub entirely and needs a full re-dispatch (C2-02 events intelligence, discard the 635-byte stub), and one (B-POD-GDL) has its contract and all inputs ready but never started. | Recovery checkpoint, DL-020 | Re-dispatch per `SESSION_HANDOFF.md` §"Recommended next wave", Tier 2 (Sonnet High) for all seven except the F-06 Day-1-synthesis completion (Tier 3, tracked separately). | planned, not yet dispatched (owner said STOP before the next wave) |
