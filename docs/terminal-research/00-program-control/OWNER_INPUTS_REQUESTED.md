# OWNER INPUTS REQUESTED (Document C Part CLXXX)

Facts only the owner can supply. Each entry: question · why it matters (which decision it changes) · the default assumption the program is proceeding on · artifacts stamped PROVISIONAL because of it. Batches are issued at the end of Day 1 and Day 3, or when a critical-path item blocks. Answer inline; the program updates the artifacts and clears the stamps.

## Batch 1 — Day 1a (2026-09-02). Seeded from OWNER_SEED_FACTS §6 DEFAULT items plus Day 1a discoveries.

| ID | Question | Why it matters | Default in force | Stamped artifacts |
|---|---|---|---|---|
| OI-01 | Member count and tier mix today (free / paid / annual / trial), and the paid conversion trend. | Cost scenarios (Part XLII), commercial questions Q36–40, whether member-facing real-time data is affordable per user. | Under ~750 community members; one paid tier whose paywalled item is the Morning Wire; $7 weekly promo; mix unknown. | cost model, commercial synthesis |
| OI-02 | Internal dogfooders: how many people, which roles (trader / analyst / partner), and who decides "we prefer it". | MVP acceptance ("traders voluntarily prefer it"), persona weighting, D-001. | 2–5 internal users: the owner plus at least one partner. | MVP definition, dogfood protocol |
| OI-03 | SHARPENED by E-01 (2026-09-02): (a) Which Massive tier does UCT hold: Business/Enterprise (whose ToS permits display to Edge Users) or an individual tier (individual-use only)? (b) Does UCT hold an FMP Data Display and Licensing Agreement (FMP bars multi-user display without one)? (c) What agreement text was presented at Finviz Elite purchase (no public terms exist)? (d) Any signed addenda on storage, derived data, or AI processing? | CP-03; every member-facing data feature; licensing register; whether costs scale with member count. | ⚰️⚰️⚰️⚰️ **(a), (b) and (c) ALL ANSWERED-BY-OWNER, 2026-09-19/20 — NOT a blanket "licensing is fine," each answered on its own, across several rounds.** **(b) FMP:** DDLA CONFIRMED TO EXIST — 15 of 19 FMP-gated rows R→LA; 4 stay U regardless (transcript AI, TheFly chain, an attribution clause, copyrighted-text AI). **(a) Massive:** CONFIRMED Business/Enterprise, already held. 30 of 38 Massive-gated rows R→LA. **Every one of the 8 rows that stayed U after the tier answer is now resolved**: ESC-05 (OPRA sub-agreement, 5 rows) and ESC-14 (alerting, 1 row) answered from written Massive confirmation; **T-31** (same-day dark-pool lane) measured, found non-compliant, fixed, and **DEPLOYED TO PRODUCTION 2026-09-20** (`408b9a33f`); **T-20/ESC-21** (paid-Substack Edge User question) answered 2026-09-20 — Massive and FMP responded in writing that it is allowed. **(c) Finviz, Finnhub, AlphaVantage, Schwab: all CONFIRMED** (ESC-07/08/09/10) — 9 rows move OUT OF R itself, the rest U→LA; specific confirmation documents not yet captured for the record. Reddit confirmed in principle, not yet applied pending its agreement document. **yfinance is NOT a licence answer** — `OWNER_DECISIONS.md` D-004 records it as an explicit, informed risk-acceptance instead (Yahoo sells no licence to accept). **FRED remains a structural per-series compliance task**, not something any provider confirmation can close. **(d) signed addenda:** no adverse addenda named anywhere this round touched. Net register effect: of 118 rows, **18** stay Restricted (was 81), **76** Likely Allowed (was 7), **12** Unknown (was 18) — see the licensing register's §1C tally. | licensing register, provider ledger (licensing column), member-facing scope, S9 entitlements CP2 (SIGNED, fingerprint `f808508f7`) |
| OI-04 | Which providers are contractually active today, and which have lapsed? Keys present on the web service include BULLFLOW_API_KEY (retired in code 2026-08-29), POLYGON_API_KEY (duplicate of Massive), UW_API_KEY (Unusual Whales, half-dormant), plus FINNHUB, ALPHAVANTAGE, TWITTERAPI_IO; FRED, Reddit, and TheFly keys are absent everywhere. Are Bullflow, Unusual Whales, Polygon-direct, TheFly still billed? | Provider ledger CONTRACT-ACTIVE rows; avoiding duplicate spend; retiring dead keys. | **DEFAULT-APPLIED 2026-09-18, with a correction:** this row's own "retired"/"absent" framing is STALE — `grep` of `api/` (2026-09-18) shows `BULLFLOW_API_KEY` still actively read and gating `api/liveflow_worker.py` (not retired in code), and `FRED_API_KEY`/`THEFLY` keys ARE referenced (`api/services/fred_economic.py`, `api/services/thefly_news.py`) — "absent everywhere" is wrong. Code-liveness ≠ billing status, which stays unmeasurable from this box. Default: the existing "no OBSERVED-CALLED evidence in 30 days ⇒ retired" rule stands as the operational proxy; reversal: any owner billing statement overrides it instantly. | provider ledger |
| OI-05 | Asset classes actually traded and researched by the desk and members. | Part CCXVI scope; symbol master; non-goals. | US equities primary; options active; indices/ETFs as context; futures positioning (COT) as a research rail; no FX, fixed income, crypto in V1. | non-goals, IA, symbol-master scope |
| OI-06 | The tools the desk and members open daily outside UCT, in order of time spent, and the one workflow each is opened for. SHARPENED by the desk-tool reports: no repository references thinkorswim at all, TradingView appears only as links and embeds, and Finviz is a narrow hard dependency (three automated scans plus a chart tab). Which of thinkorswim, TradingView, Finviz, Market Chameleon, Unusual Whales, SpotGamma do you and the partner actually open by hand on a trading day, and does the desk run any TradingView alerts? | CP-06; Executive Q8–10; the fourth desk-tool benchmark slot; MVP workflow selection. | ⚰️ ~~**ANSWERED-BY-MEASUREMENT 2026-09-14** — assume NONE opened by hand, assume NO TradingView alerts (conservative default; telemetry cannot see third-party sites).~~ **ANSWERED-BY-OWNER 2026-09-19** — the question was put to Patrick directly for the first time (`DECISION_CARDS_2026-09-18.md` §7d). Real answer, both directions **opposite the 2026-09-14 default**: thinkorswim, TradingView, Finviz, and Unusual Whales are all opened by hand on a trading day, **plus others** ("many many many others" — Market Chameleon/SpotGamma/further tools not itemized); TradingView alerts **are** part of the workflow, alongside other unspecified workflow elements ("many many many things"). Patrick declined to rank by time spent or itemize the "others" precisely — if a specific desk-tool benchmark slot or a ranked S1/S2 rebuild ever needs that, it is a narrower follow-up question, not this one. The telemetry-derived S1/S2 defaults (dashboard-first manifest order) are UNCHANGED by this — they were never contingent on the third-party-tool answer. | desk-tool benchmarks, workflow library |
| OI-07 | Business priority between desk-first and member-first (this is escalation D-001; listed here for the batch). | Product vision, tiers, first slice. | **ANSWERED-BY-PRIOR-RULING** — `OWNER_DECISIONS.md:9`, D-001: **(a) Desk first, members second**, ruled Day 1a. Matches the MVP definition, keeps licensing exposure low, fastest feedback loop. Not a new ruling — this row was carried in the batch redundantly with an already-decided item. | see `OWNER_DECISIONS.md` |
| OI-08 | Benchmark evidence access: does the owner or partner hold (or can borrow) access to Bloomberg, LSEG Workspace, FactSet, Capital IQ, Koyfin Pro, TradingView paid, thinkorswim, or know a practitioner who would give a 20-minute walkthrough? Screenshots or a recorded session would raise the evidence ceiling on the deepest dossiers. | CP-09; gate item 7 depth; Part XII evidence ceilings. | No privileged access; ceilings recorded honestly; public evidence only. | Bloomberg dossier confidence, benchmark dossiers |
| OI-09 | Real-time data entitlement: what does the Massive plan permit for display to members (non-professional attestation, OPRA/CTA/UTP fees, delayed vs real-time), and has any exchange agreement been signed? | CP-03/E-03; whether member-facing live quotes are feasible; cost scaling with member count (escalation trigger). | ⚰️ **The vendor-of-record HALF of this question is ANSWERED-BY-OWNER 2026-09-19: UCT stays downstream — not a vendor of record, no non-professional attestation / entitlement reporting / audit-trail machinery is built by UCT itself; Massive carries that compliance under its Business tier for its own customers.** This resolves the general real-time single-symbol quote rows (T-01, T-03, N-01, N-04 and similar — already Bus **LA** in the register, now confirmed rather than conditional). **It does NOT resolve two narrower, separate unknowns that survive it unchanged:** (1) **ESC-05 — the OPRA sub-agreement**: whether Massive's specific Third-Party Agreement (P3 §2.5, currently unpublished) actually names customer-facing OPRA display and covers the $1,500/mo redistribution fee + $1.25/member/mo real-time; T-23–T-27, T-29, N-07–N-09 stay **U** on this alone, regardless of the downstream answer. (2) **ESC-14 — alerting categorization**: whether a member-configured server-side alert is that member's "display" (covered) or the platform's "non-display use" (a separate $2,000/mo CTA + $2,000/mo OPRA fixed fee category); N-19 stays **U** on this alone. Both need a written answer from Massive, not an owner policy stance — they are contract-text questions, not "who runs the compliance machinery" questions. | real-time classification, first slice scope, licensing register ESC-05/ESC-14 |
| OI-10 | Current monthly spend baseline: Railway, data vendors, AI API, storage, other; and the AI API monthly ceiling you are comfortable with for member-facing lanes. | Cost model labeled assumptions (Part XLII, CCXC); escalation thresholds. | Unknown; cost model states every assumption and shows deltas, not absolutes. | cost model |
| OI-12 | The code makes the Morning Wire the ONLY free page (`FREE_PAGES = ['/morning-wire']`) and paid-gates everything else server-side; the seed facts said the Morning Wire is the paywalled item. Which is the intended commercial model going forward? | Tiering questions Q36–40, member-facing scope, cost scenarios, and whether Terminal-Next is a paid-only surface from day one. | ⚰️ **ANSWERED-BY-OWNER DIRECTLY 2026-09-19** (was DEFAULT-RECONFIRMED via grep only, 2026-09-18): *"We will only be paid full access."* Confirms the model matches today's code exactly — Morning Wire free, full/complete access requires payment. No code change needed; the "intended going forward" business-model judgement this row existed to settle is now a real owner statement, not an inference from unchanged code. Terminal-Next stays paid-tier. | commercial synthesis, tiers, cost model |
| OI-14 | Production sets `CATALYST_OPUS_MODEL=claude-sonnet-4-6` (a Sonnet-class model under an Opus-named variable). Deliberate? Does the never-downgrade doctrine apply to the catalyst lane? | AI model-routing rules; cost model. | **DEFAULT-RECONFIRMED 2026-09-18:** `railway variables --service web --kv` (read-only) still shows `CATALYST_OPUS_MODEL=claude-sonnet-4-6`, unchanged. Default stands: treat as deliberate for the catalyst lane; doctrine applies to member-facing answer lanes, not this one. Reversal: flip the env var if the owner says otherwise — no code change needed. | AI architecture, cost model |
| OI-15 | The classified #tsdr room export (7,766 messages, 2024-03-11 to 2026-02-20) is a candidate moat asset. Does its consent basis cover product use (showing members what the room said about a ticker), and will the corpus be refreshed past 2026-02-20? | Proprietary-advantage inventory, community integration (Part LXXXVI), privacy. | Internal-only use (desk sees it); no member display until answered; corpus treated as frozen at 2026-02-20. | proprietary inventory, product tiers |
| OI-16 | Where does the Discord bot (`C:\Users\Patrick\uct_intelligence`, not under git) actually run today: this PC, another machine, or nowhere? No scheduled task or process was observed. | System map completeness (gate item 2); whether bot-side data (buzz, RAG memory) is a live dependency for Terminal-Next. | **DEFAULT-UPDATED 2026-09-18, real measurement on this box:** `schtasks /query` — no task matching discord/intelligence/bot. `wmic process ... get CommandLine` — no running process references `uct_intelligence`. Directory contents (`C:\Users\Patrick\uct_intelligence`) unchanged since Feb 2026 (only `.env`/`.gitignore`, no newer files). **Definitively not running on this PC**; a second machine can't be ruled out from here. Default updated: treat as NOT a live dependency for Terminal-Next until activity evidence surfaces (weaker claim than "unmonitored dependency", stronger than the old assumption). Reversal: any sign of activity (a new file, a webhook firing) reopens it instantly. | system map, dependency graph |
| OI-17 | `/api/live-prices`, `/api/snapshot/{sym}`, `/api/movers` (and the handler behind `/api/gex/data`) answer without a login (verified 2026-09-02). Is any of that deliberate (landing-page tiles, external consumers), or should these routes be behind the session like the rest? | Security and entitlement architecture; licensing exposure (D-002); which routes a Terminal-Next slice may reuse as-is. | ✅ **RESOLVED 2026-09-23 — owner delegated the decision, gated live.** Verified zero legitimate anonymous callers first (every real frontend caller sits inside `AuthGuard`; no external/Discord-bot HTTP caller found). Added `Depends(get_current_user)` to `/api/live-prices`, `/api/snapshot`+`/{ticker}`, `/api/movers`+`/api/extended-movers`, `/api/gex/data`. 73 tests, hygiene clean, pushed `caebdab16`, confirmed live on `origin/production` — a real anonymous `curl` to `/api/live-prices` and `/api/movers` now returns `401`, was `200`. A fifth named endpoint, `/api/live/massive/ticker-flow` (found later, partner-owned `live_massive_router.py`), stays explicitly out of scope, folded into the owner's own direct coordination with the file's partner. | security architecture, licensing register |
| OI-18 | Two products offer free trials that would raise the two most relevant dossiers from 🟡 to 🟢: Gödel Terminal and Benzinga Pro (14 days, no card). Program agents never sign up or accept terms. Would you open either trial yourself and share screenshots or a short screen recording? | Evidence ceiling on the Gödel dossier (gate item 8) and the news-terminal benchmark. | No trials; ceilings recorded. | Gödel and Benzinga dossiers |
| OI-19 | Does the desk trade options structures (spreads, condors) or mostly directional single legs? | Decides the fourth desk-tool slot (Market Chameleon vs OptionStrat) and the options panel scope in Terminal-Next. | **ANSWERED-BY-MEASUREMENT 2026-09-18:** `SELECT strategy_type, COUNT(*) FROM j2_option_strategies GROUP BY strategy_type` against production `auth.db` (read-only, `railway ssh`) — 6,131 recorded strategies: `long_call` 5,263 · `long_put` 733 · `short_call` 94 · `short_put` 40 · `vertical_debit_put` 1. **98%+ directional single-leg; essentially zero spread/condor usage.** This is member-wide journal data, not the owner's personal trades alone, but it directly answers the product-scope question the OI exists to settle. Confirms the existing default exactly. Market Chameleon is the resolved slot. | desk-tool benchmark, options domain |
| OI-20 | SnapTrade bills about $2 per connected user (public price) and is already live for broker sync; is this line in your current spend, and is it acceptable as a per-member scaling cost (the seed-facts escalation rule)? | Cost model; the only member-scaling cost already in production. | Treated as accepted for existing broker-sync members; any new per-member line still escalates. | cost model |
| OI-13 | Does GitHub branch protection or a required status check exist on `master` for the dashboard repository? (Not visible from the repo; `gh` is not installed on this box and the GitHub MCP server fails to connect.) | Deploy-gating reality for the rollout plan (D-07 found nothing gates a deploy). | **ANSWERED-BY-MEASUREMENT 2026-09-18:** `curl https://api.github.com/repos/unchartedterritory5995-cyber/UCT-Dashboard/branches/master` — **anonymous access works, the repo is public** (no `gh`/token needed, contrary to this row's own note). Response: `"protected": false`. Confirmed: no branch protection, no required status check; push to master deploys unguarded — exactly as the default assumed, now measured rather than assumed. | rollout/rollback plan |
| OI-11 | Naming during coexistence: any constraint on how a Terminal-Next beta may be labeled to members (e.g., is "Terminal Beta" acceptable alongside the surface now displayed as "UCT Terminal")? | Part CCXXXII; coexistence options; member communication. | Research decides; no member-visible naming until the coexistence decision. | coexistence plan (naming section) |

## Batch 2 — Phase 2 close (2026-09-02). Seeded from READINESS_REVIEW_DAY1.md and the Phase 2 architecture pass.

| ID | Question | Why it matters | Default in force | Stamped artifacts |
|---|---|---|---|---|
| OI-21 | Four read-only production telemetry queries (or against a copy). ⛔⛔ **CORRECTED 2026-09-14 — this cell used to say “against `auth.db`/`bars.db`” and TWO OF THE FOUR TABLES ARE IN NEITHER.** A measurement took the cell at its word, resolved against `auth.db` alone, and filed **F-OI21-1**, asserting both tables were absent from the product. They are not. ⚠️ **RETRACTED → F-B-1** (`208d39f44`; `12-decisions/gates/packet-b-schema-resolution-gate.md` §2). **Name the database per table, from `01-existing-system/database-and-infrastructure.md`, never a blanket one:** `page_views` → **`auth.db`** (which surfaces get used) · `calendar_seen` → **`auth.db`** (per-user read state) · `calendar_alerts_fired` → **`calendar_alerts.db`** (alert volume) · `ai_search_log` → **`ai_search_log.db`** (AI lane usage); plus the `charts_workspace_layout` blob-size and shape distribution — ⚠️ that one is **not a table**, it is a `pref_key` VALUE inside `user_preferences` in `auth.db`. ⭐ `/data` holds **63 live SQLite databases** (73 files counting `backups/**` and the installed brain pack), so “the database” is not a thing this product has. `tools/sql_resolves.py` resolves a query against all of them and names the one that answers. None of these require code changes to run. | Sharpens S6 Personalization's build order (product-architecture.md §5-B.5), the workspace model's final lock (D1), and A8's curated-vs-browsable-feed posture (P-δ) — currently all designed on labeled assumptions, not measurements. | **ANSWERED-BY-MEASUREMENT — already run 2026-09-14**, same investigation as OI-06 (`verification/2026-09-14/OI-06-telemetry-derived-defaults.md` §5, retraction → `12-decisions/gates/packet-b-schema-resolution-gate.md` §2, F-B-1): all four queries ARE runnable (the first attempt wrongly resolved against `auth.db` alone; re-run across all 65 `/data` SQLite files found every table). Results: `page_views` (`auth.db`) 4,949 rows · `calendar_seen` (`auth.db`) 16 rows · `calendar_alerts_fired` (`calendar_alerts.db`) **956 rows** · `ai_search_log` (`ai_search_log.db`) **79 rows** · `charts_workspace_layout` (a `pref_key` in `auth.db`'s `user_preferences`, not a table) 17 stored layouts, 7 distinct widget signatures. S6/S7 alert boundary and "is personal grounding worth extending" are **not unsized** — populations are small (existence-check, not a rate) but real. No longer PROVISIONAL. | product-architecture.md, information-architecture.md, capability-infrastructure-matrix.md (all carry this as a named PROVISIONAL item — **should be un-stamped now**) |

## Answered

⛔ **R-TRIAGE applied 2026-09-18** (session: TERMINAL-NEXT close-the-merge-track prompt). Every
OI-03..OI-21 item was classified ANSWERED-BY-MEASUREMENT / DEFAULTABLE / OWNER-ONLY; the full
table with justifications is `docs/terminal-research/DECISION_CARDS_2026-09-18.md` §OI-triage.
Only the fully closed items are listed below — DEFAULTABLE items keep their default annotated
in the row above (they remain open in the sense that the owner can still veto in one word) and
OWNER-ONLY items are cards in the same file, not rows here.

| ID | Answer (date) | Artifacts updated |
|---|---|---|
| OI-06 | ⚰️ Was "telemetry-silent, conservative default: assume none" (2026-09-14). **Real owner answer 2026-09-19**: thinkorswim, TradingView, Finviz, and Unusual Whales all opened by hand, plus unitemized others; TradingView alerts and other unspecified elements are in the workflow — reverses the default in both directions, not itemized by rank. S1/S2 manifest order (telemetry-derived, unaffected) still stands. | desk-tool benchmarks, workflow library — this file's OI-06 row |
| OI-07 | Already ruled — D-001, desk-first, `OWNER_DECISIONS.md:9` (2026-09-02) | none new; this file's OI-07 row corrected to point at the existing ruling |
| OI-13 | No branch protection on master, confirmed via anonymous GitHub API (2026-09-18) | rollout/rollback plan — this file's OI-13 row |
| OI-19 | 98%+ directional single-leg options, measured from 6,131 production `j2_option_strategies` rows (2026-09-18) | desk-tool benchmark, options domain — this file's OI-19 row |
| OI-21 | All four queries runnable across the correct per-table database; real counts recorded (2026-09-14, corrected same day via F-B-1) | product-architecture.md, information-architecture.md, capability-infrastructure-matrix.md — PROVISIONAL stamp should be removed |
