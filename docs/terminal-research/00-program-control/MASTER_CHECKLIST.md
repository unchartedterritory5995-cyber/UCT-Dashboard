# MASTER CHECKLIST — every deliverable (Document C Part CLXIII) → gate item (Document B §49) → artifact

Status: NOT STARTED / IN PROGRESS / DRAFT COMPLETE / MET / MET WITH BOUNDED UNKNOWNS / NOT MET. Updated at every checkpoint. Refreshed in full at the 2026-09-02 recovery checkpoint (prior version had drifted behind Wave 1/1b completion).

| # | Deliverable (Part CLXIII) | Gate item | Artifact path | Owner role(s) | Status |
|---|---|---|---|---|---|
| 1 | Executive Research Summary (one-page pointer to the Owner Decision Memo) | 23 | `13-executive-synthesis/executive-summary.md` | F-06 | NOT STARTED |
| 2 | Existing UCT System Architecture Map (all repos, both machines) | 2 | `01-existing-system/system-map.md` | F-03a ← D-01..D-14 | DRAFT COMPLETE (81 KB, 6 code locations, 5 Railway services, both machines, 25 reconciliations) |
| 3 | Existing UCT Capability Ledger | 3 | `01-existing-system/capability-ledger.md` | F-03a | DRAFT COMPLETE (178 rows measured directly by row-pattern count, 2026-09-02 — this line previously said 211; corrected during the Phase 2 adversarial validation pass, the same defect class this program repeatedly flags elsewhere: a hand-typed count beside the artifact it describes. Sections A-P.) |
| 4 | Data Provider / API / Licensing Ledger | 4 | `02-data-providers/provider-ledger.md` | F-03b ← D-03, D-14, E-* | DRAFT COMPLETE (48 provider rows: 20 core, 7 retirement candidates, 9 dormant) |
| 5 | Benchmark Terminal Universe | 6 | `03-competitive-research/benchmark-universe.md` | B-VAL-01 | DRAFT COMPLETE (71 cited URLs; universe revised DL-017) |
| 6 | One Product Dossier per product | 6 | `03-competitive-research/<p>/dossier.md` | B-<P>-*, B-POD-<P> | 11 of 11 leaf dossiers ACCEPTED (Unusual Whales, TradingView, Koyfin, Benzinga Pro, AlphaSense, Fiscal.ai, Quartr, FactSet, LSEG Workspace, SpotGamma, adjacent light note [TIKR/YCharts/CIQ]); Wave-2 verifiers/reconstructors not yet dispatched |
| 7 | Bloomberg Deep-Dive Dossier | 7 | `03-competitive-research/bloomberg/dossier.md` | B-BBG-01..08, B-POD-BBG, B-BBG-DEEPEN, B-BBG-VERIFY | **ACCEPTED (2026-09-02 close)** — 171.8 KB, sections A-Q + Reconciliations + M/N/O/P + GAPS/SOURCES/NOT INSPECTED complete; a dedicated multi-asset deepening pass (new leaf `09-multi-asset-analytics.md`) closed the original equities-only ceiling across 33 of 36 owner-named topics, independently adversarially verified (zero fabrication, zero regression). 3 honest ceilings remain (preferred-securities depth, cross-company relationship mapping, cross-screen regime-chaining) — owner-input-bound (OI-08) |
| 8 | Gödel Terminal Dossier | 8 | `03-competitive-research/godel/dossier.md` | B-GDL-01..03, B-POD-GDL | **ACCEPTED (2026-09-02)** — full synthesis from the three accepted leaves; VERIFIED ~55 capabilities, CLAIMED ~9, REPORTED ~15, DEMONSTRATED 0 (structurally unreachable, no video channel); OI-18 (trial) is the only ceiling-raiser |
| 9 | Cross-Product Capability Matrix (competitor-facing) | 9 | `05-product-strategy/capability-matrix/capability-matrix.md` | F-05 | NOT STARTED — inputs now genuinely complete (11 dossiers + Bloomberg + Gödel all accepted, no longer blocked on synthesis). Distinct from `05-product-strategy/capability-infrastructure-matrix.md` (Phase 2, ACCEPTED 2026-09-02), which maps UCT's OWN capabilities to UCT's OWN infrastructure, not to competitors — do not conflate the two. ⭐⭐ **PROMOTED BY CARD 25 (owner thesis, 2026-09-26) TO THE MOST GOAL-ALIGNED NOT-STARTED ITEM IN THE PROGRAMME.** The owner's goal is *"aggregate all the best features so someone can only use our site instead of the others"* — an AGGREGATION thesis, under which the binding constraint is **coverage**, not differentiation, and **one missing capability defeats a hundred proprietary ones.** This row IS that coverage-gap ledger. ⚠️ Must record the **no-execution** boundary as a STRUCTURAL non-gap, never as a coverage failure. |
| 10 | Best-of-Breed Matrix | 9 | `05-product-strategy/capability-matrix/best-of-breed.md` | F-05 | **DRAFT COMPLETE 2026-09-26** — **1,259 lines**. ⚰️ This row first said 317 lines, because the orchestrator committed the file BEFORE the author's hand-back arrived and caught a mid-write snapshot; the file already ended with its own §10 and GAPS, which is exactly what made it look finished. ⛔ **Do not commit a delegated file until its report lands.** ⭐ Three judgements worth carrying: **Bloomberg's decisive wins are all GRAMMAR, not data** (and grammar is the cheapest thing in the set to copy — its data rows are 🟡 with its own collateral disagreeing with itself, its price and performance 🔴, and two of three moats are absences); on **three rows UCT most needs the best mechanism costs under $120/mo and no winner is a big name** (alert AUTHORING → Unusual Whales' field-to-field `where` grammar; AI trust → TradingView, where the artefact IS the citation; member-facing feature status → Gödel's BETA pills at the point of use); and **two rows have no incumbent and are the same row twice** — a decisive grounded verdict (LSEG, FactSet and Quartr each publish a WRITTEN REFUSAL to give one) and the per-ticker history join, the only rows where UCT can be first rather than behind, with the honesty tax that its own exam for the first reads 12/50. ⛔ **Latency, density and performance are UNRANKABLE for every product on every row** — no input holds a single measured figure and six dossiers say so themselves; `S11 Session & Market Clock` is ◻ for all fifteen. ⚰️ **Two provenance findings the orchestrator should own:** my dispatch roster OMITTED Gödel Terminal, which holds a full dossier plus three leaves and which `benchmark-universe.md` calls the single most programme-relevant comparison available — the author included it anyway; and the "$31,980/yr Bloomberg" figure has **two incompatible provenances inside this programme**, one of which forbids citing it, so §5 reports a dated range and never a number. ⚠️ §2.2's desk weighting is 🔴 except for Finviz. Per capability: best-in-class product, the MECHANISM that makes it best rather than an adjective, the runner-up and why it loses, what UCT holds today from the capability ledger, and evidence strength with its reason. ⚠️ It was briefed to control for an uneven research budget — Bloomberg got a dedicated deep-dive and a deepening pass while some products got one dossier against an exhausted search quota — because a naive matrix ranks the well-researched products highest. ⛔ "not offered" and "not established" are kept as distinct cells; collapsing them would turn a research ceiling into a product verdict. Feeds items 16 and 17 |
| 11 | Anti-Pattern Library | 13 | `05-product-strategy/anti-patterns.md` | F-01 | **DRAFT COMPLETE 2026-09-26** — 2,622 lines, **65 entries** in nine families, 55 repo-evidenced and 10 competitor-evidenced. Every entry carries all seven fields: pattern · mechanism · **why it survives review** · recorded instance · cost · structural fix · detector. ⭐ The "why it survives review" field is the load-bearing one — every instance in here passed review — and an entry without it is advice rather than an anti-pattern. **Detectors: 31 YES, 14 PARTIAL, 20 NONE, and the 20 are listed as the work items** (cheapest three named). Top entry: an auditor whose aperture excludes the failure class it exists for. ⚠️ 2.6× the requested length, deliberately, with the mechanical cut named if it must come down. ⚠️ Its own ceiling is stated: no `file:line` was opened by its author and no dossier file was read directly, so citations sit one to three removes from source and §2.8's load-bearing figures want re-quoting by a verifier. ⭐ It found two live defects in this programme's own control documents — row 20's false error-boundary precondition (corrected) and this worktree's stale `CLAUDE.md` (bannered) |
| 12 | User Persona Framework | 10 | `04-workflows/personas.md` | F-07 | NOT STARTED |
| 13 | Jobs-to-be-Done Library (30+) | 10 | `04-workflows/jobs-to-be-done.md` | F-07 | **DRAFT COMPLETE 2026-09-26** — 783 lines, **45 jobs** (count grep-derived from `^#### JTBD-`, never typed), **6 quarantined** under `WEAK-` rather than dropped. ⭐⭐ **THE FINDING THAT INVERTS THE ROADMAP'S PREMISE: of the five tools this programme calls "things the terminal replaces", exactly ONE is verdicted absorb-outright.** Two are harden/bridge, one is **structurally undisplaceable** (it is inseparable from a funded brokerage account, so no feature can displace it), and one **has never been studied as a desk tool at all**. Its own sentence: *"a roadmap treating the four tools as four rebuilds is reading the evidence backwards."* ⛔ **This bounds the MVP candidate set, not just the roadmap's tone** — a definition of done written against a harden/bridge row or the undisplaceable one is unachievable by construction rather than merely ambitious, which is why item 27 was sent this finding mid-flight. ⭐ It also settles a provenance question the programme had backwards: **CARD 17 RESTORED a seed fact rather than introducing one** — `charter/OWNER_SEED_FACTS.md:61`, §6, dated **2026-09-01**, already read *"one paid tier whose paywalled item is the Morning Wire, with a $7 weekly promo."* ⚰️ **That same line ALSO spliced in another PRODUCT's promo price, and I misread it as a rival price for this one.** ✅ Settled by the owner 2026-09-26 (**CARD 23**): UCT Intelligence is **$200/month or $2,000/year**; the **$7/week is the Whop plan**, a separate live-trading-Discord product. `cost-model-data.md:290` had recorded the promo correctly all along. ⚠️ It reports two internal contradictions it did NOT resolve, each between two documents of equal standing: **posts_total 88 vs 92** and **cap_universe 3,742 vs 3,721** |
| 14 | Professional Workflow Library | 10 | `04-workflows/workflow-library.md` | F-07 | **DRAFT COMPLETE 2026-09-26** — 1,597 lines, 123,825 bytes. **38 workflows + 6 quarantined** (`WEAK-WF-`), counts grep-derived. ⭐⭐ **Carries the field CARD 25's thesis needs and nothing else in the programme produces: a per-workflow BREAK-OUT POINT — where the flow leaves our site and for what** — classed NONE 5 · ADDRESSABLE 7 · STRUCTURAL 14 · NOT MEASURED 1 · N/A 11, **summing to 38 by construction so a missing field is catchable by one addition.** Order placement is STRUCTURAL by the no-execution default; nothing proposes execution. ⛔⛔ **ITS HEADLINE IS A VERIFIED PRODUCTION DEFECT, NOT A RESEARCH FINDING — CARD 27: the morning-wire missed-run watchdog CANNOT FIRE on the morning it names.** It runs at 09:05 ET and tests `wire_date < expected`, but `expected_wire_date()` rolls back a day before 09:30, so a one-run miss compares `yesterday < yesterday` → false; it can only fire at TWO days stale, and the member badge reads "fresh" until 09:30. **Verified at source by the integrator.** The incident it was built for is recorded twelve lines away in `wire_freshness`'s own docstring (2026-08-14, the 06:35 run crashed and the dashboard served the prior day all day). One-line fix (re-time the cron past 09:30) + a rail; ⛔ **unshipped, needs owner deploy authorization.** ⚰️ **It named its OWN likeliest error, named the settling check, and was RIGHT: `WF-C03` and `WF-C06` are WITHDRAWN from ADDRESSABLE** — the trailing implied-vs-realized calibration they assumed missing ships as the **hero** of `SetupSection.jsx`. ⭐ An "UCT absent" cell is the most dangerous kind in any coverage document because it is the one that gets acted on; these two were caught only because the author labelled its confidence honestly. ⚠️ It also records **six unresolved contradictions** it deliberately did not settle, incl. the breadth collector's hour having THREE readings (15:15 CT / 16:15 ET / "the 4:15 collector") while gating a member-visible state change, and `rth-scheduling.md:3` saying "Nine" tasks above its own 8-row table |
| 15 | UCT Proprietary Advantage Inventory | 11 | `05-product-strategy/proprietary-advantage-inventory.md` | F-05 ← D-13 | Raw inventory ACCEPTED (D-13, 68 KB: decision provenance and first-party narrative as the moat); F-05 synthesis into the final inventory NOT STARTED ✅✅ **CANONICAL PASS DRAFT COMPLETE 2026-09-26** — 817 lines, 85,401 bytes, built ON the accepted raw inventory rather than duplicating it. Sorts **71** assets into four classes with ids derived by grep, never typed: **ACCUMULATED 14 · BUILT 20 · LICENSED 18 · CLAIMED-NOT-EVIDENCED 19** (re-derived independently here; no duplicate or missing ids). ⭐⭐ **The shape IS the finding: the quarantine (19) is larger than the moat (14).** ⭐ Strongest accumulated asset is the **decision record** — `wire_universe`, 22,574 rows over 60 issues, **19,611 of them names CONSIDERED AND REJECTED** with the stage each died at and its feature vector at the time: judgement under uncertainty, which no vendor sells and no scrape recovers, and unambiguously THIS product's. ⛔ **And it reaches no member — `NO-SURFACE-FOUND`**, 5 of the 14 being that shape, because a store with no surface has no home in the capability ledger's taxonomy at all. ⚠️ Caveat stated in-file: 60 issues over ~104 weekdays is five months, so the shape is irreplaceable and the depth is not yet. ⚰️⚰️ **IT FALSIFIED A CLAIM THIS SESSION HAD PROPAGATED INTO THREE AGENT BRIEFS:** the capability ledger does NOT "always understate what ships" — its row A8 says `cap_universe.json (3,742)` while the file at `origin/master` holds **3,640**, an **OVERSTATEMENT** (verified independently by `len(json.load(...))`). Five understated, one overstated, two exact, across seven re-derived cells. **A ledger cell is a dated measurement whose error has no reliable sign**, and the old framing licensed reading a small cell as a floor. ⛔⛔ **It also found the WISDOM LOOP shipping with ZERO ledger rows** — 111 files, six packages, a 32-table schema, six routers included unconditionally, `grep -ci wisdom` → **0** — reported precisely as **ADMIN-MOUNTED**, a third state distinct from absent and from serving members, with no flag state asserted anywhere. Superseded cells it re-derived: KB 9,605→9,797 (so `CLAUDE.md`'s "8,500+" too), `book_plans` 420→760, `setup_triggers` 243→540, wire snapshots 28→44, `market_regimes` 148→165. ⚠️ Self-flagged weakest row: the curriculum's ACCUMULATED-vs-BUILT boundary (authoring is purchasable; a two-year trading-room log is not), and the class totals are a function of row-splitting — published for **shape**, never quotable as measurements of the product |
| 16 | Feature Opportunity Backlog | 13 | `05-product-strategy/feature-opportunity-backlog.md` | F-05, A-03 | **DRAFT COMPLETE 2026-09-26** — 1,540 lines, **85 items**, count DERIVED by pattern over the finished file with a duplicate check rather than typed. Covers **30 of the 33** spine rows plus three proposed extension rows; A2, A14 and E1 are empty **for stated reasons**. Every item carries all nine fields including *how you would know it worked*. ⛔ **It does not score or rank — that is item 17**, and the document says so as its first non-decision. ⭐⭐ **Most consequential: the per-ticker history join.** Best-of-breed found it one of only two rows with NO incumbent across fifteen products, several conceding they structurally cannot do it, and the capability matrix calls it the single most consequential normalization item in the whole spine — buildable on data this product already owns, so the only thing between it and an uncontested differentiator is an internal dependency, not a vendor. ⭐ **The one-paid-tier ruling did real work as a filter**, with four recorded consequences — including striking half of one item's own source sentence, because best-of-breed prescribed making the removal of a limit *the upgrade*, and there is no tier to upgrade to. ⭐⭐ **And it caught FOUR near-misses on the duplicate-a-shipped-capability error**, which is the error the brief warned about: the command palette, the freshness badge, per-widget error boundaries, and the print explainer are all recorded as absent somewhere and all actually ship. ⚠️ The 24-day-old capability ledger was insufficient on its own — four superseded cells in the four places checked — and **a ledger refresh is the highest-value input this file lacked**. ⚠️ Its ceiling: **no source file was opened**, so every "what exists today" is an artifact's measurement at its own date. §4 is a 22-row not-build list with re-open triggers; §5 separates solved from ruled-out from unexamined; §6 holds 3 appendix items with 2 labelled unevidenced. ⭐ It also found and corrected a hand-typed count in its own draft, by grep |
| 17 | Feature Scoring Matrix | 13 | `05-product-strategy/feature-scoring.md` | F-05 | **DRAFT COMPLETE 2026-09-26** — 1,234 lines, all **85** items scored, roster RE-DERIVED by grep rather than carried. **54 hand-assigned cells of 680 = 7.9% judgement**, published as a table, with three columns carrying none at all. ⭐⭐ **Headline: the binding constraint is VERIFIABILITY, not size or dependency.** 29 items are both size S and depth 0; **11 survive all seven axes.** The attrition ladder is 29 → 22 → 17 → 14 → **11**, and ⭐ **the two filters that remove the most are the two a value/effort matrix has no columns for** — whether it can be rolled back, and whether its observable can actually be computed. ⛔⛔ **The rollback plan has six tiers and NONE describes the shape 22 of the 85 items are**, including 8 of the 11 largest: a sweep of item 37 for migration/schema/irreversible language returns nothing, so 4 items are cheap to reverse, 59 need a deploy, and **22 have no tier written down at all.** ⛔ **Gap size is silent on two thirds of the backlog** — best-of-breed places 25 of 85, because the other 60 came from the capability spine rather than from a competitor, so **a conventional competitive model would rank most of this backlog at zero.** ⭐ Top band is **HELD (10) — not builds**, first because it is the only band that costs nothing and unblocks others, and its highest-value member is **declaring a quiet measurement window**, which is a scheduling decision rather than work. ⚠️ ⚰️ **Its own extractor manufactured a finding and it caught it**: one item's body ran to end-of-file and absorbed 10,003 characters, and re-deriving from a bounded extraction changed 9 cells — the contaminated run would have reported gap-size placing 30 items, not 25. ⚠️ It also found **three wrong attributions in the orchestrator's brief** and cites the correct sources instead |
| 18 | Product Vision (+ one-sentence philosophy, non-goals) | 13 | `05-product-strategy/product-vision.md`, `non-goals.md` | A-03, F-06 | PARTIALLY SATISFIED — `05-product-strategy/product-architecture.md` (Phase 2, ACCEPTED 2026-09-02) carries a sharpened product thesis and an explicit "not reproduced" list as part of a fuller system-architecture document; the standalone one-page `product-vision.md`/`non-goals.md` pair is still NOT STARTED as separate files ⭐ **CARD 25 HANDS THIS ITEM ITS ONE-SENTENCE PHILOSOPHY, from the owner verbatim (2026-09-26):** *"the goal is to aggreagte all the best features so someone can only use our site instead of the others."* ⚠️ And it hands the NON-GOALS half a hard boundary: **no execution or order management**, which is why full substitution stops at research and analysis. ✅✅ **DRAFT COMPLETE 2026-09-26 — BOTH FILES DELIVERED** (`product-vision.md` 489 lines / 31,516 B; `non-goals.md` 272 lines / 28,304 B; both pure LF, verified). The vision opens with the owner's sentence **verbatim, typo intact and explicitly not improved**, and puts the no-execution ceiling in the body rather than an appendix. ⭐⭐ **The load-bearing move, and it is flagged by its own author as the document's likeliest error: "all the best features" is read as the best MECHANISM per JOB, not the literal named features of other sites** — reconciling CARD 25's breadth against the charter's no-Frankenstein clause. ⛔ **That choice sets the UNIT item 9's coverage ledger counts**, so item 9 was told mid-flight to carry competitor features underneath each job, making a feature-level recount a re-aggregation rather than a rewrite. **One owner sentence settles it.** ⭐ It also hands item 9 its cell vocabulary: **COVERED / GAP / STRUCTURAL BREAK-OUT**, the third value being what keeps the execution ceiling out of the gap count. **`non-goals.md` carries 24 rows (NG-01..NG-24, derived)**, each with WHY, WHO RULED IT, a five-state class and a re-open trigger or an explicit "no re-open trigger" — plus a §9 listing what is explicitly **NOT** a non-goal, which is the half that stops a boundary being over-read. ⚠️ Its own weakest row is self-flagged: **NG-03 (no broker write path) is an EXTENSION of the governing default, not a cited ruling** — demote it to a consequence note under NG-01 and the count becomes 23 by the same grep. ⭐ Built ON `product-architecture.md` (Phase 2, ACCEPTED) by citation, never restating it |
| 19 | Information Architecture | 14 | `06-ux-and-information-architecture/information-architecture.md` | ARCH-08 | **ACCEPTED (Phase 2, 2026-09-02)** — 753 lines: 5 shared primitives, 5-level hierarchy, 6 workflow chains, 18 named rejected Bloomberg/Gödel paradigms, adversarially validated |
| 20 | Workspace Interaction Architecture (fixed / modular / hybrid decision) | 14 | `06-ux-and-information-architecture/fixed-modular-hybrid.md` | C5-03, ARCH-01..03 | **DRAFTED 2026-09-25, PROVISIONAL LOCK: HYBRID — red-team required before this gate item closes.** The early signal held and sharpened: six of seven observed failure modes are persistence, so the document's three commitments are (1) promotion must be generic or the board inherits the 18-entry registry ceiling, (2) one VERSIONED workspace document and NOT in `user_preferences` — which resolves a direct contradiction between D-06 (version the blob in place) and D-11 (that store is the wrong primitive) by sequencing them as bridge and destination, (3) per-panel error isolation is a PRECONDITION of choosing hybrid, and ✅ **IT ALREADY SHIPS — ⚰️ this row asserted "there are currently zero per-widget error boundaries" and that was FALSE when written.** `ErrorBoundary` wraps `WidgetBody` at `WidgetHost.jsx:107-111`, the header renders OUTSIDE and before it (`:227`/`:254`) so a dead panel can still be closed, and `PANEL_MOUNT_CAP = 3` at `ChartsWorkspace.jsx:84` — all in `424bf3355`, an ancestor of `origin/production`. ⛔ Corrected 2026-09-26 because **agents are dispatched with these rows as their brief**, so a false precondition here is an active wrong-precedent source, which is the exact defect class this programme's own anti-pattern library files as DOC-1. ⚠️ `PANEL_MOUNT_CAP` caps concurrent MOUNTS, not board size; there is no `MAX_WIDGETS`. C5-01's 🔴 "do users customize?" axis is now MEASURED (production aggregate, §3): 17 of 29 accounts hold a board, none empty, modal size 5 widgets, 0 of 17 versioned, 0 unparseable — ⚠️ but the cohort is admins/staff/testers under `COMING_SOON_MODE`, so it retires "nobody customises" and cannot carry the lock alone. OI-06 remains the confirming input |
| 21 | Data Architecture | 15 | `07-technical-architecture/data-architecture.md` | ARCH-04 | **ACCEPTED (Phase 2, 2026-09-02)** — 1,715 lines: ACL provider-abstraction pattern, canonical entity/symbol master, adjustment policy, provenance/freshness/licensing metadata, F-09 integrated, adversarially validated |
| 22 | AI Architecture | 15 | `08-ai/ai-architecture.md` | ARCH-05 | **DRAFT COMPLETE 2026-09-26** (818 lines, file:line on every claim, scope pinned to a named SHA). ⭐⭐ Headline: **the contract this role was commissioned to invent has ALREADY SHIPPED** — `api/services/ticker_explain.py` implements evidence domains chosen deterministically before the model runs, a blocking grounding gate over the union of every model-authored free-text field, five coerced response states, a derived refusal reason used only as a fallback, spend ledgered to the shared durable guard, and an AST renderer-boundary rail. So this is a generalise-and-rail job, not a design job — and **seven of the spec's nine cited line numbers had moved in two days**, which is why the first requirement is a derived rail rather than more prose. Also: Claude (not Perplexity) writes every member AI-Search answer and the brain facade is armed on both surfaces (from the accepted live flag read), while `COMPASS_MENTOR_MODE` — the flag deciding whether a COMPUTED TRADING VERDICT reaches members — **is not a key in `docs/feature_flags.json` at all**, so it has no declared default, no exposure and no dated owner decision. Six of six price tables re-read: one fixed 2026-09-22 (so E-06 §5.2's "still mis-priced" is stale), four still wrong including a realtime-voice rate three orders of magnitude off the vendor page — ⭐ and the one that got fixed is the one with NO rail while the one that never drifted is the one WITH the rail |
| 23 | Security & Entitlement Architecture | 15 | `09-security-licensing-cost/security-entitlement-architecture.md` | ARCH-06 | **DRAFT COMPLETE 2026-09-26** (873 lines, file:line on every claim). R-17's named routes now DO declare auth dependencies in master's source — live-prices, snapshot, movers, gex-data, the whole bars + bars-stream family, and the live breadth-monitor route (which F-04 had as NOT DETERMINED) — but the finding is deliberately **NOT reported closed**, because `web` deploys from `production` and the doc could not run git; it names the closing measurement instead (one read-only GET per route). ⛔ Still dependency-less in the same read: the price-stream route, **gex-compare, four lines below the gex route that WAS fixed**, the three dealer-positioning diagnostics, plus flow-scoreboard and the render routes which are open by their own docstrings. ⭐ Highest-leverage engineering change identified: `api/auth_surface_check.py` audits **mutating methods only** (`MUTATING = {POST,PUT,PATCH,DELETE}`), so every route above was structurally invisible to the one instrument that looks — widening that single aperture is additive and fails closed. ⭐ And a per-request role-derived cohort rung already ships on the auth payload (new since D-10), so D-10's gap narrows to rung 2 only, a NAMED cohort: `user_tags` exists, is indexed on both columns, is admin-written with no member write path, and **no gate reads it**. ⚰️ D-10 §4.2's "FREE_PAGES has three hand-synced copies" is CLOSED — one module now, do not re-report |
| 24 | Performance Architecture | 15 | `07-technical-architecture/realtime-performance-architecture.md` | ARCH-07 | **DRAFT COMPLETE 2026-09-26**, written by the orchestrating session because it depends on this session's own measurements. Answers C7-01's ten questions (ANSWERED / NARROWED / OPEN, each with what would close it) and rules on all ten D1–D10 rows. ⛔⛔ **§1 WITHDRAWS Protocol D and CARD 19**: `/api/flow/data` is gated, so the probe measured a **401**, and the recorded `application/json` cannot be a route that serves `text/csv`. The header the card said was missing exists (`flow_router.py:131-134`). ⭐⭐ The controls make the replacement finding STRONGER: three unrelated gated routes answer 401 with `DYNAMIC` while the flow path alone gives `BYPASS`, and §4.3's pre-gate 2026-07-25 reading of the real body was `DYNAMIC` — so a configuration was added to `/api/flow/*` between those dates, and **D6 is INVERTED**: the route is paid gated tape whose pre-gate state its own docstring calls the largest raw-data leak in the product, so caching it without reading the cache key could serve that tape to an anonymous caller from the edge. ⭐⭐ **D9 re-scored: the pod LEAKS at +7.9 MB/min, which partly inverts the deploy-frequency argument** — a 26-minute median pod life is also what keeps RSS near 2.4 GB instead of 11 GB, so the leak must be fixed before anyone argues for longer-lived pods. ⛔ And "the deploy window" is THREE quantities the accepted docs conflate: 82–119 s of `/api/*` unavailability, 3m40s–6m40s push-to-SUCCESS, ~10 min end-to-end — and the first was **not observed on any of five orderly deploys**; the only 502 on record came from a SUPERSEDED deploy, so the architectural cost of deploying is a COLD CACHE and the availability cost belongs to pushing twice inside one build |
| 25 | Observability Architecture | 21 | `10-roadmap/observability-plan.md` | ARCH-07, H-06 | **DRAFT COMPLETE 2026-09-26** (904 lines). ⭐⭐ Headline: **the monitor this item would have designed ALREADY EXISTS as its own Railway service** — `api/terminal_next_monitor_main.py`, cron-invoked with `--once`, holding NO in-memory state, whose own comments already solve every failure mode this item would re-derive (runs no measurement of its own so it cannot become a second authority; reaches `web` over the private network because a volume mounts to one service; admin channel only with a test asserting the public channel's name is absent; UNREADABLE is never zero; refuses to fire on the normal case — *"a CLOSED MARKET IS NOT A FAULT, and alerting on it every weekend is how a monitor gets muted"*). **So the question is "which jobs", not "what monitor".** ⛔⛔ Two of the three signals the programme needs are **UNREADABLE, not merely unread, same cause**: the event-loop watchdog's `max_lag_ms` and the bars drop total are per-process and reset on every deploy, against a **26-minute median pod life** — while the watchdog's own runbook says to watch it "for a few days". ⭐ That generalises into the doc's one new idea: **per-process state is fatal for a CUMULATIVE quantity (leak slope, drop total, max-over-days) and fine for a DISTRIBUTIONAL one measured inside one pod's life (a p95)** — that property, not convenience, decides the tier. ⛔⛔ And **CARD 16's replacement p95 gate cannot be evaluated today on the timeframe that motivated it**: `tools/bars_warmth_audit.py` still classes `stale-swr` as COLD, so daily's p95 is computed over an empty set and **no p95 prints for daily at all** — the "daily p50 104 ms PASS" was read off the COLD line. Kind 3a: the ruling changed the definition, the instrument did not. Nine defaultable rulings OBS-1…OBS-9, each vetoable in one word |
| 26 | Migration / Coexistence Strategy (+ legacy parity matrix, migration gates) | 16 | `10-roadmap/coexistence.md` | H-07 ← D-08, D-09 | Current-mechanisms input ACCEPTED (D-08: 4 precedents, replace-cost inventory); H-07 synthesis NOT STARTED |
| 27 | MVP Definition | 18 | `10-roadmap/mvp.md` | A-03, H-01 | **DRAFT COMPLETE 2026-09-26** — 823 lines. Picks ONE displacement (`JTBD-M02`'s chart-inspection step off Finviz onto UCT's own chart) and admits **1 of 85** backlog items, arguing the other 84 out. ⭐⭐ **Its best structural finding: the only displacement the programme verdicts absorb-outright has NO BACKLOG ITEM BEHIND IT** — item 16's A2 row reads "⛔ No item." — so **a backlog-picked MVP would have missed it**, because 60 of 85 items carry no gap reading and provenance was never "a competitor has this". ✅✅ **THEN ITS OWN §8.2 SETTLING GREP WAS RUN AND THE MVP LOST ITS BUILD ENTIRELY (CARD 24):** `chart.ashx` has **zero occurrences in all of `app/src`** on master, `Breadth.jsx:49` already lazy-imports `ChartPane`, and `Breadth.jsx:343` records the Finviz-tabbed `DrillModal` (~320 lines) as **DELETED** — so the in-app incumbent is **absent, not merely matched**, and that half of the trial is unrunnable rather than already-won. The **finviz.com-by-hand half survives** and is still the real question, still unmeasured (CP-06). ⛔⛔ **CONSEQUENCE FOR ITEM 13, flagged NOT acted on: its absorb-outright verdict is the row this MVP was selected from, and it rests on a product state that no longer holds** — the highest-value correction outstanding, deliberately not re-cut from one grep. ⭐ Deliberately omits K and N from its pre-registration (naming them would re-commit what CARD 22 §3 killed) and **refuses to remove the Finviz tabs during a trial** — *"voluntarily" requires a subtraction the SUBJECT makes*, and eligibility is a property of the occasion, never of our output, so a morning our chart renders blank is a **failed** occasion, not an excluded one. ⚠️ Still owner-blocked on a named non-builder subject; with none it returns **INCONCLUSIVE-BY-CONSTRUCTION, never a PASS** |
| 28 | Implementation Roadmap (NOW / NEXT / LATER / NOT PLANNED; three horizons) | 21 | `10-roadmap/roadmap.md` | H-03, A-01 | NOT STARTED |
| 29 | Technical Dependency Graph (+ parallel build graph) | 19 | `10-roadmap/dependency-graph.md` | H-04 | **DRAFT COMPLETE 2026-09-26** — 836 lines, verified against the author's byte count. **118 nodes, 109 edges**, counts DERIVED by parsing the finished adjacency table back out of the file. Edges are TYPED (code 67 · decision 21 · rail 10 · measurement 9 · vendor 2) because ⭐ they have different unblocking actions, and a graph that conflates them tells a reader to write code when they should send an email. ⭐⭐ **THE FINDING CHANGES WHAT THE CONSTRAINT IS: the graph is WIDE AND SHALLOW — depth 3, and 40 of 85 items have no hard prerequisite — so delivery is NOT dependency-bound, it is CONCURRENCY-bound.** Quantified from figures already paid for: 40 startable → **3 lanes** (agent cap) → **1 at verification** (one gate at a time on this box) → **1 master merge in flight** → **0 lanes** on flow-worker watch paths during RTH. ⛔ And parallelism DECAYS with depth — four chains disjoint at depth 0–1 converge to one front by depth 3 — the opposite of what a 40-wide in-degree-zero set suggests. ⭐ Names a **sixth shared resource nobody counts: this box is also the DATA PRODUCER** for the scheduled member-facing jobs, so a 46–92 min gate can contend with one. ⭐ Top unblocker: the shared provenance set plus a rail that every panel uses it — 14 transitive dependents across 8 families, waiting on nothing, sized M because two of its four primitives already ship (independently confirmed by the same night's ledger correction). ⛔⛔ **Two of its top nine nodes cannot be cleared by engineering at all** and together sit above 15 nodes: an identifier measurement (✅ since SETTLED from source — see the ledger banner) and **the owner naming a quiet window**. ⭐ Disagrees with item 16's dependency fields in 5 places each with settling evidence, rejects one edge deliberately with a reversal condition, and finds **4 edges declared only on the prerequisite's side**, so a graph built from dependents' fields alone loses them. ⚠️ Self-flagged most-likely error: the four-chain assignment is a judgement from row letters, not file-derived, so two lanes it calls disjoint could collide in one file |
| 30 | Engineering Backlog (Part CCI schema) | 20 | `10-roadmap/backlog.md` | H-03 | NOT STARTED |
| 31 | Architecture Decision Records | 15 | `12-decisions/adr/ADR-*.md` | ARCH-*, A-01 | NOT STARTED (individual formal ADRs, written when a decision genuinely locks) — but `12-decisions/ARCHITECTURAL_DECISION_REGISTER.md` (Phase 2, ACCEPTED 2026-09-02) is now the living tracker feeding them: 13 decisions, 4 already LOCKED with no counter-evidence found (D3 Entity Master, D4 Provider Abstraction, D6 Provenance Component, D7 Alert Taxonomy) |
| 32 | Risk Register | 22 | `00-program-control/RISK_REGISTER.md` → final copy `11-risks-and-open-questions/risk-register.md` | orchestrator | DRAFT (19 risks, R-01..R-19) |
| 33 | Open Questions Register | 22 | `00-program-control/OPEN_QUESTIONS.md` → final copy `11-risks-and-open-questions/open-questions.md` | orchestrator | DRAFT (16 questions, OQ-01..OQ-16) |
| 34 | Cost Model (six scenarios; labeled assumptions) | 22 | `09-security-licensing-cost/cost-model.md` | E-05, E-06 | Inputs COMPLETE (E-05 data/infra, E-06 AI/infra, both accepted); final merged `cost-model.md` NOT STARTED ⛔⛔ **OWNER DE-SCOPED 2026-09-26 (CARD 25 §5): "Dont worry aobut anything else on costs or uses."** I am reading that as standing this item down. ⚠️ **Stated as a READING, not a closure** — one owner sentence reinstates it. ✅ **Licensing and permission work is NOT affected** and remains load-bearing: *can this feed legally serve members* is a compliance question, not a cost question. |
| 35 | Success Metrics | 21 | `10-roadmap/success-metrics.md` | A-02, H-06 | **DRAFT COMPLETE 2026-09-26** — 628 lines. **21 metrics**, 12 defaultable rulings, **13 explicitly REJECTED**. ⭐⭐ **Computability is a SEPARATE FIELD from threshold, reported YES/PARTIAL/NO and never as a percentage: 9 YES · 2 PARTIAL · 9 NO.** Baselines: 10 measured from dated artifacts, **3 of those n = 1 and labelled readings rather than baselines**, and **6 of 6 market-sensitive ones taken with the market closed.** ⛔⛔ **CARD 16's ratified p95 bar has never once been computed**, and the mechanism is confirmed at source: the audit tool still classes `stale-swr` as COLD, so daily's warm set is empty, the guard never fires, and the figure recorded as a PASS was the COLD line's p50-and-max. ⭐⭐ **There is no readable business metric and the reason is arithmetic, not diligence:** the denominator is **13 accounts with any page-view row at all**, six of the roster being admins, so one account moves any rate by 3.85 points — coarser than a resolution already rejected elsewhere. ⭐ The metric it recommends that nobody proposed is **gate-evaluability**: the count of live definitions-of-done whose instrument is named and working — the only one in the set whose failure has already cost this programme twice. ⭐⭐ **And it found the programme's own MVP definition of done, identical in four charter places, is UNMEASURABLE AS WRITTEN** because the question of who decides "we prefer it" has been open since 2026-09-02: **eight of its nine NOs are missing an instrument; that one is missing a person.** ⚠️ It corrected the orchestrator's brief with citations — a price DOES exist in code — and expressed the cost metric as cap coverage per AI lane, a property of the code that does not divide by a price |
| 36 | Testing Strategy | 21 | `10-roadmap/testing-plan.md` | H-06 | **DRAFT COMPLETE 2026-09-26** — 806 lines, verified against the author's reported byte count before committing. Ten doctrine families, each rule carrying its incident; tiers T0–T7 where the third column is **the defect class each engine is structurally blind to**; a 12-clause contract for what a test must carry to count; the gate split merge / deploy / advisory; 13 defaultable rulings. ⭐⭐ **Load-bearing conclusion: the full-suite gate is LONGER THAN THE DISTURBANCE INTERVAL, so as a per-merge gate it can never finish, and there is no CI net under it** — 46–92 min against master moving 56 commits in 92 min, while the master deploy gate runs five fast checks and **no vitest at all**. So the local six-shard gate is the only full-suite verification a landing gets and this box cannot deliver it. **Putting vitest on a host that is not this machine is the single highest-leverage change identified** — it dissolves the arithmetic rather than negotiating with it. ⛔ **Gate on no-NEW-failures against a named dated baseline, never on green, and neither the exit code nor the task status is the verdict** — the wrapper has lied in BOTH directions. ⭐ It MEASURED rather than quoted, and the stale denominators are themselves the finding: 1,845 frontend test files against 1,178–1,283 in prose, and **`api/main.py` at 11,066 lines against its own docstring's ~9,800 — both re-verified by the orchestrator**. The gate's real denominator is DERIVED from disk each run, which is why it cannot drift while the prose beside it already has. ⚠️ Its ceiling: no test was run, so every number is a file count and never a pass count, and the 1,845-vs-1,283 gap is flagged as a work item rather than reconciled |
| 37 | Rollout Strategy (+ rollback) | 21 | `10-roadmap/rollout-rollback.md` | H-07 | **DRAFT COMPLETE 2026-09-26** — 901 lines. Ladder S0–S4 with a **reach** column, a rollback decision table, 13 pre-stage-one prerequisites each with its incident, and 11 defaultable rulings. ⭐⭐ **Headline, verified by the orchestrator: the lever a rollback actually pulls is INVISIBLE to the only flag inventory that exists.** `feature_flag_index.needs_declaration()` returns `not defaults_on(...)`, so a **kill switch — which defaults ON — is outside `docs/feature_flags.json` by construction**; `HUB_PREVIEW_ENABLED` occurs **0 times** in that file (measured). ⭐ **Framed correctly, this is a deliberate design whose rationale covers the wrong job**: the docstring reasons about DRIFT DETECTION (an off-and-unset gate is indistinguishable from a forgotten one, while an on-by-default gate is *self-evidently a live decision*) and never about INCIDENT RESPONSE — so the artifact a responder opens to find the rollback lever does not contain it, and the ledger auditor cannot see it either. ⛔⛔ **And since the 2026-09-16 cutover, revert-and-push is a rollback REQUEST, not a rollback**: `web` deploys from `production`, the promotion workflow promotes nothing unless the gate concluded success and is fast-forward-only — so the moment you need a push to land is the moment you least know the gate's verdict. Hence the **pre-authored, pre-gated rollback branch is ranked ABOVE plain revert**, being the only revert whose gate result is known before the incident. ⭐ **Staging is cohort then surface, never tier and never percentage** — tier is foreclosed by CARD 17, and percentage independently by population: it buckets BROWSERS not users, over ~26 production accounts, where the precedent it would copy ramped over ~200. ⚠️ Its own ceiling: no live flag read, so the headline proves absence from the LEDGER and not the pod's state; and **no rollback was ever rehearsed — a tier nobody has pulled in anger is a procedure, not a capability** |
| 38 | Final Executive Recommendation (Owner Decision Memo, ≤4 pages) | 23 | `13-executive-synthesis/owner-decision-memo.md` | F-06, A-01 | NOT STARTED |
| — | Master Plan (Part CC) | 24 | `13-executive-synthesis/MASTER_PLAN.md` | F-06, A-01 | NOT STARTED |
| — | Forty executive questions | 12 | `13-executive-synthesis/executive-questions.md` | F-06 | DRAFT COMPLETE — all 40 answered; scoreboard 10 green / 23 yellow / 7 red; reallocation advice included |
| — | Hypothesis register | 12 | `13-executive-synthesis/hypothesis-register.md` | F-08 | DRAFT COMPLETE — 35 hypotheses (H1-H5 seed + H6-H35 evidence-raised), 57 table rows |
| — | Licensing register | 5 | `09-security-licensing-cost/licensing-register.md` | F-04 | DRAFT COMPLETE — 118 rows (3 Allowed, 7 Likely Allowed, 81 Restricted, 18 Unknown, 8 Unsuitable), 24 escalations, 19 addenda |
| — | Terminal-Current map | 1 | `01-existing-system/terminal-current-map.md` | D-09 | DRAFT COMPLETE — 4 views, 12-panel modal, 31 `/api/calendar/*` routes, 9 readers of the API, 32-item what-users-would-lose list |
| — | Red-team verdicts | 17 | `12-decisions/red-team/` | G-* | NOT STARTED (G-01-D2 light pass contract written, not yet dispatched) |
| — | First vertical slice | 18 | `10-roadmap/first-slice.md` | H-01 | NOT STARTED |
| — | Protection rail | 25 | `00-program-control/protection-rail.md` | orchestrator | PASS at R1 (recovery checkpoint) — app diff empty, frontend 31/31, backend 374 passed, prod health/calendar 200 |
| — | Readiness test | 26 | `00-program-control/readiness-test.md` | H-08 | NOT RUN (Day 7) |
| — | Day 1 executive synthesis | 12 (Doc A required Day-1 artifact) | `13-executive-synthesis/DAY_1_EXECUTIVE_SYNTHESIS.md` | F-06 | NOT STARTED — deliverable 2 of F-06's contract; killed by the 3rd pause before it began; executive-questions.md (deliverable 1) is done and is its primary input |
| — | Orientation memo (Part CLXXIX) | — | `13-executive-synthesis/orientation-memo.md` | orchestrator | COMPLETE (delivered before Day 1b approval) |
| — | Cost model: data/infra | 22 (feeds #34) | `09-security-licensing-cost/cost-model-data.md` | E-05 | DRAFT COMPLETE |
| — | Cost model: AI/infra | 22 (feeds #34) | `09-security-licensing-cost/cost-model-ai-infra.md` | E-06 | DRAFT COMPLETE |
| — | Data-use classification | 5 (feeds #4/#38) | `09-security-licensing-cost/data-use-classification.md` | E-02 | DRAFT COMPLETE |
| — | Tech-debt register | 22-adjacent | `01-existing-system/tech-debt-register.md` | F-03a | DRAFT COMPLETE (72 entries, 4 classes) |
| — | Ecosystem cartography (both machines, scheduler) | 2 (feeds #2) | `01-existing-system/ecosystem-cartography.md` | D-14 | DRAFT COMPLETE (34 scheduled tasks confirmed; 4 silently failing) |
| — | AI-native tools survey | 9 (feeds #9) | `08-ai/ai-native-tools-survey.md` | C6-01 | DRAFT COMPLETE (15 products) |
| — | Grounding/citation architectures | 15/22 (feeds #22) | `08-ai/grounding-architectures.md` | C6-02 | DRAFT COMPLETE |
| — | Workspace systems survey | 14 (feeds #20) | `06-ux-and-information-architecture/workspace-systems-survey.md` | C5-01 | DRAFT COMPLETE |
| — | Command grammars | 14 (feeds #19) | `06-ux-and-information-architecture/command-grammars.md` | C4-01 | DRAFT COMPLETE |
| — | Streaming/caching architectures | 15 (feeds #21/#24) | `07-technical-architecture/domain-streaming-caching.md` | C7-01 | DRAFT COMPLETE |
| — | Personalization patterns | 14 (feeds #20) | `06-ux-and-information-architecture/personalization-patterns.md` | C5-02 | PARTIALLY COMPLETE — §1-2 internal patterns done; external research pending completion re-dispatch |
| — | Symbol master / time model | 15 (feeds #21) | `07-technical-architecture/domain-symbol-master-time.md` | C7-02 | PARTIALLY COMPLETE — §0-1 internal baseline done; external patterns + GAPS/SOURCES pending completion re-dispatch |
| — | Vendor abstraction / data platform | 15 (feeds #21) | `07-technical-architecture/domain-data-platform.md` | C7-03 | NOT STARTED (killed before any output; re-dispatch pending) |
| — | News architecture patterns | 4 (feeds #9) | `05-product-strategy/domain-news-intelligence.md` | C2-01 | NOT STARTED (killed before any output; re-dispatch pending) |
| — | Events intelligence patterns | 1/37 (feeds #26) | `05-product-strategy/domain-events-intelligence.md` | C2-02 | NOT STARTED (stub discarded; re-dispatch pending) |
| — | Desk-tool reports (thinkorswim, TradingView, Finviz, Market Chameleon) | 6 (feeds #9) | `03-competitive-research/desk-tools/*.md` | B-DESK-01..04 | DRAFT COMPLETE — all 4 accepted |
