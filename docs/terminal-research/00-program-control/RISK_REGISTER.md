# RISK REGISTER (Document C Part XCVI)

Likelihood / impact: H / M / L. Updated when risks change; reviewed at every checkpoint.

| ID | Category | Risk | L | I | Mitigation | Owner | Status |
|---|---|---|---|---|---|---|---|
| R-01 | Program | Subscription usage-limit pause mid-wave loses in-flight agent work and the dispatch plan. | H | M | Contracts on disk; `RESUME.md` updated at every checkpoint and before dispatches; agents write files incrementally; re-dispatch is exact. | orchestrator | open |
| R-02 | Evidence | Bloomberg and institutional dossiers hit an evidence ceiling (paywalled docs, video unreachable) and get filled by inference. | H | H | Ceiling rule in every contract; DISCARD rule for uniform-🟢 dossiers; OI-08 asks the owner for access; verifiers on the five most consequential claims. | B-*, F-01 | open |
| R-03 | Licensing | Member-facing data uses turn out Restricted or per-user-fee-bearing, invalidating member scope after design. | M | H | Licensing classified before Tier S/A; desk-first default (D-001) keeps internal display ahead of redistribution; escalation on any per-member cost. | E-*, F-04, A-06 | open |
| R-04 | Technical | Single-replica web pod with SQLite on one volume cannot sustain a multi-panel terminal's fan-out; jobs cannot move off web. | M | H | D-05/D-04 measure the envelope first; ARCH-07 designs to it; aggregation endpoints and subscription dedupe evaluated before any streaming expansion. | D-05, ARCH-07 | open |
| R-05 | Technical | Partner-owned live-flow files sit on the real-time path a terminal would extend; coordination friction or accidental edits. | M | M | Partner files listed in GOVERNING_PRINCIPLES; agents read-only; code-ownership map (Part CCIV) names them; no touch without acknowledgment. | orchestrator, H-05 | open |
| R-06 | Evidence | Archaeology misled by dead or duplicated code and by CLAUDE.md claims of unreachable idioms. | H | M | CLAIM vs CONFIRMED labeling; active/dormant/dead classification required; synthesis cross-checks D-01/D-02 against D-07 and D-14. | D-*, F-03a | open |
| R-07 | Product | Directive volume on workspaces/panels biases the program toward a modular workspace before evidence. | M | H | Part XXI equal-standing rule; fixed/modular/hybrid is a gated, red-teamed deliverable; three independent ARCH proposals. | C5-03, G-01, G-02 | open |
| R-08 | Program | Context compaction loses the plan for the next wave or a synthesis held only in conversation. | H | M | Document B §35 discipline: control files updated before dispatch and at checkpoints; nothing important lives only in context. | orchestrator | open |
| R-09 | Operational | The owner's PC is the scheduler host for ~36 daily jobs; a terminal that depends on their outputs inherits a single point of failure. | H | M | D-14 maps dependencies; reliability architecture states which surfaces degrade when the PC is off; not a Phase-Zero fix. | D-14, ARCH-07 | open |
| R-10 | Safety | Accidental production impact (push to master, script on the pod, mutating Railway command). | L | H | Protection rail at every checkpoint; DO NOT clause in every contract; orchestrator is the only committer; branch pushed only to `origin/terminal-research`. | orchestrator | open |
| R-11 | Program | Coverage map executed unevenly: breadth roles finish, depth roles (Bloomberg, licensing) lag, and synthesis proceeds on thin evidence. | M | M | Critical path tiers; deadline-health tracking; reallocation rule (YELLOW → parallelism, not quality). | orchestrator, F-06 | open |
| R-12 | Commercial | Costs that scale with member count (real-time fees, AI per user) discovered late. | M | H | E-03/E-06 explicit per-user cost columns; escalation rule on any per-member cost; population reserve doctrine. | E-05, E-06, A-02 | open |
| R-13 | Technical | The existing workspace persistence has a silent, unrecoverable data-loss path (unparseable layout → empty board → autosaved over) and no versioning; building Terminal-Next state on the same key→TEXT blob would inherit it. | M | H | Any Terminal-Next state model must adopt the repo's existing versioned/tombstoned/hydration-gated patterns (D-11 names them) and a recovery/reset path before it stores anything; not a Phase-Zero fix for `/charts`. | D-11 → ARCH-01..03, H-01 | open |
| R-14 | Licensing | Member-facing display of Massive/FMP/Finviz data may sit outside the tier UCT holds (retail tiers are individual-use; FMP requires a DDLA for multi-user display). Applies to production today as well as to Terminal-Next. | M | H | Escalated as D-002; OI-03 sharpened; the licensing register classifies every member-facing raw display Restricted-pending-contract; desk-first default. | E-01 → E-02, F-04, A-06 | open |
| R-15 | Operational | The local-backend recipes (`run-local.ps1`, the CLAUDE.md port-8077 recipe) set neither DATA_DIR nor AUTH_DB_PATH, so any local backend runs against the owner's live `C:\data`; a program prototype started the documented way would touch live data. | M | H | Program rule: no local backend during Phase Zero unless started with explicit DATA_DIR/AUTH_DB_PATH pins derived from `conftest.SHARED_DATA_ENV_PINS`; recorded in GOVERNING_PRINCIPLES hazards. | D-04, orchestrator | open |
