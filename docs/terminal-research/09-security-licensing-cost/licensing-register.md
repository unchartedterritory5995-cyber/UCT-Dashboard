---
id: F-04
title: Licensing register — every data use today and every TERMINAL-NEXT candidate use, classified with the driving clause, the owner fact that settles it, the scenario outcomes and the safest design
role: Licensing/cost synthesizer (Group F) — single writer of the licensing register (gate item 5)
wave: 2
group: F
category: synthesis
scope: read-only synthesis over E-01, E-02, E-03, E-04, F-03b, OWNER_DECISIONS D-002, OWNER_INPUTS_REQUESTED OI-03/04/09/12/15/17, RISK_REGISTER R-14/R-17, OPEN_QUESTIONS OQ-09/15/16, RESEARCH_GAPS RG-21/RG-22, OWNER_SEED_FACTS §5–6, AGENT_REGISTRY (ARCH-04/05/06, A-06, E-05/E-06 roles), charter Part XLI; no application code read, no web research, no production call, no vendor API called. Written in two runs (a usage-limit pause after §1, DL-019): §1 unchanged from the first run; §2–§5, GAPS and NOT INSPECTED from the completion run
confidence: 🟡 medium (clause text 🟢 inherited from E-01/E-03/E-04's dated primary documents; production flag state 🟢 via ORCH-RAILWAY-01 as relayed by E-02/F-03b; which CONTRACTS UCT holds 🔴 — owner facts nobody has seen)
evidence_ceiling: No vendor contract, order form, invoice, plan page or account console was seen by any agent in the program. The Massive tier (Individual vs Business) and the FMP Data Display and Licensing Agreement are UNKNOWN and between them move 57 of the 118 rows below. Finviz publishes no terms at all; Schwab's Market Data Agreement and TheFly's terms are login-gated or client-rendered; OpenAI/Perplexity/Reddit primary terms were unreachable. Whether Massive-sourced prices arrive real-time or 15-min delayed was never measured. This register adds no new evidence — it arranges the pod's evidence into one table and names the fact that moves each row.
sources: docs/terminal-research/09-security-licensing-cost/data-use-classification.md (E-02), .../vendor-terms-evidence.md (E-01), .../realtime-and-exchange-classification.md (E-03), .../derived-data-rights.md (E-04), docs/terminal-research/02-data-providers/provider-ledger.md (F-03b), docs/terminal-research/00-program-control/OWNER_DECISIONS.md (D-002), .../OWNER_INPUTS_REQUESTED.md (OI-03/04/09/12/15/17), .../RISK_REGISTER.md (R-14, R-17), .../AGENT_REGISTRY.md (ARCH-04/05/06 roles), .../charter/C-master-directive.md Part XLI, .../OPEN_QUESTIONS.md (OQ-09/15/16), .../RESEARCH_GAPS.md (RG-21/22), .../DECISION_LOG.md (DL-004/DL-019), .../charter/OWNER_SEED_FACTS.md (§5–6)
uct_relevance: high
status: draft
date: 2026-09-02
---

# F-04 — Licensing register (canonical, gate item 5)

**Vocabulary.** TERMINAL-CURRENT = the `/calendar` surface display-named "UCT Terminal" (route, keys, `/api/calendar/*` unchanged). TERMINAL-NEXT = the product this program designs. UT is the parent brand; UCT Intelligence is the product.

**This register classifies risk, not law.** Every row is a research read of public terms against inspectable code and a read-only production pass. It is not legal advice. Contract facts come only from the owner (charter Part XLI; `OWNER_SEED_FACTS` §6): **nothing below is Allowed on the strength of a vendor contract**, because no vendor contract has been seen. Unknowns are escalated in §2 and are never buried.

## Legend (read before the table)

| Column | Meaning |
|---|---|
| **Class in force** | The class the program proceeds on **today**, under the defaults already in force: D-002 (b)+(c) — every member-facing raw-vendor display is *Restricted-pending-contract*; OI-E02-01 — **assume Massive Individual**; OI-E02-02 — **assume no FMP DDLA**. Codes: **A** Allowed (no vendor licence anywhere in the path — public domain or UCT's own content only) · **LA** Likely Allowed (verify contract) · **R** Restricted (a public clause on its face collides with the behaviour; **not** a finding of breach) · **U** Unknown (clause unreachable or tier unresolved) · **X** Unsuitable (prohibited with no purchasable remedy — only Yahoo/yfinance, TheFly-direct, and model training on X/Reddit content reach it). |
| **Status** | **LIVE** = in production today (armed flag or observed artifact, per F-03b/ORCH-RAILWAY-01) · **DORMANT** = code path exists, key or flag absent on every Railway service · **RULE** = a use nobody performs; the row exists so nobody starts · **PLAN** = a TERMINAL-NEXT candidate use, not built. |
| **Scenario** | What the class becomes under each answer to the two facts that flip the most rows: **Ind / Bus** = Massive Individual tier / Business or Enterprise tier; **noDDLA / DDLA** = FMP without / with a Data Display and Licensing Agreement. `n/a` = the row does not depend on that fact. |
| **Driver clause** | The sibling file and section holding the verbatim clause (E-01 §n, E-02 §n, E-03 §n, E-04 §n, F-03b §n). Quotes here are ≤ a sentence; the primary text and its URL live in the cited section. |
| **Owner fact** | The single fact that settles the row, keyed to `OWNER_INPUTS_REQUESTED.md` (OI-nn), E-02's sub-questions (OI-E02-nn), or the sibling's open question. |
| **Per-user cost?** | Whether the exchange or vendor cost scales with member count (R-12 escalation trigger): Yes / No / Possibly / Fixed. Dollar figures are public list prices quoted by E-03/E-01, not a cost model (E-05 owns that). |

Audience vocabulary: **desk** = owner and staff only · **member** = a paid, logged-in surface · **community** = guild-locked UCT Discord servers · **paid newsletter** = the Substack paid tier · **public** = anyone: an unauthenticated route, a public Discord channel, public YouTube, a free newsletter tier.

---

## 1. THE REGISTER (Deliverable 1)

One row per (use, provider, data class, audience). **Part A** is today's uses (E-02 §1/§2, reconciled by F-03b §6); **Part B** is TERMINAL-NEXT candidate uses. Row ids are stable: `T-nn` today, `N-nn` next, `X-nn` addenda (§3).

### 1A. Today's uses

#### Massive.com (formerly Polygon.io) — equities

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-01 | Member display — live quotes and snapshots (`/api/live-prices` 15 s, `/api/snapshot`, `/api/snapshot/{sym}`) | Massive REST · equity last sale + quotes | member (as designed) | LIVE | **R** | Ind: P1 §1 "may not use the Market Data to build an application intended for use by end users other than you"; §2 "strictly for display use only"; §5(c) Derived Works (E-03 §3.1). Bus: P3 §2.2 + §6.1(e) Edge Users (E-01 §1a). Massive's own gate: "customer-facing display, or 200+ users, you'll need a Business plan" (E-03 §3.1) | OI-03(a) Massive tier · OI-09 real-time entitlement / who is vendor of record | Ind **R** · Bus **LA**; FMP n/a | 15-min delayed last price + **real-time volume** + live % of ADV + prior close; `Del-15` at the top of the page; Financial Status Indicator (UTP — E-03 §2.3). Removes the exchange fee; **does not touch the vendor tier** (E-02 §7.1) | **Yes** — non-pro/member/mo on the tapes + the Business gate at 200+ users |
| T-02 | The same endpoints served **without a session** (R-17 CONFIRMED 2026-09-02 08:05 UTC: `/api/live-prices?tickers=SPY` 200, `/api/snapshot/SPY` 200) | Massive REST · quotes | **public** — anyone with the URL | LIVE | **R** (survives the Business tier) | Edge Users are "users of Customer's products and services"; the public is not (E-02 §2.1). CTA Exhibit A: an entitlement system that cannot produce audit information licenses NYSE to "bill for all devices on your network" (E-03 §2.2, §4.2 row 1a) | OI-17 (deliberate or not) · OI-E02-10 | Ind **R** · Bus **R** | Server-side auth on every data route (ARCH-06 Tier-S per R-17). Nothing contractual fixes this row | **Unbounded by construction** |
| T-03 | Member display — developing intraday bar (`/api/stream/bars`, 250 ms; `STREAM_BARS_ENABLED=1`) and current-session intraday bars (`/api/bars` 1m–60m) | Massive WS `wss://socket.massive.com/stocks` + REST · aggregates | member (route unauthenticated — E-03 §1.1 rows 3–4) | LIVE | **R** | P1 §5(c); a 250 ms developing bar "is not meaningfully different from a quote feed and should not be argued as derived" — it fails UTP Derived Data test 2 (E-03 §2.5, §4.2 rows 3/4a) | OI-03(a) · OI-09 | Ind **R** · Bus **LA** | Complete history + a 15-min-delayed developing bar + a **live volume bar** (E-02 §7.2 "Charts") | **Yes** |
| T-04 | Member display — D/W/M history (`/api/bars`, `/api/bars-history`, 5,000 bars, 30-year cap) | Massive REST · EOD aggregates | member (route unauthenticated) | LIVE | **R** | Ind: P1 §1/§2. On the plans End-of-Day Information is the cheapest class and multi-day history is not a facsimile of live Information — E-03 §4.2 row 4 reads it LA on the plans | OI-03(a) | Ind **R** · Bus **LA** | Keep — "the safest data in the product" (E-03 §2.5); authenticate the route | No |
| T-05 | Member display — movers rails (`/api/movers`, `/api/extended-movers`) | Massive gainers/losers snapshot | member (R-17: `/api/movers` answers 200 unauthenticated) | LIVE | **R** | P1 §1/§2. Plans: a multi-symbol ranked list is not fee liable (UTP §2) but each name's live % change is single-security price data (E-03 §4.2 row 6) | OI-03(a) · the vendor's classification of a movers list | Ind **R** · Bus **LA** | Rank on multi-symbol metrics; show a delayed or prior-close-referenced %; authenticate | Possibly |
| T-06 | Member display — Massive news (`/v2/reference/news`, third in the news chain) | Massive · news + sentiment | member | LIVE | **R** | P1 §1/§2; Bus §6.1(e) (E-02 §2.1) | OI-03(a) | Ind **R** · Bus **LA**; external **R / R** | Promote above AlphaVantage; RSS + SEC EDGAR + the licensed vendor's news is the cleanest combination (E-02 §7.2) | No |
| T-07 | Member display + external — reference data (splits, dividends, tickers, conditions) | Massive · reference | member + external | LIVE (underused: `breadth_dividends.py:60` and `dividends_calendar.py` still call yfinance — F-03b §3.1) | **R** | P1 §1; Bus §2.2 — the **only** Massive row whose external-publication column is LA (E-02 §2.1) | OI-03(a) | Ind **R** · Bus **LA (incl. external)** | Move dividends/splits off yfinance onto this endpoint family | No |
| T-08 | Desk display — raw Massive data to owner and staff only | Massive · all classes | desk | LIVE | **R** | P1 §3: commercial use "is incompatible with Non-Professional status, even if the business or commercial use is on behalf of an organization not in the securities industry" — with an indemnity (E-02 §7.1). Bus: Authorized Users, §2.1 | OI-03(a) | Ind **R** · Bus **LA** | None at the Individual tier — **the desk-only escape does not exist at Massive** (E-02 §1 finding 4) | No |
| T-09 | Storage — `bars.db` (no prune found; effectively indefinite), R2 `uct-bars-snapshots` (not observed pruned), the `bars-api` R2-synced copy | Massive · aggregates | internal | LIVE | **R** | P1 is silent on storage and §2 is display-only → no grant. P3 §2.2 grants `store` expressly — the only express storage right at any vendor in the stack (E-02 §3.1, §4). Deletion on termination P1 §10 / P3 §11.4 | OI-03(a) · OI-03(d) addenda · **is the R2 bucket UCT's own account?** (E-02 §4 open question — if not, storage is redistribution) | Ind **R** · Bus **LA** | License retention explicitly; choose a horizon per store ("a retention window nobody chose is a retention window nobody can defend" — E-02 §4) | No |
| T-10 | Caching — in-memory TTLCache, `/data/bars_cache` (D 48 h … 5 m 2 h), browser IndexedDB `barsIDB` on members' devices | Massive · aggregates + quotes | internal + members' browsers | LIVE | **R** | No caching clause exists in P1–P6 (E-04 §3.1); the deletion duty (P1 §10 / P3 §11.4) reaches a browser tier UCT cannot delete on demand (E-02 §4) | OI-03(a) / (d) | Ind **R** · Bus **LA** (deletion duty persists) | Keep bounded TTLs; add a delete/invalidate primitive (`CACHE_LOGIC_VERSION` bump) to the vendor-exit runbook | No |
| T-11 | Derived analytics — intraday breadth (~40 metrics) + lenses, RS rank/line, correlation, sector flow, theme returns, base-structure library, candle library, pattern engine (50 detectors), server indicators, Pine parity | Massive · aggregates + full-market snapshot | member | LIVE | **R** | Ind: P1 §5(c) reaches "any data, charts, analytics, research, or other works based on, referring to, or derived from" — **broader than the exchange plans**, which make multi-security derived data fee-free (UTP §2; CTA note 8) (E-03 §4.4A; E-04 §3.1). Bus: §6.1(j) is enumerated and narrow; most of this inventory sits outside it | OI-03(a) — **and whether the Business grant reaches Derived Works, not only display** (E-02 §2.1 open question) | Ind **R** · Bus **LA** | Nothing is needed on the plans. Frame the Massive conversation around Derived Works rights, not display rights (E-03 §4.4A) | **No** — the free lane on the plans |
| T-12 | Derived analytics — the composites: UCT20 portfolio **NAV** (`uct20_nav.py compute_portfolio_returns()`), the Exposure Rating (0–150, an indicative value), published entry/stop/target on named tickers | Massive · aggregates (+ the yfinance EOD breadth row, T-75) | member + paid Substack + Discord | LIVE | **R** (either tier) | P3 §6.1(j) enumerates "any index, indicative value, net asset value, investment product, financial contract … settlement value or investment strategy" — closer to these three outputs than to "analytics" (E-01 §1a; E-02 §2.1 last row) | A specific written answer from Massive on §6.1(j) (OI-03(d)) | Ind **R** · Bus **R** | Ask the question; do not add new composite publications until answered; desk-first (D-001) | No |
| T-13 | Derived analytics — breadth drill-down lists (the names behind a cell) | Massive · symbol lists | member | LIVE | **LA** | A symbol list carries no price; not Information (E-03 §4.2 row 11a) | Confirm the drill payload carries no live price | n/a | Keep | No |
| T-14 | AI processing — Massive numbers into Claude: AI Search synthesis (`AI_SEARCH_CLAUDE_SYNTH=1` — the default member lane), agent/deep/dossier, Compass chat/voice/brain tools, catalysts, significant catalysts, Model Book recaps, theme engine, wire rundown | Massive · quotes, bars, snapshots, flow | member (wire → paid Substack) | LIVE | **R** | Ind: an LLM output over the data is a work "derived from" it (P1 §5(c)). Bus: LA — plus Anthropic §L.1, under which UCT warrants it has "all rights and permissions required to submit Inputs" (E-01 §13; E-02 §2.13, §5) | OI-03(a) | Ind **R** · Bus **LA** | Ground on EOD + multi-symbol derived facts, never on a live single-symbol quote; copy the COT lane (a facts module that is the only thing the model may cite + a grounding gate) — E-02 §7.2 | No |
| T-15 | AI processing — a chart screenshot (a rendering of Massive bars) to OpenAI `gpt-4o` (voice chart vision) | Massive · bars → OpenAI | member (voice) | LIVE | **R** | Tier-conditional (E-02 §5 row 8); OpenAI terms themselves U (primary 403 — E-01 §14) | OI-03(a) · OpenAI terms re-fetch | Ind **R** · Bus **LA** | As T-14 | No |
| T-16 | External publication — `#TSDR` index-close charts, 15:45 ET (8 charts/day incl. the developing session) | Massive · bars | **public** Discord ("THE PUBLIC COMMUNITY CHANNEL" by the code's own docstring) | LIVE (`DISCORD_INDEX_CLOSE_ENABLED=1`) | **R** (either tier) | The public is not an Edge User (E-02 §2.1, §6 row 4). A chart is display of the underlying data, not a derived work — OHLC values are readable off it (E-04 §4 #1). The no-numerals rule is an anti-hallucination guard, not a licensing control (E-03 §1.2) | E-03 Part 6 #7: marketing or member content? · OI-03(a) | Ind **R** · Bus **R** | Last bar ≥ 15 min old with "Data delayed 15 minutes" burned into the image (E-03 §1.2); or post into a member-gated channel | No (fixed) |
| T-17 | External publication — `/r/*` render panels (chart, flow, movers, breadth, internals, econ, earncards) behind `CHART_RENDER_TOKEN`, which is inlined into the JS bundle | Massive et al. · whatever the panel draws | **public** ("treat these as EFFECTIVELY PUBLIC" — the router's own docstring); deliberately exempt from the COMING SOON gate | LIVE | **R** | E-03 §1.1 row 14; E-02 §1 finding 3, §6 row 3 | E-03 Part 6 #8: real auth on `/r/*`? | Ind **R** · Bus **R** | Real auth on `/r/*` (the three out-of-repo consumers are UCT's own), or delayed panels | No |
| T-18 | External publication — Desk sessions → YouTube **PUBLIC**: screen recordings of the dashboard (Massive bars and quotes, Schwab-derived GEX, FMP tables), `DESK_PUBLIC_SHOWS=*` | Massive + Schwab + FMP · whatever was on screen | **public** YouTube — including the paywalled Live Trading Sessions | LIVE — recorded in code as an **owner decision dated 2026-08-19** (`desk_daily_session.py:106-109`) | **R** (either tier) | The widest vendor-data redistribution surface in the product, and deliberate (E-02 §1 finding 1, §6 row 1); the wildcard short-circuits `privacy_for_section()` before any show matching | OI-E02-04: **re-confirm now that the redistribution cost is written down** — an accepted-risk entry for `OWNER_DECISIONS.md`, not a bug report | Ind **R** · Bus **R** | Name shows instead of `*`; unlisted for vendor-data-heavy shows. This register has no standing to reverse the decision | No |
| T-19 | External publication — Sunday Scans charts → Substack, **including a free mid-week tier** (`uct-sunday-scan/sunday_scan/boilerplate.py:323`) | Massive · bars via `/r/chart` | **public** newsletter tier | LIVE | **R** | E-02 §6 row 5; E-04 §8 — a free tier is public | OI-E02-11: is the free tier active, and does it carry charts? | Ind **R** · Bus **R** | Delay + notice on the images; or paid-only distribution of charts | No |
| T-20 | External publication — Morning Wire → **paid** Substack (charts, levels, exposure, book, FMP grades, AV news, Finviz-derived scans) | Massive + FMP + AV + Finviz | paid newsletter audience | LIVE (`WIRE_ENABLED=1`; `send_gate` fails closed) | **R / U** | A paid newsletter audience is arguably not "users of Customer's products and services" in the app sense (E-02 §6 row 6, 🟡); FMP §2.6.1(i); the TheFly chain (T-45) | A specific question to Massive and FMP (OI-03(d)) · OI-12 commercial model | Ind **R** · Bus **U** | Ground prose on EOD/derived facts; delayed charts; note that FMP attribution is **not** available as a remedy (§10.4) | No |
| T-21 | External publication — Discord `/chart` house images | Massive · bars | **community** — guild-locked to two UCT servers (`discord_interactions.py:863-897`); user-installs and DMs refused | LIVE | **R / LA** | E-02 §9 #2 takes the guild lock over E-03's "PUBLIC + USER_INSTALL" app-registration default; that default is a latent re-opening | OI-03(a) | Ind **R** · Bus **LA** (community display) | Keep the guild lock; fix the app-registration default so code and registration agree | No |
| T-22 | External publication — dark-pool **record** alerts (ticker + notional of record off-exchange prints) to Discord | Massive SIP trades · derived | Discord channel per `darkpool_eod._webhook` — **audience NOT DETERMINED** | LIVE (`DARKPOOL_RECORDS_ENABLED=1`, code default `"0"`) | **R** | E-02 §6 row 7; E-04 §7 — is a single print's ticker + notional Market Data or a Derived Work? | OI-03(a) · the channel's audience | Ind **R** · Bus **R if public / LA if member-only** | Post only into member-gated channels; delay | No |

#### Massive.com — options (OPRA) and dark pool

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-23 | Member display — real-time OPRA options tape `/live-massive` (`/api/live/massive/recent`, `/curated`, `/by-contract`, SSE `/stream`) | Massive OPRA WS `wss://socket.massive.com/options`, wildcard `T.*` (flow-worker) · every options trade | member (proxy-vouched via `api/flow_proxy.py`) | LIVE (`MASSIVE_WS_ENABLED=1` on flow-worker, `=0` on web — both true, E-02 §9 #8) | **R** | OPRA Non-Professional Subscriber Agreement ¶2 (P1 §4 Schedule 1): "shall not retransmit or otherwise furnish the OPRA Data to any person, other than your own employees"; OPRA Redistribution Fee $1,500/mo "whether on a current or delayed basis"; $1.25/non-pro/mo real-time (E-03 §2.4). Bus: P3 §2.5 Third-Party Agreements are unpublished (E-01 §1a) | OI-03(a) — and whether the Business agreement names **customer-facing display of OPRA data** · OI-09: is Massive the vendor of record paying the $1,500 and reporting UCT's members as its non-pros? | Ind **R** · Bus **U** | **Decide the tier explicitly and early** — (i) retire, (ii) historical-only (the only free shape), (iii) delayed ($1,500/mo floor, zero per-user), (iv) real-time (E-02 §7.2) | **Yes** — $1.25/member/mo real-time on top of a $1,500/mo floor |
| T-24 | Member display — options NBBO histogram / current quotes (`/nbbo-histogram`, `POST /current-quotes`) | Massive OPRA · options quotes | member | LIVE | **R** | As T-23; quote data is Basic Service (E-03 §4.2 row 8) | As T-23 | Ind **R** · Bus **U** | As T-23 | Yes |
| T-25 | Storage — `flow.db` full-tape OPRA archive of **unbounded age** (`FLOW_PRUNE_ENABLED` written, wired, and set on **no** service); tape spool 72 h; gap-fill archive 30 d; `top_flow_picks.json` never pruned | Massive OPRA · trades | internal | LIVE | **R** | No storage grant at Ind (T-09). Whether a stored `T.*` tape is "historical OPRA Data" (the fee-exempt case) or ongoing redistribution when `/by-contract` history is served from it is not answered by the fee schedule on its face (E-03 §2.4; E-02 §1 finding 5, §4) | OI-03(a)/(d) · the OPRA sub-agreement | Ind **R** · Bus **U** | Arm `FLOW_PRUNE_ENABLED` to a chosen horizon (E-02 §4 REC) | No |
| T-26 | Derived analytics — SWEEP/BLOCK events, server-side flow analytics, Top-Flow picks + grades, dealer positioning (the OPRA flow side; the OI side is Schwab, T-77) | Massive OPRA · derived | member | LIVE | **R** | Massive Derived Works (P1 §5(c)). OPRA publishes **no** derived-data definition, no substitutability test, and no fee treatment of derived products across eleven public documents — the treatment is decided administratively from a free-text Exhibit A description (E-04 §4 #3) | Ask Massive/OPRA in writing — "the highest value-per-email question in the register" (E-02 GAPS) | Ind **R** · Bus **LA (verify)** | Keep member-gated; never public | No |
| T-27 | External publication — the Flow Scoreboard `GET /api/flow-scoreboard` (hit rates, grade calibration, recent picks, **contract-price gains from the OPRA tape**) | Massive OPRA · derived | **public**, unauthenticated, mounted unconditionally | LIVE — deliberate ("a public trust asset"; "No auth on the GET — read-only, cacheable, public", `flow_scoreboard.py:11,19-20`) | **R** (either tier) | Viewers are not Edge Users (E-04 §7 #1). Cboe §15: derived data "analogous to price data (e.g. returns, percentage change, and performance data)" is fee liable (E-04 §4 #2) | OI-E02-05: a deliberate accepted risk? → record in `OWNER_DECISIONS.md` | Ind **R** · Bus **R** | Authenticate it, or publish aggregates with no contract-price series; record the accepted risk | No |
| T-28 | Member display — options chain + Greeks + IV (`polygon_options.py`, `/v3/snapshot/options/*`) → implied / expected move (calendar enrichment, research) | Massive · chain | member; calendar; wire → Substack | LIVE (`IMPLIED_STORE_ENABLED=1`) | **R** | P1 §1/§5(c); Bus LA. An implied move in dollars is a single-security price derivation — fee liable under UTP §1 and Cboe §15 (E-04 §4 #2) | OI-03(a) | Ind **R** · Bus **LA** | Show it as % of price in multi-symbol context; keep in-app | Possibly (single-security) |
| T-29 | Storage — implied-capture history (`implied_snapshots`, nightly pre-report) | Massive chains + the FMP/Finnhub reporter list | internal | LIVE | **R** | As T-09; FMP §6.3 attaches to the reporter join (T-42) | OI-03(a) / (b) | Ind **R** · Bus **LA** | License retention explicitly | No |
| T-30 | Member display — dark-pool prints, T+1 SIP flat files (`us_stocks_sip/trades_v1`, ≥ $4 M notional) | Massive S3 flat files · SIP trades | member (proxy-vouched) | LIVE (`DARKPOOL_FLATFILE_ENABLED=1`) | **R** | Historical/EOD is the cheapest class in every plan; OPRA's historical exemption is the analogue (E-03 §4.2 row 10) | OI-03(a) · the flat-file product licence | Ind **R** · Bus **LA** | Keep T+1 | No |
| T-31 | Member display — same-day per-ticker `/v3/trades` dark-pool lane | Massive REST · trades | member | LIVE | **R** | Inside the 15-min delay interval the prints are Information; past it, Delayed Information (E-03 §4.2 row 10a) | Measurable in code — whether the lane runs inside or outside the 15-min interval (E-03 Part 6 #10; not measured) | Ind **R** · Bus **U** | Enforce a ≥ 15-min lag on the same-day lane | Possibly |
| T-32 | Storage — `darkpool.db` (~120 trading days) + `darkpool_records`, never pruned by design | Massive SIP trades | internal | LIVE | **R** | As T-09; the records table is the accretive asset (E-02 §4, citing D-13 §7) | OI-03(a) | Ind **R** · Bus **LA** | License retention; keep | No |

#### FMP — Financial Modeling Prep

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-33 | Member display — statements, fundamentals, ratios, key metrics, profile, float (research pages; the screener's `fundamentals_bulk` — ten whole-market columns in six requests) | FMP · fundamentals | member | LIVE (OC 🟢 — the coverage monitor's 06:08 UTC cycle, F-03b row 4) | **R** | §2.2.2 Data Display: prohibited "on platforms including … applications designed for utilization by multiple individuals, irrespective of whether such usage is complimentary or paid, and whether it pertains to internal or external organizational purposes" (E-01 §2; E-04 §3.2). Pricing page: "requires a specific Data Display and Licensing Agreement with FMP" | OI-03(b) the DDLA · and the Acceptable Data Use Policy text (ToS §2.6.2 binds it; the URL 404s) | noDDLA **R** · DDLA **LA** | The DDLA is the single artifact — "the cheapest large conversion available" (E-02 §3.2). Without it: SEC EDGAR filings-derived fundamentals (A). **yfinance is not a fallback** (T-73) | No |
| T-34 | Member display — analyst estimates, grades, price targets, consensus (calendar enrichment, research, nightly `screener_analyst_pass`) | FMP · estimates | member | LIVE (OC 🟢 `price_target` 0.958, `consensus` 0.958) | **R** | §2.2.2; §2.6.1(i) no providing "data or information contained in or derived from The Services" to any third party | OI-03(b) | noDDLA **R** · DDLA **LA** | DDLA | No |
| T-35 | Member display — earnings calendar, forward (one of **four** providers assembling one field, with EarningsWhispers, Finnhub and a Finviz column) | FMP · calendar | member | LIVE | **R** | §2.2.2 | OI-03(b) | noDDLA **R** · DDLA **LA** | Consolidate the four-provider field onto the licensed vendor + SEC EDGAR 8-K; event data is the lowest-risk class (E-02 §7.2 "Calendar") | No |
| T-36 | Member display — earnings history / surprises (the Model Book earnings table, calendar beat history) | FMP `stable/earnings` | member | LIVE (OC 🟢 `beat_history` 1.0) | **R** | §2.2.2 reaches these "even though they are barely derived" — the clause turns on display to multiple individuals, not on derivation (E-04 §7) | OI-03(b) | noDDLA **R** · DDLA **LA** | DDLA | No |
| T-37 | Member display — economic calendar (all non-current weeks; ForexFactory `thisweek` is the other leg) | FMP · econ calendar | member | LIVE | **R** | §2.2.2 | OI-03(b) | noDDLA **R** · DDLA **LA** | DDLA | No |
| T-38 | Member display — news / press releases (fourth in the news chain) | FMP · news | member | LIVE | **R** | §2.2.2 | OI-03(b) | noDDLA **R** · DDLA **LA** | RSS + EDGAR + Massive news (T-06) | No |
| T-39 | Member display — ETF holdings, index constituents; institutional ownership, insider trading | FMP | member | LIVE | **R** | §2.2.2 | OI-03(b) | noDDLA **R** · DDLA **LA** | SEC EDGAR Form 4 / 13F are public domain and already reachable (`edgar.py`) — F-03b §5.4 | No |
| T-40 | Member display — intraday bars **fallback** (`historical-chart/{interval}`) reaching a live chart | FMP · intraday bars | member | LIVE (fallback — invisible when Massive works) | **R** | §2.2.2; "Access to real-time or delayed data may be subject to additional agreements with the relevant exchange" (E-03 §3.4) | OI-03(b) | noDDLA **R** · DDLA **LA** | Remove FMP from the live-chart path; Massive is primary | Possibly |
| T-41 | Desk display — raw FMP to owner and staff | FMP · all | desk | LIVE | **R** | §2.2.2 "internal or external"; §2.2.1 a personal licence "may not be used on behalf of a company, partnership, organization" — **no internal-use carve-out exists** (E-02 §2.2) | OI-03(b) | noDDLA **R** · DDLA **LA** | None — the desk-only escape does not exist at FMP | No |
| T-42 | Storage / caching — per-surface TTL caches; 30-day Model Book cache; the engine KB's `earnings_analytics` (40,731 rows, FMP/Finnhub-sourced) **shipped to Railway inside the Brain Pack** into a member-facing RAG; transcript FTS5 index on the volume | FMP · all | internal → member-facing RAG | LIVE | **R** | §6.3 delete on termination "including data cached" + a signed Data Deletion Agreement (Exhibit A) + an audit right; **§2.8** "notify FMP of the IP and domain aliases of any location where data is stored or processed" — nothing in the pod suggests that notification was ever made (E-02 §2.2 fn 1, §4) | OI-03(b)/(d) · has the §2.8 notice ever been sent? | noDDLA **R** · DDLA **LA with live obligations** (§6.3, §2.8) | Inventory every store holding FMP rows (including the Brain Pack) into the vendor-exit runbook; send the §2.8 notice | No |
| T-43 | AI processing — earnings-call transcript **bodies** (FMP `stable/earning-call-transcript` primary; Finnhub fallback 403) → Claude summary, stored and displayed | FMP · transcripts (copyrighted prose) | member | LIVE (`TRANSCRIPT_INDEX_ENABLED=1`; coverage monitor `transcript: null (n=0)` this cycle) | **U** — "the sharpest AI row" | §2.6.1(i) "derived from"; §11.1 FMP claims the IP in derived information; **no vendor in the stack has an AI clause** — the derived-works clauses govern; Anthropic §L.1 (E-02 §2.2, §5; E-04 §5) | A written FMP answer on summarising and storing transcript derivatives — in the same email as the DDLA | noDDLA **R** (display) / **U** (AI) · DDLA **U** — a DDLA about *display* may not speak to *summarising copyrighted prose* | Settle before scaling; treat as a named content-licensing question separate from market data (E-04 §5 REC) | No |
| T-44 | AI processing — FMP numbers (fundamentals, analyst, earnings) into AI Search synth/agent, Compass, stock briefs, Model Book | FMP · numeric | member | LIVE (`AI_SEARCH_CLAUDE_SYNTH=1`) | **U** | No AI clause; derived-works clauses govern; §L.1 re-attaches §2.2.2 at the prompt boundary (E-02 §5 rows 4–8, §2.13) | OI-03(b) | noDDLA **U/R** · DDLA **U (verify)** | Map data classes to prompts as well as pixels (E-01 §13) | No |
| T-45 | External publication — FMP grades (TheFly + StreetInsider origin, `stable/grades-latest-news`) summarised into the Morning Wire → paid Substack | FMP · analyst actions — **a sublicence chain nobody named** | paid newsletter | LIVE | **R / U** | §2.6.1(i); §10.4; UCT's licence is with FMP, FMP's with TheFly, and UCT cannot see the middle link (E-02 §2.9; E-04 §9C). TheFly's own consumer terms bar text mining and commercial use (E-01 §7) | Ask FMP in the same email: does the licence cover redistributing TheFly-sourced analyst actions in a paid newsletter? | noDDLA **R** · DDLA **U** | Name the chain in the register (done); ground wire prose on the action's existence, not TheFly text | No |
| T-46 | Attribution — naming FMP as a source anywhere public (a "Data sources" page, a footer) | FMP | public | RULE | **R** | §10.4 "Customer may not identify FMP as the source of the Data to any third party without FMP's prior written consent" — the instinctive display remedy is closed off (E-04 §3.2). Collides with FRED's **mandatory** notice (E-01 §2) | Written consent (the DDLA) | noDDLA **R** · DDLA governed by the agreement | Do not build a sources page that names FMP without consent | No |

#### Finviz Elite

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-47 | Derived / internal — the nightly whole-market export as a scanner input (dashboard `screener_finviz_universe` 02:45 ET; the engine's three scans inside the 06:35 CT wire; the morning-wire client) | Finviz Elite `export.ashx?v=152` · screener universe rows | internal → the member screener composite | LIVE (OC 🟢 engine log 2026-08-31/09-01) | **U** | **No terms document exists** — four entry points + a Wayback check with zero snapshots (E-01 §3; E-04 §3.4b). `robots.txt` disallows `/export` and `/api/v1/screener-export-csv`. The one substantive statement is Finviz's own upstream constraint: "not allowed to sell raw historical data to third parties" (E-02 §2.3). Reachability is not a licence | OI-03(c): the agreement presented at Elite purchase (the account page or the purchase-confirmation email) | n/a | The lower-risk half; keep internal | No |
| T-48 | Member display — float / short float / short ratio / short interest / ownership columns in the screener row (**the only short-interest source in the product**, nightly, sparse, no history) | Finviz Elite · export columns c=30/31/84 | member | LIVE | **U** (single-sourced) | As T-47. A Finviz "no" is a **capability deletion**, not a swap — no second vendor exists (E-02 §2.3; F-03b §5.5) | OI-03(c) | n/a | Price short interest as a coverage gap; FINRA's public bi-monthly short interest is not integrated (F-03b §5.5) | No |
| T-49 | Member display — Finviz-rendered chart PNGs (`chart.ashx`) served into member pages (`schwab_router.py:424,440` CONFIRMED, partner-owned; Breadth DrillModal / Theme Tracker panels per CLAUDE.md = CLAIM) | Finviz · rendered chart images | member | LIVE (CLAIM for the two panels) | **U, leaning R** | Republishing Finviz's own rendered work product; `robots.txt` disallows `/chart` and `/image`; no grant anywhere because there is no document to grant one (E-04 §3.4b) | OI-03(c) — and: is Finviz an internal input only, or are images served to members? | n/a | **Retire Finviz images from member surfaces first** (OI-E02-03 default); render from Massive bars through `/r/chart` | No |
| T-50 | AI processing + external publication — Finviz-derived scan output in the wire rundown → paid Substack | Finviz · scan output | paid newsletter | LIVE | **U** | As T-47; Anthropic §L.1 | OI-03(c) | n/a | Ground on Massive-derived scan facts | No |

#### Finnhub

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-51 | Member display — the real-time equity tick stream `/api/stream/prices` (SSE), the **primary** live-price source for every quote tile | Finnhub WS `wss://ws.finnhub.io` · trades | member (route unauthenticated) | LIVE — on the free tier by the code's own tell (`api/routers/stream.py:25` `MAX_SSE_TICKERS = 50  # Finnhub free tier cap`) | **R** | "All plan listed on Finnhub website is strictly for personal use"; "not redistribute or share access to data **or derived results from the data** … without written approval"; "Personal plan can't be used by any business **even internally**" (E-01 §6; E-03 §3.2). ⚠️ CLAUDE.md says this stream is Massive/Polygon; the register carries the code (E-02 §9 #3) | OI-E02-06: plan tier, and any written approval | n/a | **Retire onto Massive WS/snapshot** (already called) — E-02 §2.4, E-03 §3.2 and E-04 §3.3 converge independently; keep the two-feed isolation pattern, reduce the vendor count | Yes (or a vendor swap removes the row) |
| T-52 | Member display — earnings-calendar past-day backfill; company profile / metrics / logo; insider transactions; IPO calendar | Finnhub · calendars + reference | member | LIVE (OC 🟡 `avg_vol` 1.0; `calendar_hour_resolved` 0.816) | **R** | Same clauses; two endpoints (recommendations/PT, transcripts) 403 on this plan for months (F-03b row 6) | OI-E02-06 | n/a | Move to FMP (+ EDGAR Form 4); verify FMP `earnings-calendar` by single day covers past days **before** cutting the backfill (F-03b §4 #5) | No |
| T-53 | Derived analytics — earnings-intel rollups, insider rollups, calendar enrichment, `avg_vol` | Finnhub · derived | member | LIVE | **R** | "or derived results from the data" — undefined and unbounded; no non-reversibility test, no aggregation threshold (E-02 §2.4) | OI-E02-06 | n/a | As T-52 | No |
| T-54 | AI processing — Finnhub fields in Compass / AI Search tool packs | Finnhub | member | LIVE | **R** | "derived results"; Anthropic §L.1 | OI-E02-06 | n/a | As T-52 | No |
| T-55 | Storage / caching — profile 24 h on disk (`/data/ticker_meta_cache`), earnings intel 6 h, insider 4 h; forbidden-endpoint cache 24 h | Finnhub | internal | LIVE | **R** | "All data must be deleted should your subscription to that data ends" (E-01 §6) | OI-E02-06 | n/a | Add the Finnhub stores to the vendor-exit runbook | No |
| T-56 | Desk display — any internal use | Finnhub · all | desk | LIVE | **R** | "even internally without a written approval" — Finnhub is one of three vendors where desk-only does not help (E-02 §2.4) | OI-E02-06 | n/a | None — retire | No |

#### AlphaVantage

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-57 | Member display — news + sentiment (`NEWS_SENTIMENT`, the primary in `engine.get_news()`, behind a 25-request-per-**day** budget broker) | AlphaVantage · news | member | LIVE (KP + CR; the boot warm of `get_news()` is a CLAIM — F-03b row 7) | **R** | §2(a)(ii) use "as or on behalf of a corporation, firm, partnership" and §2(a)(iii) "any type of commercial activity that allows individuals or entities other than User to access information" — each catches UCT independently. "The free tier is not a mitigating fact; it is the statement of the problem" (E-01 §5; E-04 §3.4; E-02 §2.5) | OI-E02-07: free key, or a written commercial agreement (`premium@alphavantage.co`) | n/a | Retire onto RSS + Massive news (the working fallback) — "close to free" | No |
| T-58 | AI processing + storage — verbatim earnings-call transcripts (`EARNINGS_CALL_TRANSCRIPT`, lazy, 24 h per (ticker, quarter)) → Claude summary | AlphaVantage · transcripts | member | LIVE (lane exists; key set on web) | **R** | §2(a) — settled at the licence-grant level; no AI, derived or storage clause needed (E-04 §3.4) | OI-E02-07 | n/a | Retire onto FMP transcripts (primary since 2026-08-05) | No |
| T-59 | Economic indicators / commodities via AV, which import the FRED API Terms (§20) | AlphaVantage · econ | member (if any econ endpoint is called — **NOT DETERMINED**) | LIVE? (consumer census not done) | **R** + FRED obligations | AV §20: "By using our Economic Indicators APIs and/or Commodities APIs, you agree to be bound by the FRED® API Terms of Use" — a transitive obligation the code path hides (E-01 §4–5) | OI-E02-07 · a consumer census | n/a | Retire with the AV lane; the FRED flow-down goes with it (F-03b §4 #6) | No |

#### FRED (Federal Reserve Bank of St. Louis) — dormant in production

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-60 | Desk/member display, caching (30-min TTLCache at `fred_economic.py:24,158`), AI processing (values into Compass via `voice_tool_impls.py:442,453`), risk-free rate (`options_chain.py:32`) — ~30-series catalog, **no page consumes it** | FRED · economic series | member (voice) | **DORMANT** — `FRED_API_KEY` absent on every Railway service; `fred_economic.py:105-111` returns an error dict | **R if armed** | Legal Notices §Prohibitions: no "store, cache, or archive any portion"; no use "in connection with the development or training of … large language models" — framed as binding on "all use … including non-commercial, educational, and personal use". API Terms: the notice "This product uses the FRED® API but is not endorsed or certified by the Federal Reserve Bank of St. Louis" must appear prominently — **zero hits in the codebase** (E-04 §3.4b; E-02 §2.6). Two documents in tension (the Graphs License grants display and reproduction) | OI-E02-14: arm deliberately or retire (default: retire) · counsel on whether "in connection with" reaches inference-time prompting · thirty `fred/series/search` lookups to settle which catalog series carry a third-party copyright tier (⚠️ E-04 §7 retracted a guess at three named series — do not reintroduce it) | n/a | If armed: the exact notice + a link to the FRED terms + a clause binding members + a per-series copyright check + no cache + no LLM on any pre-approval-tier series. Arming is one variable that makes three collisions live at once | No |
| T-61 | External publication — FRED series in newsletters | FRED | public | DORMANT | **LA†** (citation-requested / citation-required tiers) · **R** (pre-approval tier, e.g. Visa SMI) | §IV Commercial Use expressly permits "textbooks, newsletters, or reports to clients provided that appropriate attribution is given" for two of the three tiers (E-04 §3.4b) | As T-60 | n/a | Per-series tier check first; attribution always | No |

#### twitterapi.io → X / Twitter

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-62 | Member display — verbatim tweet text on the Dashboard `TapeFeed.jsx` (four curated accounts) | twitterapi.io → X · tweet text | member | LIVE (`TWITTERAPI_IO_ENABLED=1`) | **R** — display requirements **measurably unmet** | X Developer Agreement §III.A(d) (no redistribution to third parties); X Display Requirements (author avatar, @username and display name linking to the profile; timestamp linking to the permalink; the X logo) — `TapeFeed.jsx:53-68` renders the text, a *relative* time and a `↗` only (E-04 §3.4b #3). twitterapi.io §4 grants no content licence and pushes X compliance downstream — "a reseller cannot grant more than it holds" (E-01 §8–9) | Counsel: does the Developer Agreement bind a scraper-intermediated consumer, or does exposure run to X's anti-scraping ToS? Both readings are unfavourable. Is a direct X developer account wanted? (F-03b Q-17) | n/a | Bring the tile to the Display Requirements (a UI fix, hours); or show cashtag counts + jump links only (T-66) | No |
| T-63 | Storage — `tweets.db` verbatim bodies, `TWEET_RETENTION_DAYS` = 7; **no deletion-sync** | X · tweet text | internal | LIVE | **R** | X Developer Policy: delete or modify stored content "as soon as reasonably possible, or within 24 hours" of it being deleted or modified on X — `tweet_cleanup.py:12` implements an age sweep only (E-04 §3.4b #4). The 7-day window bounds exposure to a week by coincidence, not by design | None external — a code decision | n/a | Implement deletion-on-request / re-check; keep the 7-day bound | No |
| T-64 | Storage — `catalysts.db` `raw_signals` copies the same tweet bodies **and third-party RSS items** and never prunes (`catalyst/engine.py:1163-1165`) | X + RSS publishers · text | internal | LIVE | **R** | Defeats the 7-day window: "one store honours the window and another quietly keeps the same bodies forever" (E-02 §2.7 #3; E-04 §6) | None — actionable today | n/a | Stop persisting bodies — store ids + counts (the `buzz_store` idiom) or extend the sweep to reach them | No |
| T-65 | AI processing — tweet text into the catalyst thesis (Claude, inference) plus `advanced_search` per top-20 candidate | X · tweet text | member | LIVE | **U** | §III.A(k) reads to "fine-tune or train a foundation or frontier model", not inference — the better side of the line, and "exactly the line a vendor may read more broadly than a customer does" (E-02 §2.7); Anthropic §L.1 | Counsel read | n/a | Pass counts and ids, let the model narrate; never train | No |
| T-66 | Derived analytics — cashtag-derived counts and signals, no text | X | member | LIVE | **LA** | No content is redistributed (E-02 §2.7 row 3) | None | n/a | The safe shape — copy it | No |
| T-67 | Training any model on X content | X | — | RULE (not done) | **X** | §III.A(k) (E-01 §9) | None | n/a | Never | — |

#### Reddit · TheFly · Stocktwits

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-68 | All uses — PRAW sentiment over five subreddits (voice lane only, no page surface) | Reddit · posts + comments | member (voice) | **DORMANT** (`REDDIT_CLIENT_ID`/`_SECRET` absent everywhere) | **R if armed** (AI inference **U**; training **X**) | Data API Terms §3.1 commercial use "will need to enter into a separate agreement with Reddit"; §2 no AI training without rightsholder permission; §Termination reaches "any data or models that were derived from User Content" (E-04 §3.4b; E-01 §10 secondary-only) | OI-E02-14: leave unwired (default) · was a commercial agreement ever approved? | n/a | The row never becomes live | No |
| T-69 | Direct TheFly (`api/services/thefly_news.py`, `THEFLY_API_KEY`) | TheFly · squawk | — | **DORMANT** — never keyed anywhere; `morning-wire/thefly.py:5-11` removed `fetch_analyst_actions()` 2026-07-29 | **X** (consumer terms) / **U** (an unseen syndication agreement) | "ANY COMMERCIAL USE OF THE CONTENT AND ONLINE SERVICES IS STRICTLY FORBIDDEN"; no "regularly providing other users with access"; no "data or text mining"; one user per login (E-01 §7). `robots.txt` `ai-train=no`, `ClaudeBot` disallowed (E-04 §3.4b) | E-01 §18 #6: consumer or syndication? | n/a | Delete or stub the wrapper (F-03b §4 #10). A squawk feed for TERMINAL-NEXT is a syndication contract priced new — never a consumer subscription | No |
| T-70 | Stocktwits — unauthenticated public JSON, bull/bear ratio + sample messages (voice lane) | Stocktwits | member (voice) | LIVE (voice-only) | **U** | Terms not researched; "no auth required" is not "permitted" — the 200/hr rate limit was read as a technical budget, not a permission (E-04 §3.6) | Stocktwits' terms | n/a | If it graduates to a visible tile, obtain a credentialed path first | No |

#### Yahoo Finance via yfinance — the only rows where "verify the contract" is not a move

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-71 | Member display — intraday + daily bars **fallback** (`bars_fetch.py:1095 _fetch_intraday_yfinance`; the pre-2003 D/W/M tail) | Yahoo · bars | member | LIVE (a fallback — invisible when Massive works, serving members when it doesn't) | **X** | Yahoo ToS §2.4(ix) no automated collection "using any automated means … robots, spiders, scrapers"; §2.5 no commercial reuse; §2.8 no derivative works for commercial purposes; §2.4(x) no "database, archive … data feed … that competes with or constitutes a material substitute" — all four independently engaged. **No licence exists to buy at any price**; yfinance's Apache-2.0 governs the code, not the data (E-01 §11; E-02 §2.10) | None — a build decision | n/a | Remove yfinance from every member-facing path; Massive covers bars, `/v3/snapshot/indices` the index tail (F-03b §3.1) | No |
| T-72 | Member display — index / futures / crypto snapshots (`^GSPC`, `^NDX`, `^DJI`, `^RUT`, `^VIX`; BTC; NQ/ES/RTY) | Yahoo · quotes | member | LIVE | **X** | As T-71 | None | n/a | Massive index snapshots; **futures quotes have no licensed provider in the stack** (F-03b §2) | No |
| T-73 | Member display — `.info` fundamentals, ownership, short interest, dividends, estimates: **24 importing modules** incl. `fundamentals.py`, `research/{estimates,financials,ownership}.py`, `institutional_holdings.py`, `short_interest.py`, `earnings_table.py`, `dividends_calendar.py`, `setup_grade.py` | Yahoo · reference | member | LIVE | **X** | As T-71 (E-04 §9B) | None | n/a | Move to FMP (`fundamentals_bulk.py` already pulls ten whole-market columns in six requests) + SEC EDGAR | No |
| T-74 | Member display (voice) — options chain + locally computed Black-Scholes Greeks (`options_chain.py`) | Yahoo · chain | member (voice) | LIVE | **X** | As T-71 | None | n/a | Retire onto `polygon_options.py` (native Greeks/IV) — F-03b §4 #16 | No |
| T-75 | Derived analytics — the **authoritative EOD breadth row** (`uct-intelligence/scripts/breadth_collector.py:377,776,1986`, `yf.download(..., auto_adjust=True)`) → the Exposure Rating the whole product is organised around → member app + paid Substack + Discord | Yahoo · dividend-adjusted daily history | member + external | LIVE (OC 🟢 — collector log 2026-09-01 15:39:52, 92 keys pushed) | **X** | As T-71. "The input to the Exposure Rating rests on an Unsuitable source" (E-04 §9B; F-03b §2 "Breadth") | None — E-04 §9B's open question: *is the owner aware?* | n/a | Its own project, not a sweep: the dividend-adjusted basis is baked into every stored level and `bars.db` is split-adjusted only | No |
| T-76 | The wire's `generate_top_picks()` prev-day OHLC via `yf.download`; `schwab_router.py` earnings via yfinance; direct `query1/query2.finance.yahoo.com` calls in partner-owned files | Yahoo | paid Substack / member | LIVE | **X** | As T-71 | None | n/a | Massive prev-day bars (the scanner already carries `prev_day_*`) | No |

#### Charles Schwab (Trader / Market Data API) — partner-owned integration

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-77 | Member display — option chains + Greeks + OI → GEX, gamma walls, zero-gamma, dealer positioning (`/api/gex/*`, `/api/dealer-positioning/*` — **no auth dependency on either router**; R-17: `/api/gex/data` reaches its handler unauthenticated) | Schwab `api.schwabapi.com/marketdata/v1/chains` · chains | member — and anyone reaching the route | LIVE | **U, leaning R** | Schwab non-pro subscriber language: data "solely for their personal, non-business use … shall not furnish market data to any other person or entity"; **one process-wide OAuth token** at `/data/schwab_token.json` — one account holder's entitlement fanned out to the membership; Individual Developer vs Commercial/Redistribution (requires Schwab review + exchange-data agreements) (E-03 §3.3; E-01 §12; E-04 §3.5). The developer portal 403s; the Market Data Agreement is behind login | OI-E02-08: which developer tier; does any accepted agreement contemplate display to non-account-holders — **routed through the partner** | n/a | **Re-source the chain from Massive** (`polygon_options.py` already serves chains + Greeks natively) — an owner *sourcing* question, never a change request against a partner file; authenticate the routes (E-04 §8) | No (fixed) |
| T-78 | Storage — Schwab OI snapshots for ΔOI attribution; the persisted token | Schwab | internal | LIVE | **U** | Market Data Agreement unread (E-01 §12) | As T-77 | n/a | As T-77 | No |
| T-79 | External publication — Schwab-derived GEX inside public Desk YouTube recordings (T-18) | Schwab · derived | public | LIVE | **R** | Schwab passes through exchange entitlements — the professional/non-professional classification and NYSE/CTA/CQ/NASDAQ fees are squarely in play (E-02 §2.11) | As T-18 + T-77 | n/a | As T-18 | No |

#### SnapTrade (Journal 2.0 broker mirror)

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-80 | Member display — the member's own positions, activities, balances and option holdings, mirrored to that member | SnapTrade · member account data | that member | LIVE (`BROKER_SYNC_ENABLED=1`) | **LA** | The member's own data, to the member (E-02 §2.11) | None | n/a | Keep | No (per-connection pricing NOT DETERMINED) |
| T-81 | Cross-member use — a leaderboard, a "what the room owns" tile, an AI over member fills | SnapTrade · aggregated End User data | members / AI | RULE (not built) | **R** | SnapTrade Developer Terms: no mining, re-selling or re-packaging End User data "without the express written consent of both the End User and SnapTrade" — bars onward **use**, not merely sale (E-01 §15; E-02 §2.11) | Both consents | n/a | Do not build without per-member consent plus SnapTrade's | — |

#### Public-domain sources and UCT's own content

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-82 | All seven uses — CFTC Commitments of Traders (62 symbols, 10 y; the LLM weekly read behind a grounding gate; optional Discord post, blank webhook posts nothing) | CFTC · public zips | member + optional public | LIVE | **A** | US Government work, public domain (E-02 §2.12) — "the cleanest AI surface in the product" | None | n/a | The template for every AI lane: a facts module that is the only thing the model may cite + a grounding gate that stores nothing on a mismatch | No |
| T-83 | All seven uses — SEC EDGAR filings, full-text search, CIK map, 8-K RSS; Form 4 / 13F (unused for ownership today) | SEC · public domain | member | LIVE | **A** | Public domain; the descriptive User-Agent and ~10 req/s fair-access rule are etiquette, not display restrictions (E-01 §15) | None | n/a | The free, unrestricted substitute for FMP filings-derived fundamentals and ownership (E-02 §7.2) | No |
| T-84 | All uses — `/buzz` ticker-mention counts + jump links, **no message text stored** → Discord digest | Discord · UCT's own community | members | LIVE (`BUZZ_DIGEST_ENABLED=1`) | **A** | Own content; `buzz_store.py:3-6` stores no text by design — the one deliberate data minimisation in the codebase (E-02 §2.12) | None | n/a | Copy the idiom to `raw_signals` (T-64) | No |
| T-85 | Member display / AI / storage — the 7,766-message classified `#tsdr` corpus (text + author ids; 591 rows promoted into the engine KB → the Brain Pack on Railway → a member-facing RAG) | Discord · member-authored text | desk **LA** · member **U** · AI **U** · external **U** | LIVE (the KB ships) | **U** | Not a market-data licence — member consent, privacy and Discord's own terms (E-02 §2.12; OI-15) | OI-15: what consent basis covers the export, and will it refresh past 2026-02-20? | n/a | Internal-RAG use only until answered (OI-15 default); no member display of message text | No |
| T-86 | Member display / storage — The Floor / community board content (member-authored) | UCT · own | members | LIVE | **LA** (AI **U**, external **U**) | E-02 §2.12 row 5 | A terms-of-use clause for member-posted content (incl. pasted charts — E-03 §4.3) | n/a | A ToU clause | No |

#### The LLM providers as processors

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-87 | AI processing — every Claude lane (AI Search synth/agent/deep/dossier, Compass, catalysts, transcripts, call recap, COT, wire rundown + Top 5, wire critic, Desk insights, theme engine, Model Book, community, indicators, bot RAG) | Anthropic API · processor | member | LIVE (key on web; ~40 `*_MODEL` vars; `COMPASS_MENTOR_MODE=admin`) | **LA — conditioned on §L.1** | §B "Anthropic may not train models on Customer Content"; §B assigns Outputs to Customer; §A.1 contemplates powering "products and services Customer makes available to its own customers and end users"; §D.4 no resale; **§L.1: Customer "has all rights and permissions required to submit Inputs"** — so FMP's display ban, Finnhub's "derived results", TheFly's text-mining ban, X's content rules and AV's commercial definition all re-attach at the prompt boundary (E-01 §13, §16; E-02 §2.13). "The AI vendor is permissive; the AI pipeline inherits the strictness of its worst-licensed input" | None for Anthropic itself — every R in the input rows above | n/a | Map data classes to prompts as well as pixels; the grounding-gate shape. Calendar reminder: Cboe Non-Display Category 4 "Enterprise Derived" (effective 2026-09-01) names LLM-over-market-data products whose outputs are "distributed externally" as a declarable fee category (E-04 §4 #4) | No |
| T-88 | AI processing — Whisper STT, `gpt-4o-mini` cleanup/intent, `gpt-realtime`, `gpt-4o` chart vision, `tts-1`, `text-embedding-3-small`, `gpt-image-1` covers | OpenAI · processor | member | LIVE | **U** | Primary business terms returned 403 to every fetch path; secondary: no training on API inputs by default, 30-day retention, ZDR available (E-01 §14) — "nothing looks like a blocker; 'looks like' is doing real work" | Re-fetch the terms from an ordinary browser (F-03b Q-11) | n/a | Verify; per-user monthly caps already exist in `voice_usage_monthly` | No |
| T-89 | AI processing — Perplexity Sonar (AI Search fast lane `sonar-pro`; catalyst discovery A1/D1/F1 + four enrichment shapes; earnings deep-dive; sector framing; wire enrichment) with Output displayed **in-app** | Perplexity · processor | member | LIVE (`CATALYST_ENGINE_ENABLED=1`) | **U** | Secondary: customers may "display such Output solely within Customer Applications" — in-app fits; Output routinely quotes cited sources and "citation is not permission" (E-01 §14; E-02 §2.13) | Tier (standard vs Enterprise terms; which governs Output ownership) — E-01 §18 #9 | n/a | Keep in-app | No |
| T-90 | External publication — Perplexity-derived text leaving the app (the paid Substack wire, the Sunday Scans free tier, Discord posts) | Perplexity · output | newsletter / Discord | LIVE | **R-shaped U** | Not obviously "within Customer Applications" (E-02 §2.13) | Ask Perplexity; tier | n/a | Ground external prose on public-domain / EOD facts | No |
| T-91 | AI processing — the Anthropic **subscription seat** (`C:\Users\Patrick\uct-recaps\desk_insights_polish.py`, `claude -p`) rewriting Desk headline / summary / chapter titles → YouTube descriptions + the Desk player | Anthropic seat · processor | public artifacts | LIVE (task rc 0, 2026-09-01) | **U — owner question** | The product's own written rule: the index-close note "RUNS ON THE API KEY, NEVER THE SUBSCRIPTION SEAT … Anthropic's legal terms … do not permit routing requests through Pro/Max plan credentials on behalf of your users" (`discord_close_note.py:17-25`). The polish lane is not member request traffic — but it produces publicly consumed artifacts. Two lanes, one doctrine, opposite answers (E-02 §2.13 row 4) | OI-E02-13 | n/a | Move the lane to the API key (default) | No |

#### Not researched by the licensing pod — recorded as U, never inferred

| # | Use | Provider · data class | Audience | Status | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|---|
| T-92 | Member display / internal — EarningsWhispers scrape (schedule, BMO/AMC, anticipation rank); openinsider clusters; ForexFactory `thisweek`; seven RSS publishers + Google News RSS (headlines); the logo chain (logo.dev → Parqet → Clearbit; logos are third-party trademarks, cached onto `/data`); Unusual Whales (partner-file-only on the dashboard; behind FMP in the wire); earningscall.biz / Quartr (dormant); Notion / Dropbox / MS Graph note connectors (dormant); Picovoice (dormant; per-user caps typical); Webshare / `HF_TOKEN` (dormant) | various | member / internal | LIVE (EW, openinsider, FF, RSS, logos, UW-wire) · DORMANT (the rest) | **U — not researched** | F-03b §1B records each as "U — not researched, never inferred"; logo.dev returned HTTP 429 (E-01 §15) | E-01 §15 / §18 · OI-04 (is Unusual Whales still billed?) · one follow-up fetch of `logo.dev/terms` | n/a | Consolidate the four-provider earnings-date field (T-35); credentialed paths before any of these graduates to a visible tile | No (Picovoice per-user caps possible — E-01 §15) |

### 1B. TERMINAL-NEXT candidate uses

Every row is **PLAN**. The class in force is what ARCH-04/05/06 must design against today (§4); the scenario column is what changes when the owner answers.

| # | Candidate use | Provider · data class | Audience | Class in force | Driver clause | Owner fact that settles it | Scenario | Safest design alternative | Per-user cost? |
|---|---|---|---|---|---|---|---|---|---|
| N-01 | **Security page — real-time single-symbol quote header** | Massive · quotes | member | **R** | Massive P1 §1 / §5(c) (Ind); CTA/UTP display fees; non-professional attestation required before real-time access (CTA Exhibit A) — none exists in the product (E-03 §2.1–2.2). "The single most expensive primitive in the product" (E-02 §7.2) | OI-03(a) · OI-09 / OI-E02-09 (vendor of record) | Ind **R** · Bus **LA** (with either upstream vendor-of-record coverage or UCT's own entitlement machinery) | 15-min delayed last price + **real-time volume** + live % of ADV + prior close; `Del-15` at the top of the page; Financial Status Indicator (E-02 §7.2) | **Yes** — non-pro/member/mo + the Business gate |
| N-02 | **Security page — the delayed-price / live-volume variant (the load-bearing lever)** | Massive · quotes + volume | member | **R (Ind) / LA (Bus)** | UTP Data Policies: "there is no charge for UTP Delayed Information distributed on Controlled Products"; "Vendors are currently not required to obtain Subscriber Agreements"; Derived Data §3 real-time volume "at no additional charge" (E-03 §2.3). Solves the exchange fee; **does not touch the vendor tier** (E-02 §7.1) | OI-03(a) · does the owner's own workflow tolerate the delay when live volume and breadth are preserved? (E-03 Part 5) | Ind **R** · Bus **LA** | This *is* the safest design — and a **new build**: zero delay-notice strings exist in `app/` or `api/` (E-02 §1) | No on Tape C; CTA delayed service still needs a contracted delayed service + a conspicuous notice |
| N-03 | **Security page — chart, complete D/W/M + intraday history** | Massive · aggregates | member | **R (Ind) / LA (Bus)** | EOD/historical is the cheapest class (E-03 §4.3) | OI-03(a) | Ind **R** · Bus **LA** | Keep; the cheapest data in the taxonomy | No |
| N-04 | **Security page — chart, real-time developing bar** | Massive WS · aggregates | member | **R** | A 250 ms developing bar is a real-time single-symbol display (E-03 §2.5) | OI-03(a) · OI-09 | Ind **R** · Bus **LA** (+ exchange fee) | A 15-min-delayed last bar with a live volume bar | Yes |
| N-05 | **Watchlist — quotes × N rows** | Massive · quotes | member | **R** | The same primitive × N (E-02 §7.2) | As N-01 | Ind **R** · Bus **LA** | Delayed price + live volume; **rank and sort on multi-symbol derived metrics** (RS rank, relative volume, breadth context — fee-exempt under UTP §2) | Yes if real-time |
| N-06 | Watchlist — the Finnhub tick stream carried forward | Finnhub | member | **R** | T-51 (E-03 §3.2) | OI-E02-06 | n/a | Do not carry forward; Massive only | Yes |
| N-07 | **Options flow panel — real-time OPRA prints** | Massive OPRA | member | **R** | OPRA redistribution $1,500/mo + $1.25/member/mo; P3 §2.5 unpublished (E-03 §2.4) | OI-03(a) — does the Business agreement name OPRA display? · OI-09 | Ind **R** · Bus **U** | An explicit tier decision (T-23) | **Yes** |
| N-08 | Options flow panel — delayed (≥ 15 min) | Massive OPRA | member | **R / U** | Delayed removes the per-user fee, **not** the $1,500/mo floor (E-03 §2.4) | As N-07 | Ind **R** · Bus **U** | — | No per-user; $1,500/mo fixed |
| N-09 | Options flow panel — historical-only (the T+1 tape; `/by-contract` history) | Massive OPRA (stored tape) | member | **R / U** | OPRA exempts historical-only redistribution from the fee; whether the stored full `T.*` tape counts as "historical OPRA Data" is unanswered (E-03 §2.4) | The OPRA sub-agreement | Ind **R** · Bus **LA (verify)** | **The only free shape** | No |
| N-10 | Options — chain + Greeks + IV panel; GEX / dealer positioning rebuilt on Massive chains | Massive `/v3/snapshot/options` | member | **R** | Massive P1/P3; "Additional exchange fees apply" on the options business plan (E-03 §3.1) | OI-03(a) — the options business agreement | Ind **R** · Bus **LA** | Re-source from Schwab to Massive (T-77) | Possibly (options exchange fees) |
| N-11 | **Fundamentals panel — statements, ratios, profile** | FMP | member | **R** | §2.2.2 | OI-03(b) | noDDLA **R** · DDLA **LA** | The DDLA; else EDGAR-derived (A) | No |
| N-12 | **Estimates panel — consensus, grades, price targets, and a revision timeline** (derivable by retaining `screener_analyst_pass` snapshots — F-03b §5.2) | FMP (+ TheFly-origin actions) | member | **R** | §2.2.2; §6.3 / §2.8 attach to the retained snapshots | OI-03(b) · the TheFly-chain question (T-45) | noDDLA **R** · DDLA **LA** (TheFly-origin rows **U**) | The DDLA; retention licensed explicitly | No |
| N-13 | **Earnings events — calendar, BMO/AMC, beat history, expected move** | FMP + EarningsWhispers + Finnhub + Finviz (four providers for one field) + Massive chains | member | **R / U** (mixed) | Event data is not market data — "the lowest-risk surface in the product; provider sprawl is the risk" (E-02 §7.2) | OI-03(b) · OI-E02-06 · OI-03(c) | noDDLA **R** · DDLA **LA** (FMP leg) | Consolidate onto the licensed vendor + SEC EDGAR 8-K; expected move as % not $ | No |
| N-14 | **Economic events — calendar + macro series** | FMP econ calendar; FRED (dormant) | member | **R / R-if-armed** | FMP §2.2.2; FRED's three deliverables + the caching and LLM bans (T-60) | OI-03(b) · OI-E02-14 | noDDLA **R** · DDLA **LA** | FMP under a DDLA; FRED only with the notice, a per-series copyright check, no cache and no LLM on restricted series | No |
| N-15 | **News panel — headlines with a price attached** | RSS + EDGAR + Massive news (the clean combination); AV (R), X (R), TheFly (X) | member | **LA** at the headline · **R** if the attached price is real-time | The headline is not exchange data; attach a delayed or prior-close price (E-03 §4.3) | OI-03(a) for the Massive-news leg | Ind **R** (Massive news) · Bus **LA** | RSS + EDGAR + Massive news; retire AV; keep the X tape only with the Display Requirements met | No |
| N-16 | **AI answers over vendor data — numeric inputs** (AI Search / Compass inside the terminal) | Massive + FMP (+ Finnhub) numbers → Claude | member | **R (Ind / noDDLA)** | The derived-works clauses govern (no vendor AI clause); Anthropic §L.1 (E-02 §5 class (b)) | OI-03(a) / (b) | Ind **R** · Bus **LA**; noDDLA **R** · DDLA **U→LA** | Ground on EOD + multi-symbol derived facts, never a live single-symbol quote; a facts module + grounding gate | No |
| N-17 | **AI answers — copyrighted TEXT inputs** (transcripts, tweets, TheFly-origin text, FRED series) | FMP/AV transcripts, X, TheFly, FRED | member | **U / R** | The sharp exposure class (E-02 §5 class (a)); X §III.A(k); TheFly's text-mining ban; FRED's LLM ban | Written FMP/AV answers · counsel on X · OI-E02-14 | noDDLA **R** · DDLA **U** | Settle before scaling; summarise only licensed text | No |
| N-18 | **AI answers distributed externally** (public briefs, marketing, "scan of the day") | any vendor | public | **R** | Cboe Non-Display Category 4 "Enterprise Derived": market data used "to develop, train, operate, or enhance … artificial intelligence systems … large language models" whose outputs are "distributed externally" (effective 2026-09-01) — "TERMINAL-NEXT is, by its own charter, exactly the product that category describes" (E-04 §4 #4). Perplexity Output in-app only (T-90) | The Massive compliance desk; a calendar reminder | Ind **R** · Bus **R** (public) | Keep AI outputs inside the controlled product | Possibly — a newly priced category |
| N-19 | **Alerts — server-side price evaluation** (watchlist price alerts, awareness stop-watch, indicator alerts; exists today in `watchlist_alert_service`, `awareness/rules.py`) | Massive quotes · **non-display use** | member | **R / U** | CTA note 8 / OPRA note 10 name "surveillance programs" and "operations control programs" as Non-Display Use — Category 1 is **$2,000/mo on CTA Network A and again on OPRA**; whether a member-configured alert is that member's display or the platform's non-display is interpretive and both plans reserve "the sole determination" (E-03 §4.4B) | A written vendor answer: is alerting covered by the Business licence? (E-03 Part 6 #9) | Ind **R** · Bus **U** | Enter the cost model as a **fixed non-display line**; "the surface most likely to be omitted from a licensing review precisely because nothing is on screen" | Fixed, not per-user |
| N-20 | **Alerts — delivery carrying a price** (email / Discord; exists today via `deliver_alert_payload`) | Massive quotes | member, outside the controlled product | **R if real-time** | Redistribution outside the controlled product (E-03 §4.3) | OI-03(a) | Ind **R** · Bus **R** (real-time) / **LA** (delayed or level-only) | Send the level and the direction, not a live quote; delay the delivered price | No |
| N-21 | **Screener / scan results with live price columns** | Massive + Finviz + FMP | member | **R** | UTP Derived §1 (single-symbol price columns fee-liable) vs §2 (the aggregate is free) (E-03 §4.3); Finviz U; FMP §2.2.2 | OI-03(a) / (b) / (c) | Ind **R** · Bus **LA** (Massive columns) | Rank on multi-symbol derived metrics; a delayed price column; `scan_evaluator.cadence_ceiling` already proves the tree is EOD by construction (E-03 Part 5) | Possibly |
| N-22 | **Breadth / RS / sector flow / theme-return panels** | Massive | member | **R (Ind) / LA (Bus)** | "Allowed on the plans, Restricted on individual vendor terms" — UTP §2 and CTA note 8 free it; Massive P1 §5(c) still bites (E-03 §4.3, §4.4A) | OI-03(a) — and Derived Works in the Business grant | Ind **R** · Bus **LA** | The best-margin lane; the Business tier removes the vendor bar and the plans never charged | **No** |
| N-23 | **External publication — TERMINAL-NEXT marketing surfaces** (a landing page with live stats, an open leaderboard, a public scan of the day) | any vendor | public | **R** (either tier) | Edge Users ≠ the public (E-02 §2.1); each is a new redistribution question (E-04 §8) | An owner decision per surface → `OWNER_DECISIONS.md` | Ind **R** · Bus **R** | **One publication chokepoint** that asks "whose data is in this, and may it go out?" once; a vendor-of-origin field on every published artifact; delayed, aggregated or public-domain content only | No |
| N-24 | External publication — member-pasted charts in community chat | member-generated | members | **U** | Member-generated redistribution (E-03 §4.3) | A ToU clause | n/a | A terms-of-use clause; the images are member-posted | No |
| N-25 | **Retained derived history for years** (implied-capture pairs, base-structure statistics, flow scoreboards, breadth analogues) | Massive-derived | internal → member | **R (Ind) / LA (Bus)** | Retention is a product requirement, not an accident; only P3 §2.2 grants `store` (E-02 §4; E-04 §6) | OI-03(a) / (d) | Ind **R** · Bus **LA** | License retention explicitly; a chosen horizon per store; arm the prunes | No |
| N-26 | **Non-professional attestation + entitlement + monthly reporting** (only if UCT becomes a vendor of record with the SIPs / OPRA) | the exchange plans | members | **—** (machinery, not a data use) | CTA Exhibit A: a click-on subscriber agreement before real-time access; unique non-shared credentials; concurrent-session prevention; monthly entitlement reports; a three-year audit trail — otherwise NYSE "reserves the right to bill for all devices on your network" (E-03 §2.2). Zero attestation strings exist today (E-03 §2.1) | OI-E02-09: vendor of record, or stay downstream? (default: downstream) | — | Stay downstream of a vendor whose redistribution licence names UCT's members; if not, budget the machinery before any real-time surface | $1 + $1/member/mo (Tapes A+B non-pro) + $1,000 + $1,000/mo redistribution + the entitlement build |

### 1C. Tally — how many rows in each class, and what the two facts move

**Class in force** (program defaults: Massive Individual, no FMP DDLA), counted on the leading class of each row:

| Class | Part A (today, 92 rows) | Part B (TERMINAL-NEXT, 26 rows) | Total (118) |
|---|---|---|---|
| **A** Allowed | 3 (T-82, T-83, T-84 — public domain and own content only) | 0 | **3** |
| **LA** Likely Allowed (verify) | 6 (T-13, T-61, T-66, T-80, T-86, T-87) | 1 (N-15 headline) | **7** |
| **R** Restricted (pending contract) | 59 | 22 | **81** |
| **U** Unknown | 16 (T-25 counted R; T-31 counted R) | 2 (N-17, N-24) | **18** |
| **X** Unsuitable | 8 (T-67, T-69, T-71–T-76) | 0 | **8** |
| — (machinery) | 0 | 1 (N-26) | **1** |

**The two facts that flip the most rows:**

| Fact | Rows whose class changes when it is answered | Direction |
|---|---|---|
| **OI-03(a) — the Massive tier** | **38**: T-01, T-03–T-11, T-14, T-15, T-20–T-26, T-28–T-32 (24 today) + N-01–N-05, N-07–N-10, N-16, N-19, N-21, N-22, N-25 (14 next) | Business: 30 go R→LA, 8 go R→U (the OPRA rows, the paid Substack, same-day dark pool, alerting). Individual: every one stays R and **"stop and re-plan"** (E-02 §3.1) |
| **OI-03(b) — the FMP DDLA** | **19**: T-33–T-46 (13 today, two partial: T-43, T-46) + N-11–N-14, N-16, N-17 (6 next) | DDLA: 15 go R→LA (with §6.3/§2.8 as live obligations), 4 stay U (transcript AI, TheFly chain, attribution "governed by the agreement", copyrighted-text AI) |

**Rows that survive both answers as R** (the actionable half of the register, per E-02 §2.1): T-02, T-12, T-16–T-19, T-22, T-27, T-79 (public surfaces, the composites, the unauthenticated routes) and, on other vendors, T-51–T-59, T-62–T-64, T-68, T-81, N-06, N-18, N-20, N-23. With both facts favourable, **R falls from 81 to 27** — and of those 27, thirteen are fixed by engineering (auth, delay, deletion-sync, a display fix, a retirement) with no vendor conversation at all.

### 1D. Findings block for the register

**OBSERVATION.** 118 rows; 81 Restricted, 18 Unknown, 8 Unsuitable, 7 Likely Allowed, 3 Allowed. Every Allowed row is public-domain or UCT's own content; not one row is Allowed on the strength of a vendor contract. Two owner facts move 57 rows; the Massive tier alone moves 38.

**EVIDENCE.** Every clause cited traces to E-01/E-03/E-04's primary, dated documents (🟢); every production value to ORCH-RAILWAY-01 as relayed by E-02 §1/§6/§9 and F-03b §1 (🟢, CONFIRMED by a read-only Railway pass, 2026-09-02); every code line to E-02/E-04/F-03b's source reads. R-17's four unauthenticated GETs are CONFIRMED by the orchestrator (2026-09-02 08:05 UTC). No row rests on a CLAUDE.md claim: where CLAUDE.md and code disagreed (the tick-stream vendor; the Finviz DrillModal images) the register carries the code and marks the doc a CLAIM.

**INTERPRETATION.** Three shapes: (1) **Concentration** — Massive rows are the same unanswered binary written 38 times. (2) **Personal-tier terms on paid-product surfaces** — Massive (if Individual), FMP, Finnhub, AlphaVantage, Schwab and Reddit each sell the compliant tier; Yahoo and Finviz are the exceptions with nothing to buy or read (E-01 §16). (3) **Computation is rarely the problem; publication is** — the rows that survive every scenario are the ones where vendor data leaves the controlled product or is served to nobody in particular (E-04 §0 #3).

**RELEVANCE TO UCT.** TERMINAL-NEXT's licensing shape is set by four primitives, not 118 rows: the real-time single-symbol quote, the real-time options print, the option chain, and everything derived (E-03 §1.1). The first three are contract-gated and per-user-priced; the fourth is free on the plans and gated only by the Massive tier. A "delayed price, live volume, live breadth" terminal is coherent, differentiated, and close to free on the equity side — **after** the tier answer, never instead of it.

**CONFIDENCE.** 🟢 on clause text and production state; 🟡 on the classification logic (traceable, but every input contract is missing); 🔴 on which contracts exist. **EVIDENCE CEILING:** one look at the Massive billing account and one FMP reply would re-class 57 rows; the Finviz purchase email a further four.

**RECOMMENDATION.** Do not remediate row by row. Get OI-03(a) and OI-03(b) first (one lookup and one email); execute the engineering rows that need no vendor (§3); then re-run this register. Route every "buy" proposal through F-03b §3 first (charter B §15).

**OPEN QUESTION.** Does the Massive Business agreement's grant reach **Derived Works** — charts, analytics, breadth — or only display of the underlying data? A display licence that does not clearly cover multi-symbol analytics would leave UCT's best-margin lane exactly where it is now (E-03 §4.4A; E-02 §2.1).

---

## 2. ESCALATION LEDGER (Deliverable 2)

*Every **U** in §1, every "owner fact that settles it", and every Restricted row that a contract (not engineering) would move, mapped to the program entry that already carries it. Nothing here is new — the point of the ledger is that no unknown lives only inside a table cell. Ordered by rows moved. **Default in force** is the position the register proceeds on today (D-002 (b)+(c); E-02 §8 defaults). "Rows" are §1 ids.*

| # | Unknown | Program entry | Rows it holds | Default in force | What changes when answered | Who answers · cost of the answer |
|---|---|---|---|---|---|---|
| ESC-01 | **Which Massive tier UCT holds** — Individual (Basic → Advanced, "Individual use only") or Business / Enterprise (Edge Users + `store`) | **D-002** · OI-03(a) · OI-E02-01 · OQ-09 · R-14 · RG-22 · E-03 Part 6 #1 · F-03b Q-1 | **38** (§1C): T-01, T-03–T-11, T-14, T-15, T-20–T-26, T-28–T-32; N-01–N-05, N-07–N-10, N-16, N-19, N-21, N-22, N-25 | **Assume Individual** (OI-E02-01). Every Massive row is R; Terminal-Next planning proceeds on "upgrade or re-scope", not on delayed-design mitigations (E-02 §7.1) | **Business:** 30 rows R→LA, 8 R→U (OPRA, paid Substack, same-day dark pool, alerting); the remaining work is narrow — public surfaces, §6.1(j), the OPRA sub-agreement (E-02 §3.1). **Individual:** "stop and re-plan" — no delay banner, aggregation or desk-only design fixes it; E-05 must carry a Stocks Business scenario from $2,499/mo (E-01 §1a), an escalation-threshold spend (OWNER_SEED_FACTS §6) | Owner · one look at the Massive billing page or order form. The single cheapest, highest-yield fact in the program |
| ESC-02 | **Whether UCT holds an FMP Data Display and Licensing Agreement** (and the Acceptable Data Use Policy text, which §2.6.2 binds and whose URL 404s) | **D-002** · OI-03(b) · OI-E02-02 · R-14 · F-03b Q-2 · E-01 §18 #2 | **19**: T-33–T-46 (T-43, T-46 partial); N-11–N-14, N-16, N-17 | **Assume no DDLA.** Every FMP-sourced member-facing field is R; add no new ones | **DDLA:** 15 rows R→LA with §6.3 (delete incl. cached + Deletion Agreement + audit) and §2.8 (notify FMP of every IP/domain where data is stored or processed) as **live obligations**; T-43/T-45/T-46/N-17 stay U. **No DDLA:** the second-largest block of the product stays R and yfinance is not a fallback (T-73); EDGAR-derived fundamentals (A) are the substitute | Owner · one email to FMP (quote + ADUP text). "The cheapest large conversion available" (E-02 §3.2) |
| ESC-03 | **Does the Business grant reach Derived Works** (charts, analytics, breadth) or only display of the underlying data? | OI-03(d) · E-02 §2.1 open Q · E-03 Part 6 #1(c) · E-04 §4 open Q · §1D OPEN QUESTION | T-11, T-14, N-16, N-22, N-25 (sub-question of ESC-01) | Assume the grant is display-only until the text says otherwise; the best-margin lane stays R even at Business | **Yes:** UCT's most differentiated, least-per-member-cost lane is free on the plans and licensed at the vendor. **No / silent:** frame the Massive conversation around Derived Works rights, not display rights (E-03 §4.4A); the exchanges already make multi-security derived data fee-free | Owner + Massive sales · the same conversation as ESC-01; ask for the clause in writing |
| ESC-04 | **Are UCT's composites §6.1(j) "derivative works"** — UCT20 portfolio NAV, the 0–150 Exposure Rating, published entry/stop/target — "index, indicative value, net asset value … investment strategy" | OI-03(d) · E-01 §1a · E-02 §2.1 last row | T-12 (**R at either tier**); the composites inside T-20 and N-23 | Assume yes: no new composite publications until answered; desk-first (D-001) | **Licensed:** T-12 → LA. **Not licensed:** a separate licence line or a redesign of what is published (levels as % of price, no NAV series) | Owner + Massive · a specific written question. Nothing in the public terms answers it |
| ESC-05 | **The OPRA sub-agreement and the vendor of record** — does UCT's Massive agreement name customer-facing OPRA display; is Massive paying the $1,500/mo redistribution fee and reporting UCT's members as its non-pros, or is UCT's own redistribution unlicensed? | **OI-09** · OI-E02-09 · E-03 §2.4 open Q, Part 6 #2 · E-01 §18 #1 · F-03b Q-8 | T-23–T-27, T-29; N-07–N-09 (**U even at Business** — P3 §2.5 Third-Party Agreements are unpublished) | **Assume downstream** (OI-E02-09): do not design surfaces that require UCT to run subscriber entitlement; the options tape's tier is an explicit product decision (E-02 §7.2) | **Named and covered:** real-time OPRA to members = LA with a **per-user** line ($1.25/member/mo on top of the $1,500 floor). **Not covered:** historical-only is the only free shape; delayed still owes the floor; real-time to members requires UCT to become a vendor of record (N-26 machinery) | Owner + Massive compliance desk · one email. "The highest value-per-email question in the register" (E-02 GAPS) |
| ESC-06 | **Should the data endpoints be authenticated** — `/api/live-prices`, `/api/snapshot`, `/api/snapshot/{sym}`, `/api/movers` CONFIRMED open; `/api/bars/*`, `/api/stream/*`, `/api/gex/*`, `/api/dealer-positioning/*` open by source | **OI-17** · **R-17** · OQ-15 · OI-E02-10 · E-03 Part 6 #6 · F-03b Q-9 | T-02 (**R at either tier**), T-03, T-04, T-05, T-51, T-77; §3B rows X-06–X-13 | **Assume unintended** (OI-17); ARCH-06 requires auth on every data route Terminal-Next uses; no program change to production | **Unintended:** engineering, no vendor conversation — the Business tier's Edge Users carve-out starts to apply because the audience becomes bounded. **Deliberate (landing tiles, external consumers):** an accepted-risk entry in `OWNER_DECISIONS.md` with the cost written down: CTA Exhibit A lets NYSE "bill for all devices on your network" when entitlement cannot be audited (E-03 §2.2) — an unbounded per-user line | Owner · a yes/no. Then a normal engineering session (not this program) |
| ESC-07 | **The Finviz Elite agreement** presented at purchase — and whether Finviz-rendered PNGs are served to members or Finviz is an internal scanner input only | OI-03(c) · OI-E02-03 · E-01 §18 #3 · E-04 GAPS #12 · F-03b Q-3 | T-47–T-50; N-13, N-21 (Finviz legs) | **Assume no grant.** Internal export stays U; retire Finviz **images** from member surfaces first; price short interest as a coverage gap with no second vendor | **A grant exists:** U→LA on its terms. **None / silent:** the export stays the lower-risk internal half (U); images retire; short interest (T-48) is a capability deletion, not a swap (F-03b §5.5) | Owner · the account page or the purchase-confirmation email. "The single largest documentary gap in the audit" (E-04 §3.4b) |
| ESC-08 | **Finnhub plan tier and any written approval** to redistribute data "or derived results" | OI-E02-06 · E-03 Part 6 #3 · E-01 §18 #4 · F-03b Q-5 | T-51–T-56; N-06 | **Assume free/personal, no approval.** Plan the retirement onto Massive rather than a negotiation | **Written approval:** T-51–T-56 R→LA. **None:** retire — E-02 §2.4, E-03 §3.2 and E-04 §3.3 converge independently; the tick stream is "the simplest remediation in the whole artifact" (E-03 Part 6 #3) | Owner · one look at the Finnhub account. Or skip the question and retire |
| ESC-09 | **AlphaVantage — free key or a written commercial agreement** | OI-E02-07 · E-01 §18 #5 · F-03b Q-6 | T-57–T-59 | **Assume free.** Retire both call sites onto their existing fallbacks (RSS for news, FMP for transcripts) — close to free | **Commercial agreement:** R→LA. **Free:** §2(a)(ii)/(iii) catch UCT independently; retire. The FRED flow-down (T-59) goes with the lane | Owner · one look, or retire |
| ESC-10 | **Schwab developer tier** (Individual vs Commercial/Redistribution) and whether any accepted Market Data Agreement contemplates display to non-account-holders | OI-E02-08 · E-01 §18 #7 · E-03 Part 6 #4 · E-04 GAPS #5 · F-03b Q-7 | T-77–T-79; N-10 | **Assume Individual Developer.** Scope the GEX / dealer-positioning **re-source** onto Massive chains (`polygon_options.py` already serves chains + Greeks); a sourcing question for the owner, never a change request against a partner file | **Commercial + agreement covers display:** T-77 U→LA (still authenticate). **Individual:** one account holder's entitlement fanned out to the membership stays R-shaped; re-source | Owner, **routed through the partner** (partner-owned integration) · the Developer Portal login |
| ESC-11 | **Transcript-derivative AI rights** — may UCT summarise and store a derivative of a copyrighted earnings-call transcript body and display it to paying subscribers? | E-02 §5 REC · E-04 §5 REC · F-03b Q-2 (same email as the DDLA) | T-43, T-58; N-17 | **U — settle before scaling** (E-04 §5). A DDLA about *display* may not speak to *summarising prose* | **Yes in writing:** T-43 U→LA (FMP leg); T-58 stays R at AV unless AV answers too. **No / silent:** the lane stays a named content-licensing question; summarise only licensed text | Owner · one paragraph in the FMP email; a second to AV if the AV lane survives ESC-09 |
| ESC-12 | **The TheFly sublicence chain** — does FMP's licence cover redistributing TheFly-origin analyst actions (`stable/grades-latest-news`) in a paid newsletter? | E-04 §9C · E-02 §2.9 · E-01 §18 #6 · F-03b Q-2 | T-45; N-12 (TheFly-origin rows) | **R / U.** Ground wire prose on the action's existence, not TheFly text; name the chain (done) | **Covered:** T-45 → LA (with DDLA). **Not covered:** drop grades from external prose; TheFly-direct stays X (T-69) | Owner · the same FMP email |
| ESC-13 | **Are the public surfaces deliberate accepted risks?** `DESK_PUBLIC_SHOWS=*` (an owner decision dated 2026-08-19); the Flow Scoreboard ("a public trust asset"); the 15:45 ET #TSDR chart post (marketing or member content?); real auth on `/r/*`; the Sunday Scans free tier | OI-E02-04 · OI-E02-05 · **OQ-16** · OI-E02-11 · E-03 Part 6 #7–8 · F-03b Q-10 | T-16–T-19, T-27, T-79; N-23 | **Assume each stands as decided** (E-02 §8): the register has no standing to reverse an owner decision; it records the licensing cost the decision never weighed | **Re-confirmed:** each becomes an `OWNER_DECISIONS.md` accepted-risk entry — and stays **R at either tier** (the public is not an Edge User). **Reversed:** name shows instead of `*`; authenticate the scoreboard or publish aggregates only; delay + notice on public charts; real auth on `/r/*` | Owner · a decision batch, not a contract. No vendor conversation changes these rows |
| ESC-14 | **Server-side alerting** — is a member-configured alert evaluated server-side that member's display or the platform's non-display use (CTA/OPRA Category 1, $2,000/mo each)? | E-03 §4.4B · E-03 Part 6 #9 · E-02 §7.2 "Alerts" | N-19, N-20; §3C row X-15 (the same shapes run today) | **R / U.** Enter the cost model as a **fixed non-display line**, not a free by-product of display | **Covered by the Business licence:** N-19 → LA, fixed. **Separate category:** a product-scope decision, not a surprise invoice; both plans reserve "the sole determination" | Owner + Massive · a written question; E-05 carries the line either way |
| ESC-15 | **FRED and Reddit — arm deliberately or retire?** Both keyless on every Railway service | OI-E02-14 · F-03b Q-13 · E-04 GAPS #10 | T-60, T-61, T-68; N-14 (FRED leg) | **Assume retire.** Arming FRED makes three collisions live at once (X-01, X-02) | **Arm FRED:** the exact notice, a ToU link + a clause binding members, a per-series copyright check (thirty `fred/series/search` lookups — never a guess, E-02 §9 #9), **no cache**, no LLM on any restricted series. **Arm Reddit:** a separate commercial agreement first. **Retire:** the rows never become live | Owner · a decision; the lookups are a measurement |
| ESC-16 | **The `#tsdr` corpus consent basis** (7,766 messages → 591 KB rows → Brain Pack → member-facing RAG) and whether it refreshes past 2026-02-20 | **OI-15** · OI-E02-12 · F-03b Q-14 | T-85 | **Assume internal-RAG consent only.** No member display of message text; corpus frozen | **Product-use consent exists:** T-85 member/AI cells U→LA (privacy and Discord's own terms still apply). **None:** desk-only; strip message text from any member-facing retrieval | Owner · a fact about how the export was consented, not a vendor |
| ESC-17 | **The Anthropic subscription seat lane** — `desk_insights_polish.py` (`claude -p`) producing publicly consumed YouTube artifacts | OI-E02-13 · F-03b Q-12 · OWNER_SEED_FACTS §5 | T-91 | **Assume not covered.** Move the lane to the API key, matching `discord_close_note.py:17-25`'s written rule | Either answer ends in the same place: the API key. The question is whether it is a doctrine fix (now) or a compliance fix (already due) | Owner · a decision; zero contract cost |
| ESC-18 | **Is the R2 bucket (`uct-bars-snapshots`, `brain/`) UCT's own account, and can any third party read it?** | E-02 §4 open Q · E-04 §6 open Q · F-03b Q-15 | T-09 | Assume UCT's own, private — unverified | **Own + private:** storage (tier-conditional). **Third-party-readable:** "storage quietly becomes redistribution" — R at either tier | Owner · one look at the Cloudflare console |
| ESC-19 | **OpenAI business terms and the Perplexity tier** (standard vs Enterprise; which governs Output ownership; is Output "within Customer Applications" when it reaches Substack/Discord?) | E-01 §14, §18 #9 · F-03b Q-11 | T-88–T-90; N-18 (Perplexity leg) | **U.** Keep Perplexity Output in-app; ground external prose on public-domain / EOD facts; per-user OpenAI caps already exist | **Favourable:** T-88 U→LA, T-90 → LA. **Unfavourable:** external Perplexity-derived prose stops | Owner · re-fetch the OpenAI terms from an ordinary browser (primary 403s to every program fetch path); one Perplexity account look |
| ESC-20 | **Does the X Developer Agreement bind a scraper-intermediated consumer**, or does exposure run to X's anti-scraping ToS? Is a direct X developer account wanted? | E-02 §2.7 · E-01 §8–9 · F-03b Q-17 | T-62, T-65; N-15 (X leg) | **R** on display (requirements measurably unmet — X-03); **U** on inference. twitterapi.io §4 grants nothing and pushes compliance downstream | Both readings are unfavourable; the answer changes the *remedy* (a direct developer account + the Display Requirements, or counts-and-links only), not the class | Counsel · a read, not a contract. The UI fix (X-03) is independent of the answer |
| ESC-21 | **The commercial model** — the code makes the Morning Wire the only free page; the seed facts call it the paywalled item. Is a paid Substack audience an "Edge User"? | **OI-12** · E-02 §6 row 6 (🟡) | T-20 (R / U); N-23; the audience vocabulary of every "paid newsletter" cell | Proceed on the code (Morning Wire free, everything else paid; Terminal-Next paid-tier); treat the paid newsletter as *arguably not* users of Customer's products in the app sense | **Massive/FMP say yes in writing:** T-20 → LA at Business / with DDLA. **No:** external prose grounds on EOD / derived facts; delayed charts; FMP attribution is **not** an available remedy (§10.4) | Owner (the model) + a specific question to Massive and FMP (OI-03(d)) |
| ESC-22 | **Which providers are still billed** — Bullflow (retired in code), Unusual Whales (half-dormant), Polygon-direct (duplicate), TheFly | **OI-04** · F-03b Q-4 | T-69, T-92 | Any key with no OBSERVED-CALLED evidence in 30 days is treated as retired | Retiring dead keys removes rows; a still-billed UW subscription adds a "U — not researched" licence question for the wire's UW leg | Owner · invoices |
| ESC-23 | **Seat counts and addenda** — how many people hold logins per vendor (TheFly one-user, FMP account-sharing ban, Finnhub personal plan); any order forms or written exceptions for any vendor | OI-E02-15 · OI-E02-16 · E-01 §18 #10–11 · F-03b Q-16 | cross-cutting — every row | Assume one owner seat per vendor; assume no addenda ("the structural ceiling on the whole pod") | Any addendum can move any row in either direction; a shared login is a separate finding | Owner · the vendor consoles |
| ESC-24 | **Providers the pod never researched** — EarningsWhispers (scraped), openinsider, ForexFactory, the RSS publishers, the logo chain (logo.dev 429), Stocktwits, Unusual Whales, earningscall.biz / Quartr, the note connectors, Picovoice, Webshare | E-01 §15, §18 · E-04 §3.6 · F-03b §1B, Q-18 | T-70, T-92 | **U — not researched, never inferred.** Credentialed paths before any graduates to a visible tile | A second research pass (not a contract) reclassifies each; Picovoice may carry a per-user cap (E-01 §15) | A future leaf role · web reads only |

**What answering them costs, in order.** One lookup (ESC-01) · one email (ESC-02 + ESC-11 + ESC-12 in the same message) · one email (ESC-05 + ESC-03 + ESC-04 + ESC-14 to Massive) · one owner decision batch (ESC-06, ESC-13, ESC-15, ESC-16, ESC-17, ESC-21) · four console looks (ESC-07, ESC-08, ESC-09, ESC-18) · one partner-routed question (ESC-10). Everything else is a read, a counsel note, or a later research pass. **No unknown in §1 lacks a row above**; if a future edit adds a U cell without an ESC id, that is the defect this section exists to catch.

**Measurements that are not escalations** — facts a read-only observation settles, listed so nobody buys a contract to learn them: (m1) whether Massive-sourced prices arrive real-time or 15-min delayed — one timestamped `/api/live-prices` sample during RTH against the wall clock (E-03 GAPS; F-03b §7.2), moves the exchange-fee side of T-01/T-03/T-05/N-01; (m2) whether `/api/bars/*`, `/api/stream/*` and `/api/breadth-monitor/live` answer unauthenticated — three GETs (E-03 GAPS "per-route auth not exhaustively swept"); (m3) whether the same-day dark-pool `/v3/trades` lane runs inside or outside the 15-minute interval (E-03 Part 6 #10; T-31); (m4) which FRED catalog series carry a copyright tier (ESC-15); (m5) store sizes and row counts on the volume (owner-present only); (m6) `bars.db` retention — a scheduler-side sweep outside the inspected files cannot be excluded (E-04 GAPS #6). None was run by this program (production and the volume are contract-forbidden).

---

## 3. PUBLICATION AND AI ADDENDA (Deliverable 3)

*Three finding families that cut across the vendor-ordered table in §1, each carried here as register rows with a **production today** flag that is distinct from the **Terminal-Next plan** column. Ids are `X-nn`. Every row cross-references its §1 home so the same fact is never classified twice.*

**Flag vocabulary.** **LIVE-TODAY** = the collision or exposure exists in production now (an armed flag or a CONFIRMED probe, per F-03b / ORCH-RAILWAY-01 / R-17) · **DORMANT** = the code path exists, the key or flag is absent on every Railway service · **BY-SOURCE** = confirmed from a code read, live reachability not probed (a CLAIM about production until one GET is run).

### 3A. The five clause-vs-code collisions (E-04 §0, §3.4b; RG-21)

*Found by reading the clauses against the code. None needs a vendor conversation; three are a few hours of engineering. Recorded to the owner as production defects for a normal session (RG-21) — nothing here was changed by this program.*

| # | Collision | Production today | Terminal-Next plan | Clause · evidence | Class | §1 home | Fix shape (no vendor needed) |
|---|---|---|---|---|---|---|---|
| X-01 | **FRED's mandatory attribution notice is displayed nowhere** — "This product uses the FRED® API but is not endorsed or certified by the Federal Reserve Bank of St. Louis" | **DORMANT** — `FRED_API_KEY` absent on every service; `fred_economic.py:105-111` returns an error dict (E-02 §1 finding 2). Zero hits for the notice across `api/**` and `app/src/**` (E-04 §3.4b #1; E-02 §1 re-measured) | If N-14 arms FRED, the notice, a link to the FRED terms and a ToU clause binding members are **mandatory deliverables**, not polish (E-01 §4) | FRED API Terms — "Place the following notice prominently on your application" (E-01 §4, 🟢 primary) | **R if armed** | T-60 | One line of UI text + a terms link. "The cheapest compliance fix in the entire report" (E-04) |
| X-02 | **FRED content is cached (30-min TTL) and passed to an LLM** — against a flat ban on storing or caching "any portion" and on use "in connection with the development or training of … large language models" | **DORMANT** — same key absence; `fred_economic.py:24,158` (cache) and `voice_tool_impls.py:442,453` (Compass) describe a path that currently returns nothing | Arming = three collisions live at once (X-01, both halves of X-02). ARCH-05 must treat FRED as **no-cache, no-LLM on any restricted series** or not at all (§4) | FRED Legal Notices §Prohibitions (E-04 §3.4b #2, 🟢). Whether "in connection with" reaches inference-time prompting is a genuine counsel question | **R if armed** | T-60 | Remove the TTLCache on the FRED path; exclude FRED values from prompts until counsel reads the clause |
| X-03 | **`TapeFeed.jsx` does not meet X's Display Requirements** — renders tweet text, a *relative* time and a `↗` only; no avatar, @username, display name, permalink timestamp or X logo | **LIVE-TODAY** — `TWITTERAPI_IO_ENABLED=1`; `app/src/components/tiles/TapeFeed.jsx:53-68` (E-04 §3.4b #3, source-read 🟢) | N-15 (news panel): keep an X tape **only** with the Display Requirements met; else cashtag counts + jump links (the T-66 / `buzz_store` shape) | X Developer Agreement §III.A(d) + Display Requirements (E-01 §9; the Display Requirements themselves read via secondary summaries — E-01 GAPS) | **R** | T-62 | A UI fix, hours. Independent of ESC-20's counsel answer |
| X-04 | **No deletion-sync for stored tweets** — X requires deleting or modifying stored content within ~24 h of its deletion or modification on X; `tweet_cleanup.py:12` implements an age sweep only | **LIVE-TODAY** — `tweets.db`, `TWEET_RETENTION_DAYS` = 7. The 7-day window bounds exposure to a week "by coincidence, not by design" (E-04 §3.4b #4) | Any Terminal-Next store of third-party social text inherits the duty; ARCH-04's retention rules must carry a *re-check* primitive, not only an age sweep (§4.6) | X Developer Policy (E-04 §3.4b; E-01 §16 "caching is a licence obligation") | **R** | T-63 | Re-check stored ids against X on a schedule; delete on miss; keep the 7-day bound |
| X-05 | **`catalysts.db` `raw_signals` copies the same tweet bodies and third-party RSS items and never prunes** — "a back door around the tweet retention window" | **LIVE-TODAY** — `CATALYST_ENGINE_ENABLED=1`; `api/services/catalyst/engine.py:1163-1165` writes `{"tweets": …, "rss": …}` into a store with no prune (E-04 §3.4b #5, §6; E-02 §4) | ARCH-04: **store ids and counts, never bodies** (the `buzz_store.py:3-6` idiom — "the one deliberate data minimisation in the codebase"); any retained derived row must be prunable to the shortest window its inputs carry | The same X duty as X-04, plus each RSS publisher's own terms (U — not researched) | **R** | T-64 | Stop persisting bodies in `raw_signals`, or extend the sweep to reach them. "This needs no vendor's terms to be worth fixing" |

⚠️ **One count, stated once.** E-04 §0 says "five collisions" and RG-21 carries five; E-04 §3.4b numbers them 1–5 as above (FRED attribution · FRED caching-and-LLM · X display · no deletion-sync · `raw_signals`). E-02 §1 finding 2 says "three of E-04's five … are dormant" because it counts FRED's caching and FRED's LLM collisions separately. **The register carries E-04's own numbering**: two of the five (X-01, X-02) are dormant because both live on the keyless FRED lane; **three are live today** (X-03, X-04, X-05) and all three are engineering.

### 3B. The unauthenticated endpoints (E-03 §1.1; E-04 §8; R-17; OI-17; OQ-15)

*The finding that survives every contract scenario: a route that answers without a session is not "member display", it is public redistribution — and a Business-tier Edge Users carve-out cannot reach an audience that is nobody in particular. The orchestrator's read-only pass (2026-09-02 08:05 UTC, browser user agent, no mutation) CONFIRMED three; the rest are BY-SOURCE.*

| # | Route | What leaves | Provider · data | Production today | Terminal-Next plan | Evidence | Class | §1 home | Per-user cost bearing |
|---|---|---|---|---|---|---|---|---|---|
| X-06 | `GET /api/live-prices?tickers=…` | Real-time last trade, day change, volume, up to 250 tickers per call (15 s cache) | Massive REST · equity snapshot | **LIVE-TODAY — CONFIRMED 200** with a live quote, no session (R-17) | ARCH-06 Tier-S: server-side auth on every data route; the SPA `FREE_PAGES` guard (`AuthGuard.jsx:112`) is a client-side route guard and does not protect the API | `api/routers/live_prices.py` — no `Depends(get_current_user)` (E-03 §1.1 row 1, 🟢); orchestrator probe (🟢 CONFIRMED) | **R at either tier** | T-01 / T-02 | **Unbounded by construction** — CTA Exhibit A: no auditable entitlement ⇒ NYSE "reserves the right to bill for all devices on your network" |
| X-07 | `GET /api/snapshot`, `/api/snapshot/{sym}` | Last price, change %, extended-hours snapshot | Massive REST | **LIVE-TODAY — CONFIRMED 200** (`/api/snapshot/SPY`) | As X-06 | `api/routers/snapshot.py` ~20 lines, no dependency (E-03 §1.1 row 5); probe (CONFIRMED) | **R at either tier** | T-01 / T-02 | Unbounded |
| X-08 | `GET /api/movers`, `/api/extended-movers` | Gainers/losers with live % change and reasons | Massive gainers/losers snapshot | **LIVE-TODAY — CONFIRMED 200** for `/api/movers`; `/api/extended-movers` BY-SOURCE | Rank on multi-symbol metrics (fee-exempt); the per-name live % is the fee-liable part (E-03 Part 5); authenticate | `api/routers/movers.py` ~35 lines, no dependency (E-03 §1.1 row 6); probe (CONFIRMED) | **R at either tier** | T-05 | Possibly (per-name single-security %) |
| X-09 | `GET /api/gex/data`, `/api/gex/compare`; `/api/dealer-positioning/*` | Gamma walls, zero-gamma, dealer net positioning — **derived from Schwab chains** under one process-wide OAuth token | Schwab `marketdata/v1/chains` (partner-owned integration) | **LIVE-TODAY — handler reached unauthenticated** (422 for a missing `ticker`, not 401 — R-17). "Unauthenticated **by omission**": neither router declares a dependency; `auth_middleware.py:3` — "Does NOT block any existing endpoints" (E-04 §8) | Re-source the chain from Massive (T-77 / N-10) **and** authenticate; the defect class was already remediated once on the flow admin routes (`admin_guard.py`) and recurred here — ARCH-06 needs a rail, not a fix (§4.4) | `api/gex_router.py:11`, `api/dealer_positioning_router.py:45`, `api/main.py:7087-7088` (E-04 §8, 🟢); probe (CONFIRMED handler) | **U leaning R** (Schwab) · **R** as a public surface | T-77 | Fixed (Schwab passes exchange entitlements through one account) |
| X-10 | `GET /api/bars/{ticker}`, `/api/bars-history/{ticker}` | OHLCV 1m → M, 5,000 bars; intraday real-time to the last closed bar | Massive REST (yfinance / FMP fallback) | **BY-SOURCE** — the read path is open; only `POST /api/bars/warm` and the admin routes use the auth import (`api/routers/bars.py:14,288,317,691` — E-03 §1.1 row 4). Not probed | Authenticate; the D/W/M history is "the safest data in the product" once the audience is bounded | E-03 §1.1 (🟢 source) · live reachability = measurement m2 | **R** | T-03 / T-04 | No (history) · Yes (the developing bar) |
| X-11 | `GET /api/stream/prices`, `/api/stream/bars` (SSE) | Real-time equity ticks (Finnhub WS) · developing bars at 250 ms (Massive WS) | Finnhub · Massive | **BY-SOURCE** — `stream.py` declares no dependency; `STREAM_BARS_ENABLED=1` (E-03 §1.1 rows 2–3). Not probed | Retire the Finnhub leg (N-06); authenticate; a 15-min-delayed developing bar + live volume bar is the safest shape (N-04) | E-03 §1.1 (🟢 source) · m2 | **R** | T-51 / T-03 | Yes (real-time single-symbol) |
| X-12 | `GET /api/flow-scoreboard` | Hit rates, grade calibration, recent picks, **contract-price gains from the OPRA tape** | Massive OPRA · derived | **LIVE-TODAY — public by design**: "No auth on the GET — read-only, cacheable, public" (`flow_scoreboard.py:11,19-20`); mounted unconditionally (`main.py:7036`) | ESC-13 / OQ-16: an accepted-risk entry, or authenticate, or publish aggregates with no contract-price series (Cboe §15 names returns and performance data as fee-liable derived data) | E-04 §8 (🟢 source) | **R at either tier** | T-27 | No (fixed) — but OPRA's derived-data treatment is decided administratively (ESC-05) |
| X-13 | `/r/chart`, `/r/flow`, `/r/movers`, `/r/breadth`, `/r/internals`, `/r/econ`, `/r/earncards`, … | Whatever the panel draws, real-time at capture | Massive et al. | **LIVE-TODAY — "EFFECTIVELY PUBLIC"** by the router's own docstring (`render_panels.py:1-9`); `CHART_RENDER_TOKEN` inlined into the JS bundle; deliberately exempt from the COMING SOON gate (E-02 §1 finding 3) | Real auth on `/r/*` (the three out-of-repo consumers are UCT's own — the wire's Playwright, the Sunday Scan PNG, the chart renderer), or delayed panels with the notice burned in | E-03 §1.1 row 14 (🟢 source); E-03 Part 6 #8 | **R at either tier** | T-17 | No (fixed) |

Not classified because not checked: `/api/breadth-monitor/live` and the darkpool routers were "not individually checked for gating" (E-03 GAPS) — **NOT DETERMINED**, measurement m2. `/api/live/massive/*` and the darkpool lanes are proxy-vouched through `api/flow_proxy.py` (E-03 §1.1 rows 7–8, 10) and are not in this table.

**What the addendum means for the two scenarios.** Authentication is the one remediation in the register that makes a *contract* answer start to work: with the routes open, even the Business tier's Edge Users grant fails on audience (T-02); with them closed, X-06–X-11 collapse into their §1 homes and follow ESC-01. It is also the only row family with an **unbounded** per-user cost bearing, which is why R-17 makes it Tier-S for ARCH-06 and why A-06 (Security, Licensing & Reliability) lists every unauthenticated `/api/*` data route from D-02's route table.

### 3C. The LLM input-rights warranty (E-01 §13; E-02 §5; E-04 §5)

*Anthropic's Commercial Terms (effective 2025-06-17) are the most permissive in the stack — no training on Customer Content (§B), Outputs assigned to Customer (§B), end-user products anticipated (§A.1) — and §L.1 makes UCT warrant that it "has all rights and permissions required to submit Inputs to the Services". So every R in §1's input rows re-attaches at the prompt boundary. "The AI vendor is permissive; the AI pipeline inherits the strictness of its worst-licensed input" (E-01 §13). Carried here as one cross-cutting row with four input classes, because a single "we use AI on vendor data" line would over-state one class and under-state another (E-02 §5).*

| # | Input class entering a model | Lanes (production flag) | Production today | Terminal-Next plan | Governing clause | Class | §1 home |
|---|---|---|---|---|---|---|---|
| X-14a | **Copyrighted TEXT** — verbatim earnings-call transcript bodies (FMP primary, AV lane, Finnhub fallback 403); verbatim tweet text (X via twitterapi.io) | Transcript summary (`TRANSCRIPT_INDEX_ENABLED=1`; coverage monitor `transcript: null (n=0)` this cycle — RG-15) · AV transcript lane (key set) · call recap (`CALL_RECAP_WARM_ENABLED=1`) · catalyst thesis (`CATALYST_ENGINE_ENABLED=1`, `TWITTERAPI_IO_ENABLED=1`) | **LIVE-TODAY** — the only lanes where UCT stores and displays a derivative of a vendor's *prose* rather than its numbers; the summaries are themselves stored (§4.6 deletion duties reach them) | N-17: **settle before scaling** — summarise only licensed text; ids and counts for social text | FMP §2.6.1(i) "derived from" + §11.1 (FMP claims the IP in derived information); AV §2(a) settled at the grant; X §III.A(k) reads to training, not inference — "the line a vendor may read more broadly"; **no vendor in the stack has an AI clause except FRED**, so the derived-works clauses govern; Anthropic §L.1 | **R** (display) / **U** (the AI derivative) | T-43, T-44, T-58, T-65 |
| X-14b | **Numeric vendor data** — Massive quotes, bars, snapshots, flow; FMP fundamentals, analyst, earnings; Finnhub fields; a chart screenshot to OpenAI `gpt-4o` | AI Search synthesis (**`AI_SEARCH_CLAUDE_SYNTH=1`** — the default member lane, wider than D-12's code-default read; E-02 §1 finding 6) · agent / deep / dossier (`AI_SEARCH_DOSSIER_ENABLED=1`, `AI_SEARCH_DAILY_LIMIT=40`) · Compass chat / voice / brain (`BRAIN_TOOLS_ENABLED=1`, `COMPASS_MENTOR_MODE=admin`) · stock brief · Model Book recaps · theme engine (`THEME_ENGINE_ENABLED=1`) · voice chart vision | **LIVE-TODAY** on the busiest AI lane in the product | N-16: ground on EOD + multi-symbol derived facts, never a live single-symbol quote; a facts module + grounding gate (the COT / `flow_explain.py` shape) | Massive P1 §5(c) — an LLM output over the data is a work "derived from" it (Ind **R**); Bus **LA**; FMP §2.2.2 re-attaches at the prompt boundary (noDDLA **R**); Finnhub "derived results" | **R (Ind / noDDLA)** · **LA (Bus / DDLA, verify)** | T-14, T-15, T-44, T-54 |
| X-14c | **Public-domain and own content** — CFTC COT zips; SEC EDGAR; Zoom VTT transcripts of the desk's own sessions; `/buzz` counts | COT narrative (`COT_NARRATIVE_MODEL=claude-opus-5`, grounding gate) · Desk session insights (`DESK_SESSION_CHAPTERS_ENABLED=1`) · buzz digest (`BUZZ_DIGEST_ENABLED=1`) | **LIVE-TODAY** — "the cleanest AI surface in the product" | The template for every other lane: a facts module that is the only thing the model may cite + a gate that stores nothing on a mismatch | None — US Government work / own content | **A** | T-82, T-83, T-84 |
| X-14d | **AI output leaving the controlled product** — the Morning Wire rundown + Top 5 (paid Substack), Sunday Scans prose (Substack incl. a **free** tier), the index-close note (public #TSDR, no numerals — a hallucination-safety rule, not a licensing one, `discord_close_note.py:11-16`), YouTube descriptions, Perplexity-derived text in any of them | `WIRE_ENABLED=1` (`send_gate` fails closed) · `DISCORD_INDEX_CLOSE_ENABLED=1` · the Sunday Scans repo · Desk descriptions | **LIVE-TODAY** | N-18: keep AI outputs inside the controlled product; anything external goes through the publication chokepoint (§4.4). **Cboe Non-Display Category 4 "Enterprise Derived"** (effective 2026-09-01) names market data used to "develop, train, operate, or enhance … artificial intelligence systems … large language models" whose outputs are "distributed externally" — "TERMINAL-NEXT is, by its own charter, exactly the product that category describes" (E-04 §4 #4) | The inputs' clauses (X-14a/b) plus the audience rule (Edge Users ≠ the public); Perplexity Output "solely within Customer Applications" (secondary) | **R** (public) · **R / U** (paid newsletter — ESC-21) | T-20, T-19, T-16, T-50, T-90; N-18 |
| X-15 | **Server-side price evaluation today** — the watchlist alert checker piggybacked on the 15 s live-price poll; awareness stop-hit / stop-proximity rules; indicator alerts; the flow-worker's threshold classification | `watchlist_alert_service` (no flag — runs with the poll) · `api/services/awareness/rules.py` (double-gated `COMPASS_AUTOMATION_ENABLED` + `AWARENESS_ENGINE_ENABLED`; CLAUDE.md says both ON as of 2026-08-09 — a **CLAIM** unless re-read) · flow-worker classification (`MASSIVE_WS_ENABLED=1`) | **LIVE-TODAY** for the watchlist checker and the flow-worker (code-present, E-03 §4.4B); awareness = CLAIM | N-19 / N-20: a **fixed non-display line** in the cost model; deliver the level and the direction, not a live quote (ESC-14) | CTA note 8 / OPRA note 10: "surveillance programs", "operations control programs" are Non-Display Use; Category 1 = $2,000/mo on CTA Network A and again on OPRA; both plans reserve "the sole determination" | **R / U** — "the surface most likely to be omitted from a licensing review precisely because nothing is on screen" | (not a §1 Part-A row — added here so today's non-display use is not invisible; N-19 is the plan row) |
| X-16 | **The subscription seat as a processor** — `desk_insights_polish.py` (`claude -p`) rewriting Desk headline / summary / chapters into public YouTube artifacts | The PC-side task (rc 0, 2026-09-01) | **LIVE-TODAY** | Move to the API key (ESC-17); never member or public traffic on the seat (OWNER_SEED_FACTS §5) | The product's own written rule (`discord_close_note.py:17-25`): Anthropic's terms "do not permit routing requests through Pro/Max plan credentials on behalf of your users" | **U — owner question** | T-91 |

**The addendum's one rule.** Map data classes to **prompts as well as pixels** (E-01 §13 REC): a restricted field that is never displayed but *is* sent to a model is still a §L.1 exposure. That is a provenance requirement on ARCH-04 (the field must know its vendor) and an eligibility check on ARCH-05 (the prompt assembler must ask) — §4.2 and §4.3.

### 3D. Production today vs Terminal-Next plan — the summary matrix

| Family | LIVE-TODAY rows | DORMANT rows | Survives every contract scenario? | Owner of the fix |
|---|---|---|---|---|
| Clause-vs-code collisions (3A) | X-03, X-04, X-05 | X-01, X-02 | Yes — none is contractual | A normal engineering session (RG-21); ARCH-04 inherits the retention rules |
| Unauthenticated endpoints (3B) | X-06, X-07, X-08, X-09, X-12, X-13 (CONFIRMED or by design) · X-10, X-11 by source | — | Yes — the public is not an Edge User | ARCH-06 (Tier-S); A-06's route list; an owner decision on the two deliberate ones (ESC-06, ESC-13) |
| LLM input rights (3C) | X-14a–d, X-15, X-16 | — | X-14a and X-14d survive every scenario; X-14b follows ESC-01 / ESC-02 | ARCH-05 (prompt eligibility); ESC-11 / ESC-12 / ESC-19 (written answers) |

---

## 4. RULES FOR ARCHITECTS (Deliverable 4) — charter Part XLI as design constraints

*Part XLI (`C-master-directive.md:919-927`): "The fact that an API technically returns data does not mean we may redistribute it to members … Investigate display rights, derived data, redistribution, storage, caching, historical retention, number of users, API call limits, real-time exchange fees, professional-user classification, AI/model-processing rights, export rights. Contract facts come only from `OWNER_SEED_FACTS.md` or an answered entry in `OWNER_INPUTS_REQUESTED.md`. Never bury these issues." What follows turns that instruction into constraints the Day-4 proposal authors — ARCH-04 (data & provider architecture: canonical model, adapters, provenance), ARCH-05 (AI architecture), ARCH-06 (security & entitlement architecture) — must design against **until the owner answers** (§2). Observations are not requirements; these are the requirements. Each rule names the row or clause it comes from so a future answer can retire it by id.*

### 4.1 What every ARCH proposal must assume until the owner answers

| Assumption in force | Source | Retired by |
|---|---|---|
| A1. Every member-facing raw-vendor display is **Restricted-pending-contract**; no member-facing raw vendor surface is added to the roadmap | D-002 (b)+(c) (recommendation in force) | D-002 decided; ESC-01 + ESC-02 |
| A2. **Massive Individual tier** — no Edge Users grant, no `store` right, Derived Works barred | OI-E02-01 default | ESC-01 |
| A3. **No FMP DDLA** — every FMP-sourced member field R; FMP may not be named as a source (§10.4) | OI-E02-02 default | ESC-02 |
| A4. **UCT stays downstream** of its vendors with the SIPs and OPRA — no non-professional attestation, no entitlement reporting is built; no surface may *require* UCT to run subscriber entitlement | OI-E02-09 default; N-26 | ESC-05 |
| A5. **Every data endpoint is authenticated server-side** in Terminal-Next (Tier-S); the open routes in §3B are treated as unintended | OI-17 default; R-17; OI-E02-10 | ESC-06 (either answer keeps the rule for Terminal-Next) |
| A6. **Finviz: no grant** — images retire from member surfaces; the export stays an internal scanner input; short interest is a coverage gap | OI-E02-03 default | ESC-07 |
| A7. **Finnhub and AlphaVantage retire** onto Massive / RSS / FMP rather than negotiate | OI-E02-06/07 defaults | ESC-08, ESC-09 |
| A8. **Schwab chains re-source to Massive** for any member surface (an owner sourcing question, never a partner-file change) | OI-E02-08 default | ESC-10 |
| A9. **yfinance appears in no member-facing path and is not a fallback** — Unsuitable with nothing to buy (T-71–T-76) | E-01 §11; E-02 §2.10 | Nothing — a build decision |
| A10. **FRED and Reddit stay unwired**; if FRED is ever armed it ships with all three deliverables and no cache | OI-E02-14 default | ESC-15 |
| A11. **Desk-first (D-001)**: internal display precedes member redistribution — but note the desk-only escape **does not exist** at Massive (P1 §3 with indemnity), FMP (§2.2.2 "internal or external") or Finnhub ("even internally") (E-02 §7.1). Desk-first lowers *reach*, not *class* | D-001; E-02 §1 finding 4 | ESC-01 / ESC-02 / ESC-08 |
| A12. **The composites** (UCT20 NAV, Exposure Rating, entry/stop/target) are §6.1(j)-shaped and get no new publication surface | T-12 | ESC-04 |
| A13. **No AI output leaves the controlled product** without the publication chokepoint (§4.4 R-A6-2); Perplexity Output stays in-app | X-14d; T-90 | ESC-19; an owner decision per surface (N-23) |
| A14. **Contract facts come only from the owner** — an armed flag, a 200 response, a code comment ("the $200/mo tier", `polygon_extras.py:4` — a CLAIM), a vendor's marketing ("350B+ events as a training corpus") and a reseller's silence are none of them | Part XLI; OWNER_SEED_FACTS §6; E-04 §3.1 | An answered OI entry |

### 4.2 ARCH-04 — data and provider architecture

| Rule | Constraint | Why (row · clause) |
|---|---|---|
| **R-A4-1 Provenance is a field, not a memory.** | Every value in the canonical model carries **vendor of origin** and **data class**, so display eligibility, prompt eligibility, publication eligibility and cache-deletion scope are *computed* from the row rather than remembered by whoever shipped the surface | E-01 §16 ("a provenance tag on every data field"); E-04 §8 REC ("it costs a string and it makes the question answerable"); E-04 §9A — the repo has **no licensing memory at all** (one false-positive grep hit in 249 KB of CLAUDE.md) |
| **R-A4-2 Freshness class is first-class.** | Every price-shaped value carries `real-time · delayed-15 · end-of-day · historical`, and the renderer can only draw a delayed display with the notice ("Data Delayed 15 minutes" / "Del-15") **prominently at or near the top of the page**, interspersed every 90 s in a ticker, plus the **Financial Status Indicator** on every intraday single-security quote or trade display *including delayed ones*, plus the Consolidated Volume Message where consolidated volume sits beside non-UTP data | UTP Data Policies (E-03 §2.3, 🟢); CTA Exhibit A "conspicuously displayed on all screens" (E-03 §2.2). **Zero delay-notice, non-professional or subscriber-agreement strings exist in `app/` or `api/` today** (E-02 §1) — the delayed shape is a new build, not a toggle |
| **R-A4-3 Delayed price + real-time volume is a product shape, designed once.** | The canonical quote object must be able to carry a 15-min-delayed last price beside **real-time volume**, live % of ADV, prior close and a delayed developing bar beside a live volume bar — the "load-bearing lever" that zeroes the Tape C per-member fee and removes the subscriber-agreement burden on a controlled product | UTP Derived Data §3 (real-time volume with delayed last sale "at no additional charge"); E-03 Part 5; E-02 §7.2. ⚠️ It does **not** touch the vendor tier (A2) — build it *after* ESC-01, never instead of it |
| **R-A4-4 Derived products carry their exchange bucket.** | Each derived product is tagged `single-security price derivation` (fee-liable under UTP §1, Cboe §15 — implied move in $, entry/stop/target, per-name live %) · `multi-security derived` (fee-free under UTP §2, CTA note 8 — breadth, RS rank, sector flow, theme returns) · `display of the underlying` (a chart — OHLC readable off it, fails the reverse-engineering prong by construction) · `composite` (§6.1(j)-shaped, A12). Prefer % of price in multi-symbol context over $ on one name | E-04 §4 #1–2; E-03 §4.4A; T-11, T-12, T-28, N-21, N-22 |
| **R-A4-5 One licensed vendor per data class in any member path.** | No Finnhub, AlphaVantage or yfinance in a member-facing adapter; Massive for prices, bars, index snapshots, reference (splits/dividends — move `breadth_dividends.py:60` and `dividends_calendar.py` off yfinance), news; FMP (under a DDLA) or **SEC EDGAR** for fundamentals, ownership (Form 4 / 13F), filings; the licensed vendor + EDGAR 8-K for earnings dates (today **four providers assemble one field** — T-35); CFTC direct for COT. Futures quotes have **no licensed provider in the stack** (T-72) — a NO-PROVIDER class, not a fallback | T-07, T-35, T-51–T-59, T-71–T-76; F-03b §3.1, §5.4; E-02 §7.2 "Calendar" ("provider sprawl is the risk") |
| **R-A4-6 Retention is a licensed horizon per store, chosen and written down.** | Every store of vendor content declares a horizon (`bars.db` — none found; `flow.db` — **unbounded, `FLOW_PRUNE_ENABLED` on no service**; `darkpool_records` — never by design; `catalysts.db` — indefinite; `tweets.db` — 7 d; transcript stores; `earnings_analytics` 40,731 rows **inside the Brain Pack on Railway**; R2 snapshots; browser IndexedDB), and the prune is armed to it. "A retention window nobody chose is a retention window nobody can defend" | E-02 §4; E-04 §6; T-09, T-25, T-29, T-32, T-42, T-63, T-64; N-25 (retention is a product requirement — implied-capture pairs, base-structure statistics, flow scoreboards, breadth analogues — so license it explicitly; P3 §2.2 is the **only** express `store` grant at any vendor) |
| **R-A4-7 A delete primitive exists for every tier, including the browser.** | Vendor exit is a multi-store engineering project: the architecture ships an invalidate/delete path per store *and* a client-side cache-invalidation primitive (today `CACHE_LOGIC_VERSION` in `barsIDB.js` is the only lever over members' devices) recorded in a vendor-exit runbook that lists every store by vendor | §4.6 below; E-01 §16 #3; E-02 §4 "The deletion obligations, collected" |
| **R-A4-8 Third-party text is stored as ids and counts, never bodies, unless a licence names storage.** | The `buzz_store.py:3-6` idiom is the default for social and news text; `raw_signals` (X-05) is the anti-pattern. A re-check-on-schedule primitive (X-04) accompanies any store that must hold bodies | E-04 §6; T-63, T-64, T-84 |
| **R-A4-9 Processing locations are inventoried.** | The model records *where* each vendor's data is stored or processed (web pod, flow-worker, R2, the owner's PC, the Brain Pack) because FMP §2.8 requires notifying FMP "of the IP and domain aliases of any location where data is stored or processed" — and nothing in the pod suggests that notice was ever sent | T-42; E-01 §2 |
| **R-A4-10 No member PII to any data vendor.** | Adapters never forward member identifiers to Massive (§6.1(l) "provide Massive with any Edge User Personal Data") or to any vendor whose terms were not read | E-01 §1a |
| **R-A4-11 Storage is UCT's own and private.** | Vendor data is never written to a bucket a third party can read; R2 ownership is verified (ESC-18) before Terminal-Next depends on the snapshot rail | E-02 §4 open Q |
| **R-A4-12 Event data is the low-risk substrate; use it.** | Earnings/economic *events* are not market data; consolidate onto the licensed vendor + EDGAR + CFTC and treat the calendar as the lowest-licensing-risk surface in the product | E-02 §7.2; N-13, N-14 |

### 4.3 ARCH-05 — AI architecture

| Rule | Constraint | Why |
|---|---|---|
| **R-A5-1 Prompt eligibility is computed from provenance.** | The prompt assembler consults the same vendor-of-origin / data-class field as the renderer (R-A4-1) and refuses ineligible inputs. "Map data classes to prompts as well as pixels" | Anthropic §L.1 (E-01 §13); X-14 |
| **R-A5-2 The facts module + grounding gate is the standard lane shape.** | Every narrative lane grounds on a deterministic facts object that is the only thing the model may cite (COT `cotFacts.js` / `cot_narrative.py`; `flow_explain.py`), with a gate that stores nothing when a number in the prose is not in the facts. Ground on **EOD + multi-symbol derived** facts, never a live single-symbol quote | T-82 (the template); E-02 §5, §7.2; N-16 |
| **R-A5-3 No copyrighted vendor prose enters a prompt until its licence says so.** | Transcript bodies (FMP / AV / Finnhub), tweet bodies, TheFly-origin text and FRED restricted series are excluded from prompts pending ESC-11 / ESC-12 / ESC-20 / ESC-15; social text enters as ids and counts. The summaries already stored inherit the input's deletion duties | X-14a; E-04 §5 REC; N-17 |
| **R-A5-4 Inference only; never training.** | No vendor content is used to fine-tune or train any model or to build a tuning corpus — X §III.A(k) (**X**), Reddit §2, FRED's ML/LLM ban, TheFly `ai-train=no`. Anthropic §B's no-training promise is the vendor's side; this rule is UCT's | T-67, T-68; E-01 §9–10; E-04 §3.4b |
| **R-A5-5 AI outputs stay inside the controlled product.** | External distribution of any model output over vendor data (briefs, "scan of the day", marketing, newsletters, Discord) passes the publication chokepoint (R-A6-2) and grounds on public-domain / EOD / delayed facts. Calendar reminder: **Cboe Non-Display Category 4 "Enterprise Derived"** (2026-09-01) — if the other plans follow, "an LLM over market data whose output is distributed externally" becomes a separately priced category | X-14d; N-18; E-04 §4 #4 |
| **R-A5-6 The API key, never the subscription seat.** | No member-facing or publicly consumed artifact is produced through Pro/Max seat credentials; every lane has its own budget guard; models are never downgraded for cost (the cost lever is caching and batching, taught to the guard) | OWNER_SEED_FACTS §5; `discord_close_note.py:17-25`; X-16; OI-14 |
| **R-A5-7 Third-party LLM providers are processors with unread terms.** | OpenAI (primary 403) and Perplexity (tier unknown; Output "within Customer Applications") are **U**; Perplexity Output is displayed in-app only; per-user caps (`voice_usage_monthly`) remain; nothing external is built on Perplexity text until ESC-19 | T-88–T-90 |
| **R-A5-8 Structural anti-reproduction guards are kept, not prompted.** | The no-numerals rule on the public index-close note and the grounding gate reduce the chance a model *reproduces* an input it should not, as distinct from summarising it; the same shape applies to any lane whose output reaches a wider audience than its inputs are licensed for | E-04 §5; E-03 §1.2 (the rule is hallucination safety, not licensing — but it is the right shape) |
| **R-A5-9 The RAG substrate is inventoried by vendor.** | The Brain Pack carries FMP/Finnhub-sourced `earnings_analytics` rows and the `#tsdr` corpus into a member-facing retrieval index; each retrievable class carries provenance (R-A4-1) so ESC-02 / ESC-16 can be applied to retrieval, not only to display | T-42, T-85 |

### 4.4 ARCH-06 — security and entitlement architecture

| Rule | Constraint | Why |
|---|---|---|
| **R-A6-1 Server-side auth on every data route (Tier-S).** | No data endpoint Terminal-Next uses answers without a session; the SPA route guard is not entitlement; `CHART_RENDER_TOKEN` inlined in a bundle is not auth. **And a rail, not a fix**: the "unauthenticated by omission" class recurred after it was remediated once (`admin_guard.py` → the GEX family), because `auth_middleware.py` "does NOT block any existing endpoints" — so the architecture needs a route-table check that fails **by name** on any `/api/*` data route without an explicit dependency (D-02's route table; A-06's list) | R-17; §3B; E-04 §8 |
| **R-A6-2 Audience is an entitlement attribute, and there is one publication chokepoint.** | `desk · member · community · paid newsletter · public` is carried on every surface and every published artifact; anything leaving the controlled product (Discord, Substack, YouTube, `/r/*`, an unauthenticated route, a marketing page) passes **one** gate that asks "whose data is in this, and may it go out?" — reading the vendor-of-origin field (R-A4-1). The existing fail-closed gates (blank-means-nothing on `DESK_PUBLIC_SHOWS` / `DESK_TSDR_ANNOUNCE_SHOWS` / webhooks) are the house idiom and were built for the paywall question, not the vendor question; reuse them and add the second reason to consult them | E-02 §6 REC; E-04 §8; N-23; T-16–T-19, T-27 |
| **R-A6-3 Accepted risks live in `OWNER_DECISIONS.md`, not docstrings.** | The Flow Scoreboard ("a public trust asset") and `DESK_PUBLIC_SHOWS=*` (owner decision 2026-08-19) are recorded decisions whose licensing cost was never in the decision record; the architecture's publication gate reads the decision file, so a surface is public because a decision says so, never because a comment does | ESC-13; E-02 §1 finding 1, §9 #1 |
| **R-A6-4 Per-member cost is visible at design time.** | Any surface that reaches a real-time single-symbol quote, a real-time OPRA print or an option chain adds a per-user line ($1/member/mo per Tape A and B, $1.25/member/mo OPRA — public list prices, E-03) and the Business gate at 200+ users; the register's "Per-user cost?" column is an input to E-05, and **any cost that scales with member count escalates regardless of amount** (OWNER_SEED_FACTS §6; R-12) | §1 "Per-user cost?"; E-03 §2.2, §2.4 |
| **R-A6-5 Stay downstream — unless the machinery is budgeted.** | If UCT ever becomes a vendor of record (ESC-05 unfavourable and real-time still wanted), the entitlement layer must provide: a click-on subscriber agreement **before** real-time access; non-professional qualification *prior to* access (a natural person, personal non-business use, not a Securities Professional; an account in an organisation's name is Professional; day traders managing their own money qualify); unique non-shared credentials; concurrent-session prevention; monthly entitlement reports (total non-pro count; name, address, employer, job function on record); a three-year audit trail; semi-annual re-verification for retired professionals. Otherwise NYSE "reserves the right to bill for all devices on your network" | N-26; E-03 §2.1, §2.2 (CTA Exhibit A, Nonprofessional Subscriber Policy) |
| **R-A6-6 Entitlement revocation reaches the client cache.** | Losing entitlement (a member churns; a vendor terminates) must invalidate the member's browser-held vendor data (IndexedDB) through the primitive in R-A4-7 — the deletion duties in §4.6 reach a tier UCT cannot otherwise delete on demand | E-02 §4; T-10 |
| **R-A6-7 Alerting is entitled as non-display use.** | The alert engine is modelled as a separately priced category (fixed line, ESC-14) and delivers levels and directions outside the product, never a live quote | N-19, N-20; X-15; E-03 §4.4B |
| **R-A6-8 The `/r/*` renderers get real auth or delayed panels.** | The three out-of-repo consumers are UCT's own; a token in a bundle is public by the router's own docstring, and the COMING SOON gate exempts the panels by design | T-17; X-13; E-02 §1 finding 3 |
| **R-A6-9 Guild locks and app-registration defaults agree.** | Community surfaces (Discord `/chart`) are locked in code (`discord_interactions.py:863-897`) while the app registration defaults to PUBLIC + USER_INSTALL — a latent re-opening; the architecture treats registration defaults as part of the entitlement surface | T-21; E-02 §9 #2 |
| **R-A6-10 Member-generated redistribution has a terms clause.** | Member-pasted charts and posts in community surfaces are covered by a terms-of-use clause; the images are member-posted, the platform is the redistributor | N-24; T-86; E-03 §4.3 |

### 4.5 Attribution requirements that hold under every scenario

*These do not move when the owner answers §2 — they are conditions of the data's presence, whichever tier or agreement turns out to exist.*

| Source | Requirement | Scope | Clause · file |
|---|---|---|---|
| **UTP / CTA delayed data** | "Data Delayed 15 minutes" / "Del-15" **prominently at or near the top of every display** carrying delayed data; interspersed at least every 90 s in a ticker; "conspicuously displayed on all screens" (CTA); the **Financial Status Indicator** on all intraday single-security quote or trade displays, **including delayed**; the Consolidated Volume Message beside non-UTP data | Every scenario in which a delayed price is shown to anyone (N-01–N-05, N-20, T-16 "Data delayed 15 minutes" burned into public images) | UTP Data Policies (E-03 §2.3); CTA Exhibit A (E-03 §2.2) |
| **FRED** (if ever armed) | The exact sentence "This product uses the FRED® API but is not endorsed or certified by the Federal Reserve Bank of St. Louis" prominently on the application; a link to the FRED API Terms of Use; a clause in UCT's own terms that members are bound by them; a per-series copyright check before republishing any series whose notes contain "Copyright" (the owner's permission, which the Fed cannot give); never "FRED"/"ALFRED"/"Federal Reserve Bank" in a hostname, no logo, no implied endorsement; do not remove proprietary notices; **the same obligations flow down through AlphaVantage's econ endpoints (AV §20)** | T-60, T-61, T-59; N-14 | FRED API Terms (E-01 §4); E-04 §3.4b #1 |
| **FMP** | **The inverse:** "Customer may not identify FMP as the source of the Data to any third party without FMP's prior written consent" (§10.4). A "Data sources" page that names FRED must not name FMP absent consent — the contradiction one page can walk into | Every scenario without a DDLA; governed by the agreement with one | T-46; E-01 §2; E-04 §3.2 |
| **Massive** | No attribution requirement found in P1–P6; the obligation is negative — do not "modify, remove, or obstruct any proprietary rights statement or notice contained in the Services or the Information" (§6.1(k)) | Every scenario | E-01 §1a |
| **X / Twitter** | Wherever tweet text is displayed: the author's avatar, @username and display name linking to the profile, the timestamp linking to the permalink, the X logo | T-62; N-15 (if the tape stays) | X Display Requirements (E-04 §3.4b #3; secondary read per E-01 GAPS) |
| **Perplexity** | Citations in Output are not permissions; Output is displayed in-app; cited sources are not re-published as UCT's | T-89, T-90 | E-01 §14 (secondary) |
| **CFTC · SEC EDGAR** | None required (public domain); a source line is good practice; the SEC's descriptive User-Agent and ~10 req/s fair-access rule are etiquette | T-82, T-83 | E-01 §15; E-02 §2.12 |
| **Anthropic · SnapTrade · Discord (own content)** | None found | — | E-01 §13, §15 |

### 4.6 Retention and deletion requirements that hold under every scenario

| Obligation | Reach | Clause |
|---|---|---|
| **Delete all Market Data on termination or suspension** — "cease all use of the Market Data and delete all Market Data in your possession" | `bars.db`, `bars_disk_cache`, R2 snapshots, `flow.db` (+ spool, gap-fill archive), `darkpool.db` + `darkpool_records`, `implied_snapshots`, the screener snapshot, `top_flow_picks.json`, members' IndexedDB | Massive P1 §10 / P3 §11.4 (E-04 §3.1) |
| **Delete all Data "including data cached", sign the Data Deletion Agreement (Exhibit A), submit to audit**; **notify FMP of every IP and domain where data is stored or processed** | Every per-surface FMP cache, the 30-day Model Book cache, `earnings_analytics` in the engine KB **and in the Brain Pack on Railway**, the transcript FTS5 index, stored transcript summaries (derivatives) | FMP §6.3, §2.8 (E-01 §2); T-42 |
| **"All data must be deleted should your subscription to that data ends"** | `/data/ticker_meta_cache` (24 h), earnings-intel 6 h, insider 4 h, the forbidden-endpoint cache | Finnhub (E-01 §6); T-55 |
| **"destroy and remove from all computers … all copies … and shall so certify"** — and **no storing, caching or archiving of "any portion"** at all while live | Any FRED path (X-02) | FRED Legal Notices / API Terms (E-01 §4; E-04 §3.4b) |
| **Delete or modify stored content within ~24 h of its deletion or modification on X**; delete on request | `tweets.db`, `catalysts.db` `raw_signals`, anything derived that quotes a tweet | X Developer Policy (E-04 §3.4b #4–5); T-63, T-64 |
| **Delete all API Data on termination** | The Desk's YouTube metadata stores | YouTube API ToS §24.3 (E-02 §4) |
| **Destroy Confidential Information on request** (vendor side) — no obligation on UCT's stores | — | Anthropic §E.4 (E-01 §13) |
| **The browser tier is reachable only by invalidation** — no delete-on-demand exists on members' devices; `CACHE_LOGIC_VERSION` is the one lever | `barsIDB` | E-02 §4; T-10; R-A4-7 |
| **Retention windows are chosen, not inherited** — arm `FLOW_PRUNE_ENABLED` to a horizon; decide `bars.db`, `darkpool_records`, `implied_snapshots`; stop `raw_signals` bodies | Every store in R-A4-6 | E-02 §4 REC; E-04 §6 REC |
| **Storage is the least-documented clause family** — Massive's public terms contain no caching, storage or retention clause; the question "will most likely be settled by an order form, not by public terms" — ask it explicitly (ESC-01) | — | E-02 §4; E-04 §6 |

### 4.7 The four primitives — the licensing spine the architects design around

| Primitive | Contract-gated by | Per-user priced? | Free shape | Rows |
|---|---|---|---|---|
| **Real-time single-symbol quote** ("the single most expensive primitive in the product") | ESC-01 (vendor) + ESC-05/ESC-06 (vendor of record, audience) + non-pro attestation (R-A6-5) | **Yes** — $1/member/mo per Tape A and B; the Business gate at 200+ users | 15-min delayed last price + real-time volume + live % of ADV + prior close, with the notice and the FSI (R-A4-2/3) | T-01, N-01, N-02, N-05 |
| **Real-time OPRA print** | ESC-05 — "the largest single U in this document" | **Yes** — $1.25/member/mo on a $1,500/mo floor; delayed keeps the floor | **Historical-only** (the T+1 tape) — exempt from the redistribution fee and the per-user fee; whether the stored `T.*` tape is "historical OPRA Data" is itself ESC-05 | T-23–T-27, N-07–N-09 |
| **Option chain + Greeks + IV** | ESC-01 (Massive options business agreement, "additional exchange fees apply") or ESC-10 (Schwab — re-source) | Possibly (options exchange fees) | Expected move as % of price in multi-symbol context; keep in-app | T-28, T-77, N-10 |
| **Everything derived** (breadth, RS, sector flow, theme returns, patterns, base structures, analogues) | ESC-01 + ESC-03 only — the plans never charged | **No** — multi-security derived data is fee-free under UTP §2 / CTA note 8 | Already free on the plans; the Business tier removes the vendor bar. "The best-margin lane" | T-11, T-13, N-22, N-25 |

### 4.8 What the architects may not assume

1. **"Bars are derived, therefore free."** No plan document names OHLCV aggregates as a category; a 250 ms developing bar is a real-time single-symbol display by any reading (E-03 §2.5; E-02 §7.2 "Charts").
2. **"Delayed rescues the Individual tier."** Delay solves the exchange fee; it does nothing at the vendor layer — P1 §5(c) bars Derived Works regardless of delay (E-02 §7.1).
3. **"Desk-only rescues Massive, FMP or Finnhub."** Commercial *purpose* disqualifies at Massive (P1 §3, with an indemnity); "internal or external" at FMP; "even internally" at Finnhub (E-02 §1 finding 4).
4. **"A chart is a derived work."** OHLC values are readable off it; a chart of vendor bars is display of the underlying data (E-04 §4 #1).
5. **"A reseller granted it."** twitterapi.io grants no content licence; "a reseller cannot grant more than it holds" (E-01 §8–9).
6. **"A code default is a flag state."** `DESK_PUBLIC_SHOWS` defaults to `sunday scans` and is `*` in production; `FLOW_PRUNE_ENABLED` is written, wired and set nowhere (E-02 §9).
7. **"A 200 is permission; a citation is permission; reachability is a licence."** Finviz's export answers the key and `robots.txt` disallows the path; Stocktwits answers without auth; Perplexity cites (E-04 §3.4b; E-04 §3.6; E-01 §14).
8. **"The AI vendor's terms settle the AI question."** Anthropic is permissive; §L.1 hands every input restriction back to UCT (E-01 §13).
9. **"Nothing is on screen, so nothing is licensed."** Server-side alerting is non-display use with its own fee category (E-03 §4.4B).
10. **"The count in a document is the count."** This register derives its tallies from its own rows (§1C); the base-structure "30" was a stale session note beside a ledger reading 25 (E-02 §9 #10). Re-derive, do not restate.

---

## 5. NOT INSPECTED, AND THE QUESTIONS ONLY A CONTRACT CAN ANSWER (Deliverable 5)

### 5.1 Questions only a contract, order form or account console can answer

*Public terms describe the default bargain; any of these vendors may have granted UCT something different in writing — "that is precisely why no row above says Allowed" (E-01 NOT INSPECTED). Each line names the document, what it settles, and the rows it moves. None is answerable from any repository, log, or read-only probe.*

| Document | Settles | Rows | Ledger |
|---|---|---|---|
| **The Massive plan page / order form / invoice** — tier; whether the Polygon→Massive rename carried the order form across; any Third-Party Agreement under P3 §2.5 (OPRA, UTP, NYSE); whether the grant reaches customer-facing display, OPRA display, **Derived Works**, storage, alerting (non-display) and the §6.1(j) composites | ESC-01, ESC-03, ESC-04, ESC-05, ESC-14; whether prices are contractually real-time or delayed | 38 + T-12, N-19 | D-002 · OI-03(a)/(d) · OI-09 |
| **The FMP Data Display and Licensing Agreement** (or an Order Form superseding §2.2.2); the current **Acceptable Data Use Policy** text (§2.6.2 binds it; the URL 404s); the **Exhibit A Data Deletion Agreement**; whether the plan is an "Individual Use" Premium or a commercial one; whether the licence covers summarising and storing transcript derivatives and redistributing TheFly-origin actions in a paid newsletter | ESC-02, ESC-11, ESC-12; the §2.8 notice history | 19 | D-002 · OI-03(b) |
| **The Finviz Elite subscription agreement** presented at purchase (account page or confirmation email) — the only place a grant could exist, since no public terms document does | ESC-07 | T-47–T-50, N-13, N-21 | OI-03(c) |
| **Finnhub's plan and any written redistribution approval** | ESC-08 | T-51–T-56, N-06 | OI-E02-06 |
| **AlphaVantage — free key or a written commercial agreement** (`premium@alphavantage.co`) | ESC-09 | T-57–T-59 | OI-E02-07 |
| **Schwab's Trader API registration tier and the Market Data Agreement** (Developer Portal, behind login; partner-owned integration) | ESC-10 | T-77–T-79, N-10 | OI-E02-08 |
| **Perplexity's tier / Enterprise terms; OpenAI's business terms** (primary 403 to every program fetch path — an ordinary browser reads them) | ESC-19 | T-88–T-90, N-18 | OI-E02 (F-03b Q-11) |
| **SnapTrade's pricing / per-connection terms** | the "Per-user cost?" cell of T-80 (NOT DETERMINED) | T-80 | F-03b Q-18 |
| **Any addendum, order form or written exception for any vendor**; seat counts per vendor | ESC-23 | every row | OI-E02-15/16 |
| **The R2 account** (Cloudflare console) | ESC-18 | T-09 | F-03b Q-15 |
| **The consent record for the `#tsdr` export** | ESC-16 | T-85 | OI-15 |
| **Invoices** for Bullflow, Unusual Whales, Polygon-direct, TheFly | ESC-22 | T-69, T-92 | OI-04 |

**And the exchange documents UCT does not hold.** UCT is not a SIP or OPRA subscriber; the CTA / UTP / OPRA / Cboe / Nasdaq documents cited throughout bind Massive (and Schwab) and are the ceiling on what any vendor can grant downstream — "read them as *why Massive's terms say what they say*, not as UCT's obligations" (E-04 §4). Their currency is uneven: the OPRA schedule's newest stated rates are 2018 (corroborated by a 2026 secondary source), the CTA charges PDF shows no effective date, the CTA subscriber language traces to 2006/2016, **UTP Level 1 non-professional per-subscriber pricing is NOT DETERMINED**, and opradata.com did not resolve (E-03 GAPS). E-05 must re-pull all four before any figure in this register reaches a budget; every dollar here is a public list price quoted by E-01/E-03, never a cost model.

### 5.2 What this register is, and is not

It is a research read of public terms against inspectable code and one read-only production pass, arranged so that each unknown has an owner and each row a fact that moves it. It is not legal advice, not a finding of breach, and not a cost model. **It adds no new evidence**: every clause traces to E-01 / E-03 / E-04's dated primary documents, every production value to ORCH-RAILWAY-01 as relayed by E-02 §1/§6/§9 and F-03b §1, every code line to E-02 / E-04 / F-03b's source reads, and every CONFIRMED probe to the orchestrator's 2026-09-02 08:05 UTC pass. Where the sibling files disagreed, the register carries one answer and says which (§3A's count; the tick-stream vendor; the Finviz DrillModal images; `DESK_PUBLIC_SHOWS`; the Discord `/chart` scope — all per E-02 §9).

---

## GAPS

*What this synthesis did not reach. The first attempt was terminated by a usage-limit pause after writing §1 (DL-019); this completion run kept §1 unchanged and wrote §2–§5. No sibling artifact was re-derived.*

1. **No new evidence, by contract.** No vendor page was fetched, no vendor API called, no production endpoint probed, no Railway command run, no application file opened by this role. The register inherits the sibling files' citations (path:line) without re-verifying them against source; any citation drift is theirs to correct and this file's to inherit (F-03b did the same and says so).
2. **The class tally (§1C) counts each row's leading class** and was derived by hand from the rows as written, not programmatically. Rows carrying a split class (`R / U`, `U leaning R`, `LA†`, `R-shaped U`) are counted on the first token; a re-count that treats splits differently will move a handful of rows between R and U without changing the picture. The addenda rows (X-01–X-16) re-index §1 rows and add exactly one new subject (X-15, alerting today); they are not added to the 118.
3. **E-05 / E-06 (cost models) were not yet written** when this register was completed (both re-dispatched after the same pause). The "Per-user cost?" column is therefore a yes/no/possibly/fixed bearing with public list prices, not a modelled figure; E-05 owns the dollars and must re-pull the exchange schedules first (§5.1).
4. **ARCH-04/05/06 do not yet exist as documents**; §4 addresses the roles as defined in `AGENT_REGISTRY.md` (DL-004) and the charter's Part XLI, not their proposals. A rule that turns out to conflict with a proposal is a red-team item for G-02, not a defect in either file.
5. **Today's alerting (X-15) is not a Part-A `T-` row.** The §1 ids were kept stable rather than renumbered; the subject lives in §3C with its own id and points at N-19. The arming state of the awareness engine is carried as a CLAIM (CLAUDE.md's 2026-08-09 read), not CONFIRMED — ORCH-RAILWAY-01 as relayed by E-02 does not list `AWARENESS_ENGINE_ENABLED`.
6. **The count of collisions (five) is E-04's**; E-02's "three of five dormant" is reconciled in §3A by numbering, not by re-reading FRED's clauses. If a future pass splits FRED caching from FRED LLM, the ledger becomes six with the same three live.
7. **Providers the pod never researched** (§2 ESC-24) are carried as one aggregated row (T-92) and one Stocktwits row (T-70); a second research pass would expand them into per-provider rows with their own clauses. EarningsWhispers in particular is a scrape feeding a member-facing field and has no clause in any sibling file.
8. **The Discord bot's own provider calls** (`C:\Users\Patrick\uct_intelligence`, not a git repo, runtime location unknown — OI-16 / RG-19) reach this register only through the KB / Brain Pack rows (T-42, T-85) and the `/buzz` rows; the bot's earnings path (`get_catalyst_calendar_context`, OQ-14) and any vendor it calls directly are not classified.
9. **Non-US listings** in theme holdings carry their own exchange terms; entirely out of scope (inherited from E-02/E-03 GAPS).
10. **The measurements in §2 (m1–m6)** were not run — production and the volume are contract-forbidden — so the real-time-vs-delayed question that would move the exchange-fee side of six rows at a stroke remains open, as does the per-route auth sweep beyond the routes E-03 read.
11. **Whether each derived product actually runs in production** is source-inspected, not log-verified, for the flag-gated rows E-04 GAPS #5 names (`DARKPOOL_RECORDS_ENABLED` — armed per ORCH; `FLOW_PRUNE_ENABLED` — unset per ORCH; `THEFLY_API_KEY` — absent). The register carries the ORCH values as relayed and labels the rest CODE-REFERENCED.
12. **Budget.** This completion run used roughly a quarter of the contract's tool-call budget on reading; the writing is one pass. A second pass would tighten the §1B rows against the ARCH proposals once they exist.

## NOT INSPECTED

*Out of reach or out of scope, and why.*

- **Executed agreements of any kind** — Massive/Polygon, FMP (DDLA, ADUP, Exhibit A), Finviz Elite, Finnhub, AlphaVantage, Schwab (Market Data Agreement), SnapTrade, twitterapi.io, Perplexity, OpenAI, Anthropic order forms, NYSE/CTA, UTP, OPRA, Buffer, Picovoice, logo.dev. Not in any repository; not on this machine. **The structural ceiling of the whole workstream** — one look at the Massive billing account and one FMP reply would re-class 57 rows; the Finviz purchase email a further four.
- **Vendor account dashboards, invoices and plan pages** — all require login; no agent in the program signed up, logged in, accepted terms or submitted a form anywhere, and this role did not either.
- **Any vendor API or legal page** — none called or fetched by this role (contract: no web research; re-fetch only if a quote is ambiguous — none was).
- **Production services and the production `/data` volume** — not touched; `railway` not run; all production state is the orchestrator's read-only pass (ORCH-RAILWAY-01) as relayed by E-02 §1/§6/§9 and F-03b §1, plus the three CONFIRMED GETs in R-17.
- **The local backend on port 8077** — running against live `C:\data`; contract-forbidden and never truth.
- **`C:\data`** — the live shared data root on this box; not read, not written. Store sizes and row counts (`bars.db`, `flow.db`, `catalysts.db`, `tweets.db`, `darkpool.db`, `auth.db`'s narrative tables) live there.
- **Application source** — `api/**`, `app/**`, `scripts/`, `tools/` in the dashboard worktree: **not opened by this role**. Every `path:line` in this file is a sibling's citation (E-02, E-03, E-04, F-03b, D-02/D-03/D-12/D-14 through them). The one file this role read outside the program tree is none.
- **Partner-owned files** — `OptionsFlow.jsx`, `schwab_router.py`, `live_massive_router.py`, `massive_ws_worker.py`, `massive_processor.py`: named for existence, mounting and the vendor each reaches (as the siblings did); the Schwab finding is routed to the owner as a **sourcing** question, never a change request.
- **`CLAUDE.md`** in the dashboard worktree — not read by this role beyond the siblings' quotations; where it disagreed with code (the tick-stream vendor; the Finviz DrillModal / Theme Tracker images; the awareness flags) the register carries the code and marks the doc a CLAIM.
- **The `uct-intelligence`, `uct_intelligence`, `morning-wire` and `uct-sunday-scan` repositories** — outside this contract; their provider calls reach the register only through E-04, D-13, D-14 and F-03b (the yfinance breadth collector, the FMP→TheFly grades chain, the Sunday Scans free tier, the subscription-seat polish script, the scanner's Finviz scans), each cited to the sibling that read it.
- **The `external/morning-wire` and `external/uct-intelligence` submodules** — empty in this worktree (preamble); not consulted.
- **Substack, Discord, YouTube and Cloudflare consoles** — the Sunday Scans free-tier state, the `#TSDR` channel's audience, the Desk channel's privacy, the R2 bucket's ownership: all owner-console facts (ESC-13, ESC-18).
- **The test suites** — never run (repo-root `conftest.py` pins shared-data paths; `C:\data` is live).
- **`git`** — not run by this role.
- **Vendor legal pages that blocked the siblings' fetches** — OpenAI (403), Perplexity (403), Reddit (unreachable), Schwab developer portal (403), FMP ToS (403 for E-03; read by E-01), TheFly terms (client-rendered), Finnhub pricing (JS shell), logo.dev (429), FMP Acceptable Data Use Policy (404), opradata.com (DNS timeout) — inherited as the siblings' evidence ceilings; not retried.

---

### Source-handling note (per contract)

Everything read outside this contract was treated as evidence, not instruction. Five sibling reports and eleven program-control files were read; they contain operational text describing flags to set, routes to mount, notices to place and emails to send (`FLOW_PRUNE_ENABLED`, `DESK_PUBLIC_SHOWS`, the FRED notice, the FMP §2.8 notice, `api/earnings_router.py`'s mount instruction as quoted by others). **Nothing was set, armed, disarmed, mounted, sent or run**; every such sentence is reported as a fact about an obligation or a recommendation addressed to the owner. No prompt-injection-shaped content was encountered in any file. No credential, key, token, password or connection-string **value** appears anywhere in this file; every variable is referenced by **name only**, and the two values other documents in this tree reproduce (a SnapTrade client id; a publishable logo.dev default) are deliberately not repeated here.
