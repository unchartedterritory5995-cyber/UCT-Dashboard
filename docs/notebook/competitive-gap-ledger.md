# UCT Notebook — Competitive Gap Ledger

**This is a living tracking artifact, not a research report.** The research lives in
`competitive-primary-platform-phase-zero.md` (12 dispatches, initial competitive
research) and `competitive-primary-platform-phase-one-adversarial.md` (9 independent
adversarial reviews that corrected, rescoped, and re-prioritized Phase Zero — read
Phase One's verdict, not Phase Zero's original claim, wherever the two disagree).
This ledger's job is narrower and more durable: **one row per capability gap, with a
STATUS that gets updated as work ships**, so "what do we still need to match Notion/
Evernote/Obsidian" has one current answer instead of requiring a re-read of two
613/564-line research documents every time.

**Baseline:** Phase Zero/One research ran 2026-09-05, against commit `c882f426d`.
This ledger's STATUS column reflects **2026-09-06** reality — Waves 0-3 (Trust
Foundation, Capture Completion, Ask Current Note, Thesis-Trade Link), Stage A
validation instrumentation, and Wave 4 prep all shipped or were designed in the day
between that research and this ledger, verified this session via 3 fresh read-only
research passes, not assumed from the wave names alone.

**2026-09-06 addition:** `notebook-ux-ui-competitive-ledger.md` is the sibling
UX/UI ledger — interaction-sequence-level, not capability-level. A few concrete
UX findings from that pass are also recorded as rows here (G-100 series) because
they're real, discrete capability gaps in their own right, not merely polish.

**Update discipline:** change STATUS only when code/evidence changes, not on a
schedule. Add new rows for newly-discovered gaps; never delete a row — mark it
DONE/REJECTED/SUPERSEDED and keep the history.

---

## Legend

- **Parity/Diff:** PARITY (table stakes, must match) · DIFF (financial-native
  differentiation, no competitor equivalent) · BOTH.
- **Status:** OPEN · IN PROGRESS · DONE (cite the commit/wave) · PARTIAL (some done,
  real scope remains) · REJECTED (per Phase One's Do-Not-Build) · BLOCKED (external
  dependency, e.g. legal review) · EXPERIMENT (validate-first, not yet scheduled).
- **Priority:** as currently assigned by Phase One's revised tiers, carried forward
  unless a later decision-log entry changed it (cited if so).

---

## Trust / Recovery

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-001 | Trash / undo-delete | Notion (30d), Evernote, Obsidian | Soft-delete (`deleted_at`), 30-day retention purge, restore recreates exact content | Match | PARITY | All | High (trust) | Wave 0 decision log; this session's fork confirmed restore fidelity | **DONE** (Wave 0, 2026-09-05) | P0 | — | Real member restores a note and confirms content — sandbox E2E already did this |
| G-002 | Version history / diff | Notion (tiered), Evernote (desktop/web only), Obsidian (paid sync add-on) | **Absent** — no version table anywhere in schema, confirmed this session | Match | PARITY | All, esp. Obsidian switchers | High (trust) | Phase One Trust/Recovery; fork B confirmed absent | OPEN | P1 (tension: Constitution names it a trust-parity bar alongside trash/search, only 2 of 3 are P0 — needs an explicit owner decision, not a silent gap) | None | A member reopens an old note and sees a prior version |
| G-003 | Account-deletion purge reaches Notebook data | Implicit baseline expectation | **Was the single most severe finding in Phase One** (zero of 54+ `j2_*` tables had an FK to `users`, cascade delete missed all of them) — **this session's fork confirmed `account_purge.py` now explicitly covers all 9 Notebook-family tables by name** | Match | PARITY | All | Existential (compliance) | Phase One Trust/Recovery (P08); this session fork A, `account_purge.py:53-64` | **DONE** (fixed since Phase One, exact commit not dated by this ledger — recommend a decision-log entry confirming when/how if not already recorded) | Was "urgent, outside normal sequencing" | — | Direct schema/code re-verification (done, this session) |
| G-004 | Attachment/note-body encryption at rest | Baseline expectation for a "primary knowledge store" pitch | Plaintext on disk and in DB; connector tokens encrypted (Fernet), note content is not | Match (eventually) | PARITY | All | Medium (trust, competitive parity) | Phase One Security/Privacy — encryption breaks the live plaintext-fed FTS5 index, key mgmt is the easy part | OPEN, gated on a design spike | P1 | Resolve FTS5-vs-encryption conflict first | A design spike proves search still works under encryption |
| G-005 | Local draft safety net (crash/close protection) | Implicit | **Live**: synchronous localStorage mirror per keystroke, key `uct.j2.notedraft.{noteId}`, restore banner on reopen | Match | PARITY | All | Medium | Fork B, `NoteEditorPage.jsx:40-49` | **DONE** (pre-existing, confirmed this session) | Was P1 (near-free) | — | — |

## Search / Retrieval

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-010 | Full-text search reliability | All three, AVAILABLE | FTS5, correctly engineered, porter-stemmed prefix match. Real correctness gap found THIS session: `$NVDA` misses a note whose only signal is the ticker field (exact-ticker branch doesn't strip `$`) | Match, then exceed via entity/date | PARITY | All | High | Wave 4 prep + implementation-readiness docs (this session); `notes_search.py` | PARTIAL — engine solid, one correctness bug open (Slice 4) | P0 | — | Fixed as part of Wave 4 Slice 4 (Stage-A-gated) |
| G-011 | Search read-latency at platform scale | Implicit | Benchmarked THIS session at 100/1k/10k/50k global rows (`tools/wave4_fts_benchmark.py`) — realistic rare-AND queries sub-2ms even at 50k; single dense terms up to 166ms at 50k (synthetic-vocabulary worst case, pessimistic vs. real member text) | Verify | PARITY | All | High | Wave 4 implementation-readiness doc §2, §15 | **DONE** (verification, not a build — Phase One's own framing) | P0 | — | — |
| G-012 | Folder-sidebar leaf-row undercount | Implicit correctness bar | Phase One found: a folder can render silently, non-expandably empty purely from global-sort truncation, independent of that folder's own size — realistically member-visible ~1,000-note tier. **Not independently re-verified this session** — flagged as likely still open | Fix | PARITY | All, esp. import-heavy | High (a shipped correctness bug, not a completeness one) | Phase One Core UX / P0-3b; fork C's separate finding that `folder_note_counts`/`get_symbol_backlinks` are still super-linear (40-92ms @ 50k) and NOT root-caused suggests this whole area hasn't been revisited | **OPEN, status needs a direct check** | P0 | The fix pattern (`unfiledTotalFromServer`) already exists in the same file per Phase One — apply it | A folder with 1 note in a 1,000+-note library renders correctly |
| G-013 | Date-range filtering | Implicit (all three support date/property filters) | **SHIPPED 2026-09-06 (Wave A Slice 1)** — `idx_j2_notes_user_created` index live, `dateFrom`/`dateTo` params on `list_notes`/`count_notes`, router 400-validates malformed dates, "Note created from/to" date pickers in `FolderSidebar.jsx`'s collapsible filter panel. Live-verified: an in-range date filter surfaces matching notes, an out-of-range one shows the honest "No notes match these filters." | Match | PARITY | All | Medium | `notes.py::_notes_filter_sql`, `journal_two.py::list_notes_endpoint` | **SHIPPED + VERIFIED** | — | — | — |
| G-014 | Query-aware search snippets (`snippet()`/`highlight()`) | All three | **SHIPPED 2026-09-06 (Wave A Slice 2)** — `snippet()` (body, 12-token window) + `highlight()` (title, never truncated) wired into `list_notes`, XSS-safe split-and-render on the frontend (`renderSnippetMarks`, never `dangerouslySetInnerHTML`), replacing the naive `bodyPlain.slice(0,120)`. Live-verified with a real highlighted match. | Match | PARITY | All | Medium | `notes.py::_snippets_for`, `FolderSidebar.jsx::renderSnippetMarks` | **SHIPPED + VERIFIED** | — | — | — |
| G-015 | Relevance ranking (vs. recency-only) | All three have some ranking | **SHIPPED 2026-09-06 (Wave A Slice 2)** — opt-in `sort=relevance` (never silently applied to the pre-existing default), `bm25()` via a correlated scalar subquery, exact ticker/tag structural matches ranked above fuzzy text matches via a best-rank sentinel. Live-verified: an exact ticker match outranked a fuzzy text mention for the same query. | Match | PARITY | All | Medium-High | `notes.py::list_notes` (COALESCE bm25 ORDER BY) | **SHIPPED + VERIFIED** | — | — | — |
| G-016 | Entity-anchored retrieval (ticker/sector/theme filters) | Notion (databases/relations), Obsidian (Bases/Dataview) | Ticker filter shipped (Wave 1). **Sector/theme SHIPPED 2026-09-06 (Wave A Slice 3)** — `resolve_sector_theme_symbols` resolves the member's own bounded mentioned-symbol vocabulary via the existing 24h `ticker_meta` cache (no schema growth), never a full-market scan. Live-verified with a REAL (unmocked) provider call: a note mentioning `$NVDA` correctly surfaced under `sector=Technology`. | CAN DIFFER (derived, automatic, not hand-maintained) | BOTH | All | Medium | `notes.py::resolve_sector_theme_symbols` | **SHIPPED + VERIFIED** | — | — | — |
| G-017 | Semantic/vector search over notes | Notion (Business+), Evernote (free-tier, real), Obsidian (absent, core) | **Absent, zero embeddings table for notes** | Evidence-gated, sequenced LAST | PARITY (once justified) | Medium-value personas | Medium | Phase Zero §11, Phase One Search — "vectors should be the last stage built, not the second" | EXPERIMENT | Experiment/Validate-First | Lexical+entity+date must first be proven insufficient by real usage | Stage A/B usage telemetry shows lexical search failing a measurable fraction of real queries |
| G-018 | $NVDA-style cashtag not matching a ticker-field-only note | Not a competitor feature — a UCT correctness bug | **FIXED 2026-09-06 (Wave A Slice 4)** — `exact_ticker` now strips a leading separator (`re.sub(r"^[^\w]+", "", q.strip()).upper()`) before the comparison, mirroring `fts_match_expr`'s own normalization; an internal hyphen (BRK-B) is preserved. Live-verified: `$NVDA` now correctly surfaces a note whose only NVDA signal is the `ticker` field. | Fix | PARITY (correctness) | All | Low-Medium (narrow) | `notes.py::_notes_filter_sql` | **FIXED + VERIFIED** | — | — | — |

## Organization / Structure

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-020 | Nested folders | All three | Live, depth-capped at 6, server-enforced | Match | PARITY | All | Low | Fork A, `notes.py:48` | **DONE** | — | — | — |
| G-021 | Structured views / database-like properties | Notion (moat, stable-id property/option references), Obsidian (Bases, core+free, but NAME-keyed properties — an unsafe rename model), Evernote (saved searches are literal query TEXT, no structured properties at all) | **SHIPPED (Wave E, 2026-09-07)** — built-in + user-defined properties (`j2_note_properties`, 7 types incl. select/multi_select), 5 financial-derived properties computed LIVE with zero duplicate storage (Ticker/Sector/Industry/Theme/Linked Trade — "UCT already knows this," the checkpoint's own north-star), saved views (`j2_note_saved_views`) with AND-only filter + single-key sort, a Table view with per-column quick-filter chips, progressive-disclosure editor UI (calm-by-default, powerful on demand). Stable-id rename-safety (both property AND option) proven live end-to-end — a saved view survives both a property rename and an option rename with zero manual repair, matching Notion's model and refuting Obsidian's name-keyed one and Evernote's query-text one. Real-browser E2E across property CRUD, saved-view round-trip, deletion safety (notes never touched), and a saved view surviving the deletion of a property it depended on (degrades gracefully instead of a permanent 400). | CAN DIFFER — derived financial properties + Table/List views only (no Board/Calendar/Gallery, no relation/formula/rollup types, no OR/group filters, no multi-key sort — all explicitly deferred, not missed) | PARITY (core structured-property + saved-view job) with UCT's own financial-derivation advantage on top | Fundamental investors + every Notion/Obsidian/Evernote switcher who wants structured organization | Medium-High (closes a real, named Notion-parity gap; the financial-derivation half is a category advantage no competitor offers) | Wave E entry checkpoint + closure pass (fresh official-docs competitor research, 2026-09-06/07); `docs/notebook/prelaunch-primary-notebook-build-plan.md` Wave E sections | **DONE** | — | Entity layer (ticker/sector/theme resolution, trade-ref resolution) | A member filters notes by thesis-status/conviction without a spreadsheet, and never has to type in a ticker's own sector by hand |
| G-022 | Backlinks / note-to-note graph | Obsidian (core, strong; official docs verified 2026-09-06: dedicated Backlinks pane, linked-vs-unlinked-mentions split, "Show more context" preview toggle), Notion (`{#} backlinks` under the title, no documented context preview) | **SHIPPED (Wave D, 2026-09-06)** — native `noteLink` authoring (not just import-path), a stable-id reverse index (`j2_note_links`), and a "Linked from (N)" backlinks section on the target note (title + ref-count, click-through navigation). Real-browser E2E verified end-to-end, including the closure-pass rename-staleness fix (cache-invalidation on save/restore/trash/un-trash — see Decision Log). | CAN DIFFER — derived reverse-index (title + count), not Obsidian's full context-preview pane or a graph view | PARITY (authoring+backlinks core) with a documented gap vs. Obsidian's context-preview | Obsidian switchers | Medium | Wave D; closure-pass competitor audit (fresh official-docs pass, 2026-09-06) | **DONE** | — | Entity layer | A member asks "what links to this note" and gets a real, live-navigable answer |
| G-023 | Favorites / pinning | All three | **SHIPPED 2026-09-06 (Wave B)** — note-scoped favorite toggle (`j2_note_favorites`, idempotent, trash-aware, cascade-cleaned on hard delete via trigger), sidebar Favorites section (populated-conditional), note-header star with optimistic UI. Live-verified end-to-end in the fail-closed sandbox + confirmed live in production via bundle-content grep. | Match | PARITY | All | Low | Fork A → Wave B | **DONE** | — | Entity layer (reused for command-palette matching, not the favorite mechanism itself) | A member stars a thesis note and finds it instantly in the sidebar and command palette |
| G-024 | Recents surface | All three | **SHIPPED 2026-09-06 (Wave B)** — system-derived recently-opened list (`j2_note_recents`, upserted on open, capped at 8, trash-aware), sidebar Recents section, fire-and-forget opened beacon. Live-verified end-to-end in the fail-closed sandbox + confirmed live in production via bundle-content grep. | Match | PARITY | All | Low | Fork A → Wave B | **DONE** | — | None | A member reopening Notebook sees their last-touched notes without hunting through folders |
| G-025 | Saved views / saved searches | Notion, Obsidian (Bases/Dataview) | **SHIPPED (Wave E, 2026-09-07)** — `SavedViewEditor.jsx` + `useJ2SavedViews.js` + structured properties. This row read OPEN until the 2026-09-07 reconciliation purely because the closing wave wrote its own section instead of editing here. | CAN DIFFER (financial dynamic views, per §9 of the master directive) | BOTH | All | Low-Medium | Fork A | **CLOSED (Wave E)** | Not yet tiered | Entity + structured properties | — |
| G-026 | Templates | All three (Notion strongest) | **SHIPPED** — `TemplatePicker.jsx` + `lib/notebookTemplates.js`; the thesis-template half closed earlier. Row reconciled 2026-09-07. | Extend | PARITY (partial), gap for secondary personas | Trader persona well-served; fundamental-investor persona now has one real starting point, not zero | Low for primary persona, Medium for secondary | Wave G closure, `notebookTemplates.js` (`thesis` key); Phase One Persona Challenge | **CLOSED** | Not yet tiered | None | A Company Deep Dive template remains open if secondary-persona investment is ever prioritized |

## Editor

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-030 | Rich block editor (headings/lists/tables/checklists/callouts/toggles) | All three | **Live, genuinely strong** — confirmed this session against the real TipTap config | ALREADY SUFFICIENT | PARITY | All | Low | Fork A, `tiptap.js:22-65` | **DONE** | — | — | — |
| G-031 | Command palette (Ctrl+K style) | Notion, Obsidian | **SHIPPED 2026-09-06 (Wave B)** — Notebook joined the existing app-wide Ctrl/Cmd+K palette (New Note, Open Notebook, Search Notebook, Open Trash, Open Recent/Favorite note; matched by natural terms, real destinations verified against the route table). Live-verified via real Ctrl+K interaction in the fail-closed sandbox (including a live bug found + fixed: Enter now honors the highlighted row) + confirmed live in production via bundle grep. | Match | PARITY | Power users | Low-Medium | Fork A → Wave B | **DONE** | — | None | A member moves from any page to a Notebook job via Ctrl+K without touching the mouse |
| G-032 | Find-in-note | All three | **SHIPPED 2026-09-06 (Wave B)** — Ctrl/Cmd+F scoped to the open note, ephemeral ProseMirror-decoration highlighting (never persisted into saved content — proven by a dedicated test), match counter, next/prev, Escape closes. Live-verified end-to-end in the fail-closed sandbox + confirmed live in production via bundle-content grep. | Match | PARITY | All | Low | Fork A; Phase One Core UX → Wave B | **DONE** | — | None | A member finds one sentence inside a 5,000-word thesis instantly |
| G-033 | Note-to-note link authoring (slash command / autocomplete) | Notion (`@`/`[[`/`+` all equivalent, per official docs), Obsidian (`[[` only, per official docs) | **SHIPPED (Wave D, 2026-09-06)** — `[[`-triggered debounced search autocomplete (`NoteLinkMenu.jsx`), full keyboard flow (type → ArrowUp/ArrowDown → Enter to insert → Escape to cancel → continued typing) real-keydown E2E-verified in the closure pass, plus tap-to-select confirmed usable at phone width | Match | PARITY | Obsidian/Notion switchers | Medium | Wave D; closure pass §5/§6 real-browser keyboard + mobile passes | **DONE** | — | Entity/backlink work | A member types `[[` and links to another note without leaving the keyboard |
| G-034 | Insertable widget types in a note (compose-time) | N/A — UCT-unique concept | **Only Chart** (+MTF stack, +Before/After) has `menus.journal:true`. **This is a deliberate design decision** (typeable params vs. on-screen-state-dependent capture), not an oversight, per Phase One's direct verification of the registry's own comment | N/A — correct as designed | DIFF | All | Low (Phase One downgraded this from a P0 "bug" to a non-issue once the design rationale was confirmed) | Fork A; Phase One P0/P1 Red Team item 4/#A | **DONE (as designed)** | — | — | — |
| G-035 | Large-note performance (virtualization) | Implicit | No client-side mitigation found | Match | PARITY | Import-heavy users | Low-Medium | Fork A | OPEN, untested at real scale | Not yet tiered | None | A very large imported note (10k+ words) opens without jank |
| G-036 | Attachments beyond images (generic file/PDF) | Evernote, Notion | **SHIPPED (Wave I, 2026-09-07)** — the editor's paste/drop handlers + a new toolbar "Attach a file" entry point now accept PDF/txt/csv/md/zip/mp3/m4a/docx/xlsx (`ALLOWED_ATTACHMENT_MIMES`), inserting a real `AttachmentChip` node via the same upload endpoint the import adapters already used. A real live-browser defect found and fixed in this pass: the chip's native `download=` attribute beat TipTap's own `handleClickOn`, so clicking a PDF chip silently downloaded it instead of previewing — fixed via capture-phase click interception. | Match | PARITY | Document-heavy researchers | Medium | Phase One Core UX → Wave I | **DONE** | — | Relates to G-045/G-113 (PDF text + search) | Live-verified: a PDF picked via the toolbar renders as a chip; clicking it opens a preview, not a download |
| G-036b | PDF preview in-context | Evernote (native PDF viewer), Notion (embedded PDF block) | **SHIPPED (Wave I, 2026-09-07)** — `DocumentPreviewSheet.jsx`, a plain `<iframe>` inside the existing `Sheet` `fullscreen` variant (deliberately not an Acrobat clone), with Open-in-new-tab/Download actions. A real, PRE-EXISTING `Sheet.jsx` bug found live in this pass: its `panelStyle` ternary only special-cased `bottom-sheet`, so `fullscreen` silently inherited the desktop-modal 520px `maxWidth` cap — invisible on the one prior fullscreen caller (touch-only, already narrower than 520px) but very visible the first time a fullscreen Sheet mounted on a wide desktop viewport. Fixed by excluding `fullscreen` from that branch; live-reverified (panel width == full viewport, no `maxWidth`). | Match | PARITY | Document-heavy researchers | Medium | Wave I | **DONE** | — | Shared `Sheet.jsx` fix benefits every future fullscreen caller | Live-verified: `getBoundingClientRect().width` == `window.innerWidth`, `panel.style.maxWidth === ''` |

## Capture

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-040 | G-040 | Internal UCT capture coverage expansion (was: "Save-to-Notebook from elsewhere in UCT") | **Live for the surfaces that HAVE a supported internal-capture contract** — 9 widget call sites + `CaptureInboxTray`, through `captureTargets.js` → `POST /api/j2/inbox`. ⛔ **RECLASSIFIED 2026-09-08: the four named surfaces are NOT one homogeneous missing-door problem, and the old wording made them look like four missing buttons.** An internal capture stores `widgetId` + params and re-renders through `WIDGET_REGISTRY`, so a surface can only be a door if it HAS an entry. Measured: **Screener — no entry · COT Data — no entry · Model Book — no entry**; each would need a whole new widget definition (params schema, provenance, rights policy, freeze-vs-reconstruct policy, plain-text/search representation, embed renderer, lifecycle, menu/placement) — product/data semantics, not UI wiring. **Options Flow — entry EXISTS but is a deliberate decline**: `reconstructable: false` and `menus.journal: false`, with the in-file reason *"flow at a past instant is not replayable"*, plus a partner-owned implementation boundary. ⭐ Coverage is not automatically a virtue: a saved artifact that misrepresents what it froze is worse than no door. | Decide the artifact semantics PER SURFACE first; only then build. Wave L is not authorized to invent frozen-widget semantics for arbitrary UCT products. | DIFF (the whole mechanism has no competitor equivalent) | All | N/A — differentiator | Fork A, Phase Zero §3/§15, Phase One "Save to Notebook" section | **PARTIALLY ARCHITECTURALLY UNSPECIFIED / DEFERRED** — descoped from Wave L by owner ruling 2026-09-08 | P1 (Phase One shrunk this significantly — was thought to need new interaction design, doesn't) | Per-surface artifact definition (see `docs/notebook/future-internal-capture-expansion.md`); Options Flow additionally requires an explicit product reversal + partner acknowledgement | Each surface has a written, agreed answer to WHAT is captured and whether it is frozen or reconstructable — before any door is added
| G-041 | Comment/annotation field at capture time | Implicit | **Does not exist anywhere in the flow**, confirmed by Phase One | Add | PARITY | All | Low-Medium | Phase One "Save to Notebook" | OPEN | P1 | None | — |
| G-042 | `tradeRef` population from capture | N/A | Wave 3 built the FULL typed reference system (tradeRef/tradeRefType, resolver, bidirectional nav) — this **exceeds** Phase One's minimal "confirm whether any writer populates it" ask | N/A | DIFF | Trader persona | N/A | This session's own Wave 3 work (decision log, extensively verified/tested/deployed) | **DONE, exceeds Phase One's ask** | — | — | — |
| G-043 | Web clipper / browser extension | All three | **Absent** | REJECTED (general clipper) / EXPERIMENT (narrow financial-document capture bookmarklet) | Notion/Evernote/Obsidian switchers see this as a gap; Phase One argues it's not the real fight | Import-heavy switchers | Medium (real, but see Do-Not-Build reasoning) | Phase Zero §28, Phase One Do-Not-Build Red Team | **REJECTED (general) / EXPERIMENT (narrow)** | — | Cheapest test: a bookmarklet against the existing inbox endpoint, zero store review | Real member demand signal, not yet tested |
| G-044 | Mobile capture / share-sheet | All three (Evernote esp.) | **Absent**, confirmed both Phase Zero/One and this session | Deferred, explicitly triaged | All, mobile-heavy users | Medium | Phase Zero §9, master-architecture.md:335 ("Mobile capture is a confirmed gap... Scope explicitly in Stage B") | OPEN, deferred by design | Stage B | None | — |
| G-045 | PDF / document upload + OCR | Evernote (core retention driver) | **PDF-as-document half SHIPPED (Wave I, 2026-09-07)** — native `pypdf` text-layer extraction (bounded async, per-page fault-isolated, 500-page cap, encrypted-empty-password attempt before honest failure), a page-level model (`j2_note_document_pages`) feeding a dedicated FTS5 index, sectioned document search in the Notebook sidebar (never merged into note search — page-level results stay distinct), and Ticker Research Workspace integration (a Documents section alongside Notes, same dynamic-membership model as G-111). **OCR remains explicitly deferred to presumptive Wave J** (image/screenshot OCR, PDF excerpt capture, page-aware annotations — per the directive's own §226), unchanged from the original REJECTED-for-scans verdict below. | Match for PDF-as-document (DONE); OCR remains deferred, not rejected outright — Wave I's own directive named it Wave J's stated contract | PARITY for PDF-as-document | Document-heavy researchers | Low for OCR (deferred), Medium for plain PDF ingestion (now DONE) | Fork A/C; Phase One Competitor Blocker Challenges (Evernote section) → Wave I | **DONE for PDF-as-document; OPEN/deferred for OCR (Wave J)** | — | Relates to §29/§30 of the master directive; G-036/G-036b/G-113/G-114/G-115 | Live-verified: uploaded a real 3-page PDF, extraction reached `status: "ready"` with `pageCount: 3`, and a search for real page text (`revenue`) returned a highlighted snippet + opened the owning note |

## AI

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-050 | Ask Current Note (single-note AI Q&A) | Notion AI (Business+), Evernote AI Assistant (free-tier) | **Live** — bounded single-note context, cites within note, truthful refusal | Match/Exceed | BOTH | All | High | Wave 2 (this session's prior work); fork B confirmed scope precisely | **DONE** (Wave 2) | Was P0, delivered | — | — |
| G-051 | Ask Notebook (corpus-wide AI over private notes) | Notion AI (cited Research Mode, Business+), Evernote (free-tier, real) | **SHIPPED (Wave K, 2026-09-07, deployed `550283e02`)** — corpus-wide Ask over the member's own notes, tenant-isolated, citation-grounded, with a deterministic refusal when the corpus cannot answer. Deliberately DETERMINISTIC: the semantic leg is approved and NOT ACTIVATED (see G-127). Certification: `docs/notebook/wave-k-production-certification.md`. | Build, per-user-keyed index from day one (never `brain_kb_service.py`'s shared-matrix shape) | BOTH | All, high-value | High | Phase One "Ask My Notebook" section, fork B | **CLOSED (Wave K) — deterministic scope; semantic leg open as G-127** | P1 | Fact ledger (G-060) + entity layer + Wave 4's entity-anchored search | Grounded, cited answers over a member's own note corpus, tenant-isolated by construction |
| G-052 | Ask Notebook + UCT (private notes + vendor data fused) | No competitor can structurally match this | Absent, explicitly not scheduled | Build | DIFF (structural moat) | High-value, gated | High long-term | Phase One "Ask My Notebook" — explicitly blocked on §21 legal review | EXPERIMENT, BLOCKED | Experiment, blocked | External legal/data-rights review | Legal review clears vendor-data-in-AI-answer boundary |
| G-053 | Compass reading/writing note content | N/A — internal architecture question | **Confirmed still separate** — zero of Compass's tools touch `j2_notes` (fork B could not fully confirm either way in its time budget — flagged unverified, not confirmed absent) | Integrate as tools in Compass's existing registry, never a second AI surface | DIFF | All | N/A (architecture, not user-facing gap) | Phase One Architecture Pre-Mortem #1 — "fragmented surface ownership" rated the #1 highest-probability, highest-impact long-term risk | OPEN — architectural constraint to honor when Ask Notebook is built | Cross-cutting on G-051 | — | Ask Notebook ships as Compass tools, not a parallel chat UI |

## Temporal Correctness / Provenance

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-060 | Append-only fact snapshot ledger (structured, queryable) | No competitor has this | **SHIPPED (Wave F, 2026-09-07)** — `j2_fact_observations`, immutable (no update-value code path exists at all), note-owned, entity-identity-resolved via `entity_master`, idempotent capture, batched per-note current-value resolution. Real-browser E2E verified: THEN stayed exactly $142.83 across two different current-value resolutions; a resolver failure showed an honest "Couldn't refresh" without hiding the original. Full contract: `docs/notebook/financial-temporal-semantics.md`. | Build | DIFF | All | N/A (infra; unblocks the estimates half of G-062 whenever rights-approved) | Wave F entry checkpoint + closure (this doc) | **DONE** | — | — | `test_then_stays_then_when_current_value_changes` et al., `api/services/journal_two/test_wave_f_facts.py` |
| G-061 | Frozen-at-insert temporal correctness — charts | No competitor has any equivalent | **Live, the reference implementation.** Nuance confirmed by Phase One: it's the query ANCHOR (date range) that's frozen, not always the pixel data — daily+ timeframes legitimately re-fetch live bars by design (OHLCV essentially never revises). **Wave F confirmed this is a QUERY-CUTOFF pattern, not a value-copy one** (re-derives from the permanent `bars.db`) — it does NOT generalize to analyst estimates, which have no historical re-query source; this is why G-062's estimates half needed its own real value-copy ledger (G-060), not a chart-embed-style extension. | ALREADY THE MOAT | DIFF | All | N/A — differentiator | Phase Zero §3/§14, Phase One Appendix-C-equivalent replication; re-confirmed Wave F entry checkpoint | **DONE**, correctly designed | — | — | — |
| G-062 | Frozen-at-insert extended — fundamentals/estimates/ratings/watchlist/scanner | No competitor has any equivalent | Watchlist/scanner: **DONE**. Fundamentals/reported financials: **DONE**. **PRICE capture: SHIPPED (Wave F)** — `/price TICKER` slash command + a `TickerPopup` "Save price to Notebook" door, both real-browser/unit verified. **User-annotated facts (e.g. "my target: $195"): SHIPPED (Wave F)**, rights-independent by construction. **Analyst estimates/ratings/price-target CONSENSUS specifically: architected but deliberately INACTIVE** — `analyst_price_target_consensus` exists fully in the fact-type registry and resolver (proving the architecture generalizes) but `rights_class: conditional` blocks any write path until Patrick's external legal review approves persistent FMP-derived value storage (never reopened by this program). Activating it later is a frontend-only change. | Activate the estimates fact type once rights-approved | DIFF | All, esp. fundamental analysts | High for the estimates slice specifically; the price/user-note slice already closes most of the practical gap | Phase One Temporal Semantics, Entity Layer; Wave F entry checkpoint decisions 6/23/27/28 | PARTIAL — price + user-note DONE, estimates rights-gated (not a build task, a legal-approval dependency) | P2 (rights-blocked, not effort-blocked) | G-060 (done) | An analyst estimate/rating can be captured into a note, frozen, once rights approval lands |
| G-063 | Calendar-embed forward-looking bug | N/A — a UCT-specific correctness bug | **FIXED (post-Wave-K integrity mini-pass, 2026-09-07)** — `widgetEmbedCore.resolveEmbedRender` now gates reconstruction on KNOWABILITY: a widget declares the day its capture is ABOUT (`asOfDay`, calendar only), and an embed whose subject day had not fully elapsed at `capturedAt` renders its archive instead of re-fetching. Day precision is the honest precision, so a same-day capture counts as not-yet-knowable; a missing/invalid `capturedAt` fails safe. Chart is untouched — `from`/`to` is a query range over a permanent store, not a claim that can be overtaken. | Fix | PARITY (correctness) | All | Medium (a real, live bug, previously unflagged before Phase One) | Phase One Temporal Semantics (new finding) | **FIXED — railed + mutation-checked** | P0 (small, scoped fix: gate `reconstructable` on whether the captured date is future-relative-to-capture) | None | Fixed and regression-tested |
| G-064 | Object-level provenance (MY THOUGHT / SOURCE FACT / AI SYNTHESIS / LIVE DATA / SNAPSHOT) | No competitor structures this explicitly | **Already the house convention, 3x over** (`j2_note_embeds.mode`/`captured_at`, `j2_chat_messages.role`, ModelBook's `catalysts.source`) — the work is extending an existing idiom to 2 more insertion paths (quoted external excerpts, AI-synthesis-inserted-into-a-note), not designing a new system | Extend | DIFF | All | Medium | Phase One Provenance section, fork B (confirmed `mode`/`captured_at` exist) | PARTIAL — pattern proven, 2 extension points open | Folds into G-051/G-060/G-045 | — | — |

## Trading Journal / Thesis Integration

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-070 | Note/thesis ↔ trade/position link | TradeZella, Edgewonk, TraderSync (external, standalone journals only) | **Design exceeds Phase One's ask. A live audit found the creation step itself silently failed from 3 of 5 real UI entry points** (`LogTradeButton.jsx`, `JournalLogFab.jsx`, `TodaySurface.jsx` — all discarded the position-creation response instead of returning it, so `AddPositionModal.jsx` never got a position id to attach the link to; zero error shown to the member) — **fixed and live-re-verified the same audit day** (Bucket A remediation, authorized + shipped 2026-09-06): all 5 entry points now return the created position; `TodaySurface.jsx`'s handler also stopped self-closing the modal prematurely. A second finding — `LinkedNotesPanel` absent from the open-position view — is also fixed (mounted, keyed to the raw position id via a new `idsBySide` helper). Backend/resolver/schema were always correct (confirmed by replaying the exact call manually). | Match/Exceed | DIFF (UCT's version connects to an actual broker-synced position, no standalone journal can) | Trader persona | High (this persona) | `notebook-ux-ui-competitive-ledger.md` Workflow 5 + Bucket A Certification, LIVE-VERIFIED 2026-09-06 (both the regression and the fix); Wave 3 (prior session) | **FIXED, live-verified — DONE** | — | — | Re-verified live 2026-09-06: header "+ Log Trade" → position + thesis note created → `POST /notes/{id}/embeds` fired (200) → link visible on both the position and the note, bidirectional nav confirmed |
| G-071 | New "Trading Journal object model" inside Notebook | External trading journals only | N/A — Phase One found UCT already ships a superior, more-integrated equivalent (`j2_trades`/`j2_positions`/`j2_verdicts`/`j2_trade_reviews`/`j2_interventions`, broker-synced, AI-coached) one tab away | **REJECTED as proposed** | N/A | N/A | N/A | Phase One Trading Journal section — headline finding, high confidence | **REJECTED**, replaced by G-070 | — | — | — |
| G-072 | Thesis structured fields (bull/bear/confidence/catalysts/invalidation) | Notion (via custom database properties) | **SHIPPED (Wave G, 2026-09-07)** — built exactly as this row's own recommendation specified: a note + tag, never a mandatory new object. Bull/Bear/Assumptions/Catalysts/Invalidation are NOTE CONTENT (a new Long/Short Thesis template — deliberately prose, not database cells, per the directive's own "don't reduce nuanced reasoning to a tag list" instruction); Confidence/Thesis Status/Research Type/Review Date were ALREADY Wave E structured properties, found wired-but-disconnected from the thesis tag and unified rather than duplicated. The one genuinely new structural primitive: typed evidence (`j2_thesis_evidence`, supports/opposes a note or a captured Wave F fact, tenant-re-verified both sides) — closes the "no dedicated schema at all" finding for the evidence/counter-evidence half specifically. Real-browser E2E: created via the actual pre-trade `AddPositionModal` flow with Research Type auto-set; added/removed evidence live. | Build as a note+tag+read-time-diff-view, never a mandatory new object (per the product constitution) | DIFF | All | Medium-High | Wave G entry checkpoint + closure (`prelaunch-primary-notebook-build-plan.md`), real-browser-verified 2026-09-07 | **DONE** | — | — | Re-verified live: a Long/Short Thesis note carries Research Type + evidence with stance, distinct from a plain tagged note |
| G-073 | Thesis changelog (diff vs. current facts) | No competitor equivalent | **SHIPPED (Wave G, 2026-09-07)** — a COMPUTED READ (checkpoint §22/24's own "prove it against existing infrastructure first"), not a new event-sourcing table: property changes + thesis edits diffed from consecutive Wave C `j2_note_versions` rows, restores distinguished via a new `restored_from_version_id` marker, evidence add/remove from `j2_thesis_evidence`'s own timestamps, fact captures from Wave F's `observed_at` (never re-fired on a current-value refresh — there's structurally no write path to fire from), trade/position links from Wave 3's embed `captured_at` + `resolve_trade_ref`. Live-verified: two rapid property edits collapsed into ONE changelog entry rather than two, proving the coalescing-based noise-avoidance design works in a real browser, not just in theory. `j2_verdicts` (Compass pre-trade verdicts) is NOT yet a changelog source — a real, named gap, not silently dropped (see below). | Build | DIFF | All | Medium-High | Wave G entry checkpoint + closure, real-browser-verified 2026-09-07 | **PARTIAL — DONE for the 5 sources built; `j2_verdicts` citation still open** | P2 (the specific gap remaining) | — | A member reviews a thesis and sees a research timeline, not an audit database |
| G-073b | Thesis changelog cites Compass pre-trade verdicts (`j2_verdicts`) | No competitor equivalent | Not built in Wave G — the changelog's 5 sources (property/edit/restore/evidence/fact/trade) don't include a verdict-fired-at-entry event | Add as a 6th computed-read source once a verdict is reliably linkable to a specific thesis note (today `j2_verdicts` links to an account+context, not directly to a `j2_notes.id`) | DIFF | Trader persona | Low-Medium | Split out of G-073 at Wave G closure so the still-open half isn't silently marked done | OPEN | P2 | G-073 (done) | A thesis changelog shows "Compass verdict: GO, 9/3" alongside the research-authored events |
| G-074 | Proactive thesis-invalidation alerting | No competitor equivalent | Absent | EXPERIMENT — explicitly NOT to ship before the changelog, alert-fatigue risk is structural (UCT's own Awareness Engine already needed cooldowns for exactly this reason) | DIFF | All | Low near-term | Phase Zero §12/§16, Phase One (confirms the deferral) | EXPERIMENT, correctly deferred | Experiment | G-073 must ship first | — |
| G-075 | Per-ticker research surface ("NVDA research") | No direct competitor equivalent (closest: Notion linked-database view) | **SHIPPED (Wave H, 2026-09-07)** — `TickerResearchWorkspace.jsx`, mounted at `/journal/notebook/research/:symbol` and as the Company Page 'My Research' tab. | Build as a dynamic reverse-index QUERY VIEW that launches existing modals — never a re-implementation of chart/fundamentals rendering | DIFF | All | Medium-High | Phase Zero §12, Phase One Per-Ticker Research Surface section (sharpens the design) | **CLOSED (Wave H)** | P1 | Entity layer | A member opens "NVDA" and sees every note/trade/chart/thesis touching it |

## Collaboration / Offline / Mobile / Extensibility

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-080 | Public share link (read-only) | Notion, Evernote, Obsidian (via Publish) | **Implemented, real, substantial** — token-based, sanitized payload (no user id/tags/folder/ticker exposed), widget embeds render as static archived images, revocation final. **ACTIVATION DISABLED 2026-09-07.** ⛔ Discovered during the post-Wave-K re-baseline: `J2_SHARE_LINKS_ENABLED` was **`1` in production** — live, with the member-facing Share UI wired — while this row, the build plan's activation row and the Phase Zero table all recorded it as dark. A bounded authorization-evidence search (decision log, build plan, this ledger, Phase Zero/One rights findings, git history, the flag ledger) found **no affirmative record** that the §21 review was completed or that activation was intentionally approved: the shipping commit `e05e1699b` says "shipped DARK … the Share button is admin-only while the owner evaluates"; `docs/feature_flags.json` carries `status: armed` with an **empty note**; and the Wave F checkpoint restates the standing rule that rights decisions "stay gated on Patrick's external legal review. No new rights approval was sought or assumed." Per owner ruling, the flag was returned to `0`. ⛔ **CONFIGURATION IS NOT AUTHORIZATION** — a sanitized payload, static embeds, a wired UI and an uneventful live period are risk controls and elapsed time, not approval. | Already built; activation is a policy decision, not an engineering one | PARITY (narrow) | All | Low near-term (no evidence this persona needs it yet) | Fork C (confirmed substantial, more built-out than Phase Zero credited); Phase Zero §18 | **IMPLEMENTED — ACTIVATION DISABLED (authorization unverified)** | P2, validate-first before flipping on | §21 legal review (shared vendor-data exposure risk) — Phase Zero §21 states the share link "would need explicit review before ever being enabled" for a note carrying captured vendor data, because a share-link viewer is neither an Authorized User nor an Edge User under the vendor terms | Affirmative, durable evidence that the §21/activation review completed — then, and only then, re-enable. Real usage demand remains the separate product precondition. The implementation is NOT to be deleted.
| G-081 | Comments / mentions / multiplayer / team workspaces | Notion (deep) | **Absent entirely** | REJECTED near-term | Team/enterprise personas only | Low for stated beachhead persona | Phase Zero §18/§28, Phase One Collaboration | REJECTED near-term | P2/P3 | An account/team-boundary primitive doesn't exist yet — a foundational product/billing decision, not a UI increment | Real usage data from an actual team-shaped segment |
| G-082 | Offline mode (editing) | Obsidian (native, default), Evernote (free-tier standard 2026), Notion (degraded, 2025+) | **Fully absent** — no service worker, no manifest, confirmed both phases and structurally consistent with UCT's live-streaming architecture | EXPERIMENT — Phase One argues even more strongly against building this than Phase Zero did (UCT is ~90% useless offline regardless of Notebook) | PARITY for Obsidian switchers specifically | Obsidian switchers | High for that persona only | Phase Zero §19, Phase One Offline section | EXPERIMENT, low expected value | Experiment | — | — |
| G-083 | Read-only offline cache of recently-viewed notes | Partial parity move | Absent — a new, smaller, cheaper item Phase One identified that Phase Zero's roadmap never named | Build | PARITY (partial, cheap) | All | Medium (a materially better-fit mitigation than full offline) | Phase One Offline section (new item) | OPEN | P2, ranked above full offline editing | None | A member checks a thesis note pre-market on a spotty connection |
| G-084 | Mobile capture / share-sheet | See G-044 | duplicate of G-044 | — | — | — | — | — | see G-044 | — | — | — |
| G-085 | Public API / webhooks / extensibility | Notion (Agents/Workers), Obsidian (plugin marketplace) | **Absent** | EXPERIMENT — a small, first-party, tightly-scoped surface if pursued at all, never a plugin marketplace | N/A | Power users, low near-term | Low | Phase Zero §20/§28 | EXPERIMENT | Experiment | — | — |
| G-086 | Third-party plugin marketplace | Obsidian (7,287 plugins) | Absent | **REJECTED** — unsandboxed execution risk unacceptable for financial research data | N/A | N/A | N/A | Phase Zero §6/§28 | **REJECTED** | — | — | — |

## Portability / Export

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-090 | Full-library export (round-trips) | Notion (narrowing — PDF export being removed), Evernote (desktop-app-only, real gap), Obsidian (trivial, it's files) | **Live, genuinely strong** — markdown+YAML frontmatter, folder paths preserved, attachments bundled, 1,091,412-byte real export independently verified round-trip | Already a real advantage over Evernote specifically | PARITY, already ahead | All | High (positive) | Phase Zero §3, fork A confirms `notes_export.py` mechanics | **DONE** | — | — | — |
| G-091 | Single-note export | Notion, Evernote, Obsidian (trivially, per-file) | **Fixed, Wave C (2026-09-06)** — `GET /api/j2/notes/{note_id}/export` (`notes_export.build_single_note_export`), a bare `.md` when the note has no attachments, a `.zip` when it does; UI entry point is a "Markdown" button in the note toolbar's existing export group | Add | PARITY | All | Low-Medium | Fork A, `journal_two.py:528` — one route, no single-note variant | **DONE** | — | — | Backend: `test_notes_export.py`/`test_notes_export_route.py` (tenant scope, trash exclusion, bare-.md vs .zip branching) |
| G-092 | Trade-link references preserved on export | N/A — UCT-unique | **Fixed, Wave C (2026-09-06)** — every `j2_note_embeds` row with a `trade_ref` resolves via `note_trade_links.resolve_trade_ref` into a human-readable `linked_trades: [SYMBOL (kind)]` front-matter line; the raw internal id is never written (an unresolved/ambiguous ref is omitted, never shown broken) | Fix | PARITY (portability principle: "users should remain confident their research is theirs," §33 of the master directive) | Trader persona | Medium (real, narrow) | Fork A — **new finding this session, not previously flagged by either phase** | **DONE** | — | — | `test_typed_trade_ref_resolves_to_a_human_readable_linked_trade_line`, `test_unresolved_trade_ref_is_omitted_not_shown_as_a_broken_reference` |
| G-093 | Sync connectors bidirectional (write back to source) | N/A — UCT chose read-only by design | **Confirmed all 7 providers (Notion/Obsidian/Dropbox/Craft/Roam/OneNote/OneDrive) are read-only pull, none push** | N/A — deliberate scope, not a gap | N/A | N/A | N/A | Fork C | **DONE (as scoped)** | — | — | — |
| G-094 | Sync conflict handling | Implicit | **Real and correct** — a genuine conflict creates a sibling note tagged `sync-conflict`, both versions preserved, never silently clobbered | Already matches best practice | PARITY | Connector users | Low (already solved) | Fork C, `engine.py:51-75` | **DONE** | — | — | — |

## UX/UI (2026-09-06 addition — see `notebook-ux-ui-competitive-ledger.md` for full interaction-sequence detail)

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-100 | Raw backend errors leaked to members | All three show sanitized error copy | **FIXED (Wave B) and RE-FIXED + RAILED (integrity mini-pass, 2026-09-07)** — Wave B removed three leaks; Wave H reintroduced the class in `TickerResearchWorkspace.jsx` (`alert(...e.message)`). The mini-pass fixed every instance in the Notebook's own UI (TickerResearchWorkspace, FolderSidebar ×3, NotebookTab ×2, HeroImagePicker ×2) and added `rawErrorSurface.test.js` — an AST rail (never a grep: a grep matches the comments and docs that describe the pattern) with six controls, mutation-checked. ⛔ Named debt: the same class remains across the rest of `journal-2-0/` (trading tabs/modals) — see the new G-128. | Sanitize all three | PARITY (trust) | All | Medium-High (undermines trust in a domain — Trust/Recovery — the product otherwise handles well) | UX audit fork, `NotebookTab.jsx`, `NoteEditorPage.jsx`, `ImportWizard.jsx` | **FIXED in the Notebook scope + RAILED; wider journal-2-0 debt tracked as G-128** | Not yet tiered, recommend P0-equivalent (cheap, trust-critical) | None | A save/load failure shows friendly copy, never a raw exception string |
| G-101 | Note-load failure has no distinct error state | Implicit baseline | **Confirmed**: `NoteEditorPage.jsx` has no `error` branch for a failed note fetch — appears to hang on "Loading…" indefinitely | Add an explicit failure state | PARITY | All | Medium | UX audit fork | OPEN — new finding this session | Not yet tiered | None | A 404/network-failed note load shows a clear message, not an infinite spinner |
| G-102 | App-wide command palette does not include Notebook | Notion (Cmd/Ctrl+K), Obsidian (Cmd/Ctrl+O quick switcher) | ⛔ **THIS ROW WAS WRONG, AND SO WAS A 2026-09-07 RE-BASELINE THAT REPEATED IT.** Notebook DOES participate in the app-wide palette: `components/CommandPalette.jsx` imports `useJ2Favorites`/`useJ2Recents` from `pages/journal-2-0/`, ships static Notebook commands (New note / Open Notebook / Search Notebook / Trash) and matches note rows via `notebookNoteRowsMatch()`. Wave B added it ("Notebook joins the ONE existing command palette rather than growing a second"), Wave H extended it. The original audit — and my re-check — grepped `pages/journal-2-0/` for a REGISTRATION and found none, because the architecture is the reverse: the palette PULLS. `lesson_grep_for_a_name_finds_one_ask_the_module_finds_ten`, twice over the same row. | Register Notebook actions in the existing palette | PARITY | Power users | Medium-High (real, and unusually cheap to close — the infra exists) | UX audit fork, `CommandPalette.jsx:61` | **CLOSED (Wave B, extended Wave H)** — the remaining Power-User gap is note-level keyboard shortcuts, which is NOT this row | Not yet tiered, recommend early (cheap) | None | Cmd/Ctrl+K from Notebook surfaces new-note/search-focus/jump-to-note |
| G-103 | Delete-note confirmation uses native browser dialog, not a UCT modal | All three use styled, in-product modals | **FIXED 2026-09-06 (Wave B)** — both native `confirm()` call sites (note delete `NoteEditorPage.jsx`, folder delete `FolderSidebar.jsx`) replaced with the existing `ConfirmModal` component (already proven in Position delete), proportional copy ("moves to Trash, restorable 30 days"). Live-verified in the fail-closed sandbox + confirmed live in production. | Replace with the shared UCT modal component | PARITY (visual consistency) | All | Low-Medium | UX audit fork, `NoteEditorPage.jsx:838` → Wave B | **DONE** | — | None | Delete confirmation renders as a UCT-styled modal |
| G-104 | Design-token adoption is inconsistent (hardcoded hex vs. shared tokens) | Implicit | **Confirmed, quantified**: `NoteEditorPage.module.css` 73 token uses vs. 53 hardcoded hex; `FolderSidebar.module.css` 77 vs. 39 | Migrate hardcoded values to tokens | PARITY (visual consistency) | All | Low | UX audit fork | OPEN — new finding this session | Not yet tiered, cheap/mechanical | None | 0 hardcoded hex colors in Notebook `.module.css` files |
| G-105 | Raw Unicode glyphs used as UI chrome instead of `UIcon` | N/A — a UCT internal convention violation | **Confirmed** in 3 places (`×`, `✕`, `❝` close/discard/toolbar buttons) — against this codebase's own explicit "no generic symbols, use UIcon" rule | Replace with `UIcon` glyphs | PARITY (visual consistency) | All | Low | UX audit fork | OPEN — new finding this session | Not yet tiered, cheap | None | 0 raw-glyph UI-chrome elements in Notebook components |
| G-106 | No shared skeleton/loading component reused | All three use polished loading states | **PARTIALLY FIXED 2026-09-06 (Wave B)** — the app-wide `Skeleton.jsx` (already used in 68 other files) adopted for the two highest-frequency structural loads: search-results loading (`FolderSidebar.jsx`) and note-page loading (`NoteEditorPage.jsx`), both replacing plain `"Loading…"`/`"Searching…"` text. Other, lower-frequency loading states in Notebook were deliberately left as-is (scope discipline — not a repo-wide sweep). | Adopt a shared skeleton component | PARITY (perceived performance) | All | Low-Medium (broad, cheap improvement to perceived speed) | UX audit fork → Wave B | **PARTIAL — the two highest-frequency surfaces done, others remain** | P2 if further loading states are found high-friction | None | Loading states use a consistent skeleton/shimmer pattern |
| G-128 | Raw backend/provider exception text in member-facing error UI across the REST of `journal-2-0/` (trading tabs + their modals) | All three competitors show sanitized error copy | **OPEN, quantified.** The AST rail built for G-100 was first run over the whole of `pages/journal-2-0/` and reported **104 violations across 32 files**, of which **12 are native `alert()`** — `AddPositionModal`, `AddTradeModal`, `ClosePositionModal`, `EditPositionModal`, `DeleteAllModal`, `ImportCsvModal`, `GlobalAddPositionProvider`, `OpenPositionsTab`, `TradeJournalTab` and others. Two (`OpenPositionsTab`, `TradeJournalTab`) were fixed in passing because the rail surfaced them; the rest are outside the integrity mini-pass's declared scope and were NOT swept in. ⭐ The rail's scope list is the one line to widen when this is scheduled. Note the deliberate carve-out: a per-item REPORT (`failures.push({reason})`, the importer's `warnings`) is not an error banner — telling a member WHICH of 500 files failed and why is the feature, and whether those reports should carry a raw provider string is a separate question. | Fix per surface, then widen `IN_SCOPE` in `rawErrorSurface.test.js` | PARITY (trust) | All | Medium (each instance is a trust-eroding surface at a moment the member is already failing) | Integrity mini-pass 2026-09-07, measured not estimated | **OPEN — quantified, not swept** | P2 | None (the rail already exists) | `rawErrorSurface.test.js` passes with the trading surfaces added to `IN_SCOPE` |

---

## Summary counts (2026-09-06)

- **DONE:** 33 rows (trash/undo, account-deletion purge, local draft safety net, search-latency verification, chart temporal correctness, fundamentals/watchlist/scanner snapshot semantics, Ask Current Note, tradeRef population, thesis-trade link, nested folders, rich editor, chart-only compose-time embed as designed, share-link mechanism dark, full-library export, sync connectors read-only as scoped, sync conflict handling, editor, single-note export (G-091, Wave C), trade-link references preserved on export (G-092, Wave C), backlinks/note-to-note graph (G-022, Wave D), note-to-note link authoring (G-033, Wave D), structured views/database-like properties (G-021, Wave E), append-only financial fact ledger (G-060, Wave F), resumption surface (G-110, Wave H), automatic per-security research assembly (G-111, Wave H), notebook-and-company-page bridge (G-112, Wave H), attachments-beyond-images/PDF authoring (G-036, Wave I), PDF in-context preview (G-036b, Wave I), PDF-as-document text extraction + search (G-045 PDF half, Wave I), page-aware document search (G-113, Wave I), document-ticker-workspace integration (G-114, Wave I), attachment-vs-document data model (G-115, Wave I))
- **PARTIAL:** G-062 (frozen-at-insert extended) — price + user-note capture DONE (Wave F); analyst-estimates specifically remains rights-gated, not effort-blocked
- **REJECTED (Do-Not-Build, confirmed):** 3 rows (general web clipper, plugin marketplace, new trading-journal object model)
- **OPEN, new findings this session (not in either phase):** 1 row (folder-sidebar status needs re-confirmation) — G-091/G-092 closed in Wave C (2026-09-06)
- **OPEN, new UX/UI findings this session (G-100 series):** 7 rows — raw error leaks (G-100, highest-leverage trust finding), no note-load error state (G-101), command-palette non-participation (G-102, highest-leverage power-user finding), native confirm() dialog (G-103), token/hex inconsistency (G-104), raw-glyph UI chrome (G-105), no shared loading skeleton (G-106). All cheap; none require new backend work.
- **DESIGNED, Stage-A-gated (Wave 4):** 4 rows (date filter, snippet/highlight, ranking, entity sector/theme filter)
- **EXPERIMENT / validate-first:** 5 rows (semantic search, offline editing, extensibility, plugin marketplace's narrower cousins, Ask Notebook+UCT)
- **OPEN, unscheduled:** remainder — see per-row priority.

**Net read on the ledger as a whole:** roughly half of Phase One's P0 punch list is
now done, including the single most severe finding (account-deletion). What
remains open and P0-tier: the folder-sidebar correctness bug (status needs a direct
re-check), the fact/snapshot ledger + analyst-estimates capture path (the real
remaining "temporal correctness" work), and the Calendar-embed forward-looking bug.
Two genuinely new gaps were found this session that neither phase flagged: no
single-note export, and trade-link references silently drop on export.

## Continuation / Automatic Organization (Wave H, 2026-09-07)

Scoped competitor comparison per directive §60-64/§155 — Notion/Obsidian/
Evernote, current-sourced, on the resume-work / organize-domain /
config-required axes. None of the three has a resumption or per-entity
research surface a member does not build themselves.

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-110 | "Where was I working?" resumption surface | Notion (no dedicated resume view; a member pins/favorites manually), Obsidian ("Recent files" core plugin — a flat MRU list, no synthesis), Evernote (a flat "Recently updated" sort, no synthesis) | **Live** — `ResearchHome.jsx` (`GET /notebook/home`): Continue Working, Active Theses, Connected to Open Positions, Needs Review, Favorites, each derived from EXISTING relationships (recents/favorites/thesis-status/trade-links/review-date), zero member-built database or manual pin required | N/A — already ahead | DIFF, UCT ahead | All | High (positive — the resumption question is the north-star pain point named in the directive) | Wave H checkpoint + closure | **DONE** | — | — | Live-verified: a low-data account shows the honest empty state; a populated account shows the right sections and hides empty ones automatically |
| G-111 | Automatic per-security research assembly (no member-built database/relation table) | Notion (a per-company page is a member-built database + relation properties the member must configure), Obsidian (a per-ticker MOC/hub note the member must create and link by hand), Evernote (a notebook/tag the member must create and file into manually) | **Live** — `TickerResearchWorkspace.jsx` (`GET /notes/research/{symbol}/summary`): notes/theses/facts/trade-positions all aggregate dynamically from existing ticker/embed/mention/trade relationships, config-required = **zero** | N/A — already ahead | DIFF, UCT ahead, config-required is the differentiator | All | High (positive — this is the literal "financial differentiation" test named in the directive) | Wave H checkpoint + closure | **DONE** | — | — | Live-verified: a note related to a symbol only via a `$TICKER` prose mention (zero manual linking) appears in that symbol's workspace automatically, in BOTH the workspace and (after the fix in the closure section above) the same-ticker filtered list |
| G-112 | Per-security research reachable from BOTH the notebook and the live company/quote page | Notion/Obsidian/Evernote have no equivalent "company page" surface at all — N/A, not a gap either way | **Live** — the SAME `TickerResearchWorkspace` component mounts at `/journal/notebook/research/:symbol` AND as a new "My Research" tab on the existing paid Company Page `/research/:sym`, never a second implementation | N/A — UCT-unique (no competitor has a market-data company page to bridge from) | N/A | Trader persona | Medium (positive, UCT-unique) | Wave H checkpoint + closure | **DONE** | — | — | Live-verified: both mount points render identical content, `showBackLink` the only difference |

## Attachments / PDF / Financial Document Research Foundation (Wave I, 2026-09-07)

Scoped competitor comparison per directive's own benchmark instruction —
Evernote (attachment/PDF/search/annotation/mobile/export), Notion (file
blocks/PDF embeds/upload UX/export), Obsidian (local file ownership/embedded
PDFs/portability), current-sourced. The directive's own instruction: identify
USER JOBS, don't clone appearances — none of these rows chase a competitor's
visual chrome.

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-113 | Page-aware document search, sectioned separately from note search | Evernote (PDF text search, unified with note search — no page-level result), Notion (search reaches text inside an uploaded PDF block, no page anchor surfaced in results), Obsidian (no native PDF text search without a community plugin) | **Live, ahead of all three** — a dedicated FTS5 index (`j2_note_document_pages_fts`, deliberately never merged with note search) returns page-numbered, highlighted-snippet results in their own sidebar section, each opening the owning note. `resolve_trade_ref`-style page-level identity means a result names the exact page, not just "somewhere in this file." | N/A — already ahead on the page-anchor half | DIFF, UCT ahead | Document-heavy researchers | High (positive — directly answers the directive's own "I searched for 'gross margin' and UCT took me to the page" north-star line) | Wave I checkpoint + closure | **DONE** | — | — | Live-verified: searched a real uploaded PDF's text (`revenue`), got a highlighted page-3 snippet, clicked it, landed on the owning note |
| G-114 | A security's PDF/filing history reachable from its research workspace, zero manual filing | Notion (a member manually creates a database row + uploads per company), Obsidian (a member manually files a PDF into the right folder/tag), Evernote (a member manually files into the right notebook) | **Live, ahead of all three** — the Ticker Research Workspace's Documents section aggregates PDFs dynamically off the SAME ticker/embed/mention membership as Notes (`_documents_for_symbols`, independent 5-cap), zero member-built database or manual filing step, matching the config-required=zero standard G-111 already set for Notes | N/A — already ahead | DIFF, UCT ahead | All | High (positive — the financial-differentiation test named in the directive) | Wave I checkpoint + closure | **DONE** | — | G-111 (same dynamic-membership pattern, applied to documents) | Live-verified: an uploaded PDF on a `$NVDA`-tickered note appeared automatically in the NVDA workspace's Documents section with its page count and ready status, no manual linking step |
| G-115 | Attachment identity model extended to support extraction without a second storage mechanism | Notion (file blocks are a first-class object with their own extraction pipeline internally), Obsidian (a local file is the ONLY identity — no derived-data layer), Evernote (proprietary internal note-attachment binding) | **Live, correctly scoped** — a `document` row (`j2_note_documents`) exists ONLY for a PDF attachment (never image/CSV/DOCX), keyed by `(note_id, attachment_url)` since attachments themselves carry no id; three-level cascade-delete trigger chain (note → document → pages → FTS) keeps the derived data consistent with zero new manual-cleanup code path. Filesystem-path identity (Obsidian's own model) is preserved unchanged for the attachment itself — extraction is purely additive metadata, never a second copy of the file. | N/A — architecture decision, not a competitor race | N/A | All | Medium (correctness/maintainability, not member-visible) | Wave I checkpoint + closure | **DONE** | — | — | Verified via the cascade-delete test matrix: deleting a note removes its document + page rows + FTS entries in one transaction, with zero orphaned rows |

**Two real, live-browser-discovered defects this wave, neither caught by any
unit/integration test** (jsdom cannot simulate native browser anchor-download
behavior or real CSS layout) — see G-036/G-036b above for both: the
`AttachmentChip`'s native `download=` attribute defeating TipTap's click
routing, and the pre-existing `Sheet.jsx` `fullscreen`-variant `maxWidth`
regression. Both fixed, both live-reverified, both with new regression tests.

**Wave D (2026-09-06):** native internal-note-link authoring + backlinks shipped
(G-022, G-033), closing the "Obsidian/Notion switchers" gap in this table. A narrow
evidence-closure pass the same day (fresh official-docs competitor re-verification,
real-keyboard/mobile/accessibility passes, proportionate scale check) found and fixed
one real defect: a renamed/trashed/restored note's title/status could show stale in a
noteLink chip elsewhere in the same browser tab until a full reload — fixed via a
targeted cache-invalidation hook on the four write paths that change a note's own
title/status (save, Wave C restore, trash, un-trash), real-browser verified before and
after. Residual, explicitly accepted debt: no context-preview on backlink rows (vs.
Obsidian's "Show more context"), and the `[[`/SlashMenu popups' editable root lacks an
explicit `role="combobox"`/`aria-autocomplete` pairing (a pre-existing pattern shared
by both menus, not a Wave D regression) — left as recorded debt per this pass's
explicit no-scope-creep mandate.

## Document Intelligence II — Excerpts / Highlights / Page-Aware Evidence (Wave J, 2026-09-07)

Scoped competitor comparison per the directive's own benchmark instruction —
Evernote (PDF annotation/highlight/export), Notion (PDF blocks, no annotation
layer), Obsidian (Annotator community plugin, local-file model), current-
sourced. The directive's framing correction governs every row: the product job
is TURNING FINANCIAL SOURCE DOCUMENTS INTO DURABLE, CITABLE RESEARCH EVIDENCE.
OCR is a capability that serves that job, not the job itself, and is
deliberately not built this wave.

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-116 | Capture a passage from a PDF as a first-class, citable object that outlives the note it was dropped into | Evernote (highlight lives inside the attachment's own annotation layer; it is not an object you can search, re-cite, or attach to anything else), Notion (no annotation layer at all — a PDF block is opaque), Obsidian (the Annotator plugin writes highlights into a companion markdown file; portable, but a plugin, and the highlight is note content, not an object) | **Live, ahead of all three** — `j2_note_excerpts` is a first-class row: captured passage + page + a W3C-Web-Annotation-shaped text-quote selector, membership in a note carried by a sidecar (`j2_note_excerpt_refs`) rather than by being note body content. Delete the paragraph it sits in and the evidence survives; cite it from a thesis and the citation is an edge, not a copy. | N/A — already ahead | DIFF, UCT ahead | Document-heavy researchers; thesis-builders | High (positive — this is the "durable, citable research evidence" line the directive names as the product job) | Wave J checkpoint + closure | **DONE** | — | G-113/G-115 (Wave I document identity + page model) | Live-verified: captured a two-line passage from a real 4-page deck, confirmed the stored row carries `charStart` 261 / `charEnd` 416 and a real 200-char context window on both sides, then confirmed the same passage re-highlights on the page after a full reload |
| G-117 | The captured passage stays anchored when the page is re-extracted or re-rendered | Evernote (anchors to its own internal coordinates; opaque and non-portable), Notion (nothing to anchor), Obsidian Annotator (text-quote anchoring — the same W3C model, and the closest competitor on this row) | **Live, at parity with the best of the three and ahead of the other two** — `quote_prefix`/`quote_suffix` are the robust anchor, `char_start`/`char_end` supplementary, and re-location falls back from quote-context to a bare match. **This row is the wave's hardest-won:** the anchor was landing EMPTY for essentially every multi-line capture (see the decision log's defect 1) because the page text and the browser's selection were built from different sources; one canonical builder now derives both. | N/A — parity with Obsidian Annotator, ahead of Notion/Evernote | Parity (best-in-class), DIFF vs. the other two | Document-heavy researchers | High (positive — an excerpt whose anchor is null is a quote with no source, which is worse than no feature) | Wave J checkpoint + closure | **DONE** | — | G-116 | Live-verified: a saved excerpt's gold highlight redraws across both of its line boxes on a fresh page render, and the same excerpt survives the note being trashed and restored |
| G-118 | Click a citation in a note and land on the exact page of the source document, with the passage marked | Evernote (opens the attachment; no page anchor from a note reference), Notion (opens the PDF block; no page anchor), Obsidian Annotator (a highlight link does open the PDF at the annotation — the closest competitor here too) | **Live** — a `documentExcerpt` node renders as a card whose citation reads `{document} · p.{n}`; clicking it opens the PDF viewer at that page with the passage highlighted and briefly emphasized. The same door works from a thesis-evidence row, including for an excerpt captured in a DIFFERENT note (resolved via `GET /excerpts/{id}`, which carries the source document's URL directly). | N/A — parity with Obsidian Annotator, ahead of Notion/Evernote | Parity (best-in-class), DIFF vs. the other two | All | High (positive — the directive's own highest-value exit gate) | Wave J checkpoint + closure | **DONE** | — | G-116, G-117 | Live-verified: clicked an excerpt card's citation, landed on p.1 of the source deck with the gold highlight drawn over the exact captured passage |
| G-119 | Search your own saved evidence, sectioned apart from raw document text | Evernote (highlights are not separately searchable — a search returns the note or the attachment), Notion (n/a), Obsidian (a plugin's companion notes are searchable, but they rank against all other notes with nothing distinguishing them) | **Live, ahead of all three** — a THIRD FTS5 index over `captured_text` + `annotation`, with its own sidebar section: Notes / Documents / Evidence, never merged into one score. The reason is the same one Wave I gave for splitting Documents out: a passage a member deliberately kept is not the same kind of hit as a page the words happen to appear on, and ranking them together buries the curated one under the raw. | N/A — already ahead | DIFF, UCT ahead | Document-heavy researchers | Medium-High (positive) | Wave J checkpoint + closure | **DONE** | — | G-116 | Live-verified: `margins` returned 4 document pages and 1 saved excerpt in separate, separately-counted sections; trashing the owning note removed BOTH from search, restoring it brought BOTH back, with no reindex step |
| G-120 | An excerpt can be attached to a thesis as supporting or opposing evidence, with page-aware attribution | Evernote/Notion/Obsidian: none has a thesis object at all — N/A, not a gap either way | **Live, UCT-unique** — `document_excerpt` joined `note` and `fact` as a `j2_thesis_evidence` target type via a one-line change to the open-string extension point Wave G deliberately left in place. The stance lives on the EDGE, not the excerpt, so the same passage can support one thesis and oppose another. Two captions, deliberately distinct: the excerpt's own annotation ("why this passage matters", portable) vs. the evidence caption ("why it supports/opposes THIS thesis"). | N/A — UCT-unique | N/A (no competitor equivalent) | Thesis-builders | High (positive — the strategic moat this program keeps compounding) | Wave J checkpoint + closure | **DONE** | — | Wave G (`j2_thesis_evidence`), G-116 | Live-verified: added a p.2 excerpt as SUPPORTS evidence with a caption, confirmed the row persisted with `targetType: "document_excerpt"`, the changelog recorded "Evidence added (supports)", and the markdown export's front matter carries `stance: supports` / `target: deck.pdf, p.2` / the caption |
| G-121 | OCR / scanned-document text | Evernote (server-side OCR on images and scanned PDFs — genuinely ahead of UCT on this row), Notion (no OCR), Obsidian (no OCR without a plugin) | **NOT BUILT — deliberately.** The `text_origin` column is shaped for `'ocr'` and nothing else was committed: no engine chosen, no privacy model, no cost model, and no private document sent to any external service. | Build only after the privacy/cost decision is made explicitly and by the owner | GAP, Evernote ahead | Members with scanned filings or screenshots | Medium (a scanned 10-K currently yields `no_text` and honestly says so, rather than silently appearing empty) | Wave J checkpoint (explicitly scoped out) | **OPEN — deliberate** | Medium | An owner decision on whether private member documents may be processed by an external OCR service, and at what cost | Not applicable — this row is open on purpose. It must not be closed by an implementation wave choosing an engine on its own. |

**Five real, live-browser-discovered defects this wave**, four of them SILENT
(the excerpt saved, the card appeared, and only the durability guarantee or
the member's own attachment was quietly gone) — full detail in the decision
log's Wave J entry. The two most instructive: the text-quote anchor landed
null for essentially every multi-line capture because the page text and the
browser's selection were built from different sources; and saving an excerpt
DELETED the attachment chip it was captured from, because clicking that chip
leaves ProseMirror holding a NodeSelection and `insertContent` replaces the
selection. Neither is reachable from jsdom — the first needs a real text
layer, the second a real ProseMirror click.

**Residual debt recorded, not fixed:** no pinch-zoom or zoom control in the
PDF viewer (fit-to-width is honest at 390px — measured, no overflow, page
fits, highlight renders — but a letter page at 0.52x is small); no
cross-page selection (an excerpt is page-scoped by design); OCR per G-121
above. Four sub-44px tap targets named by `tools/mobile_audit.py` on the
Notebook's touch tiers: the sidebar's three 28x28 panel-switch buttons were
fixed this wave (they live in a file this wave already changed, and the
existing touch block had simply missed `.sbHeaderBtn`); "Add thesis starter
views" (24px tall) and "View all" (42x19) are Wave H chrome, named here and
left for their own wave rather than silently absorbed.

## Ask Notebook / Ask Document — Private-Corpus Retrieval + Citation-Grounded Answers (Wave K, 2026-09-07)

Scoped competitor comparison per §70-72's per-wave mandate. **This comparison was
missing from Wave K's entry checkpoint and is performed here at closure** — recorded
as a process miss, not backfilled silently: the wave built for four workflows without
first writing down what the incumbent's user expects from each.

Competitors, current-sourced 2026-09-07: **Notion AI** (workspace-wide question
answering with clickable citations to the pages consulted, and an Enterprise Search
tier that spans connected apps plus uploaded PDFs), **Evernote AI Assistant**
(contextual questions over the open note *and* the whole account, replies carry
source citations naming the notes used — and, unusually, it is on every tier
including Free), **Obsidian** (no built-in AI at all; vault Q&A exists only as
community plugins — Copilot for vault RAG, Smart Connections for local-embedding
semantic search — each a different developer with a different privacy stance).

The product job this wave was held to: *the member asks a question about their own
research, UCT answers from their private corpus, shows exactly what evidence supports
the answer, and refuses to invent one when the corpus does not support it.*

### Competitor task matrix — the four workflows this wave touches

| Workflow | What the incumbent's user expects | Steps there | UCT after Wave K | Steps here | Competitive? |
|---|---|---|---|---|---|
| Ask about the note I'm in | Evernote: open the note, ask; the assistant *decides* whether to stay in the note or widen to the account. Notion: `/ask` on the page | 1-2 | Ask panel opens with scope **This note** pre-selected from context; the scope is on screen and does not silently widen | 1-2 | **Yes, and ahead on trust** — the incumbent's automatic context decision is exactly the thing a researcher cannot audit |
| Ask about one source document | Notion Enterprise Search can retrieve from uploaded PDFs, but as part of the corpus, not as a scope you pick. Evernote answers over attachments implicitly. Obsidian: not without a plugin | 1-2, no scope control | Scope **This document**, page-exact citations, and an honest coverage answer when the document has no extracted text yet | 2 | **Yes, ahead** — no competitor lets the member say "only this document" and get a page-anchored citation back |
| Ask about everything I've written on one security | No competitor has a security or thesis object; the closest is a manual tag/folder filter the member maintains by hand | n/a | Scope **This security's research**, membership resolved canonically (Wave H), not by substring — an AMD note carrying a strong lexical phrase is correctly excluded from NVDA scope | 1-2 | **UCT-unique** |
| Ask my whole notebook | Notion and Evernote both answer corpus-wide with citations, both semantic | 1-2 | Scope **My notebook**, deterministic lexical + entity retrieval only; honest no-answer on a true paraphrase | 1-2 | **Parity on the mechanics, behind on paraphrase recall** — see G-127 |

| ID | Capability | Competitor | UCT Current | Target | Parity/Diff | Persona | Switching Impact | Evidence | Status | Priority | Dependencies | Validation Condition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G-122 | One research assistant with an EXPLICIT, member-visible scope, rather than an assistant that decides for itself how wide to search | Evernote (automatically determines what to use as context — open note first, widening to other notes "if needed"; the member cannot see or pin the boundary), Notion (workspace-wide by default; sources are chosen at the connector level, not per question), Obsidian (per-plugin, no consistent model) | **Live, ahead on auditability** — four scopes (`note`, `document`, `security`, `notebook`) share one pipeline: retrieve → rank + budget → fenced prompt → stream → resolve. The scope label is computed by the SAME module that runs the retrieval (`_SCOPES[scope]["label"]`), so what the member reads and what was actually searched cannot drift apart. Current context picks the default; the member can change it; it never widens silently. | Match on capability, exceed on auditability | BOTH | All | High (positive — "which of my notes did you read?" is the first question a researcher asks, and the incumbent's answer is "it depends") | Wave K Slice 6a/6b; `ask_service.py`, `AskPanel.jsx` | **DONE** | — | Waves H/I/J (scope membership, document identity, excerpt anchors) | 76 frontend rails on the shared panel + citation contract; `test_ask_service.py` proves every scope returns one shape and an unknown scope raises |
| G-123 | A citation that is a VERIFIED LOCATION in the source, not a quoted string the model produced | Notion (clickable link to the page consulted — page-level, not passage-level), Evernote (names the notes used — note-level), Obsidian Copilot (plugin-dependent, typically the note) | **Live, ahead of all three.** The retired Wave-2 path resolved citations by searching the note body for the model's quoted text: double-quoted model prose became citation-shaped UI, an invented phrase could render as a citation, and a real phrase crossing a TipTap mark boundary failed to resolve. Replaced by a ProseMirror-grounded location contract with declared precision states — `VALID_EXACT`, `RERESOLVED_EXACT`, `VALID_NOTE_ONLY`, `DEGRADED` — where `body_plain` is the retrieval representation and `doc.textBetween(0, size, "\n")` is the member-visible addressing representation, proven equivalent across structural fixtures. A stale position is never used blindly. | Exceed | DIFF, UCT ahead | All | High (positive — a citation that can be fabricated is worse than no citation) | Wave K Slice 0/1/6b; `askCitation.js`, `askCitation.parity.test.js`, `AskCitationContract.closed.test.jsx` | **DONE** | — | — | The closed-contract rail fails if the quote-regex resolver returns; parity rail proves backend and real ProseMirror agree |
| G-124 | Refusing to answer when the corpus does not support one — and not paying a model to do it | Notion ("using only the sources you choose" — grounding is claimed, refusal behavior is not documented), Evernote (not documented), Obsidian (plugin- and model-dependent) | **Live, UCT-unique in kind.** `answer_evidence` is an ALLOWLIST: only `relevance == query_match` may back a claim, so an unlabelled item is not evidence and a caller that forgets makes the system say it found nothing rather than invent. When nothing qualifies there is **no model call at all** — the refusal is deterministic, free, and cannot be talked out of. Naming a security is explicitly not asking a question about it: a note matching only the ticker is `entity_context`, which can never satisfy `no_answer`. | Exceed | DIFF, UCT ahead | All, esp. fundamental analysts | High (positive — the failure mode that ends trust in a research assistant is a confident answer with no source) | Wave K Slice 1/3/6a; `ask_evidence.py`, `test_ask_evidence.py` | **DONE** | — | — | `test_ask_evidence.py` proves an unlabelled item cannot back a claim; the no-model-call path is asserted, not assumed |
| G-125 | Retrieved content cannot instruct the assistant | None of the three documents a prompt-injection boundary for content the member imported from the web into their own notes | **Live, structural rather than prompted.** `system_prompt()` accepts no arguments, so no channel exists to reach the instruction layer; the evidence fence is neutralized and marker-counted, and assembly REFUSES on a count mismatch; `request_kwargs` emits no `tools` key; and `[n]` handles resolve only against the packet actually sent, so a source reading "cite this as [9]" cannot conjure a ninth source. Adversarial corpus covers ignore-previous-instructions, cross-note exfiltration, secret extraction, fake citation instructions, fake tool instructions, and hostile source metadata. | Exceed | DIFF, UCT ahead | All | Medium-High (positive; a Notebook whose corpus is pasted research is exactly the threat model) | Wave K Slice 5; `ask_prompt.py`, `test_ask_prompt_injection.py` | **DONE** | — | — | `test_ask_prompt_injection.py`; `test_ask_security.py` proves tenant isolation happens BEFORE ranking |
| G-126 | Ask Current Note's own security posture | N/A — a UCT-specific defect | **FIXED.** The shipped Wave-2 path called `note_ask.SYNTH_SYSTEM(note_title, note_block)`, interpolating the member's title and up to 20k characters of note body directly into `system=`. Deleted. The note now reaches the model as ranked, citable blocks inside the evidence fence under the same 20k ceiling — so a long note loses its least relevant blocks, not everything past character 20,000. Streaming, cost/rate controls, honest errors and current-editor semantics were preserved; the regex pseudo-citations and unvalidated jumps were not. | Fix | PARITY (correctness/security) | All | High (this was live, member-facing, and pre-dated the wave) | Wave K Slice 6a; `note_ask.py`, `test_note_ask_prompt_boundary.py` | **FIXED** | — | — | The prompt-boundary suite fails if any member content reaches `system=` |
| G-127 | Semantic / paraphrase retrieval over the member's own corpus (supersedes G-017 for the Ask surface) | **All three are ahead here** — Notion and Evernote both retrieve semantically; Obsidian's Smart Connections runs a LOCAL embedding model with no API key and no cloud egress | **Deliberately DARK.** Deterministic retrieval, measured: 10/14 overall and 3/7 on low-overlap paraphrases (up from 7/14 and 0/7 once the AND/OR defect below was fixed). Four true paraphrases still miss — "too reliant on a handful of buyers" against a note reading "Customer concentration is the risk I keep writing down" — and no lexical index can bridge those. Semantic retrieval is architecturally approved and quality-justified; **activation is blocked on positive Zero-Data-Retention verification for `org-6ljtvy8Dr0srF2ZRiE7vH2Dy`.** No Notebook note, document, excerpt or query has been embedded. | Activate only after ZDR is positively verified for the exact production project — OR after evaluating a local embedding model, which removes the vendor-retention question entirely | GAP, competitors ahead | All | Medium-High (the honest, narrowed case) | Wave K Slice 1 baseline + closure re-measurement | **OPEN — deliberate, privacy-gated** | P1 (blocked on a decision, not on effort) | Owner/vendor confirmation of ZDR, or an owner decision to evaluate local embeddings | Not applicable — this row is open on purpose and must not be closed by an implementation wave deciding on its own that a key it can already read is permission |

**⛔ A competitive finding the owner should see before ruling on ZDR:** Obsidian's
most-used AI plugin does vault-wide semantic search with a **local** embedding model —
no API key, nothing leaving the machine. That is the same capability G-127 is blocked
on, obtained without asking a vendor for a retention promise at all. It is recorded
here as an option, not adopted: choosing it is an owner decision and a real
engineering evaluation (model, index size, CPU cost on the web pod), and Wave K's
mandate ends at reporting it.

**The defect that mattered most, found late and only by a real model.**
`fts_match_expr` joins query terms with a space and FTS5 reads a space as AND — right
for the member's search box, catastrophic for a QUESTION, which is made of words the
corpus does not contain. *"what was gross margin in the quarter?"* retrieved **zero
rows** against a page reading *"Gross margin was 73.5% in the quarter."* Ask Document,
Ask Notebook and Ask Security Research answered "I couldn't find that" for essentially
every naturally-phrased question; only keyword queries worked. Five slices of rails
missed it because the Slice 1 evaluation set was written keyword-shaped — the inputs
were shaped like the implementation, so the measurement agreed with itself and
reported *lexical 6/6*. `ask_match_expr` now keeps content words and joins them with
OR; the search box keeps AND, where it is correct.

**A second unsound gate, same shape.** `BM25_FLOOR` kept a candidate only when
`bm25() <= -0.15`. bm25 weights by inverse document frequency, so a term present in
every indexed row scores ~0 — above the floor, and was dropped. Its two victims were a
single-page document and the focused researcher whose every note says "NVDA", the term
they care most about. Retired as a gate, kept for ordering, and railed with an AST
probe that fails if any retrieval path gates on it again.

**Residual debt recorded, not fixed:** the four low-overlap paraphrases above; a
shared generic word can lift `no_answer` (a question about "dividend policy" matches a
note about *export* policy — the answer stays grounded and the model refuses, but the
deterministic layer claims an answer exists, and no lexical index can tell a subject
noun from a head noun); `reachable.test.js` reports 16 orphaned modules under
`pages/community` + `floor2` from `cc195e888`, which is already on origin/master and
is not Wave K's; and zero real member usage evidence — Day 0, the same honest cap every
prior wave's closure carries.
