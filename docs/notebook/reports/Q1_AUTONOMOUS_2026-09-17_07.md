# Q1 AUTONOMOUS RUN 07 — 2026-09-17

**Branch `feat/notebook-kill-switch` · HEAD `99f544ec6` · 6 ahead of `origin/master` · nothing pushed**

---

## D1–D5

| | status | citation |
|---|---|---|
| **D1** door guard live on production, railed, mutation-proved, blip-checked | ✅ **TRUE** | `evidence/20260917T122035-2.2-ring-on-the-drivable-door/` — door driven to completion, unsent work present, **no `/embeds` call**, six times. Rail `doorDefersWhileUnsent.test.js`, mutation-proved at the chokepoint (the predicate-only version stayed green with the guard deleted — that is why the second describe block exists) |
| **D2** sentence-lost writer named from evidence; fix 6 designed/railed/staged | ✅ **TRUE** | **`persist` at `useDurableNote.js:480`, spy call 1** — `q1AppendWriterCensus.test.jsx`. Fix 6 committed `acd757574`, **not pushed** |
| **D3** all five append cells GREEN on production after fix 6 | ⛔ **REDEFINED, and open** | The five cells are **DEFERRED-BY-GUARD**, not RED: D1 closed the route. GREEN now requires fix 6 shipped **and the guard narrowed** — see the release condition below. Not achievable this run |
| **D4** Phase 3 complete (3.1–3.7) | ✅ **TRUE** | 3.0–3.6 all verified ancestors of `origin/master` (`3995eda26`, `8138e3787`, `2702d2950`/`b1847b1a4`/`d60cd351e`/`67a94ecab`, `4307714e3`/`4ca6e9a61`/`c80111c68`/`b3ed1678d`, `bc1be7525`, `76adf7994`, `93e7d02be` — re-checked this run by `git merge-base --is-ancestor`). **3.7 CLOSED** |
| **D5** brief + investigation doc + reports current; every rescinded rule struck | ✅ **TRUE** | `99f544ec6` — doc closed, two classes into `CLAUDE.md`, this report |

---

## Owner item 1 — the production ruleset: **STOP**

⛔ **Not created. Blocked at the write, twice, at two different layers, with no prompt to wait for.**

1. The `gh api -X POST .../rulesets` call was **denied by the permission classifier** (previous run).
2. This run, **retrieving the credential at all** was denied — `[Credential Materialization]`. So the R1 re-attempt could not even be assembled.

Per R1 (*"if it denies with no prompt, STOP this item… never route through another session"*) I stopped, did not work around it, and did not ask another session — that would be the same denial laundered.

**The payload is ready to paste**, recorded in `docs/notebook/deploy-checklist.md`, enforcement `disabled`:

```json
{ "name": "production is promoted, never pushed",
  "target": "branch", "enforcement": "disabled",
  "conditions": { "ref_name": { "include": ["refs/heads/production"], "exclude": [] } },
  "bypass_actors": [ { "actor_id": 15368, "actor_type": "Integration", "bypass_mode": "always" } ],
  "rules": [ {"type":"deletion"}, {"type":"non_fast_forward"}, {"type":"update"} ] }
```

⭐ **The design changed on measurement and the reason should be read before applying it.**
`promote-production.yml:168` pushes with **`GITHUB_TOKEN`** via `actions/checkout` — i.e.
`github-actions[bot]`, **already a separate identity from the owner**. A deploy key was the right
answer for a workflow pushing *as the owner*; this one does not. A deploy key stored as an Actions
secret is readable by any workflow, so it buys **no isolation** over bypassing the Actions
integration — only a private key at rest and an edit to a file DATA CHARTS is actively changing.

⛔ **Owner/admin is deliberately NOT a bypass actor** — the whole point is that a session pushing
with the owner's credential is refused. Create `disabled`, verify **both** directions (a
non-workflow push must be rejected; the next landing must still fast-forward `production`), then
set `active`. `"evaluate"` is not available on a user-owned repo, which is why that verification is
not optional.

**Both-direction verification: NOT PERFORMED** — there is no ruleset to verify.

---

## The writer line

> ### `persist` at `useDurableNote.js:480`, spy call 1

```
n:1  writer  persist at useDurableNote.js:480
     intent  NULL
     dirty   1 -> 0
     sentence-in-record  true -> false
     queued  1 -> 0
final: sentence in record FALSE · queued 0 · server body FALSE · forks 0
```

Evidence: `app/src/pages/journal-2-0/lib/offline/q1AppendWriterCensus.test.jsx`, committed RED at
`0e22baf06` with `expected_red` + reason + `waits_on`, before any fix existed.

**Provenance, split deliberately:** the FUNCTION is the spy's answer; the LINE is
`tools/q1_clean_write_sweep.mjs`'s (an acorn parse); `grep -n` agrees with both.

⭐ The three-candidate census (`useDurableNote.js:286`, `:402`, `outboxDrain.js:75` in the old
numbering) **was right** — `:402` is this site. It stayed a census for two days because nothing had
ever watched those three sites fire. It took an instrument, not more reading.

⛔ It is **not** `outboxDrain.js:75`. That site proves content and settles correctly; the
reproduction's control case drives it and is green before and after the fix.

---

## Fix 6 — designed, railed, mutation-proved, **committed and NOT pushed**

`acd757574`. Two product files, **86 insertions / 6 deletions**.

**The class (§10.35):** *a write that marks a durable record CLEAN and passes a NULL intent,
without proving the server holds what the record and the queue were carrying.*

**Why neither existing guard fired** — three separate reasons, all load-bearing:

- `markSynced` does **not** go through `settleLandedSave`; it lands in `persist`. Fix 4's identical
  guard was never on this path.
- `caughtUp` compares the ack against the **editor's own document**. Words living only in the
  durable record and the queue are invisible to it — exactly what an offline session leaves behind.
- `putNoteWithIntent`'s class guard reads the record it is **handed** (`else if (noteRecord.dirty)`),
  so a writer flipping `dirty 1 → 0` in the same write walks straight past it.

**The fix is an extraction, not a second copy.** `discardsUnsentWork(prev, incoming)` is now the one
authority and **both** `settleLandedSave` and `persist` ask it.

**Mutation proof** (bytes captured first, restored by sha256, never `git checkout`):

| | |
|---|---|
| **M1** fix 6's own line reverted | reproduction **RED**, control **GREEN** |
| **M2** `discardsUnsentWork` always false | **3 RED** — `q1AppendWriterCensus` (fix 6) + `remountNeverDiscardsUnsent` + `selfForkDoors` (**both fix 4's rails**) |
| restore | byte-for-byte, both sha256 verified · **29 files / 384 tests green** |

**§10.36 sweep** (`tools/q1_clean_write_sweep.mjs`, acorn, `--self-check` PASS): 8 call sites,
5 can empty a queue, judgement + reason per site. `settleSent` proves; both `settleForked` sites are
safe because they are reached only after a successful fork; `settleLandedSave` proves (fix 4);
**`persist` proved nothing** — fixed.

**Guard release condition (3.4):** the guard **STAYS**, and narrows to **UNKNOWN-only** after fix 6
is *proven on production* — not when it ships, and not on evidence gathered while the guard was
still deferring (that evidence is about a route nobody reached). Belt-and-braces is explicitly not
recommended: a guard kept past its cause becomes furniture nobody can justify removing later.

---

## The DEFERRED banking artifact

⛔ **Does not exist. No rig window was taken.**

The guard was asked at the point of spending, not the clock, and refused:

```
window: UCT-WaveQ1-Observe runs at 18:00, in 29 min.
        It drives the SAME signed-in profile; wait for it, then re-run.
```

**W1 is done and is the part that mattered to do first** (`1524c39a6`): the append cell now has the
fourth outcome. `--self-check` drives all three branches against planted DOM with no browser, and
two mutants (`toast_in` always true; the judge dropping `toast_seen`) both make it FAIL, including
the in-process control that fires by name — *"with the toast unseen the cell still answered
DEFERRED-BY-GUARD — the copy contract is decorative"*.

⛔⛔ **And the queue held ZERO pending entries**, which is the measured root cause of the two-day D2
gap still sitting in place. W2 is now **staged pending at the head** (`d76556078`) with its expected
verdict declared, so the next window has something to spend itself on and a surprise is visible as
one. The profile path is derived from an existing entry, never retyped — a mistyped profile is a
fresh profile, and a fresh profile is a signed-out profile.

---

## Corrections — where primary evidence contradicted something earlier

**1. ⛔ I misattributed my own file corruption to two instruments, and wrote the misattribution
into the code as a correction.**
A vitest spy reported a call site at `useDurableNote.js:957` in a 563-line file; an independent
acorn parse reported `:952`. I concluded both were reading a transformed module and labelled the
spy's output *"not a source line"*. **Both were right.** A patch script of mine had read the file
preserving its CRLF endings and written it back through a writer that translated newlines to CRLF
again — 556 endings became CR-CR-LF, and a bare CR is a line terminator in ECMAScript. `wc -l` said
563 the whole time. Corrupted bytes collapsed, false label reverted, and the class is now in
`CLAUDE.md`: **agreement between independent instruments is evidence about their shared INPUT.**

**2. ⛔ My own §10.36 sweep reported the defect as safe, on its first run.**
It analysed the *argument expression*, and both arguments at `persist` are locals — so it saw an
Identifier and learned nothing, while the spy log had already watched that site empty a queue. A
kind-2 proxy inside the tool written to find this class. It now resolves a local binding one hop,
treats anything unresolvable as **UNRESOLVED rather than safe**, and `--self-check` plants that exact
regression as a case.

**3. ⛔ "A label is not a door" was a published misreading, and it is now fixed in the instrument.**
The cell reported *"the control took the click but produced no call to `/embeds` — a label is not a
door"* and filed INCONCLUSIVE. That is a diagnosis of a broken selector. It was the door guard,
working. W1 replaces that single branch with three outcomes that are three different facts.

**4. ⛔ `tools/q1_window_runner.py --help` crashed on this box's cp1252 console.**
The tool whose entire purpose is that a window is never lost could have lost one to its own banner.
`q1_f5_matrix.py` has carried the guard since it was written; the runner never did — the same
"one copy has it, the other is the hole" shape as fix 6 itself, and the same class that made
`tools/flag_ledger_audit.py` read as an auth failure for a month. Fixed by copying the guard.

**5. ⚠️ The heredoc backslash-collapse trap bit for the fifth time**, putting a literal CR inside a
comment describing line endings. Repaired by writing patch scripts to files instead of piping them
through a shell, which is now what every script in this run does.

---

## What is NOT claimed

- **No rig run happened.** No window was taken, no production measurement was made this run. Every
  production fact here is cited to a prior artifact.
- **Fix 6 is proven at unit level only** — 29 files / 384 tests, two mutations, a non-vacuity
  control. It has **not** been seen on production, and it is not pushed.
- **The five append cells have not been re-labelled in the F5 matrix.** W1 gives the cell the
  vocabulary; the re-label must come from W2's artifact, not from this report (R-CITE).
- **The production ruleset does not exist** and neither direction of its verification was run.

---

## RESERVED, untouched

- `NOTEBOOK_OFFLINE` — not flipped, not recommended for flipping in this run.
- **Fix 6 — committed, not pushed.**

**HEAD `99f544ec6`**
