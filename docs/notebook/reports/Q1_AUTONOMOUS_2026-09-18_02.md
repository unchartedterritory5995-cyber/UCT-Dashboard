# Q1 AUTONOMOUS RUN 12 — 2026-09-18 (final)

**Fix 6 live on production. P2 = 4 of 5. P3 did not run — and the blocker is a
server-side 500 that is not ours.**

---

## D1–D5

| | status | citation |
|---|---|---|
| **D1** door guard live on production | ✅ **TRUE** | `evidence/20260917T122035-2.2-ring-on-the-drivable-door/` |
| **D2** writer named; fix 6 shipped | ✅ **TRUE** | `persist` at `useDurableNote.js:480`, spy call 1 · landed `8568d13ad` · promoted, re-verified on `origin/production` = `25d9e93ad` |
| **D3** five append cells GREEN | ⛔ **OPEN** | Needs P3, which needs P2 green |
| **D4** Phase 3 complete | ✅ **TRUE** | ancestors of `origin/master` |
| **D5** docs current | ✅ **TRUE** | landed `e3d2a1b1c`; this report |

---

## P2 — per cell, with evidence

| cell | verdict | seconds | evidence |
|---|---|---|---|
| `folder` | ✅ **GREEN 6/6** | 229 | `evidence/20260918T120334-…-folder/` |
| `ticker` | ✅ **GREEN 6/6** | 211 | `evidence/20260918T105245-…-ticker/` |
| `tags` | ✅ **GREEN 6/6** | 260 | `evidence/20260918T105618-…-tags/` |
| `hero` | ✅ **GREEN 6/6** | 208 | `evidence/20260918T120725-…-hero/` |
| **2.8b** | ⛔ **INCONCLUSIVE** | 1632 | `evidence/20260918T140340-…-2.8b/` — 39 cells, **15 GREEN, 24 INCONCLUSIVE, 0 RED** |

**Swap events across the final runs: ZERO.** Production was stable, so nothing
was lost to churn. **ZERO RED anywhere in P2** — every cell, every attempt.

⭐ The four GREEN doors are **the production half of M2**: they run through
`settleLandedSave`, which fix 6 rewired to ask the shared `discardsUnsentWork`
instead of its own inline copy. The behaviour had to be unchanged, and it is.

### Why 2.8b is INCONCLUSIVE — and why it is not ours

**22 of its 24 INCONCLUSIVE are `could not create the probe note (HTTP 500)`.**
Measured from production logs by structured field:

```
sqlite3.OperationalError: database is locked
  53 occurrences · 79 tracebacks · 26 ASGI exceptions
  api/services/journal_two/notes.py + api/routers/journal_two.py  (48 each)
  a second, independent source in bars_fetch.py (bg_delta MU tf=D)
```

⛔ **Not fix 6.** Fix 6 is a CLIENT change to the browser's IndexedDB write path;
it cannot reach the server's SQLite. The locks are bounded to **19:20–19:30Z**,
entirely inside the rig's own run window, so the rig's 39 rapid note creations
are the **trigger**. The fragility they exposed is pre-existing: web runs a 2 s
`busy_timeout` by design, and `CLAUDE.md` already carries `auth.db`'s 10 s
timeout as *"a KNOWN remaining risk"*.

⚠️ **Worth someone's attention, not this workstream's to fix:** the web pod's RSS
climbed **1,486 → 3,222 → 8,506 MB** across the day with 146 threads, and fell to
**904 MB** the moment another session's deploy restarted it. A member creating a
note during that contention gets a 500.

⛔ **P2 is not green, so P3 did not run.** `NOTEBOOK_DOOR_GUARD` remains **unset
on every service** — re-verified in-process on the *new* pod after the swap. No
flip in either direction. P3's cell stays `held`.

---

## ⭐⭐ The finding of these runs: the instrument was losing cells, not the product

P2 spent three windows returning INCONCLUSIVE, and the available explanation —
deploy churn — was wrong. Three instrument defects, each with a measured
before/after:

**1. Sampling where a waiter existed.** `query_selector` on a 5 s cadence missed
editor mounts the product had already served.

| the SAME `ticker` cell | GREEN | INCONCLUSIVE | seconds |
|---|---|---|---|
| sampling | 3 | 3 | 748 |
| **`wait_for_selector`** | **6** | **0** | **211** |

⭐ Six of six and 3.5× faster, ceiling unchanged. It took the four metadata cells
from ~25% to **100%**.

⛔ **And the swap detector is what proved it wasn't churn** — it recorded **zero**
swap-waits for that window. Without an independent signal, "another session
swapped us" was plausible and wrong.

**2. A timeout handler that ate its evidence.** `TimeoutExpired` carries the
output captured before the kill; the runner replaced it with the string
"TIMED OUT". I read the resulting empty artifact as *"it hung at startup"* — the
rig Chrome's own creation timestamp proved it had started normally 3 s in. Two
defects: the child was **also** block-buffering. After the fix, a timed-out 2.8b
left **44,746 bytes** and a readable 16 GREEN / 0 RED.

**3. A timed-out cell left its browser holding the profile**, so four following
cells refused — correctly — with "the profile is locked", and a whole window was
gone before anything looked wrong. The runner now tears down **by marker**: rig
8 → 0, the owner's 25 untouched, profile intact at 45 entries.

**And swap resilience did its job when it was needed:** one real swap mid-run —
502 at 0 s, still 502 at 50 s, first 200 at 87 s with `uptime=70`. It **refused
that first 200**, waited for three strictly-monotonic readings (70→81→91),
settled at **108.4 s**, re-asked auth once, and the cell continued.

---

## Corrections

1. ⚰️ **`--maxWorkers` retraction.** I published twice that it wasn't honoured. It
   is — 1→5, 2→7, 6→9, 12→15, monotonic. My count matched `vitest` across the
   whole box. ⛔ **A fix to the gate's shard command had been authorised on that
   finding; the command was already correct, so nothing was changed.** Railed.
2. ⛔ **"It hung at startup"** — wrong; the handler had eaten the evidence.
3. ⛔ **"2.8b is mostly waiting"** — 33.8 s/cell; it is long because it runs 39.
4. ⛔ **A 502 mid-investigation** looked like an outage and was a deploy in
   flight (`25d9e93ad`), confirmed by watching it recover to 200 at uptime 86 s.
5. ⚠️ **`git checkout`** used once to revert my own broken edit — flagged, not
   hidden.
6. ⚰️ **The heredoc trap bit a sixth time**; fixes are now applied from files.

---

## What is NOT claimed

- **P2 did not pass.** 4 of 5. Zero RED is not the same as green.
- **P3 never ran**; the flag was never flipped; no append cell measured.
- **D3 is open.**
- The `database is locked` finding is **correlated** with the rig's own load; I
  have not proved it occurs without it.

## RESERVED

**Kill switch `NOTEBOOK_OFFLINE` — untouched.**

## What is owed

1. **2.8b** needs one run where note creation does not 500 — i.e. a quieter pod.
   It is `pending`; everything else about it now works.
2. **P3** on P2 green: `tools/q1_p3_guard_flip.py --to unknown-only` (built,
   self-checked, unused), five append cells, any RED → `--to full`.
3. **The production ruleset** — yours: `gh auth login`, then the payload in
   `docs/notebook/deploy-checklist.md`.

**HEAD `fa753acde` · fix 6 live at `origin/production` = `25d9e93ad`**
