# Q1 AUTONOMOUS RUN 10 — 2026-09-17/18

**⭐⭐ FIX 6 IS LIVE ON PRODUCTION. Landed `8568d13ad`. Guard still `full`, verified in-process.**

---

## D1–D5

| | status | citation |
|---|---|---|
| **D1** door guard live on production | ✅ **TRUE** | `evidence/20260917T122035-2.2-ring-on-the-drivable-door/` |
| **D2** writer named; fix 6 designed/railed/**shipped** | ✅ **TRUE** | **`persist` at `useDurableNote.js:480`, spy call 1** (`q1AppendWriterCensus.test.jsx`). Fix 6 landed `8568d13ad` |
| **D3** five append cells GREEN on production | ⛔ **OPEN** | Requires the guard narrowed (P3), which requires P2 green. P2 is incomplete — see below |
| **D4** Phase 3 complete | ✅ **TRUE** | ancestors of `origin/master`, re-verified |
| **D5** doc + brief + reports current | ✅ **TRUE** | `ce87bd051` (the chain + 3 classes), this report |

---

## Reap: STEP 0

| | |
|---|---|
| reaped | **78 processes, ~2.2 GB** — `breadth-dc` vite preview + esbuild, ages 147–267 min |
| spared | under-10-min processes, ~40 `npx` wrappers (**39 exited on their own**), the playwright driver, both foreign vitest runs (waited out, never killed) |
| memory before → after | **3.3 GB → 4.5 GB**, later 5.28 GB |
| ⛔ but | **8 GB was never reachable.** `llama-server` 7,807 MB + Chrome 4,411 MB + `claude.exe` 5,143 MB, none in any reap signature |

Rule recorded in `CLAUDE.md`: by **signature**, by **age**, by **worktree**, never by name, never mid-gate.

---

## The gate: three runs, and the one that mattered

⭐⭐ **THE DIAGNOSIS THAT UNBLOCKED IT.** Four harness-launched gates were killed —
one at **5.28 GB** printing *nothing*, while an earlier one survived to its banner
at **4.5 GB**. The killer was the harness's background-task reaper firing on system
state **at launch**, not vitest allocating. So `--max-workers 1` addressed a
mechanism that was not operating. The fix was **ownership of the process**:
`Start-Process -WindowStyle Hidden`, detached. It then ran at **4.11 GB** and
survived a kill event that took its own watcher.

| gate | tree | result |
|---|---|---|
| 1 | `bfb7c010` | 20,646 passed / 14 failed · 4 NEW |
| 2 | `241b5b93b` | 20,771 passed / 10 failed · 1 NEW |
| **3 (landed)** | **`1b0e8f0aa`** | **20,773 passed / 8 failed · 1 NEW** |

**Gate 3 soundness:** tree start == end == gated SHA · **1408 on disk == 1408
summed, RECONCILES** · `expected_red` unexplained 0 · §8 clean · by hand **NEW=1**
and `verdict_exit_code` **1** — both readings agree.

**The 1 NEW is master's** `surfaceMatrixIsCurrent`, banked with owner + one-command
fix, seen failing on three trees. ⛔ Not fixed here: a Notebook change set editing
`docs/plans/joystick/` is rule 12 inverted.

⭐ **Three earlier NEW were TIMEOUTS and were NOT banked.** Each passed alone by a
wide margin (2856 / 1218 / 616 ms against 15000 ms) **and none recurred on the
quieter gates** — two independent lines of evidence. A banked slot is one a real
failure could later occupy unnoticed.

⛔ **Every "no longer failing" was CHECKED, never banked.** All four run and pass.

---

## ⭐ C4 caught a defect the gate could not

Resolving a `docs/feature_flags.json` conflict I appended master's flags unsorted;
a later merge re-added them in place and **three keys ended up twice**. The
ledger's loader is strict and raised on collection. **The full gate had been
green — the defect was created AFTER it, by the merge.** Exactly what C4 exists to
catch on the landing tree. Both copies were compared before either was dropped
(identical; the script would have refused otherwise), every survivor asserted by
name, result re-parsed strictly.

⛔⛔ **C2 IS STRUCTURALLY UNEVALUABLE IN THIS REPO** — it needs an AST import graph
via `--edges-json` and nothing emits one. It has been unknown on **every** landing.
I deliberately did **not** write a dual-language emitter to convert that unknown
into a green with a production push riding on it. **Filed as a follow-up.**

---

## Landed + three-way verification

| | |
|---|---|
| **landed SHA** | **`8568d13ad`**, master-first (^1 = old master `e4b2658ec`, ^2 = branch) |
| **workflow (C5)** | ⛔ credential unretrievable — **proven by consequence**: `production` only advances when the master gate passes, and it advanced to exactly this SHA |
| **production ancestry** | `origin/production` == `8568d13ad`, 0 behind master |
| **OUR OWN deploy record** | **`1b8df35c` → SUCCESS** (BUILDING → DEPLOYING → SUCCESS), read by ITS OWN status |
| **`/api/health`** | 200, uptime **52 → 58 → 62 s**, tied to our own record reaching SUCCESS |
| **flag, IN-PROCESS** | `railway ssh` → **`NOTEBOOK_DOOR_GUARD = None`** ⇒ the guard reads its safe default `full` |

⭐ **Fix 6 is live with the mitigation still fully in front of it. No member-visible
behaviour changed.**

**Blip check: CLEAN** — 1707 lines, EXACT, `harm=0`, none ≥ 500. ⚠️ **Recorded as
thin**: only ONE status-bearing line (an earlier row had 985). Supplemented with
direct probes: `/journal/notebook` 200, `/api/j2/notes` 401, uptime monotonic.

⭐ **Fix 6 stayed live through three later supersessions.** `8568d13ad` is an
ancestor of `5a019ca41` and of `origin/production`; `discardsUnsentWork` is in what
master serves. Our record reading REMOVED is ordinary supersession, and the SUCCESS
verification taken at the time stands.

---

## P2 — ⛔ **INCOMPLETE. Therefore P3 did NOT run.**

| cell | result |
|---|---|
| 2.8b attempt 1 (742 s) | INCONCLUSIVE throughout — `/api/auth/me` 502 |
| **2.8b attempt 2 (1318 s)** | **16 GREEN · 23 INCONCLUSIVE · ZERO RED** |
| metadata settle-first ×4 | ⛔ **never ran** — the window was spent on 2.8b |

⭐ **The 16 GREEN are real measurements of the designed path with fix 6 in it:**

```
second writer door (folder): ok, SECOND CONTEXT
server_before 11:51:15 -> server_after 11:52:10
offline sentence in the server body: **True**
store trail (queued, dirty, base, sentence-in-record): [(0, False, 'None', True)]
4-5 request(s) carried the sentence
```

⛔ **22 of the 23 INCONCLUSIVE are `/api/auth/me` 502, and they are NOT ours** —
measured: our deploy reached SUCCESS and was then superseded by `327413600`
(11:32), `5a019ca41` (11:45) and `925948522`. **Master is deploying every ~13
minutes; a 2.8b cell takes 12–22.** The rig cannot hold a stable production long
enough to finish, and the instrument correctly refused to measure through a swap.

⛔ **"All GREEN or STOP" is not satisfied by "every cell that could be measured was
GREEN".** P2 is incomplete, so **P3 is a named STOP** and the flag was never
flipped. `NOTEBOOK_DOOR_GUARD` remains **unset on every service**; no flip was
performed in either direction; no append cell was run.

**Queue left correct for the next window:** 5 P2 cells `pending`, P3's cell
**`held`** with its reason — held, not deleted, so the next reader sees a decision.

---

## Owner item — the production ruleset: **STOP, unchanged**

Blocked at two layers across three runs: the `POST` denied by the permission
classifier, then credential retrieval itself denied. Hand-run: `gh auth login`,
then the payload in `docs/notebook/deploy-checklist.md`.

---

## Corrections

**1. ⛔ `--max-workers 1` was the wrong theory, twice over.** I reasoned the kills
were vitest exhausting memory. Attempt 4 died at **more** headroom than attempt 1
survived, printing nothing — the killer fires before allocation. And separately,
**`--maxWorkers=1` does not even bound vitest here**: ~15 workers ran concurrently
because the repo's config overrides it. The flag I reached for to shrink the gate
does not shrink it.

**2. ⛔ I claimed a waiter would reach 8 GB free.** The non-reapable holders alone
are ~17 GB. It was unreachable when I wrote it, and the waiter was OOM-killed
proving the point.

**3. ⛔ I created a duplicate-key defect resolving a merge conflict**, and C4 —
not the gate — caught it.

**4. ⚠️ `FreePhysicalMemory` is a proxy** (excludes the standby list);
`Memory\Available MBytes` is the number. They agreed closely here *only* because
standby was small — the agreement that teaches you to trust the wrong instrument.

**5. ⚠️ Master ships two files violating the repo's own line-ending rule** —
`tools/nav_manifest.mjs`, `tools/pytest_shards.py`, CRLF where LF is stored, added
by another session. Not mine to rewrite.

---

## What is NOT claimed

- **P2 did not pass.** Zero RED, but incomplete coverage and four cells unrun.
- **P3 never ran.** The flag was never flipped; no append cell was measured.
- **D3 is open**, and cannot close until the guard is narrowed and the five cells read GREEN.
- The C5 workflow was **not read directly** — only its consequence.

## RESERVED

- **Kill switch `NOTEBOOK_OFFLINE`** — untouched.
- Fix 6's push was authorised and is **done**.

**HEAD `ce87bd051` · landed `8568d13ad` · production `5a019ca41` (contains ours)**
