# Gate run — 2026-09-25T19:47:33

- tree: `a5668a8a829324c3c920fe5bc69cf3d5d390c7a4` (start) -> `a5668a8a829324c3c920fe5bc69cf3d5d390c7a4` (end)
- wrapper: `scripts/gate_shards.py` blob `1d52df764927587f9b307283989726acbeeb7d94`
- shards: 6

| shard | test files | tests |
|---|---|---|
| 1 | 9 failed / 311 | 11 failed / 3925 passed / 3967 |
| 2 | 7 failed / 311 | 10 failed / 3998 passed / 4013 |
| 3 | 5 failed / 311 | 12 failed / 5148 passed / 5164 |
| 4 | 5 failed / 311 | 7 failed / 4718 passed / 4725 |
| 5 | 7 failed / 310 | 8 failed / 3827 passed / 3835 |
| 6 | 2 failed / 310 | 7 failed / 5052 passed / 5059 |
| **Σ** | **35 failed / 1864** | **55 failed / 26668 passed / 26763** |

- test files on disk: **1864** — RECONCILES with the summed file total (1864).

## Failing set vs baseline (`73a4286d0dc4b3adcaea9f6f8ec9b80a1bff8735`, measured 2026-09-24 (ADOPTED from the six-shard run on master 73a4286d0; manifest docs/notebook/gate-runs/master-baseline-73a4286d0/2026-09-24T20-16-01.md; shard logs re-parsed with the fixed FAIL parser ac9987280/f5cae6ade))

- observed **57** failing tests, baseline has **126**
- **NEW failures (regressions): 3**
    - ⛔ src/components/dailyFirstPaintAcceptance.test.jsx > DAILY first-paint acceptance > NC-D camera moved after first paint
    - ⛔ src/widgets/registry.test.js > widget registry — journal embed bindings > reconstructable widgets and journal embed bindings cannot drift apart
    - ⛔ src/widgets/registry.test.js > widget registry — journal embed bindings > the binding set actually parses (non-vacuity control)
- no longer failing: 72 (fixed, or silently stopped running — check which)
    - src/components/chart/engine/__tests__/stockChartWiring.test.jsx > an engine-drawn indicator still appears in the crosshair legend > ⛔⛔ A HOVER REACHES THE RENDERER NOT AT ALL
    - src/components/screener/reachable.test.js > 🔴 every module under app/src is REACHABLE from an entry point > and nothing committed is connected to nothing
    - src/hooks/pollingSites.rail.test.js > polling sites — the opt-in helper gets a rail > no NEW bare polling site, and no allow-listed one that has since moved
    - src/hooks/usePreferences.additionsOnly.test.js > S5 CP2 — additions only > NO NEW opaque-key call site appears — the way around this rail is railed
    - src/pages/Screener.door.test.jsx > 🔴 Edit and Delete from the screener go through the ONE store door > Delete asks first; Confirm issues ONE DELETE to the store and the row leaves My scans
    - src/pages/Screener.door.test.jsx > 🔴 Edit and Delete from the screener go through the ONE store door > Edit → Save changes PUTs to the SAME id — an edit is a new VERSION, never a second scan
    - src/pages/Screener.door.test.jsx > 🔴 Edit and Delete from the screener go through the ONE store door > ⭐ a REFUSED delete reaches the member in the SERVER'S OWN WORDS, and the row stays
    - src/pages/Screener.door.test.jsx > 🔴 the authoring door on the route a member navigates to > Edit opens the SAME sheet on the row — its own source, in the box, on the Formula tab
    - src/pages/Screener.door.test.jsx > 🔴 the authoring door on the route a member navigates to > New scan → the REAL sheet opens on Conditions → a starter → Save → ONE POST through saveUserDefinition → the new scan's results
    - src/pages/Screener.door.test.jsx > 🔴 the authoring door on the route a member navigates to > ⭐ a row stored WITHOUT its source still opens on the Formula — the seed knew, and nothing else could have
    - src/pages/Screener.door.test.jsx > 🔴 the authoring door on the route a member navigates to > ⭐⭐ Evidence is reachable FROM THE PAGE, and it asks the server for the study
    - src/pages/Screener.scanmount.test.jsx > the scan surface is a real destination, not a rail-satisfying stub > a member with no saved formulas is told so — never shown a blank panel
    - src/pages/Screener.scanmount.test.jsx > the scan surface is a real destination, not a rail-satisfying stub > and a REFUSED read is reported, never rendered as "you have none"
    - src/pages/Screener.scanmount.test.jsx > the scan surface is a real destination, not a rail-satisfying stub > and the page never asks for /api/candidates at all
    - src/pages/Screener.scanmount.test.jsx > the scan surface is a real destination, not a rail-satisfying stub > the "Screens ▾" door is offered on the page App.jsx routes to
    - src/pages/Screener.scanmount.test.jsx > 🔴 the scan chip: the downgrade only the shell's scanJoins threading can produce > a scan filter applied via the manager renders scanJoins' downgrade, and clears through onReplace
    - src/pages/Screener.scanmount.test.jsx > 🔴 the scan receipt reaches a member from the route they navigate to > /screener mounts the scan surface, and CoverageLine renders all four counts
    - src/pages/Screener.scanmount.test.jsx > 🔴 the scan receipt reaches a member from the route they navigate to > and the hits are on screen, so the receipt is describing a real answer
    - src/pages/Screener.scanmount.test.jsx > 🔴 the scan receipt reaches a member from the route they navigate to > and the read it drives carries the DEFINITION the member picked, by hash
    - src/pages/Screener.scanmount.test.jsx > 🔴 the scan receipt reaches a member from the route they navigate to > the member sees the screen they saved, by the name THEY gave it
    - src/pages/Screener.scanmount.test.jsx > 🔴 withheld renders as BREADTH, never folded into "no matches" > a capped screen says what it did not look at, BESIDE the four counts
    - src/pages/Screener.scanmount.test.jsx > 🔴 withheld renders as BREADTH, never folded into "no matches" > a screen that answered NOTHING says it is a data gap, not a quiet market
    - src/pages/Screener.scanmount.test.jsx > 🔴 withheld renders as BREADTH, never folded into "no matches" > and an UNRUN session says so, rather than rendering as zero matches
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage > History lists only this symbol: the open entry + the AAPL closed trade
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage > Your Position shows the six figures from the open AAPL position only
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage > analyst bar reflects RH bucketing
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage > renders header, chart, and every RH section for the symbol
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage — Attention (Attention Signal Propagation V1) > reads a different symbol's attention entry on symbol change (no stale prior-symbol card)
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage — Attention (Attention Signal Propagation V1) > renders a distinct "could not check" state on a total fetch failure, never silence
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage — Attention (Attention Signal Propagation V1) > renders nothing when the symbol has no open position (attention map has no entry)
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage — Attention (Attention Signal Propagation V1) > renders the same fact vocabulary as PortfolioAttentionBanner when the batch endpoint has an entry for this symbol
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage — Attention (Attention Signal Propagation V1) > shows "Nothing notable" for a non-notable held symbol, never fabricating a fact
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage — Attention (Attention Signal Propagation V1) > surfaces a degraded status pill rather than hiding it silently
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage — History click-through (Journal / Trade Lifecycle Convergence V1) > clicking the closed AAPL trade row navigates to its real j2_trades.id detail page
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage — History click-through (Journal / Trade Lifecycle Convergence V1) > keyboard Enter on the closed-trade row also navigates
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage — History click-through (Journal / Trade Lifecycle Convergence V1) > the OPEN position row is not clickable (no navigation on click)
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage — linked research on the open position (P1-19 fix) > renders nothing extra for a symbol with no linked research (no forced empty state)
    - src/pages/journal-2-0/components/position/PositionDetailPage.test.jsx > PositionDetailPage — linked research on the open position (P1-19 fix) > shows the existing LinkedNotesPanel, reused verbatim, keyed to the raw position id
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage > ArrowRight navigates to the next trade in the filtered set
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage > True R shows the no-adverse dash when trueR is null on a computed excursion
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage > a real excursion renders the % + bar-approx resolution label
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage > insufficient data quality shows N/A
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage > keeps the executions section collapsed by default
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage > null excursion shows the Pending exit-efficiency state
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage > renders True R from the excursion (stop-free, signed, colored)
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage > renders the excursion-analysis placeholder copy (pending, no excursion)
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage > renders the outcome header from the fixture trade
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage > shows the no-stop R label + an Add-stop affordance
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage > underlying data quality is labeled underlying-based
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — Research trigger (Full Research / Ask AI / Compare) > Ask AI navigates to the same route with ?section=ai, tagged with a return-context marker (Seam 12)
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — Research trigger (Full Research / Ask AI / Compare) > Compare reveals the "+ Compare" picker, and a comparator navigates to the exact canonical compare route (uppercased), tagged with a return-context marker (Seam 12)
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — Research trigger (Full Research / Ask AI / Compare) > Full Research navigates to the canonical /research/:sym route, tagged with a return-context marker (Seam 12), and closes the menu
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — Research trigger (Full Research / Ask AI / Compare) > opens without disturbing the other 4 CTA elements
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — Research trigger (Full Research / Ask AI / Compare) > the Compare picker receives the real current sym, not null (Identity Normalization Hardening V1)
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — Save to Notebook (Wave 1, P1-1: tradeRef) > captures this trade's chart, framed to the holding window, tagged with tradeRef
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — trade-card PNG actions > Notes draft flush on navigate-away > does not fire a PATCH on unmount when the Notes draft was never edited
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — trade-card PNG actions > Notes draft flush on navigate-away > flushes an unsaved Notes edit to the server when the page unmounts without ever blurring
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — trade-card PNG actions > Notes draft flush on navigate-away > still flushes via onBlur for the ordinary case (unchanged behavior)
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — trade-card PNG actions > clicking Copy image renders the card then copies it to the clipboard
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — trade-card PNG actions > clicking Save image renders the card then downloads it
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — trade-card PNG actions > hides Save/Copy image when the tradePng flag is off
    - src/pages/journal-2-0/components/trade/TradeDetailPage.test.jsx > TradeDetailPage — trade-card PNG actions > shows Save/Copy image buttons when the flag is on
    - src/pages/screener/shell/FilterBand.test.jsx > FilterRail — the wire, not the component > a REFUSAL is what the select points at when there is no range
    - src/pages/screener/shell/FilterBand.test.jsx > FilterRail — the wire, not the component > a control with no band names no description — same probe, both populations
    - src/pages/screener/shell/FilterBand.test.jsx > FilterRail — the wire, not the component > mounts the band under the real control, off the real meta payload
    - src/pages/screener/shell/FilterBand.test.jsx > FilterRail — the wire, not the component > renders the server's own disclaimer verbatim, exactly once
    - src/pages/screener/shell/FilterBand.test.jsx > FilterRail — the wire, not the component > the select is DESCRIBED BY its measured band, not merely followed by it
    - src/pages/screener/shell/FilterBand.test.jsx > FilterRail — the wire, not the component > two rails on the page keep two separate associations
    - src/pages/screener/shell/ScannerShell.review.test.jsx > the screener hands its order to the review > CONTROL: with the toggle OFF the rendered order IS the snapshot one
    - src/pages/screener/shell/ScannerShell.review.test.jsx > the screener hands its order to the review > ⛔ an EMPTY screen offers a disabled door, and publishes nothing
    - src/pages/screener/shell/ScannerShell.review.test.jsx > the screener hands its order to the review > ⛔⛔ WITH THE LIVE RE-SORT ON, THE REVIEW WALKS THE LIVE ORDER
    - src/pages/screener/shell/ScannerShell.review.test.jsx > the screener hands its order to the review > 🔴 the review door is ON the toolbar, and it publishes the rendered order
- expected-red entries naming **no reason**: **0** — none

⛔ **The failing set DIFFERS from the baseline** — read the two lists above.

## Failing identities vs vitest's own count

✅ Every shard's parsed test-level identities equal its `Tests N failed`.

## §8 DO-NOT-BUILD sweep

✅ No §8 item has gained code in `app/src`, `api`, `scripts`, `tools`.

## Classification (controller, 2026-09-25 ~20:0x CT; appended at commit time 2026-09-26) -- SUPERSEDED record

This gate ran on `a5668a8a8`, before the wave-7 fix round's last commits and before waves 5 and 6 and
master were merged in. It is kept as history; the landing gate is
`docs/notebook/gate-runs/wave7-landing/2026-09-26T01-07-50.md`. Verdict `NEW_FAILURES new=3`, classified:

1. + 2. `src/widgets/registry.test.js` x2 -- WAVE 7's own: the embed-binding parser matched only `lazy(`,
   and the fix round had moved the 12 bindings to `lazyChunk` then `lazyLeaf`. Fixed in `4557201ab` (the
   parser names the three wrappers); re-run alone 27 passed, was 2 failed.
3. `dailyFirstPaintAcceptance` NC-D -- MASTER'S: a wall-clock window master pinned in `2a80003fc`
   (2026-09-24); wave 7 changes nothing under `app/src/components`; the file passes 19/19 on a tree with
   master `ba6d4fb64`.

=> PASS after classification at the time. (This record was not committed then; it was archived from the
gate worktree and committed with the landing gate.)
