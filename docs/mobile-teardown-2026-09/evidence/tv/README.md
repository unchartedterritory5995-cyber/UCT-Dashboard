# Evidence — TradingView native session recording

The 178 MB screen recording `tv-ios-native-session-01-2026-09-07.mp4` is **deliberately not
pushed** — it exceeds GitHub's 100 MB file limit and the repository does not use Git LFS.

It stays in the local research worktree on branch `research/mobile-tv-teardown`, which holds
the complete record. Everything the recording establishes is transcribed into the frozen census
(`ledger/TV_IOS_CENSUS_V1_FROZEN_2026-09-07.jsonl`) and the V2 native lanes, so no conclusion in
this package depends on the file itself.
