# `merge/pine-up-to-master` — the merge record

> **What it is.** `origin/master` merged into `feat/pine-value-model`. 194 commits
> ahead of master, 0 behind it, 16 conflicted files resolved at the merge commit, and then one
> defect at a time until the branch's own suite is 0-NEW against its baseline.

## The gate discipline this branch was held to

**0-NEW against a MEASURED baseline, never a green suite.** The repo is not green
and this branch cannot make it so. The baseline is the same scope run on
`feat/pine-value-model` itself, and the comparison is over failing **test names**,
never counts — a count that stays equal while two cases swap answers is the
failure this shape exists to catch.

Scope run both sides: `src/components/chart/engine` + `src/components/chart/builder`
(537 files).

| | branch baseline | merged tree |
|---|---|---|
| failing cases | 27 | **24** |
| NEW on the merge | — | **0** |
| fixed by the merge | — | 3 |

⛔ **Every source mutation used to prove a rail was restored from captured bytes
with its sha256 re-verified**, never `git checkout --`.

## Defect ledger — what the merge broke, and what it turned out to be

### 1. `pineBoxCreateDrop` — droppedOps 6, not 4 · `cc493b764`
Not merge damage: identical 6 measured on the branch alone. The branch taught the
object reader Pine's METHOD FORM, so `ubx.set_right` (:322) and `lbx.set_right`
(:340) are collected where master's reader could not see them, and both are
refused at the same guard that already refuses their siblings at :309/:327.
**Proved by substitution**, not inferred from a count: renaming those two calls in
a scratch copy returns master's exact row.

### 2. A COMPUTED `text_formatting` reached nothing · `70836a5d4`
Two halves of one defect, both from taking one lineage's file wholesale.
`CELL_POSITIONAL` lost slots 9/10 (Pine puts `text_font_family` in front of
`text_formatting`), and the interception returned `null` on failure two lines
above the reader that can handle a computed one. Corpus: **host_ok 41 → 43**,
screener_ok unchanged, nothing regressed.

### 3. `pine.tupleBuiltins` — the derived list lost `ta.bb` · `341e75af2`
**Master ships this red.** The test file is byte-identical on both parents, the
`ta.supertrend` branch exists only on master, and the merged region is master's
verbatim. Master's Task 3 gave that one name a refusal of its own, so it stopped
reaching the derived list. Re-pointed at `ta.percentrank`, and supertrend's own
reason is now railed separately so moving the case did not delete a guarantee.

### 4. `controlDoorCensus` — 15 callers against a ledger of 14 · `5c1b06c20`
**Master ships this red** too. `scanHarness.jsx` landed 2026-09-21; the census's
next edit, two days later, ledgered the *fundamentals* harness and walked past it.
The remedy the case asks for in its own words is the row, not a shorter list.

### 5. The pane refused a CLEAN objects-only script · `69660b75a`
The real merge defect, and the most member-visible. The two lineages disagreed:

```
clean object program (zero drops) -> ok:true,  selected -1, 0 outputs
DIRTY object program (some drops) -> ok:false, guard pine:objects-only
```

`paneGate`'s admission was written by the lineage that only ever saw the second
shape, so a table-only script that translates PERFECTLY fell into *"this script
declares nothing a chart can draw"* — the one sentence that module exists to stop
being said about a script that draws a table.

⚠️ **Two mutations were green before the rails that now cover it**, and the second
is the instructive one: `memberPaneDefinition` carries its own
`allowObjectsOnly && drawsObjects` check further down, so with `paneGate` wrongly
admitting, the DOOR still refuses and the trip looks identical from outside.
**Two gates over one decision mean neither can be proved through the other.**

### 6. `graphRuntime` — three failures, one cause · `7f34b5928`
A capability gain arriving as a refusal. `mid_engagement__22-rsi-levels-regime-map`
went from 10 refusals / 18 kept rows to 2 / 26 — Entry, Stop and Target now
translate, and those are `accum` recurrences around the whole RSI chain — so its
root expands to 13,035 nodes against the 2,048 per-plot ceiling. `expandedSizes`
calls itself THE BOMB GUARD and is firing on exactly the shape it names: 227
shared nodes that inline to thirteen thousand.

⛔ Not member-facing, and that was checked: `hydrateGraphDocument` catches it and
returns the definition unchanged, and `buildGraph` — what the chart path runs —
never calls `expandedSizes`.

### 7. `tableTextFormatting` / `tableCellSetters` — master's GAP rulings · `9b366227a`
Both said a runtime-chosen format is a named drop. It is not any more, and the
sentence that ruling rested on is the half the merge made false. **Measured before
re-taking**, because the bad outcome here is a silently wrong answer:

```
table cell, computed  close > 0 -> <td> fontWeight "bold"
                      close < 0 -> <td> fontWeight ""
cell_set_ setter      close > 0 -> italic, close < 0 -> none, text intact
```

The named-drop machinery is kept, not retired — both files keep a case for a value
that genuinely has no template (`close`).

### 8. `stockChartWiring` — the z-order suffix · `15784c4c3`
One more series after the engine: the future-axis whitespace series, which paints
nothing. The effect that creates it, `buildFutureWhitespace`, `FUTURE_MIN`,
`adjustTime`, `filteredBars` and `resolvedTf` are **all byte-identical on the two
parents** — what the chart draws did not change; the effect simply reaches its
create in this harness now. The rail is narrowed to PAINTING series by a property,
never by a name, and mutation-proved by giving that series a colour.

## Not this merge's, and left alone deliberately

**`oosMeasuredBaseline`** fails 2 cases here and **3 on the branch** — the merge
improves it. Both remaining are master's, and the diagnosis is exact:

```
tests/fixtures/pine_oos/   61 .json + 30 .pine = 91 entries
the rail               FILES = readdirSync(DIR).filter(f => f.endsWith('.pine'))  -> 30
the assertion          expect(FILES.length).toBeGreaterThan(50)
```

**The instrument counts a different population than the floor was set for** — this
repo's own *"a census is a measurement of its fixture"*. The three "missing"
census scripts are precisely those carrying a `.json` and **no** `.pine`:
`high_engagement__13-…luxalgo`, `mid_engagement__09-…breakout-context`,
`mid_engagement__23-distilled-htf-po3`.

⚰️ **AND IT HAS NEVER BEEN ABLE TO PASS.** Both the floor and the six census names
were written in `e855f62cd` (2026-09-18) — the same commit in which that directory
already held 30 `.pine` files. `origin/master` today: same floor, same 30. Red by
construction, five days before this merge, and red on master right now.

⚰️ **A CORRECTION TO MY OWN EARLIER CLAIM**, recorded rather than quietly fixed: I
first wrote that `mid_engagement__23-distilled-htf-po3.pine` *"has never existed
anywhere in this repo's history"*. That was **false** — the search that produced it
was at fault. The fixture exists, as `.json`, and has since `0e52f964a`.

Recorded rather than fixed: repairing a parent's fixture census is not this
merge's scope, and it would read as churn in this diff.

## Open

- The full six-shard gate has not been run on the merged tree yet.
- Master's three later commits were merged in at `0fa4de7b4` — clean, no conflicts —
  so the gate covers the real landing tree. C1 overlap with them was ONE file,
  `docs/feature_flags.json`; nothing they touch is in this branch's engine diff.
- Re-check `behind` before landing: master moves, and a gate costs 46–92 minutes.
