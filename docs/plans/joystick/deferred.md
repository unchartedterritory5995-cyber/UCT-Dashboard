# Joystick Hub — deferred

Everything cut from the spec, with the smallest change that would enable it. Nothing here is abandoned;
each row is a decision to make later with a known price. Evidence for every claim is in
`10-wave0-discovery.md`.

## FINAL STATE — Increment 7 (BACKLOG ZERO), part 1: rows closed without new code

Every row below is closed by a line already written in this file or in a ruling — not by a
decision made tonight to be rid of it. The quoted fragment is the closing evidence. Rows
closed by Increment 7's own work are in part 2, at the bottom, beside their commits.

⛔ **The `api/` stop, quoted once and referenced by every row that needs it.** Read live from
the Railway service manifest (`railway status --json`, read-only), 2026-09-10: the **`worker`**
service's watch list is `['/api/**', '/requirements.txt', '/railway.json', '/nixpacks.toml',
'/Procfile', '/runtime.txt']`, and **`bars-api`**'s is `['api/**', …]`. So **any** edit under
`api/` redeploys both. (`flow-worker`'s list is 20 enumerated top-level `api/*.py` modules and
contains no `routers/` path, so it is NOT implicated — but the other two are enough.)

| # | Final state | Closing evidence |
|---|---|---|
| D-04 · D-05 · D-11 | **SHIPPED** in Wave 0.5. | struck through in the table below |
| D-33 · D-34 | **SHIPPED** — verified by measurement, not by report. | `disciplineGuards.js`, `calculations.js:251` |
| D-07 | **CLOSED-BY-SPEC** — "Each needs a destination built first. **Not a hub problem**." There is no `/sectors` route to navigate to. | this file, D-07 |
| D-12 | **CLOSED-BY-SPEC** — "Each needs a per-position affordance built first." | this file, D-12 |
| D-13 | **CLOSED-BY-SPEC** — "**Genuinely new product surface.**" No read/unread concept exists anywhere in the product; `is_new` is server-computed and global. ⭐ A client-only seen-set would be buildable, and is deliberately NOT being built: it would not follow a member between devices, so it would answer "have I seen this?" wrongly for the first member who owns two. | this file, D-13 |
| D-14 | **CLOSED-BLOCKED** — needs "an append-only log" of intraday history, i.e. `api/`. See the stop above. Its own row also says "**Large**". | this file, D-14 |
| D-15 | **CLOSED-BLOCKED** — "New column + **endpoint** + filter application". `api/`. | this file, D-15 |
| D-16 | **CLOSED-BLOCKED** — the fix is "Add a `?q=` read in **`NotebookTab.jsx`**", and this build has a standing zero-edit rule on that file. Nothing else reaches it. | this file, D-16 |
| D-18 | **CLOSED-BY-SPEC** — "**Nothing to build.** The hub branches on `providers.obsidian.connected` and deep-links `/settings?section=connections` when false." Obsidian is a device-push connector; there is no "sync now" to fire. | this file, D-18 |
| D-19 | **CLOSED-BLOCKED** — "A per-event subscription **endpoint** + UI". `api/`. | this file, D-19 |
| D-22 | **CLOSED-BY-SPEC** — "**Explicitly out of scope by gate decision** — one `// TODO(hub-analytics): emit here` marks the call site." No authenticated in-app event sink exists. | this file, D-22 |
| D-23 | **CLOSED-BY-SPEC** — "**Removed, not deferred** (owner ruling, Wave 0.5 gate): there are no tiers — one product, one price… do not carry a dead one now." | this file, D-23 |
| ~~D-26~~ | **OWNER DECISION, not engineering** — "One new method on `ChartPane`'s ref returning pane rects — **needs an owner decision, not an engineering one**." The scrim ships its stated `calc(22% + 32px)` approximation meanwhile, labelled as an approximation. | this file, D-26 ✅ **DECIDED 2026-09-11 — CLOSED BY DESIGN.** Keep the stated `calc(22% + 32px)` approximation. The only fix is a new imperative method on `ChartPane`'s ref, which is outside the named exceptions, and the cost of being wrong is a scrim edge a few pixels off over a chart — no member-visible harm, no write, no gesture. Adding an imperative surface to an 11,700-line file for that is disproportionate. The approximation is labelled as one, which is the part that matters. |
| D-28 | **CLOSED-BY-SPEC** — "**Nothing web-level exists.** A PWA/standalone display could suppress it; that is a different product decision." Android 10+ claims both edge swipes and a browser tab cannot opt out. | this file, D-28 |
| D-30 | **CLOSED-BY-SPEC — NOT A HUB TASK**, and it says so in its own title. "⛔ **Must be its own task** — it must not ride along with a UI feature branch." ~68 call sites across `api/**`, which is also behind the stop above. It remains a live PRODUCTION risk and should be scheduled on its own. | this file, D-30 |

| # | Item | Section | Why it is deferred | Smallest enabling change |
|---|---|---|---|---|
| ~~D-01~~ | ~~Draw (trendline tool)~~ | chart | **RESOLVED — SHIPPED (Increment 7, P3).** `selectTool(name)` now sits beside `expandDrawToolbar` on `StockChart`'s existing `toolbarApiRef` surface — the same reveal (phone drawer, else the desktop strip through the persisting setter) with the arm attached. It VALIDATES against `ChartToolbar`'s own exported `TOOLS` roster and REFUSES (`false`, never a silent no-op) on an unknown id or a read-only mount, because `setActiveTool` accepts any string and an unreal tool id arms a chart that ignores every click. `chart.draw` is declared in the registry, `chartSection` gives it a body, and `MobileChartsApp` supplies the seam; with no seam the action is DROPPED, not shipped inert. Rail: `app/src/hub/sections/chartDrawDoor.test.jsx` — real `ChartPane`/`StockChart`, action driven off `HubProvider`'s `activeModeConfig`, asserting the real `MobileDrawBar` "Trend" tile reads `aria-pressed="true"`. | — |
| ~~D-02~~ | Indicator set (EMA 9/21/50, VWAP, AVWAP) | chart | **BLOCKED ON AN OWNER PRODUCT RULING (Increment 7, P3) — and the binding constraint is not the one this row named.** ⛔⛔ **THE CHART FAN IS STRUCTURALLY FULL.** With D-01 shipped the declared rings are outer **5/5** (`chart.planTrade, chart.alert, chart.flag, chart.draw, chart.compare`) and inner **4/4** (`chart.logTrade, chart.note, chart.voice, chart.home`) against `OUTER_MAX = 5` / `INNER_MAX = 4` (`registry.js`). A sixth outer action is REJECTED by `validateRegistry` — *"chart: DECLARED outer ring has 6, max 5"*. So an Indicator bubble cannot exist until the owner says which shipped action it displaces. ⚠️ **"Fixed 4-slot" is not quite right, and it changes the options:** `mergeChartSettings` concats stored entries past index 3 (`chartDefaults.js`), `resolvedOverlays` is `cs.overlays.filter(o => o.enabled)` (`StockChart.jsx`) and `MobileIndicatorSheet` renders `overlays.map(...)` — so a **5th overlay renders and is editable today**; what is positional is the MERGE, and new slots must go on the END. ⛔⛔ **AVWAP: "from last pivot" (master-spec v1.1 §241) matches NEITHER existing AVWAP.** `AVWAP_ANCHORS` (`chart/indicators.js`) is `session, week, month, quarter, year, swingHigh, swingLow` — "last pivot" is a choice between `swingHigh` and `swingLow`, which is a directional opinion about the member's chart, not an engineering default; the other AVWAP is the click-anchored DRAWING tool, which needs a canvas click the hub cannot supply. ⛔ **And both `vwap` and `avwap` are INTRADAY ONLY** (`timeframes: ['1','5','15','30','60']`, refused by `engine/eligibility.js` with `reason: 'timeframe'`) while the phone chart's default timeframe is **`'D'`** (`MobileChartsApp.jsx`) — so the bubble would be refused on the surface's own default, and `HUB_REQUIREMENTS` has no literal that would render it disabled-with-a-reason. | **THREE decisions, none of them engineering.** (1) **The slot:** displace one of the five outer actions (`chart.compare` is already dropped by the controller as unreachable, so it is the cheapest to retire — cost: the registry declaration goes, and the "unreachable rather than impossible" record with it), or keep the fan as-is and Indicator never ships. (2) **The MAs:** (a) overwrite slots 1-2 — cost: silently destroys a member's configured EMA20 + SMA50, which the P3 brief forbids; (b) append EMA 21 and EMA 50 as 5th/6th overlays — cost: non-destructive and renders today, but grows a positional array every stored blob is merged against, and the Indicators sheet becomes a 6-row list; (c) ship VWAP only and drop "EMA 9/21/50" from the spec — cost: the bubble no longer does what its name says. (3) **The AVWAP:** name the anchor (`swingHigh` / `swingLow` / `session`), or drop AVWAP from the quick set. With (1)-(3) answered the build is small: `addInstance()` (`chart/engine/instanceControls.js:379-398`) for VWAP/AVWAP plus a refusal toast when `tf` is not intraday, derived from `def.meta.timeframes` and never a hand-typed list. ✅ **DECIDED 2026-09-11 — CLOSED BY DESIGN (owner pre-ruled).** The chart fan is full at 5/5 outer and 4/4 inner, and replacing a shipped action needs **glass evidence about which bubble members actually use**, not a guess made from a registry. Indicator does not ship. Reopen only with device evidence naming what it displaces. |
| ~~D-03~~ | ~~Alert at crosshair price~~ | chart | **CLOSED BY ITS OWN REPLACEMENT — and the replacement had to be built first (Increment 7, P3).** The crosshair half stands closed as written: *"`crosshairData` is private `useState` (`StockChart.jsx:3471`) with no external accessor; the spec forbids lifting it in this build"*, and nothing here lifts it. ⚰️ **But "Replaced in v1.2 by Alert at last price, which is buildable today" was NOT what shipped.** Verified 2026-09-10: `chart.alert` fell through `buildChartFan`'s default arm with **no `run`**, and `HubRoot`'s confirm branch performs the write from the sheet's primary (`onConfirm: () => Promise.resolve(action.run?.(ctx))`) — so a member dragged to Alert, read "Alert on NVDA", pressed Create, and nothing was created. Silently: `Promise.resolve(undefined)` resolves. ⛔⛔ **`runActionsHaveHandlers.test.js` — the rail whose entire job is this defect — could not see it, because it filters `kind === 'run'` and this one wears `kind: 'confirm'`.** Now shipped: `chart.alert` creates at the live price, same shape as `scan.alert` (`screenerSection.js`), refusing rather than fabricating when there is no price. Rail: `app/src/hub/sections/chartAlertDoor.test.jsx`. | — |
| ~~D-04~~ | ~~Horizontal scrub = pan~~ | chart | **RESOLVED — SHIPS (Wave 0.5).** `stepBar(dir)` (`StockChart.jsx:14510-14517`) takes an unconstrained numeric delta and is already exposed unmodified on `ChartPane`'s ref | — |
| ~~D-05~~ | ~~Compare (overlay QQQ)~~ | chart | **RESOLVED — SHIPS (Wave 0.5).** The mobile `handleStore` write reaches the render path; desktop's `setComparison` is the identical `{...cs, comparisonSymbols}` write, not a separate system | — |
| ~~D-06~~ | ~~Metric-group cycling~~ | breadth | **CLOSED-BLOCKED (Increment 7).** The named mechanism is inert for the default member and destructive for everyone else. `useBreadthCustomize.js` refuses every write on the `Default` preset — `setHiddenSet`: "`if (prev.activePreset === DEFAULT_PRESET) return prev  // immutable`", and `toggleHidden`/`resetActive` say the same — and `Default` IS the shipped state (`EMPTY_STATE = { activePreset: DEFAULT_PRESET, presets: {} }`). So a group-cycling bubble would answer a deliberate gesture with silence for every member who has never made a preset: the exact defect that REMOVED `breadth.sizeRule`/`breadth.snapshot` from this same fan (B2). For a member who HAS a preset it is worse — the write replaces their persisted `hidden` array (localStorage `uct.breadth.customize.v1`) with no restore. **The stop is doubled** (writer guard AND `hidden`'s reader guard); each alone survives mutation, so only the outcome is railed: `hub/deferredRowClosures.test.js`. A hub-local group filter instead of `customize.hidden` would be a second authority over "which columns are visible" and is not a smaller change | ~~Reuse the existing reachable `customize.hidden` set as a group filter~~ — measured inert; needs a writable transient visibility layer that does not exist. **⛔⛔ RE-EXAMINED UNDER THE WIDENED AUTHORITY (L3) — STAYS CLOSED, re-measured at the source.** Not an `api/` row either, so the widening does not reach it; the re-measure went to the guards. The doubled stop is confirmed verbatim in `useBreadthCustomize.js`, and it is doubled in a stronger sense than the row first said: the WRITER guard appears **three** times — `setHiddenSet` (`:112`), `toggleHidden` (`:123`) and `resetActive` (`:185`), each `if (prev.activePreset === DEFAULT_PRESET) return prev` — and the READER guard is `:100-103`, `if (isDefaultActive) return new Set()`. `EMPTY_STATE = { activePreset: DEFAULT_PRESET, presets: {} }` (`:20`) IS the shipped state, so the default member's deliberate gesture is answered with silence and the preset owner's persisted `hidden` array is replaced with no restore. Making the gesture work means giving breadth a WRITABLE transient visibility layer that `Default` honours — a change to what a preset MEANS, inside the module that owns presets. **CLOSED — product decision — the OWNER (breadth customize) owns "is `Default` writable, or does a transient layer sit beside it"; the hub cannot answer that without becoming a second authority over which columns are visible, which this row already says.** ⛔ Not buildable as a hub-local filter: that IS the second authority, and it is not smaller |
| D-07 | Phase detail · Sectors · Scan-with-breadth-filter | breadth | No entry points exist. No `/sectors` route; no breadth-origin query param on the Screener | Each needs a destination built first. Not a hub problem |
| ~~D-08~~ | Compare prior cycle | breadth | Exists only as the **admin-only** Analogues tab. ⛔ **AWAITING AN OWNER RULING — it is a product decision about who sees what, and Increment 7 deliberately did NOT make it.** Costed so the ruling is cheap: the admin-only-ness is ONE client-side list (`breadthSection.js`'s `BREADTH_ADMIN_TAB_ITEMS`, appended by `resolveBreadthTabs(isAdmin)`) plus the non-admin bounce at `Breadth.jsx:643`. **The DATA is already paid-gated, not admin-gated** — `/api/breadth-monitor/analogues` depends on `require_paid` (`api/routers/breadth_monitor.py:557`), so every paid member's session can already fetch it and only the client declines to show it. Both facts railed in `hub/deferredRowClosures.test.js` | **Option A — make it member-visible:** move `analogues` from `BREADTH_ADMIN_TAB_ITEMS` into `BREADTH_TAB_ITEMS` and delete the `:643` bounce. **Zero backend work.** The price is editorial, not engineering: the tab's headline is a forward-return claim ("Last time breadth looked like this was {date} — SPY gained {n}% over the next month") shown to members, and the breadth tab count changes from 5 to 6 for everyone. **Option B — rule that admin-only is acceptable:** zero work; `resolveBreadthTabs` already carries the two-length list correctly and the hub walks the resolved one. This row then closes as CLOSED-BY-SPEC on that ruling ✅ **DECIDED 2026-09-11 — ADMIN-ONLY STANDS (Option B).** Recommended and ruled. The tab's headline is a **forward-return claim** — "last time breadth looked like this, SPY gained n% over the next month" — and this program's own standing lesson is that a hit rate is meaningless without its base rate. Putting that in front of members is an editorial and compliance decision, not a plumbing one, and the safe default is not to publish a forward-return number. Zero work; the two-line change stays available if the owner ever wants it with the claim reworded. |
| ~~D-09~~ | ~~Sort~~ | scan | **CLOSED (Increment 7) — and the reason was CORRECTED at integration.** ⚠️ This row first closed citing `HubConfirmPayload.fields` as "unreachable dead code". **That stopped being true the same night**: P1 closed D-35, and `fields` now reaches the member. The stream that wrote this closure branched before that landed — a stale premise carrying a verdict, which is exactly what this program keeps re-finding, so it is corrected here rather than left to read as evidence. *The hub half is still blocked, on a NARROWER fact:* `fields` offers `type: 'number'` and `type: 'text'` only, and choosing a COLUMN is a choice among a fixed set — there is no select, and inventing one to sort a table is not the smallest enabling change. *The page half is UNDECIDED, not unbuilt:* passing the props is one line, but the row's own sentence defers the part that matters — "then **decide what control renders them**" — a Screener product decision, and v1.6 §scan records the state it leaves: "Sort is deferred — the phone has no sort UI at all." The desktop/phone prop asymmetry is railed in `hub/deferredRowClosures.test.js` so it goes red the day a phone sort control appears | ~~Pass the props already computed two lines below~~ — needs (a) a field type that can offer a CHOICE, or (b) an owner decision on the phone control |
| D-10 | Chip reading the active scan's name | scan | **HALF ALREADY SHIPPED — measured, not reported.** `screenerSection.js:149` exports `activeScanName(filters)`, reading `filters.scan.label`, and `chipLabel()` renders "Powerplay · 3/41"; the chip already names the scan whenever the member arrived through `ScannerShell`'s `onUseScan(hash, name)` door, which writes `{op:'in', value: hash, label: name}`. **CLOSED for the remaining half (Increment 7)**, on the row's own sentence: *"Persisting it also touches the shared URL codec (`shell/specUrl.js`) — a real, if small, contract change."* That door is `ScreensManager.jsx:308` (`const apply = s => { onApply(s.spec) }`), which HAS `{id, name}` at the call site and drops them at `useScreenSpec.js:82-91`. ⚠️ And the codec is not the whole price: once threaded, the identity goes STALE the moment the member edits one filter, so the chip would name a screen the spec no longer is — a second authority over "which screen is this". Deciding WHEN identity is dropped is a product rule nobody has written | Thread `{id, name}` through `applySpec`, **plus** a stated rule for when an edit clears the identity. Persisting it also touches the shared URL codec (`shell/specUrl.js`) — a real, if small, contract change |
| ~~D-11~~ | ~~Cursor over unloaded virtualized rows~~ | scan | **RESOLVED — SHIPS (Wave 0.5): cheap.** `forwardRef` + one `useImperativeHandle` on `VirtualResults.jsx` and `ResultCards.jsx` (both are virtualized, including the phone card list). Now exception (d) in §2 | — |
| D-26 | LWC pane geometry for the scrim | chart | Pane rects exist inside `StockChart.jsx` but reach no host ref; a new imperative method is outside the named exceptions. The scrim uses the app's own `calc(22% + 32px)` fallback instead, stated as an approximation | One new method on `ChartPane`'s ref returning pane rects — needs an owner decision, not an engineering one |
| ~~D-27~~ | ~~Wire/Journal green separation~~ | all | **SHIPPED-CONDITIONAL (Increment 7).** The distinguishing accent exists and is one switch away; the switch is `glass-acceptance.md` **G3-16**, not this row. ⚠️ *(Corrected 2026-09-14: this said G3-15, which is the chip/Actions-button overlap row. G3-16 is Wire vs Journal, and `glass-acceptance.md:172` tags it `(D-27)`.)* `[data-hub-contrast="high"]` now redefines `--hub-mode-wire` to a teal-shifted `#D4FEE4` — riding the contrast path that was already built (`useHubSettings.js` → `HubRoot.jsx` → the `tokens.css` block), so it costs a member who has not enabled high contrast **nothing**: `:root` still says `#9FE887` and the default fan is byte-identical. Measured, WCAG ratio / CIEDE2000 vs Journal: **1.744 / 14.49 → 2.319 / 28.03**. Wire's *worst* neighbour anywhere in Home's fan: **8.30 dE00 → 17.33**. ⛔ **THE FIX CANDIDATE THIS ROW PROPOSED WAS MEASURED AND IS WORSE — do not "restore" it.** `#8FE0B0` LOWERS the WCAG ratio against Journal (1.744 → **1.634**), which is the one channel a hue-blind viewer has and the entire reason this row existed; it also drops Wire's worst dE00 neighbour to 7.71 by moving onto **Breadth** (`#5dcaa5`), which shares ring 0 with Wire. "Toward teal" was the right direction and the wrong distance — the shift has to carry lightness with it. ⚠️ **Two further corrections to this row's own prose, both derived from the registry rather than read off it:** (a) **all three greens co-occur in Home's fan, not two** — `voice('home')` takes `--hub-mode-${mode}`, so the Voice bubble wears `--hub-mode-home`; (b) the pair the row named was **not** Wire's tightest — Wire↔Home was 1.218 / 8.30 against Wire↔Journal's 1.744 / 14.49. ⭐ Wire is the token that moves, not Journal: Journal is the tightest of the three against the 3:1 glass floor (**4.06:1** on the pressed tier), so darkening it to open the gap spends the very property this row banked. Rail: `hub/modeAccentSeparation.test.js` — derives every figure from `tokens.css` + `registry.js` (no number is typed), holds the 3:1 floor for all three greens across 8 glass tiers, and asserts `[data-hub-contrast="high"]` is the ONLY non-`:root` block declaring a `--hub-mode-*` (which is also why no theme island needed a new pin — `themeIslands.test.js` keys on `[data-theme]`). Colour math moved to `styles/contrastMath.js`, shared with `tokens.test.js` rather than copied. | ⛔ **NOT a default flip.** Promoting `#D4FEE4` to `:root` requires a human answering G3-16 on real glass — the decision table lives beside that row. Icons, ring radius and bubble size remain load-bearing, not decorative, and are where D-27 reopens if the accent turns out not to be the separating channel at all |
| D-28 | Android edge-gesture suppression | all | Android 10+ claims swipes from **both** edges and a regular browser tab cannot opt out. Mitigated by design margin (`right: 24px`) plus the 20px deferred-movement rule in §2c | Nothing web-level exists. A PWA/standalone display could suppress it; that is a different product decision |
| D-12 | Stats · Tag setup | journal | No per-position entry point exists. `SetupStatsPanel` renders only inside the Add modals; setup is editable only through the full `EditPositionModal` | Each needs a per-position affordance built first |
| D-13 | Unread state · "next unread" · "Mark read" | catalysts | **No read/unread concept exists anywhere.** `is_new` is server-computed and global, not per-user | A per-user read table or a client seen-set. Genuinely new product surface |
| D-14 | Timeline scrub | catalysts | The table is a top-20 curated snapshot upserted per `(market_date, ticker)`; rows are overwritten in place and can drop off with no trace. A time readout would misrepresent what was on screen | Capture intraday history as an append-only log. Large |
| D-15 | Mute ticker | catalysts | No mute or snooze path exists in `services/catalyst/*` or `CatalystTable.jsx` | New column + endpoint + filter application |
| D-16 | Double-tap = search | notebook | `FolderSidebar`'s search mode is local state with no `?q=` deep link, unlike `?folder=`/`?ticker=`/`?new=`. The Command Palette's "Search Notebook" entry points at the bare route | Add a `?q=` read in `NotebookTab.jsx` mirroring the existing one-shot-strip pattern; pass `initialQuery`/`initialMode` to `FolderSidebar`. Backend already supports `GET /api/j2/notes?q=` |
| ~~D-17~~ | ~~Voice note~~ | notebook | **RESOLVED — SHIPPED (Increment 7, `7184b579b`).** Exactly as this row specified, and nothing under `pages/journal-2-0/**` changed (rule 12): `hub/voiceNote.js` records, POSTs to the generic `/api/voice/transcribe`, and hands the transcript to the Notebook's own `createNoteViaApi`. `notebook.voiceNote` sits on the OUTER ring (inner was already at `INNER_MAX`) with icon `wave` — deliberately not `mic`, which is `notebook.voice`, a Compass realtime conversation and a different thing. A toggle, mirroring `VoiceInputButton`'s own. ⛔ **Every refusal is a sentence the member reads**, taken from the server's own `detail` (402 paid plan · 400 voice disabled · 429 monthly cap · 503 provider), asserted as RENDERED TEXT in `hub/sections/notebookVoiceNote.test.jsx` and mutation-proved three ways. ⛔⛔ Found on the way: this controller had shipped an `onScrub` with **no `readout()`** since B10, which `contracts.js:422` forbids — it survived because the check runs at the mounted boundary and every existing notebook rail renders the hook without a provider. `notebookReadout` ships with it | — |
| D-18 | Sync (Obsidian) | notebook | Obsidian is a **device push** connector; there is no "sync now" to fire. `syncSource()` serves pull providers only | Nothing to build. The hub branches on `providers.obsidian.connected` and deep-links `/settings?section=connections` when false |
| D-19 | Event alert | calendar | Only an automatic background job (`services/calendar_alerts.py`) exists; no per-event user control. The reachable alert is a **price** alert | A per-event subscription endpoint + UI |
| ~~D-20~~ | ~~Add to Notebook from a calendar entry~~ | calendar | **CLOSED-BLOCKED (Increment 7): there is no calendar ENTRY the hub can reach.** Two measured halves, and both must change together. (1) The calendar's hub cursor walks **days** (`calendarSection.js` over `weekDates`) and `Calendar.jsx` never calls the hub's `setSymbol` — the Screener and the Journal are the only pages that do. So a `requires: ['symbol']` Note bubble would render permanently DISABLED, verbatim the reason R-17 removed `notebook.linkTicker`. (2) The one surface that DOES name an entry — `EarningsResearchModal`, `Calendar.jsx`'s `selected` — is `z-index: var(--z-modal)` = **1000**, above the hub's whole ladder (`--z-hub-open: 401 > --z-backdrop: 399 > --z-hub-rest: 360`). While an entry is selected the hub is buried underneath it; whenever the hub is reachable, no entry is selected. Both railed in `hub/deferredRowClosures.test.js` | ~~One button wired to the existing endpoint~~ — the endpoint was never the missing piece. Needs a per-entry selection the hub can see (page-set `symbol`) **and** an entry surface that does not outrank the hub, or a Calendar-page button, which is not a hub action. **⛔⛔ RE-EXAMINED UNDER THE WIDENED AUTHORITY (L3) — STAYS CLOSED, and this one was never an `api/` row.** Both halves are `app/src`, which the charter *can* edit, so the re-measure went to the halves themselves rather than to the file list. **Half (1) is cheap and real:** `HubContext` exposes `setSymbol` (`HubContext.jsx:259`), and `journalSection.js:451` plus the screener section take it through `useHubSetters()`; `calendarSection.js` does not, and one call would fix it. **Half (2) is not, and it governs.** The ladder is `--z-hub-rest: 360` < `--z-backdrop: 399` < `--z-hub-open: 401` (`styles/tokens.css:277-279`) against `--z-modal: 1000` (`:281`), and `hub.module.css:306-312` says the split is deliberate in its own words: "⛔ Do not collapse these to one value. Using `--z-hub-open` everywhere puts the resting pad above every backdrop in the app." The hub is DESIGNED to sit under app modals **everywhere**, not only here — so lifting it over `--z-modal` to reach one calendar entry re-ranks the hub against EVERY modal in the product. **CLOSED — product decision — the OWNER owns "does the joystick outrank a modal, app-wide"; that single ruling is the whole of this row, and it is not a calendar fix.** ⭐ If the answer is ever yes, half (1) is one line in `Calendar.jsx` and `hub/deferredRowClosures.test.js` already rails both halves |
| ~~D-21~~ | ~~Econ-only view~~ | calendar | **CLOSED-BY-SPEC (Increment 7).** v1.6 §calendar: *"There is no econ-only view (Earnings is effectively always on)."* The page does not merely default to earnings, it **LOCKS** the type: `CalendarHeader.jsx:347-348` — `const locked = type === 'earnings'; if (locked) return` — with the chip titled `'Always on'` and the module's own comment "Earnings is the calendar (always on)". Same one-way valve that DELETED `calendar.earnings` from the registry rather than shipping it inert. ⚠️ **This row names the wrong file:** `filterLogic.js` filters earnings ROWS (audience, market cap, volume, price, confirmed-only, sector, search) and has no notion of an event TYPE — those live as a Set in `Calendar.jsx`, read by `FeedView`/`WeekView`. Turning earnings off is a Calendar product decision, not a filter-logic change. Both facts railed in `hub/deferredRowClosures.test.js` | ~~A filter-logic change in `filterLogic.js`~~ — wrong file. Needs an owner decision to unlock the earnings event type, then a real econ layout (today, earnings-off collapses each day to `MacroOnlyDay`, a dim one-line stub) |
| D-22 | `hub_action` analytics | all | **No authenticated in-app event sink exists.** The only `track()` is anonymous, landing-scoped, with a server-side allow-list of ~20 marketing event names | A `/api/events` route + table. Explicitly out of scope by gate decision — one `// TODO(hub-analytics): emit here` marks the call site |
| D-23 | Per-action `tier` gating | all | **Removed, not deferred** (owner ruling, Wave 0.5 gate): there are no tiers — one product, one price. `HubAction` has no `tier` field and there are no locked or upsell bubbles | Nothing. If a tier model is ever introduced, add the field then — do not carry a dead one now |
| ~~D-24~~ | ~~Light-theme hub tokens~~ | all | **CLOSED-BY-SPEC (Increment 7), and the row's premise does not hold.** `tokens.css` states the decision twice, quoted rather than re-litigated: *"NO --glass-\* HERE, ON PURPOSE. Spec §3.2 ... This is a recorded decision, not an omission"*, and of the hub family, *"No `[data-theme="light"]` --hub-\* set anywhere in this file — deliberate ... **not an oversight for a later sweep to 'fix'**."* Measured: **the hub paints no `--glass-*` surface at all**, so the light omission cannot reach it; and every `--hub-*` token is `color-mix(in srgb, var(--text-heading) N%, transparent)` over `--bg`, both of which the light theme redefines — the glass adapts on its own. ⛔ **The close rests on numbers, not a reading.** Five of six real ink/surface pairs clear WCAG AA on white (5.45–16.50); one does not: `--text-muted` on `--hub-glass-tint-strong` is **4.18:1** (6.34:1 on dark) — the cursor highlight, which `[data-hub-cursor="active"]` paints onto rows the hub does not own, and the Screener phone card colours `.cardCompany`/`.cardStatLabel` with exactly that ink. It clears AA-Large (3.0) and misses AA (4.5), and is recorded by name and number with a floor in `styles/tokens.test.js`, pinned from both sides. **Zero lines of `tokens.css` changed** | The light theme needs no `--hub-*` block. The ONE open item is the 4.18:1 cursor-row pair — fixable by darkening the light `--hub-glass-tint-strong` alone, and it belongs to the post-launch app-wide sweep (§10) that owns the rest of the light theme, not to the hub |
| ~~D-25~~ | ~~Feedback FAB as a one-tap affordance~~ | all | **RESOLVED — SHIPPED (Increment 7, `672901bd8`).** The decision is reversed: Feedback has its own always-visible control again, one tap-target below the Actions button in the hub's own column, mirroring with the hub. The inner-ring alternative was MEASURED and rejected — six of ten modes are already at `INNER_MAX` (scan, chart, journal, catalysts, notebook, home), so promoting it would evict a shipped action from six fans. It is NOT at the old bottom-left: the far edge breaks §C2:769 (`mirrorsAsAUnit.test.jsx`) and strands the affordance under a left-handed member's other hand. The "never actually collided" claim is no longer a claim — `feedbackIsOneTap.test.jsx` computes the rest-state rectangles in both hands. The sheet entry STAYS as the screen-reader door. | — |
| ~~D-29~~ | ~~`[data-hub-cursor="active"]` visual tuning~~ | all | **RESOLVED — SHIPPED (Increment 7, `a9f82101d`).** Five `--hub-cursor-*` tokens are now the cursor's ONE dial; the rule declares no value of its own, so retuning the cursor no longer retunes the knob rim and the pressed Actions button (and vice versa). ⚰️ Found while unifying: the cursor never reached `[data-hub-contrast="high"]` — that block redefines `--hub-rim` while the cursor read `--hub-rim-highlight`, so "rim goes full-strength" was true of the knob and false of the one surface that tells a low-vision member which row they are on. Fill moved off the pressed tier (20% -> 12%). ⚠️ The tune is derived from token semantics, NOT from a device eyeball — no device was used. Rail: `hub/cursorAppearance.test.js` (exactly one `[data-hub-cursor]` rule app-wide, no literals in it, one `:root` definition per token). | — |
| D-30 | Single `data_root()` helper for every `/data` path **(NOT a hub task)** | all | **Latent PRODUCTION risk, surfaced by the 2026-09-08 sandbox incident.** 72 env vars name paths inside the shared root and each resolves INDEPENDENTLY — `api/services/auth_db.py:10` reads `AUTH_DB_PATH` with a `/data/auth.db` default and never consults `DATA_DIR`. Any contributor can add a 73rd, and every sandbox, staging boot and local run silently inherits the hazard. Today the only defences are the AST census + the conftest tripwire, both of which CATCH the problem rather than remove it | One `data_root()` helper every path resolver derives from, so a single env var moves the whole tree. ~68 call sites across `api/**`. ⛔ **Must be its own task** — it must not ride along with a UI feature branch, and it needs the full backend suite plus a prod-parity check that nothing resolves differently with nothing set |
| D-31 | A real tick table beside the price formatter | Journal scrub (3.4), and every price input | **Increment 2 ships `0.01` everywhere as a STATED SIMPLIFICATION, not a discovered rule.** This app has no tick logic at all — no `tickSize`, `minTick` or `priceStep` anywhere; prices are `.toFixed(2)` throughout. So a $0.30 stock gets cent granularity that is coarse relative to its spread, and a $4,000 stock gets 400,000 steps across its range | A tick table living **beside the price formatter**, which is the one place that already knows how a price is rendered, consumed by the hub like any other caller. ⛔ **NEVER a hub-side scaling hack** (owner ruling, A2): a second opinion about what a price step means, living in a gesture handler, is the second-authority defect this project keeps re-finding — and it would be invisible, because a wrong step still produces a plausible number. **RESOLVED — SHIPPED (Increment 7, `3c63c486e` + `0ada98fe3`).** `TICK_TABLE` / `tickSizeFor` / `roundToTick` live in `components/chart/drawingLabels.js`, beside `formatPrice` — which had accepted a `tick` since it was written while nothing ever computed one. Reg NMS 612: $0.01 at or above $1.00, $0.0001 below; what it deliberately does not model (the 2024 amendment's per-SYMBOL $0.005 tier; futures/FX ticks) is written down rather than guessed. The hub ASKS: `stopTickFor` forwards and `STOP_TICK` is `tickSizeFor(1)`, not a restated cent. Four member-visible defects fell out of the old fixed 2dp, all reading as "nothing happened" on a sub-dollar name — the clamp and the write re-rounded to the cent, the commit comparison read three sub-penny ticks as no movement, the chip sat still through them, and the side-flip refusal printed two different stops as the same number. Rail: `hub/tickIsAskedNotDecided.test.jsx` |
| D-32 | Screener -> Plan trade prefills the pattern entry/stop | Screener (3.3), Plan-trade sheet (3.4) | The Screener carries entry/stop only as DISTANCES (`pattern_entry_dist_pct` / `pattern_stop_dist_pct`, `columnDefs.js:386-389`) and only in the `patterns` view (`filters.py:837-841`) — the default view is `overview`, which carries neither. Deriving absolutes client-side (`price x (1 + dist/100)`) would be a SECOND AUTHORITY over a number the live tier already computes (`live_tier.py:576-577`) | A backend-computed ABSOLUTE level, requested when the patterns view is active. ⛔ NOT in Increment 2 (owner ruling): do not request the patterns columns unconditionally and do not add an endpoint for this now. Until then the sheet opens with symbol (+ last price when streaming) and the member fills the rest. **⛔ CLOSED-BLOCKED (Increment 7).** Re-measured, not re-litigated: the absolute level exists only server-side (`live_tier.py:576` `_recover_pattern_level`) and is never emitted — the client has the distances and nothing else. So the only correct implementation is an `api/` change, and `api/` is a hard stop this increment: the Railway `worker` service is GitHub-triggered on `/api/**` plus build files (CLAUDE.md, "**`watchPatterns` are set per-service in the Railway dashboard ONLY** … Worker patterns: `/api/**` + build files"; corroborated in `api/flow_worker_main.py`'s own header, "the api/** watch path"), so any `api/` edit redeploys `worker`. ⛔ The client-side derivation is NOT a fallback, and `live_tier.py:425-434` is why in the product's own words: "⛔ THIS IS THE ONE PLACE THE RECOVERY ALGEBRA FLIPS … Copying the `P₀/(1+d/100)` form onto this pair produces a level wrong by `(1+d/100)²`, silently, in the right ballpark, on a column that names a trigger price." Unblocks when `api/` is writable again. **⛔⛔ RE-EXAMINED UNDER THE WIDENED `api/` AUTHORITY (L3, member-launch charter) — STAYS CLOSED, AND THE REASON HAS CHANGED.** The mechanical stop is no longer absolute: the charter waived `api/` for `api/routers/auth.py` **on proof**, which establishes that an `api/` edit is a **priced deploy, not a bar** — the price is written out in `closure.md`'s L3 entry. This row cannot pay it with the one file it was given: the absolute is computed at `api/services/screener/live_tier.py:576` and re-emitted as a DISTANCE at `:619` (`put("pattern_entry_dist_pct", round((p_entry / p1 - 1) * 100, 2))`), so the fix lives in the screener's serialiser, which the waiver does not name. **⛔ AND THE FILE LIST WAS NEVER THE BINDING STOP.** This row already carries "⛔ NOT in Increment 2 (owner ruling): do not request the patterns columns unconditionally and do not add an endpoint for this now" — a product decision about what the Screener requests and what endpoints exist, not a deploy-cost question. Widening the file authority does not reach it. **CLOSED — product decision — the OWNER ruled "do not add an endpoint for this now"; it reopens when the owner reopens it, not when `api/` reopens.** |
| ~~D-33~~ | ~~`prefillStop` is not importable~~ | Plan-trade sheet (3.4) | **RESOLVED — SHIPPED.** Verified by measurement, not by report: `prefillStop` is exported from `pages/journal-2-0/lib/disciplineGuards.js` and `AddPositionModal.jsx:14` now IMPORTS it instead of keeping the module-private copy, while `PlanTradeSheet.jsx:32` imports the same one. ONE implementation, shared by the modal and the sheet — which is exactly what this row asked for, and the hub never re-implemented it. | —
| ~~D-34~~ | ~~No function computes R at a CANDIDATE stop~~ | Journal scrub readout (3.4) | **RESOLVED — SHIPPED.** `rAtStop(entryPrice, originalStopPrice, candidateStopPrice, side, shares)` is exported from `lib/journal-2-0/calculations.js:251` — in the calc module, beside the R functions it belongs with — and `journalSection.js:348` calls it. The forward-R opinion lives in the calc module, not in a gesture handler. | —
| D-35 | `HubConfirmPayload.fields` is unreachable — a confirm action has no steppers | Alert (3.3), any future `confirm` | **R-14.** `HubRoot`'s confirm branch builds its own payload from `action.label` + `confirmText(ctx)` and never asks the section for one, so the `fields` array `contracts.js` calls "the EQUAL path, not a fallback" is dead code. Alert therefore fires at the default value with no way to adjust it in the sheet. ⚠️ The contract asserting a capability the product does not have is the same shape as R-06 | `HubRoot` prefers `action.confirmPayload(ctx)` when a section supplies one, and passes the sheet's `values` to `onConfirm`. The Screener already has `alertConfirmPayload()` written, exported and tested, waiting for that one branch. ⭐ ACCEPTED AS-IS FOR INCREMENT 2 (owner, 2026-09-09): the action works, and the gap is a missing adjustment control rather than a wrong number. ✅ **CLOSED IN INCREMENT 7** (`inc7/p1-confirm-fields`): `HubRoot` prefers `action.confirmPayload?.(ctx)`, validates it before it becomes state, and the fallback forwards `run(ctx, values)`. Railed on the RENDERED sheet — `hub/confirmFieldsReachable.test.jsx` — because a test that the payload function was CALLED passes with the branch deleted again. ⚠️ Two sentences elsewhere called `fields` unreachable and were false the moment this landed. `registry.js`'s `notebook.templates` removal note is CORRECTED (Increment 7 / R-19 — the action is back as a `confirm` with a select, and the note beside it now records why it left and what brought it back); `RESUME-inc3.md` is still stale and is another stream's file. R-19's first return condition is met, and R-19 is CLOSED |
| ~~D-36~~ | Plan trade is a commit sheet whose action does not escalate | Plan-trade sheet (3.4) | Found while shipping the iOS visual escalation (Increment 7). `scan.planTrade` and `chart.planTrade` are `kind:'run'` with `escalate: false`, so `PlanTradeSheet` — which POSTs a planned trade — gets the ordinary `haptics.impact()` cue and, deliberately, no commit notice. B5's rule is that the escalation is about the SHEET, which would make these two escalate; but rendering the notice while the action says otherwise puts a second authority on "is this the serious kind", and flipping the flag changes the haptic every member feels | An owner ruling on whether planning a trade is a commit in the B5 sense. If yes, `escalate: true` on both actions and `PlanTradeSheet` renders `HubCommitNotice` — the notice is already written and railed; nothing else is needed ✅ **DECIDED 2026-09-11 — NO; `escalate` STAYS FALSE.** Plan trade writes to `hub_planned_trades` and **never a broker** (the registry says so in its own helper). B5's escalation marks the writes that change a live position — move stop, breakeven, close. A plan is a record of intent that cannot lose money, and flipping the flag would change the haptic every member feels on every Plan trade to say otherwise. `PlanTradeSheet` correctly renders no commit notice; the notice stays written and railed for the actions that do escalate. |
| ~~D-37~~ | ~~Cursor contrast floors in the light theme~~ | all | **RESOLVED — SHIPPED (Increment 7).** ⚰️ **THE ROW WAS FILED ON A STALE NUMBER, AND THE MEASUREMENT SAYS SO.** It recorded `--text-muted` at 4.18:1 on the cursor highlight. That figure is real but belongs to `--hub-glass-tint-strong` — the **Actions button**, whose ink is `--text` (10.354:1) — and D-29 had already moved the cursor onto its own `--hub-cursor-fill` at 12% **earlier in this same increment**. On the shipped fill the ink floor is **5.000** (light), 8.458 (dark), 10.142 (oled): already AA. I filed it without re-measuring against the merged tree — the same stale-premise mistake this increment corrected on D-09. | **The real gap was the OTHER floor, which nobody had measured.** Two floors pull opposite ways: **A** the row's dimmest ink on the cursor FILL ≥ 4.5:1 (WCAG 1.4.3), **B** the cursor OUTLINE vs the plain row ≥ 3.0:1 (WCAG 1.4.11 — the clause governing information that identifies a *state*, so 3:1 is the standard's number, not one chosen to be passed). The outline borrowed the knob's `--hub-rim-highlight` at 35% = **2.202:1 on a white row**. The knob's rim sits on glass; the cursor's sits on a page the hub does not own, so one value could not serve both. ⛔ **The fill cannot carry both floors** — swept: A holds only to 16% (where B is 1.394), B needs ~48% (where A has collapsed to ~2.5). That is why these are two tokens. **Fix:** `--hub-cursor-outline` decoupled and declared at 50% at `:root` — light 3.353, dark 5.331, oled 5.281, the smallest step clearing every theme — with **no `[data-theme]` variant, so no theme island needs a pin**. `--text-muted` untouched: it is the product's, not the hub's. Railed in `styles/tokens.test.js` (both floors computed from the token values, per theme), mutation-proved three ways: the pre-D-29 20% fill fails A; a 40% fill fails A in all three themes (the proof the fill cannot be darkened to help B); the outline reverted to the knob's rim fails B in all three and the own-token guard. Glass row G3-2 still asks a human whether it reads. |
| D-38 | Undo-capable toasts may need a longer or pinned duration | scan (Flag), any future undoable action | ⛔⛔ **FILED AS D-38, NOT D-35 — the owner's instruction said "log D-35" and that id is TAKEN** (`HubConfirmPayload.fields`, closed in Increment 7). Re-using it would have put two different items behind one name in the ledger everything else cites; the row is recorded under the next free id and the collision is recorded with it. **The item:** Flag's Undo lives exactly as long as its toast — 2200ms, from `useJournalToast`'s own timeout — because the button is rendered only while there is a message to explain it. 2.2s is short for an undo, and the member most likely to need one (a mis-flick on a phone) is the least likely to be looking at the corner when it appears. ⭐ ACCEPTED AS-IS FOR THIS INCREMENT (owner, 2026-09-12) | A **per-message duration** on the shared journal toast, so an undoable message can outlive an informational one — or a pinned toast that only a tap dismisses. ⛔ **Five other doors render `JournalToast`**, so this is a change to a shared component and not the hub's to make alone. **Decide with member feedback**, not from the desk: nobody has yet reported missing an undo. |
| D-39 | **The chip yields to page-level fixed furniture** | `HubChip.jsx` (hub-side ONLY) | ⛔⛔ **HUB-SIDE, NEVER A `journal-2-0/**` EDIT — rule 12.** The tempting fix is to move the Journal's "Log a trade" FAB; that is another workstream's file and the hub does not get to relocate a page's own controls. The hub is the newcomer on these screens and the hub yields. **Opened by G3-18**, 2026-09-12, after G3-15's fix landed: G3-15 is PASS (27/27 clear of the Actions button) and this is the same class on the opposite side — at max width the chip's left edge reaches `EDGE_OFFSET = 24` and lands under `JournalLogFab` (journal, notebook) or a `span` (breadth @360). ⚠️ Believed to predate #109 by reasoning (`inset` cancels in `maxWidth`, so the left edge is `EDGE_OFFSET` before and after), **not measured on the pre-fix build**. ⭐ Severity cosmetic-plus per owner ruling: the covering element stays on top and tappable, so nothing is unreachable; the chip's readout is partly hidden. Non-blocking for stage 1 and stage 2. | **The approach, as ruled 2026-09-12:** at layout, test `elementFromPoint` at the chip's intended LEFT edge; if a non-hub, non-page-background element is there, shrink the chip's `max-width` until it clears, with a **floor at `.chipMode`'s width** (the mode name must survive; the tap hint may yield, which is the rule `.chipHint` already follows). Re-run the probe on `ResizeObserver`, the same trigger `HubActionsButton` already uses to report its width.  ⚰️⚰️ **CORRECTED 2026-09-13 — ONE OF THE TWO SYMPTOMS WAS THE INSTRUMENT, NOT THE PRODUCT. The acceptance target is 6 pairs, not 7.** Re-measured on production `58c63b806` after `hub_chip_clearance.py` was taught to wait the **cinematic intro animation** out before sampling. That overlay runs ~9.3s on every real page load, paints over the entire app, and the sweep does a page LOAD per (mode, width) — so some readings were taken straight through it. Its capability pills are anonymous `<span>`s, which is why the old run reported *“breadth @360: 56/56 points → span”*: **a full-screen animation written down as page furniture.** With the wait in place and nothing else changed, `breadth @360` reads *“56 points, all chip”* — clear. ⛔ **The `span` (breadth @360) half of this row is WITHDRAWN.** The `JournalLogFab` half is CONFIRMED and unchanged: journal and notebook at 360, 375 and 430, coverer reported by its own `aria-label`, *“Log a trade”*. The probe now names the overlay `INTRO-ANIMATION` instead of letting a tag name stand in for it, and a reading taken through it is reported as an instrument artifact rather than a defect — still a failure, because an invalid measurement must never read as a pass. ⛔ **ACCEPTANCE TEST IS THE 6 FAILING PAIRS** (was 7), and the instrument already exists: `tools/hub_chip_clearance.py --base <deployed>` must go from 6 failures to 0 across journal + notebook at 360/375/430, with the other 21 pairs still passing. Evidence of the current state: `g3-15-clearance-sweep-2026-09-13.md` (the correction, with both runs side by side), superseding `g3-15-clearance-sweep-2026-09-12.md`. ⛔ Do NOT attempt on `launch/closure`. |
| D-40 | **The Notebook note list exposes no per-note identifier on a phone** | `journal-2-0/**` (NOT this branch — rule 12) | ⛔ **This is why the iOS-17 preview path could not be exercised on glass, and it was predicted from source before the session rather than discovered by flailing.** `NoteCard.jsx` carries `data-note-card-id` (and `data-trashed="true"` when trashed), but `NotesTableView.jsx` renders through `ResponsiveTable` with `rowKey={(n)=>n.id}` — a React `key`, which **never reaches the DOM** (`ResponsiveTable.jsx:41`). On an iPhone 15 Pro / iOS 17.6 at 393, with one note present and the sidebar itself reporting **"All notes 1"**, `document.querySelectorAll("[data-note-card-id]")` returned **0** — before and after selecting the folder and after toggling the folders panel. ⭐ So no automated check can open a specific note on a phone by a stable selector, and the only remaining route is matching rendered text, which the owner has ruled out. | Give the phone list the same `data-note-card-id` the card view already has (or put the row key on the DOM in `ResponsiveTable`). Acceptance: on a real phone with N notes, `querySelectorAll("[data-note-card-id]").length === N`, and trashing one flips its `data-trashed` to `"true"`. ⛔ Belongs to the Notebook workstream. |
| D-41 | **Two corrections owed to `iteratorGlobalFloor.test.js`, both blocked by rule 12** | `app/src/pages/journal-2-0/lib/iteratorGlobalFloor.test.js` (Notebook workstream) | ⛔ **Neither is done, and saying so plainly matters more than the fixes.** **(a) The `Array.fromAsync` since-note is wrong.** It reads `since: 'Safari 18.4'`; a real iPhone 15 Pro on **Safari 17.6** reports `typeof Array.fromAsync === "function"` (measured 2026-09-12). The entry should STAY — the rail is conservative in the safe direction and the declared floor is iOS 16, where it genuinely is absent — but the annotation should say what was measured. **(b) The bundle-scan test needs an explicit per-test timeout.** `every built asset is clear` reads and regex-scans every built chunk (292 files, tens of MB) and **times out at the 15s default under a loaded six-shard gate**, while passing alone. It is already in `gate-baseline.json::load_sensitive`, but a rail that flakes on every full gate erodes trust in the one signal that caught a live member-facing crash. | Correct the since-note to the measured value; give that one `it(...)` an explicit generous timeout (the work really is large). ⛔ **Cannot be done from a hub or hardening branch**: `rule12Paths.test.js` has no branch-identity check (known-open B7), so ANY branch touching `journal-2-0/**` goes red, and its exception is exactly one file and one shape (`NoteCard.jsx` + `data-note-card-id`). Owner refused a waiver 2026-09-12, so this is handed over rather than forced. |
| ~~D-42~~ | **The surface matrix is blind to shorthand bindings — 10 shipped surfaces have no glass step, and 2 steps assert the opposite** | `tools/hub_surface_matrix.mjs` (joystick) | ⛔⛔ **THE INSTRUMENT BUILT TO MAKE MISSING GLASS STEPS VISIBLE IS ITSELF BLIND.** `bindingsIn()` (`:81`) matches `` `key\s*:` `` — the COLON form only — so ES6 shorthand properties are invisible. `wireSection.js:282-285` and `homeSection.js:240+` pass all four bindings shorthand and report **"— none —"**; `journalSection.js:644-645` passes `onScrub,`/`onScrubCommit,` shorthand so the journal **Scrub** — the stop-adjust flagship — is missing from the matrix. `glass-acceptance-steps.md` is GENERATED from this, so 10 shipped surfaces have no step AND `GS-wire-0` publishes *"Tap, double-tap and scrub do nothing here"* for a mode that wires all four. An operator would file a FAIL against working code, or PASS a broken one. ⭐ Its own `--self-check` case 2 uses the colon form, so the control never exercises shorthand — `lesson_a_fixture_that_cannot_distinguish_is_not_a_rail`, inside the instrument meant to prevent it. | Accept shorthand in `bindingsIn` (`/(^|[^\w.])key\s*[:,}]/`), add a self-check case for the shorthand form, regenerate `surface-matrix.md` and `glass-acceptance-steps.md`, and add the 10 new rows to `owner-run.md` or the G5 sweep. ✅ **RESOLVED — SHIPPED 2026-09-13**, `6d2945d84`, merged `d153215d0`, live in `4beb06c00`. `bindingsIn` reads an **acorn parse tree** (the parser `screener/reachable.test.js:48` and `chart/engine/__tests__/singleWriterIndex.test.js:51` already import, resolved from `app/package.json` — no new dependency). Only the properties of an `ObjectExpression` count, so shorthand, the comma-list form and quoted keys resolve while a destructure, a member read, a computed key and a string literal never can; a missing parser **throws** rather than falling back. Self-check 9 → **20 cases**, count derived not typed, including the shorthand fixture that never existed and a control on the real `wireSection.js`. **Mutation-proved**: restoring the colon-only regex turns eleven cases red BY NAME and leaves nine green. Sheet regenerated: **95 → 107 steps**; by step identity 93 unchanged, 14 gained, 2 lost — both the false "declares no gesture bindings" rows. ⭐ Step ids were also made stable (`GS-<mode>-b1` for bindings, actions keep their own sequence): the single counter would otherwise have renumbered every action row in seven modes on any future binding change. ⚠️ The step TEXT is still wrong for some rows — that is **D-44**, filed separately. |
| ~~D-43~~ | **`notebook` has no Reverse** | `app/src/hub/sections/notebookSection.js` (joystick) | Spec §C3 notebook says *"Primary: next note. **Reverse: previous note.**"* The controller declares `onTap` (`:496`), `onScrub` (`:501`) and `readout` (`:519`) but **no `onDoubleTap`**. Every other cursor-bearing mode has one, and no D-number covered this. ⚠️ It was invisible until now partly because D-42 hides bindings anyway. | Either wire `onDoubleTap` to the list adapter's `prev()` — the same one-liner every other section uses — or amend spec §C3 to drop Reverse for notebook and say why. ✅ **RESOLVED — SHIPPED 2026-09-13**, `6d2945d84`, merged `d153215d0`, live in `4beb06c00`. `onDoubleTap` wired symmetrically with `onTap` — same clamp, same computed target, `prev()` through the shared cursor whose identity is already the note id. Its rail drives the **registered config** rather than the two steps a tap is made of (the R-05 shape), reads the §C3 promise out of the spec of record rather than restating it, and asserts the config against `contracts.js` behind a non-vacuity control. **Mutation-proved**: deleting the binding fails exactly those two cases and leaves 17 green. ⛔ Its glass row `GS-notebook-b2` is INCONCLUSIVE-TRANSPORT and sits on `owner-run.md` §B — a double-tap needs two presses inside 280 ms and the Live mirror floor is 260–427 ms per gesture. **It has never been on glass.** |
| ~~D-44~~ | **The glass sheet's expected results are derived from the binding KEY alone, so two of them are wrong** | `tools/hub_surface_matrix.mjs` (joystick) | ⛔⛔ **FOUND ON GLASS, 2026-09-13, AND THE FIRST ONE MAKES AN OPERATOR FIRE AN ACTION INSTEAD OF MEASURING A ROW.** `glassSheet()` builds each binding step from a lookup on the key (`onTap`/`onScrub`/…) and never consults the gesture model or the mode's controller. Two consequences, both measured: **(a) the scrub step omits the mandatory hold.** It reads *"Press and drag along y to scrub"*, but `useJoystick.js:408` is explicit — *"A hold that turns into a drag is Scrub — never a fan push, whatever the distance (C1)"*, `HOLD_MS = 500`. A drag without the hold is a **fan push resolved by direction**: on `/dashboard` it fired `home.scan` and navigated to Screener, and on `/morning-wire` it fired `wire.voice` and raised a native microphone permission prompt. ⚠️ The Screener navigation was briefly recorded as a scrub-commit PASS before the microphone prompt exposed what the gesture really was. **(b) `home`'s four expected results describe a mode that does not exist.** They promise cursor-stepping, *"the page follows it"* and *"the landing row is revealed"*; `home` NAVIGATES — `onTap` → last-used section (inert on a first visit, spec §C3:915), `onDoubleTap` → Wire, `onScrubCommit` → the section under the cursor — and `homeSection.js:175` says in so many words that nothing on the page moves during its scrub. ⭐ This is D-42's defect class one layer up: D-42 was the sheet not knowing a binding EXISTS; D-44 is the sheet not knowing what it DOES. | Two changes, both in `glassSheet()`. **(1)** Put the hold in the scrub step text — *"Press and HOLD for ~0.5s, then drag along y"* — derived from `HOLD_MS`, never retyped. **(2)** Let a mode supply its own expected results, or derive them from what the controller reaches: the registry already carries `tapHint` and `cursor`, and a mode with no `cursor` (like `home`) plainly cannot "step a cursor". A `--self-check` case should assert that a cursor-less mode never publishes a cursor-stepping expectation. ⛔ Filed 2026-09-13 by the 15 Pro run (`15pro-new-rows-2026-09-13.md`); NOT fixed there, because the ruling scoped that session to D-42, D-43 and docs. The affected rows are INCONCLUSIVE-TRANSPORT on `owner-run.md` §B regardless, so nothing is being run against the wrong text today. ✅ **RESOLVED — SHIPPED 2026-09-13**, `462d8f3d7`, merged `2b4fc75cc`. The step text is now derived from THREE sources, none of them the generator: `constants.js` for the numbers (the scrub step reads the real `HOLD_MS`, the double-tap step the real `DOUBLE_TAP_MS`), the CONTROLLER for the verb (`bindingSemantics` resolves shorthand → `const` → `useCallback` argument, then classifies **navigate** / **cursor-step** / **cycle**, with `navigate` winning because a route change beats a declared cursor — that is `home`), and **spec §C3** for the noun, quoted from the highest `00-master-spec-v*.md` present. An unresolvable binding prints **UNRESOLVED** rather than a guess. It also detects an early-return guard, so `home`'s Primary now carries the inert-on-a-first-visit caveat spec §C3:915 requires. Self-check **20 → 36 cases** with fixtures for a navigate, a cursor and a cycle mode plus the shorthand indirection, the guard and its control, and a control on the real tree. **Mutation-proved** by deleting the hold sentence: exactly three cases red by name, 33 green. Regenerated at **107 steps with step ids identical to master**. |
| ~~D-45~~ | **Only ONE action in the registry is flick-guarded — RULED, and the reason is recorded** | `app/src/hub/registry.js` (joystick) | ⭐ **Surfaced 2026-09-13 while correcting `owner-run.md` §A, which had assumed five actions were guarded.** `flickable: false` is opt-in per action (spec §396: *"default true; false = deliberate selection only, never a <120ms flick"*), and exactly one action carries it: **`journal.close`**. `journal.moveStop`, `journal.breakeven`, `journal.planTrade` and `scan.alert` are all `flickable: true`, so a sub-120ms flick fires them. ⛔ **This is not a safety hole and should not be read as one.** All five are sheet-mediated — `openStopSheet` (`journalSection.js:610`, `:612`), `openPlanSheet` (`:620`), and `scan.alert` is `kind: 'confirm'` — so the gesture opens a sheet and **nothing is written to a live position until a second, deliberate press on that sheet's commit button.** The present design is therefore defensible on its own terms. ⚠️ What was never DECIDED is whether a flick should be able to open those sheets at all: three of them commit against a live position once confirmed, and `journal.close` was singled out for the guard without a recorded reason for excluding its neighbours. | **A ruling, and it needs glass evidence first.** `owner-run.md` §A now measures the real behaviour at all five targets, so the owner will have the distribution in hand: how often a real finger's flick lands under `FLICK_MS` at a committing bubble. If that is common, adding `flickable: false` to the other three journal actions is a one-line registry change each, already supported by `FLICK_GUARDABLE_KINDS`. If it is rare, the current single guard is the right amount and this row closes by design. ⛔ Do not change the registry before §A comes back — that would be a product change made from a guess, and the measurement is twenty minutes away. ✅ **RULED 2026-09-13 — CLOSED AS DESIGNED, and the reason is now in the code.** `journal.close` stays the **only** `flickable: false` action. The four neighbours are SHEET-MEDIATED: a flick onto any of them opens a sheet and nothing happens without a second, deliberate press on that sheet's own primary — **the sheet IS the guard**, so a flick guard in front of it would be a second lock on the same door. `journal.close` is singled out because it **ends a position and writes a permanent `j2_trades` row**, so it gets belt-and-braces: the flick opens the fan, and only then does the form appear. ⛔ **Do not "fix" the asymmetry by adding `flickable: false` to the other four — it is a decision, not a gap.** The ruling is recorded beside the declaration itself (`registry.js`, above `id: 'journal.close'`), because an unrecorded asymmetry reads as an oversight and twice already a reader assumed the neighbours were guarded too. ⭐ Revisit **only** if `owner-run.md` §A — which measures all five on real glass for exactly this purpose — shows accidental flicks reaching a sheet at a rate a member would notice. |
| ~~D-46~~ | **Spec §C4 promises focus returns to the KNOB after a sheet closes. Nothing focuses the knob.** | `app/src/hub/**` (joystick) | ⛔ **Found by the scope reconciliation, 2026-09-13.** `00-master-spec-v1.6.md:952` states: *"Any action that opens a sheet returns focus to the knob on close."* Measured on master: `HubKnob.jsx` contains **no ref and no focus handling**, and `HubRoot.jsx` never calls `.focus()` anywhere. What actually happens is inherited from `components/mobile/Sheet.jsx:81`, which captures `document.activeElement` when the sheet opens and restores it at `:126`. ⭐ **So the promise holds only on the path where the member had already focused something.** Through the Actions button (the no-drag door) focus returns to that BUTTON, not the knob — close, and arguably better. But on the **gesture** path — drag, flick, or tap-to-fire — nothing was focused, so `document.activeElement` is `<body>` and focus restores there. For a screen-reader user who opened a confirm sheet by any route other than the button, the cursor lands at the top of the document. ⚠️ There is no rail for this in `app/src/hub/**`: a grep for focus assertions finds only the auto-hide-on-text-input tests, which are a different question. | Either **(a)** give `HubKnob` a ref and focus it before opening any sheet — which makes `Sheet.jsx`'s existing restore do exactly what §C4 says, with no new focus machinery — or **(b)** amend §C4 to promise what the product does: *focus returns to whatever opened the sheet, and the Actions button is the reference path.* ⛔ (b) is a spec edit, not a code change, and may well be the better answer; what is not acceptable is the current state, where the spec asserts a behaviour no code produces. Needs a rail either way. **Owner: joystick.** ✅ **RESOLVED — SHIPPED 2026-09-13, `d79e32d33`.** Option (a), and the mechanism matters: `HubRoot` puts the knob into `document.activeElement` **before** an action runs, so `Sheet.jsx`'s existing capture-and-restore does the right thing with **no change to `Sheet.jsx` at all** — one authority over focus restore instead of two. All four sheets the hub can open mount that same component, so one call covers every path including `StopConfirmSheet` and `PlanTradeSheet`, which the Journal page merely mounts (they live in `app/src/hub`, so no rule-12 edit was needed). ⛔ **The second half was the one nobody predicted:** the knob carried no `tabIndex`, so `focus()` on it was a silent no-op — shipping the call alone would have produced a second no-op that a spy-based test would have passed. `tabIndex={-1}`, not `0`, because the hub is mobile-only and §C2 says there is no keyboard in this build. The knob is now named **"Joystick, <mode>"**. Rail `hub/knobFocusReturn.test.jsx` asserts `document.activeElement`, never a spy; **mutation-proved twice** — remove `tabIndex` → 3 of 4 red with the NAME case green, remove `onBeforeOpen` → 2 of 4 red with focusable + name green, which is the original defect reproduced exactly. |
| ⏸️ D-47 | **The per-mode action editor was promised in v1.1 and never built — DEFERRED BY SPEC AMENDMENT** | `app/src/pages/settings/JoystickSettingsCard.jsx` (joystick) | ⛔ **Found by the scope reconciliation, 2026-09-13.** `00-master-spec-v1.1.md:168` promised *"Settings → Joystick: enable/disable, left- or right-handed (mirrors the hub and fan), haptics on/off, **and a per-mode editor to reorder or remove actions** (persist to the user's settings table; keep the registry as the source of allowed actions)"*, and `:77` that *"User overrides (reorder, remove, handedness, haptics) are stored as a JSON patch on top of the registry, never as a copy"*. ⭐ **The storage half shipped and the editing half did not.** `useHubSettings.js:75` carries `overrides: Object.freeze({})` with the patch semantics documented at `:37-38`, and the settings card reads it — but `JoystickSettingsCard.jsx:222` says in its own words *"`overrides` IS SHOWN, NOT EDITED"*: the UI offers a COUNT (`:225`) and a **Reset to the registry** button (`:231`), and no way to reorder or remove anything. Every other Phase 4 control from that same sentence shipped (enable/disable, handedness, haptics, hold, travel, sticky, contrast). ⚠️ **The reduction is nowhere in any ledger** — not `deferred.md`, not `71-open-items-proposals.md` — so it has been carried as silently done. | **A ruling first, because this may be correct as-is.** An editor that can remove actions is a surface that can leave a member with an empty fan, and the registry's ring caps (`OUTER_MAX` 5 / `INNER_MAX` 4) plus `validateRegistry` currently guarantee a well-formed fan for everyone — an override patch would move that guarantee to runtime. So: **build it** (an editor over the existing `overrides` patch, validated through the same `validateRegistry` path), or **amend the spec** to say the patch exists for the product's use and is not member-editable, and close this by design. ⛔ What it must not stay is unrecorded. **Owner: joystick.** ⏸️ **RULED 2026-09-13 — DEFER BY SPEC AMENDMENT, not by building it (`d79e32d33`).** `00-master-spec-v1.6.md` §8 now records that what shipped is the storage half plus a read-only surface (count + **Reset to the registry**), and that **reorder/remove is deferred post-launch pending member feedback**. ⭐ The JSON-patch storage stays exactly as specified so the editor can land later **without a migration** — that is why the patch was never a copy. ⛔ Not member-facing today: with no editor `overrides` is always `{}` for every member, so `validateRegistry`'s ring caps still guarantee a well-formed fan. An editor that can remove actions moves that guarantee from build time to runtime, which is the real question the deferral holds open. Reopen on member feedback. |
| ~~D-48~~ | **Ground rule A-1 — the `CLAUDE.md` "Joystick hub" section was never appended** | `CLAUDE.md` (joystick) | ⛔ **Found by the scope reconciliation, 2026-09-13, and it is the first rule in the spec.** `00-master-spec-v1.6.md:49-50`: *"Read the repo's `CLAUDE.md` first. At the end of the build, append a 'Joystick hub' section: registry location, how to add a mode, tuning constants, known gaps."* Measured on master: **the section this rule asks for does not exist.** ⚠️ Stated precisely, because the overstated version is easy and wrong: `CLAUDE.md` DOES carry three joystick-titled headings — the preview kill switch (`:1807`), the dismissable-control lesson (`:1836`) and the open B7 rail (`:2281`) — and 29 case-insensitive `joystick` matches overall. But every one of them is a DEPLOY flag, an incident lesson, an open rail or a passing mention inside somebody else's section (device testing `:834`, `:891`, `:1013`; the smoke account `:700`; the branch roster `:1481-1495`; the sandbox incident `:1628`). **None is the onboarding section A-1 specifies.** Of the four things the rule names: **"add a mode" → 0 hits**, **"known gaps" → 0 hits**, `constants.js`/`FLICK_MS` appears once and only as a BrowserStack example, `registry.js` twice and neither in a joystick context. ⭐ **This is the highest-leverage row in the reconciliation despite being the smallest.** The programme is closed and its plan files are thirty-odd documents deep; `CLAUDE.md` is the one file the next engineer actually reads, and it currently tells them where the hub was TESTED and nothing about how it WORKS. The feature this rule exists to protect is the one nobody is left to explain. | Append the section the rule specifies, four parts, each derived rather than retyped: **registry location** (`app/src/hub/registry.js`, with `validateRegistry`'s ring caps); **how to add a mode** (a data change — spec §2's closing rule — plus the controller contract in `contracts.js` and the `surfaceMatrixIsCurrent` regeneration step, which is the part a newcomer will miss); **tuning constants** (point at `app/src/hub/constants.js`, never restate the numbers — the generator learned that lesson as D-44); **known gaps** (D-38, D-39, D-40, D-41, D-46, D-47 and the owner run). ⚠️ It is a `CLAUDE.md` edit, which this build has treated as in-scope throughout, but it is not a five-minute job done badly: a stale onboarding section is worse than none, and this file's own history is a catalogue of exactly that. **Owner: joystick.** ✅ **RESOLVED — SHIPPED 2026-09-13, `d79e32d33`.** `CLAUDE.md:1479`, *"Joystick hub — the one section to read before touching it"*, **137 lines** (cap was 200). It carries all four parts the rule names and every claim cites a file: what it is and where it mounts; the registry as single authority with the action field table; `contracts.js` and the arity rail; the gesture vocabulary **pointing at `constants.js` rather than restating any number** (the D-44 lesson); `PREVIEW_MODES`, `ROLLOUT_STAGE` and the regenerate-in-the-same-commit rule; an eleven-row rail table saying what each rail CATCHES; the kill switch, rollback runbook, Live-only device policy and the login-link procedure; the ledgers; and the open D-numbers. ⭐ All 13 referenced rails were verified to exist before it was written, and the four required parts were re-measured after insertion ("add a mode" and "known gaps" had both been 0 hits). |

---

## PART 3 — the design pass (2026-09-17, owner ruling R4/R7–R10, branch `design/bar-and-strong-cut`)

Not Increment 7. The owner used the shipped hub and said *"tons of bugs … too glitchy and not
smooth … looks like a $5 project"*, which opened a design and quality pass rather than a feature
increment. These are the rows it produced.

| # | Final state | Closing evidence |
|---|---|---|
| ~~D-49~~ | **CLOSED — `var(--color-border)` was used at two sites in the Joystick settings card and is defined NOWHERE in `app/src`.** An invalid `var()` inside the `border-top` SHORTHAND is invalid-at-computed-value-time, so the declaration takes its unset value and **no border renders**: both section dividers had been invisible since they were written. Fixed to `var(--border)`. ⚰️ And it inverts a finding I had WITHDRAWN — `design-bar.md` §4 records me falsely accusing this token after measuring `hub.module.css` mid-edit. The withdrawal was right about that file and wrong about the token: it IS dead, in a `.jsx` inline style, which a sweep over `*.css` structurally cannot see. | `2aba54745`; rail widened in `tokens.reachable.test.js`, mutation-proved by restoring master's actual state |
| ~~D-50~~ | **CLOSED — the hub had TWO spring curves 0.16 of overshoot and 20 ms apart**, a difference no thumb can feel: one motion typed twice and drifted, plus three transitions relying on the browser default by omission. All eight now name a token. ⛔ The count is pinned at **two**, so collapsing them is a deliberate visible act in a tuning commit, never something that arrives inside a rename. | `hubMotionTokens.test.js`, mutation-proved four ways |
| ⏸️ D-51 | **OPEN, and NOT the joystick's.** `reachable.test.js` reds on `app/src/surfaces/manifest.js` — built, committed, reachable from no route. **Unassigned**, and newer than the R-29 note in `CLAUDE.md`, which names only `lib/context/focusDivergence.js`. Recorded so the next reader does not assume the one known red is the only one. | `git show origin/master:app/src/surfaces/manifest.js` exists; this branch touches nothing under `surfaces/` |
| ⏸️ D-52 | **OPEN, and rule 12 forbids this branch fixing it.** `tapFloor.test.js` reds on `journal-2-0/components/notebook/CaptureDialog.module.css`: `.actions` declares a finger target at ≤640px but not at ≤1024px, so **tablet is left short** — the exact "a floor only at ≤640 leaves TABLET broken" defect the touch-tier rule exists for. **Notebook's.** | the file is byte-identical to `origin/master` here (hash-compared, not `git status`); this branch touches nothing under `journal-2-0/` |
| ⏸️ D-53 | **OPEN, deliberately undecided from a table.** R4's cut leaves `home` with a **one-bubble fan** (Voice — it has no Home action, being home already), and `calendar`, `breadth` and `flow` with only the universal pair. A fan of one may well be worse than no fan: drag-to-open ceremony for a single destination. ⛔ Whether that reads as restraint or as ceremony is a question for the **R8 critique loop**, which looks at the rendered frame. Deciding it here would be judging the artifact instead of the product. | `registry.js` `STRONG_CUT` block comment; `design-bar.md` §5 "Open, for the R8 critique loop" |
| ⏸️ D-54 | **OPEN — the design bar's own M1–M4 have never been measured.** `hubSmoothness.js` is built, wired behind the G0 gate and mutation-proved; it has produced no number from a device. ⛔ And R9 stands: a screen mirror **never** judges flick, hold or scrub (measured floor 260–427 ms per gesture against `FLICK_MS` 120). Needs one gesture on real glass. | `w3-static-sweep.md` §4 |
