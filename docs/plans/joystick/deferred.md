# Joystick Hub — deferred

Everything cut from the spec, with the smallest change that would enable it. Nothing here is abandoned;
each row is a decision to make later with a known price. Evidence for every claim is in
`10-wave0-discovery.md`.

## FINAL STATE — Increment 7 (BACKLOG ZERO), part 1: rows closed without new code

Every row below is closed by a line already written in this file or in a ruling — not by a
decision made tonight to be rid of it. The quoted fragment is the closing evidence. Rows
closed by Increment 7's own work are in part 2, at the bottom, beside their commits.

⛔ **The `api/` stop, quoted once and referenced by every row that needs it.** Read live from
the Railway service manifest (`railway status --json`, read-only), 2026-09-10: the **`worker`**
service's watch list is `['/api/**', '/requirements.txt', '/railway.json', '/nixpacks.toml',
'/Procfile', '/runtime.txt']`, and **`bars-api`**'s is `['api/**', …]`. So **any** edit under
`api/` redeploys both. (`flow-worker`'s list is 20 enumerated top-level `api/*.py` modules and
contains no `routers/` path, so it is NOT implicated — but the other two are enough.)

| # | Final state | Closing evidence |
|---|---|---|
| D-04 · D-05 · D-11 | **SHIPPED** in Wave 0.5. | struck through in the table below |
| D-33 · D-34 | **SHIPPED** — verified by measurement, not by report. | `disciplineGuards.js`, `calculations.js:251` |
| D-07 | **CLOSED-BY-SPEC** — "Each needs a destination built first. **Not a hub problem**." There is no `/sectors` route to navigate to. | this file, D-07 |
| D-12 | **CLOSED-BY-SPEC** — "Each needs a per-position affordance built first." | this file, D-12 |
| D-13 | **CLOSED-BY-SPEC** — "**Genuinely new product surface.**" No read/unread concept exists anywhere in the product; `is_new` is server-computed and global. ⭐ A client-only seen-set would be buildable, and is deliberately NOT being built: it would not follow a member between devices, so it would answer "have I seen this?" wrongly for the first member who owns two. | this file, D-13 |
| D-14 | **CLOSED-BLOCKED** — needs "an append-only log" of intraday history, i.e. `api/`. See the stop above. Its own row also says "**Large**". | this file, D-14 |
| D-15 | **CLOSED-BLOCKED** — "New column + **endpoint** + filter application". `api/`. | this file, D-15 |
| D-16 | **CLOSED-BLOCKED** — the fix is "Add a `?q=` read in **`NotebookTab.jsx`**", and this build has a standing zero-edit rule on that file. Nothing else reaches it. | this file, D-16 |
| D-18 | **CLOSED-BY-SPEC** — "**Nothing to build.** The hub branches on `providers.obsidian.connected` and deep-links `/settings?section=connections` when false." Obsidian is a device-push connector; there is no "sync now" to fire. | this file, D-18 |
| D-19 | **CLOSED-BLOCKED** — "A per-event subscription **endpoint** + UI". `api/`. | this file, D-19 |
| D-22 | **CLOSED-BY-SPEC** — "**Explicitly out of scope by gate decision** — one `// TODO(hub-analytics): emit here` marks the call site." No authenticated in-app event sink exists. | this file, D-22 |
| D-23 | **CLOSED-BY-SPEC** — "**Removed, not deferred** (owner ruling, Wave 0.5 gate): there are no tiers — one product, one price… do not carry a dead one now." | this file, D-23 |
| D-26 | **OWNER DECISION, not engineering** — "One new method on `ChartPane`'s ref returning pane rects — **needs an owner decision, not an engineering one**." The scrim ships its stated `calc(22% + 32px)` approximation meanwhile, labelled as an approximation. | this file, D-26 |
| D-28 | **CLOSED-BY-SPEC** — "**Nothing web-level exists.** A PWA/standalone display could suppress it; that is a different product decision." Android 10+ claims both edge swipes and a browser tab cannot opt out. | this file, D-28 |
| D-30 | **CLOSED-BY-SPEC — NOT A HUB TASK**, and it says so in its own title. "⛔ **Must be its own task** — it must not ride along with a UI feature branch." ~68 call sites across `api/**`, which is also behind the stop above. It remains a live PRODUCTION risk and should be scheduled on its own. | this file, D-30 |

| # | Item | Section | Why it is deferred | Smallest enabling change |
|---|---|---|---|---|
| D-01 | Draw (trendline tool) | chart | `expandDrawToolbar()` reveals the bar but selects no tool; `activeTool` is private `useState` (`StockChart.jsx:3911-3913`) | Add `selectTool(name)` to the existing `toolbarApiRef` surface, mirroring `expandDrawToolbar` |
| D-02 | Indicator set (EMA 9/21/50, VWAP, AVWAP) | chart | MAs live in a fixed 4-slot `cs.overlays[]` array defaulting to EMA9/EMA20/SMA50/SMA200 — there is no fifth slot, so EMA 9/21/50 means clobbering the user's SMA50/SMA200. Also **two different AVWAPs exist** (engine indicator with a named anchor vs. click-anchored drawing tool) | A product ruling on slot-clobbering, then `addInstance()` (`chart/engine/instanceControls.js:379-398`) for VWAP/AVWAP. Must first say which AVWAP is meant |
| D-03 | Alert at crosshair price | chart | `crosshairData` is private `useState` (`StockChart.jsx:3471`) with no external accessor; the spec forbids lifting it in this build | Publish the crosshair price through the existing `onDateNavApi`-style callback pattern. **Replaced in v1.2 by Alert at last price**, which is buildable today |
| ~~D-04~~ | ~~Horizontal scrub = pan~~ | chart | **RESOLVED — SHIPS (Wave 0.5).** `stepBar(dir)` (`StockChart.jsx:14510-14517`) takes an unconstrained numeric delta and is already exposed unmodified on `ChartPane`'s ref | — |
| ~~D-05~~ | ~~Compare (overlay QQQ)~~ | chart | **RESOLVED — SHIPS (Wave 0.5).** The mobile `handleStore` write reaches the render path; desktop's `setComparison` is the identical `{...cs, comparisonSymbols}` write, not a separate system | — |
| ~~D-06~~ | ~~Metric-group cycling~~ | breadth | **CLOSED-BLOCKED (Increment 7).** The named mechanism is inert for the default member and destructive for everyone else. `useBreadthCustomize.js` refuses every write on the `Default` preset — `setHiddenSet`: "`if (prev.activePreset === DEFAULT_PRESET) return prev  // immutable`", and `toggleHidden`/`resetActive` say the same — and `Default` IS the shipped state (`EMPTY_STATE = { activePreset: DEFAULT_PRESET, presets: {} }`). So a group-cycling bubble would answer a deliberate gesture with silence for every member who has never made a preset: the exact defect that REMOVED `breadth.sizeRule`/`breadth.snapshot` from this same fan (B2). For a member who HAS a preset it is worse — the write replaces their persisted `hidden` array (localStorage `uct.breadth.customize.v1`) with no restore. **The stop is doubled** (writer guard AND `hidden`'s reader guard); each alone survives mutation, so only the outcome is railed: `hub/deferredRowClosures.test.js`. A hub-local group filter instead of `customize.hidden` would be a second authority over "which columns are visible" and is not a smaller change | ~~Reuse the existing reachable `customize.hidden` set as a group filter~~ — measured inert; needs a writable transient visibility layer that does not exist |
| D-07 | Phase detail · Sectors · Scan-with-breadth-filter | breadth | No entry points exist. No `/sectors` route; no breadth-origin query param on the Screener | Each needs a destination built first. Not a hub problem |
| D-08 | Compare prior cycle | breadth | Exists only as the **admin-only** Analogues tab. ⛔ **AWAITING AN OWNER RULING — it is a product decision about who sees what, and Increment 7 deliberately did NOT make it.** Costed so the ruling is cheap: the admin-only-ness is ONE client-side list (`breadthSection.js`'s `BREADTH_ADMIN_TAB_ITEMS`, appended by `resolveBreadthTabs(isAdmin)`) plus the non-admin bounce at `Breadth.jsx:643`. **The DATA is already paid-gated, not admin-gated** — `/api/breadth-monitor/analogues` depends on `require_paid` (`api/routers/breadth_monitor.py:557`), so every paid member's session can already fetch it and only the client declines to show it. Both facts railed in `hub/deferredRowClosures.test.js` | **Option A — make it member-visible:** move `analogues` from `BREADTH_ADMIN_TAB_ITEMS` into `BREADTH_TAB_ITEMS` and delete the `:643` bounce. **Zero backend work.** The price is editorial, not engineering: the tab's headline is a forward-return claim ("Last time breadth looked like this was {date} — SPY gained {n}% over the next month") shown to members, and the breadth tab count changes from 5 to 6 for everyone. **Option B — rule that admin-only is acceptable:** zero work; `resolveBreadthTabs` already carries the two-length list correctly and the hub walks the resolved one. This row then closes as CLOSED-BY-SPEC on that ruling |
| ~~D-09~~ | ~~Sort~~ | scan | **CLOSED (Increment 7) — and the reason was CORRECTED at integration.** ⚠️ This row first closed citing `HubConfirmPayload.fields` as "unreachable dead code". **That stopped being true the same night**: P1 closed D-35, and `fields` now reaches the member. The stream that wrote this closure branched before that landed — a stale premise carrying a verdict, which is exactly what this program keeps re-finding, so it is corrected here rather than left to read as evidence. *The hub half is still blocked, on a NARROWER fact:* `fields` offers `type: 'number'` and `type: 'text'` only, and choosing a COLUMN is a choice among a fixed set — there is no select, and inventing one to sort a table is not the smallest enabling change. *The page half is UNDECIDED, not unbuilt:* passing the props is one line, but the row's own sentence defers the part that matters — "then **decide what control renders them**" — a Screener product decision, and v1.6 §scan records the state it leaves: "Sort is deferred — the phone has no sort UI at all." The desktop/phone prop asymmetry is railed in `hub/deferredRowClosures.test.js` so it goes red the day a phone sort control appears | ~~Pass the props already computed two lines below~~ — needs (a) a field type that can offer a CHOICE, or (b) an owner decision on the phone control |
| D-10 | Chip reading the active scan's name | scan | **HALF ALREADY SHIPPED — measured, not reported.** `screenerSection.js:149` exports `activeScanName(filters)`, reading `filters.scan.label`, and `chipLabel()` renders "Powerplay · 3/41"; the chip already names the scan whenever the member arrived through `ScannerShell`'s `onUseScan(hash, name)` door, which writes `{op:'in', value: hash, label: name}`. **CLOSED for the remaining half (Increment 7)**, on the row's own sentence: *"Persisting it also touches the shared URL codec (`shell/specUrl.js`) — a real, if small, contract change."* That door is `ScreensManager.jsx:308` (`const apply = s => { onApply(s.spec) }`), which HAS `{id, name}` at the call site and drops them at `useScreenSpec.js:82-91`. ⚠️ And the codec is not the whole price: once threaded, the identity goes STALE the moment the member edits one filter, so the chip would name a screen the spec no longer is — a second authority over "which screen is this". Deciding WHEN identity is dropped is a product rule nobody has written | Thread `{id, name}` through `applySpec`, **plus** a stated rule for when an edit clears the identity. Persisting it also touches the shared URL codec (`shell/specUrl.js`) — a real, if small, contract change |
| ~~D-11~~ | ~~Cursor over unloaded virtualized rows~~ | scan | **RESOLVED — SHIPS (Wave 0.5): cheap.** `forwardRef` + one `useImperativeHandle` on `VirtualResults.jsx` and `ResultCards.jsx` (both are virtualized, including the phone card list). Now exception (d) in §2 | — |
| D-26 | LWC pane geometry for the scrim | chart | Pane rects exist inside `StockChart.jsx` but reach no host ref; a new imperative method is outside the named exceptions. The scrim uses the app's own `calc(22% + 32px)` fallback instead, stated as an approximation | One new method on `ChartPane`'s ref returning pane rects — needs an owner decision, not an engineering one |
| D-27 | Wire/Journal green separation | all | The three greens were split into siblings (home `#67DB44`, journal `#4FB833`, wire `#9FE887`) and all clear 3:1 against every glass tier. But **Wire and Journal co-occur in Home's fan and their separation from each other is only 1.74** — weak for a hue-blind viewer. Icons, ring radius and bubble size remain load-bearing, not decorative | Fix candidate if Phase 3 device testing shows real confusion: **shift Wire's hue toward teal (~`#8FE0B0`)**, which separates it from Journal on hue as well as luminance |
| D-28 | Android edge-gesture suppression | all | Android 10+ claims swipes from **both** edges and a regular browser tab cannot opt out. Mitigated by design margin (`right: 24px`) plus the 20px deferred-movement rule in §2c | Nothing web-level exists. A PWA/standalone display could suppress it; that is a different product decision |
| D-12 | Stats · Tag setup | journal | No per-position entry point exists. `SetupStatsPanel` renders only inside the Add modals; setup is editable only through the full `EditPositionModal` | Each needs a per-position affordance built first |
| D-13 | Unread state · "next unread" · "Mark read" | catalysts | **No read/unread concept exists anywhere.** `is_new` is server-computed and global, not per-user | A per-user read table or a client seen-set. Genuinely new product surface |
| D-14 | Timeline scrub | catalysts | The table is a top-20 curated snapshot upserted per `(market_date, ticker)`; rows are overwritten in place and can drop off with no trace. A time readout would misrepresent what was on screen | Capture intraday history as an append-only log. Large |
| D-15 | Mute ticker | catalysts | No mute or snooze path exists in `services/catalyst/*` or `CatalystTable.jsx` | New column + endpoint + filter application |
| D-16 | Double-tap = search | notebook | `FolderSidebar`'s search mode is local state with no `?q=` deep link, unlike `?folder=`/`?ticker=`/`?new=`. The Command Palette's "Search Notebook" entry points at the bare route | Add a `?q=` read in `NotebookTab.jsx` mirroring the existing one-shot-strip pattern; pass `initialQuery`/`initialMode` to `FolderSidebar`. Backend already supports `GET /api/j2/notes?q=` |
| ~~D-17~~ | ~~Voice note~~ | notebook | **RESOLVED — SHIPPED (Increment 7, `7184b579b`).** Exactly as this row specified, and nothing under `pages/journal-2-0/**` changed (rule 12): `hub/voiceNote.js` records, POSTs to the generic `/api/voice/transcribe`, and hands the transcript to the Notebook's own `createNoteViaApi`. `notebook.voiceNote` sits on the OUTER ring (inner was already at `INNER_MAX`) with icon `wave` — deliberately not `mic`, which is `notebook.voice`, a Compass realtime conversation and a different thing. A toggle, mirroring `VoiceInputButton`'s own. ⛔ **Every refusal is a sentence the member reads**, taken from the server's own `detail` (402 paid plan · 400 voice disabled · 429 monthly cap · 503 provider), asserted as RENDERED TEXT in `hub/sections/notebookVoiceNote.test.jsx` and mutation-proved three ways. ⛔⛔ Found on the way: this controller had shipped an `onScrub` with **no `readout()`** since B10, which `contracts.js:422` forbids — it survived because the check runs at the mounted boundary and every existing notebook rail renders the hook without a provider. `notebookReadout` ships with it | — |
| D-18 | Sync (Obsidian) | notebook | Obsidian is a **device push** connector; there is no "sync now" to fire. `syncSource()` serves pull providers only | Nothing to build. The hub branches on `providers.obsidian.connected` and deep-links `/settings?section=connections` when false |
| D-19 | Event alert | calendar | Only an automatic background job (`services/calendar_alerts.py`) exists; no per-event user control. The reachable alert is a **price** alert | A per-event subscription endpoint + UI |
| ~~D-20~~ | ~~Add to Notebook from a calendar entry~~ | calendar | **CLOSED-BLOCKED (Increment 7): there is no calendar ENTRY the hub can reach.** Two measured halves, and both must change together. (1) The calendar's hub cursor walks **days** (`calendarSection.js` over `weekDates`) and `Calendar.jsx` never calls the hub's `setSymbol` — the Screener and the Journal are the only pages that do. So a `requires: ['symbol']` Note bubble would render permanently DISABLED, verbatim the reason R-17 removed `notebook.linkTicker`. (2) The one surface that DOES name an entry — `EarningsResearchModal`, `Calendar.jsx`'s `selected` — is `z-index: var(--z-modal)` = **1000**, above the hub's whole ladder (`--z-hub-open: 401 > --z-backdrop: 399 > --z-hub-rest: 360`). While an entry is selected the hub is buried underneath it; whenever the hub is reachable, no entry is selected. Both railed in `hub/deferredRowClosures.test.js` | ~~One button wired to the existing endpoint~~ — the endpoint was never the missing piece. Needs a per-entry selection the hub can see (page-set `symbol`) **and** an entry surface that does not outrank the hub, or a Calendar-page button, which is not a hub action |
| ~~D-21~~ | ~~Econ-only view~~ | calendar | **CLOSED-BY-SPEC (Increment 7).** v1.6 §calendar: *"There is no econ-only view (Earnings is effectively always on)."* The page does not merely default to earnings, it **LOCKS** the type: `CalendarHeader.jsx:347-348` — `const locked = type === 'earnings'; if (locked) return` — with the chip titled `'Always on'` and the module's own comment "Earnings is the calendar (always on)". Same one-way valve that DELETED `calendar.earnings` from the registry rather than shipping it inert. ⚠️ **This row names the wrong file:** `filterLogic.js` filters earnings ROWS (audience, market cap, volume, price, confirmed-only, sector, search) and has no notion of an event TYPE — those live as a Set in `Calendar.jsx`, read by `FeedView`/`WeekView`. Turning earnings off is a Calendar product decision, not a filter-logic change. Both facts railed in `hub/deferredRowClosures.test.js` | ~~A filter-logic change in `filterLogic.js`~~ — wrong file. Needs an owner decision to unlock the earnings event type, then a real econ layout (today, earnings-off collapses each day to `MacroOnlyDay`, a dim one-line stub) |
| D-22 | `hub_action` analytics | all | **No authenticated in-app event sink exists.** The only `track()` is anonymous, landing-scoped, with a server-side allow-list of ~20 marketing event names | A `/api/events` route + table. Explicitly out of scope by gate decision — one `// TODO(hub-analytics): emit here` marks the call site |
| D-23 | Per-action `tier` gating | all | **Removed, not deferred** (owner ruling, Wave 0.5 gate): there are no tiers — one product, one price. `HubAction` has no `tier` field and there are no locked or upsell bubbles | Nothing. If a tier model is ever introduced, add the field then — do not carry a dead one now |
| ~~D-24~~ | ~~Light-theme hub tokens~~ | all | **CLOSED-BY-SPEC (Increment 7), and the row's premise does not hold.** `tokens.css` states the decision twice, quoted rather than re-litigated: *"NO --glass-\* HERE, ON PURPOSE. Spec §3.2 ... This is a recorded decision, not an omission"*, and of the hub family, *"No `[data-theme="light"]` --hub-\* set anywhere in this file — deliberate ... **not an oversight for a later sweep to 'fix'**."* Measured: **the hub paints no `--glass-*` surface at all**, so the light omission cannot reach it; and every `--hub-*` token is `color-mix(in srgb, var(--text-heading) N%, transparent)` over `--bg`, both of which the light theme redefines — the glass adapts on its own. ⛔ **The close rests on numbers, not a reading.** Five of six real ink/surface pairs clear WCAG AA on white (5.45–16.50); one does not: `--text-muted` on `--hub-glass-tint-strong` is **4.18:1** (6.34:1 on dark) — the cursor highlight, which `[data-hub-cursor="active"]` paints onto rows the hub does not own, and the Screener phone card colours `.cardCompany`/`.cardStatLabel` with exactly that ink. It clears AA-Large (3.0) and misses AA (4.5), and is recorded by name and number with a floor in `styles/tokens.test.js`, pinned from both sides. **Zero lines of `tokens.css` changed** | The light theme needs no `--hub-*` block. The ONE open item is the 4.18:1 cursor-row pair — fixable by darkening the light `--hub-glass-tint-strong` alone, and it belongs to the post-launch app-wide sweep (§10) that owns the rest of the light theme, not to the hub |
| ~~D-25~~ | ~~Feedback FAB as a one-tap affordance~~ | all | **RESOLVED — SHIPPED (Increment 7, `672901bd8`).** The decision is reversed: Feedback has its own always-visible control again, one tap-target below the Actions button in the hub's own column, mirroring with the hub. The inner-ring alternative was MEASURED and rejected — six of ten modes are already at `INNER_MAX` (scan, chart, journal, catalysts, notebook, home), so promoting it would evict a shipped action from six fans. It is NOT at the old bottom-left: the far edge breaks §C2:769 (`mirrorsAsAUnit.test.jsx`) and strands the affordance under a left-handed member's other hand. The "never actually collided" claim is no longer a claim — `feedbackIsOneTap.test.jsx` computes the rest-state rectangles in both hands. The sheet entry STAYS as the screen-reader door. | — |
| ~~D-29~~ | ~~`[data-hub-cursor="active"]` visual tuning~~ | all | **RESOLVED — SHIPPED (Increment 7, `a9f82101d`).** Five `--hub-cursor-*` tokens are now the cursor's ONE dial; the rule declares no value of its own, so retuning the cursor no longer retunes the knob rim and the pressed Actions button (and vice versa). ⚰️ Found while unifying: the cursor never reached `[data-hub-contrast="high"]` — that block redefines `--hub-rim` while the cursor read `--hub-rim-highlight`, so "rim goes full-strength" was true of the knob and false of the one surface that tells a low-vision member which row they are on. Fill moved off the pressed tier (20% -> 12%). ⚠️ The tune is derived from token semantics, NOT from a device eyeball — no device was used. Rail: `hub/cursorAppearance.test.js` (exactly one `[data-hub-cursor]` rule app-wide, no literals in it, one `:root` definition per token). | — |
| D-30 | Single `data_root()` helper for every `/data` path **(NOT a hub task)** | all | **Latent PRODUCTION risk, surfaced by the 2026-09-08 sandbox incident.** 72 env vars name paths inside the shared root and each resolves INDEPENDENTLY — `api/services/auth_db.py:10` reads `AUTH_DB_PATH` with a `/data/auth.db` default and never consults `DATA_DIR`. Any contributor can add a 73rd, and every sandbox, staging boot and local run silently inherits the hazard. Today the only defences are the AST census + the conftest tripwire, both of which CATCH the problem rather than remove it | One `data_root()` helper every path resolver derives from, so a single env var moves the whole tree. ~68 call sites across `api/**`. ⛔ **Must be its own task** — it must not ride along with a UI feature branch, and it needs the full backend suite plus a prod-parity check that nothing resolves differently with nothing set |
| D-31 | A real tick table beside the price formatter | Journal scrub (3.4), and every price input | **Increment 2 ships `0.01` everywhere as a STATED SIMPLIFICATION, not a discovered rule.** This app has no tick logic at all — no `tickSize`, `minTick` or `priceStep` anywhere; prices are `.toFixed(2)` throughout. So a $0.30 stock gets cent granularity that is coarse relative to its spread, and a $4,000 stock gets 400,000 steps across its range | A tick table living **beside the price formatter**, which is the one place that already knows how a price is rendered, consumed by the hub like any other caller. ⛔ **NEVER a hub-side scaling hack** (owner ruling, A2): a second opinion about what a price step means, living in a gesture handler, is the second-authority defect this project keeps re-finding — and it would be invisible, because a wrong step still produces a plausible number. **RESOLVED — SHIPPED (Increment 7, `3c63c486e` + `0ada98fe3`).** `TICK_TABLE` / `tickSizeFor` / `roundToTick` live in `components/chart/drawingLabels.js`, beside `formatPrice` — which had accepted a `tick` since it was written while nothing ever computed one. Reg NMS 612: $0.01 at or above $1.00, $0.0001 below; what it deliberately does not model (the 2024 amendment's per-SYMBOL $0.005 tier; futures/FX ticks) is written down rather than guessed. The hub ASKS: `stopTickFor` forwards and `STOP_TICK` is `tickSizeFor(1)`, not a restated cent. Four member-visible defects fell out of the old fixed 2dp, all reading as "nothing happened" on a sub-dollar name — the clamp and the write re-rounded to the cent, the commit comparison read three sub-penny ticks as no movement, the chip sat still through them, and the side-flip refusal printed two different stops as the same number. Rail: `hub/tickIsAskedNotDecided.test.jsx` |
| D-32 | Screener -> Plan trade prefills the pattern entry/stop | Screener (3.3), Plan-trade sheet (3.4) | The Screener carries entry/stop only as DISTANCES (`pattern_entry_dist_pct` / `pattern_stop_dist_pct`, `columnDefs.js:386-389`) and only in the `patterns` view (`filters.py:837-841`) — the default view is `overview`, which carries neither. Deriving absolutes client-side (`price x (1 + dist/100)`) would be a SECOND AUTHORITY over a number the live tier already computes (`live_tier.py:576-577`) | A backend-computed ABSOLUTE level, requested when the patterns view is active. ⛔ NOT in Increment 2 (owner ruling): do not request the patterns columns unconditionally and do not add an endpoint for this now. Until then the sheet opens with symbol (+ last price when streaming) and the member fills the rest. **⛔ CLOSED-BLOCKED (Increment 7).** Re-measured, not re-litigated: the absolute level exists only server-side (`live_tier.py:576` `_recover_pattern_level`) and is never emitted — the client has the distances and nothing else. So the only correct implementation is an `api/` change, and `api/` is a hard stop this increment: the Railway `worker` service is GitHub-triggered on `/api/**` plus build files (CLAUDE.md, "**`watchPatterns` are set per-service in the Railway dashboard ONLY** … Worker patterns: `/api/**` + build files"; corroborated in `api/flow_worker_main.py`'s own header, "the api/** watch path"), so any `api/` edit redeploys `worker`. ⛔ The client-side derivation is NOT a fallback, and `live_tier.py:425-434` is why in the product's own words: "⛔ THIS IS THE ONE PLACE THE RECOVERY ALGEBRA FLIPS … Copying the `P₀/(1+d/100)` form onto this pair produces a level wrong by `(1+d/100)²`, silently, in the right ballpark, on a column that names a trigger price." Unblocks when `api/` is writable again |
| ~~D-33~~ | ~~`prefillStop` is not importable~~ | Plan-trade sheet (3.4) | **RESOLVED — SHIPPED.** Verified by measurement, not by report: `prefillStop` is exported from `pages/journal-2-0/lib/disciplineGuards.js` and `AddPositionModal.jsx:14` now IMPORTS it instead of keeping the module-private copy, while `PlanTradeSheet.jsx:32` imports the same one. ONE implementation, shared by the modal and the sheet — which is exactly what this row asked for, and the hub never re-implemented it. | —
| ~~D-34~~ | ~~No function computes R at a CANDIDATE stop~~ | Journal scrub readout (3.4) | **RESOLVED — SHIPPED.** `rAtStop(entryPrice, originalStopPrice, candidateStopPrice, side, shares)` is exported from `lib/journal-2-0/calculations.js:251` — in the calc module, beside the R functions it belongs with — and `journalSection.js:348` calls it. The forward-R opinion lives in the calc module, not in a gesture handler. | —
| D-35 | `HubConfirmPayload.fields` is unreachable — a confirm action has no steppers | Alert (3.3), any future `confirm` | **R-14.** `HubRoot`'s confirm branch builds its own payload from `action.label` + `confirmText(ctx)` and never asks the section for one, so the `fields` array `contracts.js` calls "the EQUAL path, not a fallback" is dead code. Alert therefore fires at the default value with no way to adjust it in the sheet. ⚠️ The contract asserting a capability the product does not have is the same shape as R-06 | `HubRoot` prefers `action.confirmPayload(ctx)` when a section supplies one, and passes the sheet's `values` to `onConfirm`. The Screener already has `alertConfirmPayload()` written, exported and tested, waiting for that one branch. ⭐ ACCEPTED AS-IS FOR INCREMENT 2 (owner, 2026-09-09): the action works, and the gap is a missing adjustment control rather than a wrong number. ✅ **CLOSED IN INCREMENT 7** (`inc7/p1-confirm-fields`): `HubRoot` prefers `action.confirmPayload?.(ctx)`, validates it before it becomes state, and the fallback forwards `run(ctx, values)`. Railed on the RENDERED sheet — `hub/confirmFieldsReachable.test.jsx` — because a test that the payload function was CALLED passes with the branch deleted again. ⚠️ Two sentences elsewhere still call `fields` unreachable and are now false: `registry.js` (the `notebook.templates` removal note) and `RESUME-inc3.md`. R-19's first return condition is met |
| D-36 | Plan trade is a commit sheet whose action does not escalate | Plan-trade sheet (3.4) | Found while shipping the iOS visual escalation (Increment 7). `scan.planTrade` and `chart.planTrade` are `kind:'run'` with `escalate: false`, so `PlanTradeSheet` — which POSTs a planned trade — gets the ordinary `haptics.impact()` cue and, deliberately, no commit notice. B5's rule is that the escalation is about the SHEET, which would make these two escalate; but rendering the notice while the action says otherwise puts a second authority on "is this the serious kind", and flipping the flag changes the haptic every member feels | An owner ruling on whether planning a trade is a commit in the B5 sense. If yes, `escalate: true` on both actions and `PlanTradeSheet` renders `HubCommitNotice` — the notice is already written and railed; nothing else is needed |
