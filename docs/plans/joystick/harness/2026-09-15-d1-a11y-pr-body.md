# PR body — `fix/surface-matrix-d1-a11y` → `master`

> ⏳ **PREPARED, NOT OPENED.** No PR exists.

## Title

```
fix(joystick): the surface-matrix generator emits D1-a11y, and the baseline row comes back out
```

## Body

**This touches one generator, one regenerated artifact, one baseline row, and nothing
member-facing.** The file list below is the evidence for that claim — there is no `app/src`
change except none at all, and no `api` path.

### The file list — `git diff --name-only master...HEAD`

```
docs/plans/joystick/gate-baseline.json
docs/plans/joystick/harness/2026-09-15-d1-a11y-pr-body.md
tools/hub_surface_matrix.mjs
```

**3 files.** Merge-base **`cb0949d8c`**; master **`30fd58aef`**.
`git merge-tree --write-tree` → **exit 0, no conflicts**.

⚠️ **Three-dot, and it is measured.** `master...HEAD` reports **3**; `master..HEAD`
reports **8**. The extra are other workstreams' files landed on master since the base,
shown **backwards** — as though this branch had reverted them. A body asserting "one generator,
one artifact, one baseline row" while listing another team's work would refute itself in its own
evidence. (An earlier PR in this programme had to correct exactly that: two-dot 115 against
three-dot 29.)

---

## The defect was ours, and it was caught by someone else

PR **#137** (`789a6bab5`) renamed the no-drag door row **D1 → D1-a11y** to clear a label
collision, and hand-edited line 192 of `docs/plans/joystick/glass-acceptance-steps.md` to do it.
That file carries a `(GENERATED)` header and is byte-compared against its generator by
`src/hub/surfaceMatrixIsCurrent.test.js`. `tools/hub_surface_matrix.mjs:882` kept emitting the old
literal, so generator and artifact disagreed by exactly one line and the rail went red — **exactly
as designed.**

⭐ **Thank you to the Notebook Wave Q1 workstream.** They hit our red rail in the middle of their
own work, and instead of blocking, reverting, or quietly widening a filter, they **baselined it
with the reason, verified it was persistent rather than flaky (re-ran it alone: 1 failed / 4
passed), attributed it correctly — `owner: "the JOYSTICK workstream — NOT Notebook Q1"` — and left
an `action_for_the_owner` naming the one command that fixes it.** They also declined to run that
command themselves, on the grounds that a Notebook change set editing `docs/plans/joystick/` is the
rule-12 shape inverted. That is precisely right, and it is why this was a ten-minute fix instead of
an archaeology exercise.

## The fix: the GENERATOR, never the artifact

Established from `git show` **before touching anything**: the id is a **hard-coded literal** inside
a single `L.push('| D1 | …')` template line — *not* derived from any data source the generator
reads — so the template is the right place to fix it. One occurrence of "no-drag door" in the file.

```
-  L.push('| D1 | **The no-drag door** | With **VoiceOver** (iOS) / **TalkBack** …
+  L.push('| D1-a11y | **The no-drag door** | With **VoiceOver** (iOS) / **TalkBack** …
```

⭐ **The artifact needed no change, and that is the finding.** It was regenerated with the
generator (never by hand), and the output is **byte-identical to what was already committed**:

```
regenerated file        sha256: 7655de8d1de4536283f49f8e845bdff67ad93d007379453b94827be836c16f7b
git show HEAD:<artifact> sha256: 7655de8d1de4536283f49f8e845bdff67ad93d007379453b94827be836c16f7b
```

The artifact blob is identical to master's (`6585d35ab23e` both sides). #137's hand edit happened
to land on the **desired** content; what was wrong was that the generator disagreed with it.
⛔ **A hand edit that lands on the right answer is still the defect** — nothing downstream can tell
it from one that does not, which is the whole reason the rail byte-compares.

## The rail, and the control

Run scoped, under the box lock, with the continuous sampler **CLEAR**:

```
 Test Files  1 passed (1)
      Tests  5 passed (5)
VERDICT=CLEAR exit=0 samples=2 min_free_gb=13.5
```

⭐ **CONTROL — the fix is what turns it green, not something incidental.** The same rail with the
generator reverted to master's:

```
 Test Files  1 failed (1)
      Tests  1 failed | 4 passed (5)
 × glass-acceptance-steps.md matches the registry as it stands
```

The **other four assertions in that file pass in BOTH states**, which is what makes this a
discrimination rather than a coincidence. Generator bytes were captured first and restored by
sha256 — never `git checkout --`.

## H14 — the hazard class is "hand-edited a generated file"

#137 touched **29** files. Every path was classified on evidence, in this order: the **rail** is the
authority (`surfaceMatrixIsCurrent.test.js` names the artifacts it byte-compares in its own
header), then the file's own header marker, then a real **write** (the path next to a write idiom
or a documented `> path` redirect).

| verdict | count | evidence |
|---|---|---|
| **GENERATED** | **1** | `glass-acceptance-steps.md` — named by the rail as byte-compared; own header says `(GENERATED)` |
| HAND-MAINTAINED | 28 | no generator writes them |

Two negative findings that mattered:

- **`surface-matrix.md`**, the *other* artifact the rail byte-compares, was **not touched by #137**
  and is verified **in sync** with the generator right now. Nothing to do.
- **`owner-run.md` has no generator.** #137 also renamed `D1 → D1-eye` there, and the brief asked
  whether that is the same defect. It is not: `tools/hub_owner_intake.py` takes `owner-run.md` as an
  **argparse positional** — it *reads* it. That rename is a correct hand edit to a hand-maintained
  file and needs no fix.

⚰️ **My first sweep over-flagged and the mistake is worth recording.** It counted any generator
whose *source mentioned* a path as a *writer* of that path, which made `hub_owner_intake.py`
"generate" `owner-run.md` and `closure.md`. Same class as counting a vitest mention in a docstring
as an invocation — **a mention is not a use.** The corrected predicate is above.

⛔ **Finished evidence forms were not touched**, per the brief.

## The baseline row

Removed from `failures[]` (**11 → 10**), with the removal recorded in `removals[]` in the file's own
format (`removed`, `removed_at`, `removed_in`, `why`, `verified_by`), citing the fix commit and the
run that proves it green.

⛔ **Nothing else in that file moved** — asserted programmatically, not by eye: every top-level key
other than `failures` and `removals` was JSON-snapshotted before the edit and compared after.
`additions[]` is append-only history and keeps the other workstream's record of why they banked it.
**`#9` `presentationSingleFormatter` remains `provisional: true`, `settled_at: null`.**

⚠️ One pre-existing inconsistency left alone deliberately: `files[]` (documentation-only, and the
file's own note says it is derived from `failures[]`) never gained the surface-matrix path when the
row was added, so it is unchanged at 9 entries. Not ours to reconcile under "do not touch any other
row".

## Scoped tests

```
vitest: src/hub/surfaceMatrixIsCurrent.test.js + src/hub/rule12Paths.test.js
 Test Files  2 passed (2)
      Tests  28 passed (28)

pytest: tests/test_gate_shards.py   (the only consumer of gate-baseline.json)
 33 passed, 1618 warnings in 359.18s (0:05:59)

VERDICT=CLEAR exit=0 samples=109 min_free_gb=11.21
```

Both run scoped, under the box lock, with the continuous sampler CLEAR across 109 samples.
No retry into load was needed.

## Reviewer checks

1. One generator line changed. The artifact is byte-identical to master's — verify with
   `git rev-parse master:<artifact>` against `git rev-parse HEAD:<artifact>`.
2. Nothing under `app/src` or `api` appears in the three-dot list.
3. The baseline diff is `+7 / −1`: one row out, one removal entry in.
4. `launch/stage-2-member-preview` untouched; boxes 1 and 2 still ☐.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QhCGpjyZyKAqNZbFuwVHRW
