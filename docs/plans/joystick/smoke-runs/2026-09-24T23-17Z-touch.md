# Touch pass — 2026-09-24 23:17Z, production, live SHA `6e7b10407`

`python tools/hub_nav_smoke.py --auth --touch` against `https://uctintelligence.com`,
signed in as `smoke@uctintelligence.internal`, run by the terminal-research session as the
post-deploy check for `6e7b10407` (a docs/tests/tools-only commit: `docs/feature_flags.json`,
two docstrings, `tools/postdeploy_client_smoke.py`). The desktop pass the same evening
(`--auth`, no `--touch`) was **SMOKE PASS** — 19 routes, 31 nav entries, every one moved
both the URL and the screen, busiest main thread `/breadth` at 4.6% blocked.

## Verdict: TOUCH PASS FAILED — 6 problem(s), exit 1

Eligibility floor real: `coarse+narrow=True backdrop=True visualViewport=True (393px @ dpr 3,
maxTouchPoints 1)`. `hideOnRoute` declared by NO MODE, so the tool's expectation on every route
is MOUNTS. Verbatim per-route lines:

```
hub ✓       /dashboard             console errors: 2
hub ✓       /calendar              console errors: 2
hub ✓       /charts                console errors: 2
hub ✓       /morning-wire          console errors: 2
hub ✓       /ai-search             console errors: 2
hub ✓       /uct-20                console errors: 2
hub ✓       /breadth               console errors: 2
hub ✓       /screener              console errors: 2
hub ✓       /options-flow          console errors: 2
hub ✓       /flow-scoreboard       console errors: 2
hub ✓       /live-massive          console errors: 2
hub ✓       /catalysts/history     console errors: 13
NO HUB      /model-book            console errors: 1
NO HUB      /formulas/reference    console errors: 1
NO HUB      /desk                  console errors: 1
NO HUB      /journal               console errors: 1
NO HUB      /portfolio-heat        console errors: 1
NO HUB      /community             console errors: 1
hub ✓       /support               console errors: 2
landscape-immersive /charts: PASS — condition real and the hub is not showing
```

Each of the six reads `{'present': False, 'showing': False}` — the hub root is **not in the
DOM at all** on those routes, not merely hidden.

## What this run does and does not establish

- **Not caused by `6e7b10407`.** That commit changes no application code, and every
  reference to `docs/feature_flags.json` / `feature_flag_index` under `api/` and `app/src`
  is a comment (checked before writing this). There is nothing in it to roll back, and
  `HUB_PREVIEW_ENABLED=false` would remove the hub from the 13 routes where it IS showing
  — the wrong direction. So H15's roll-back-first rule has no lever here; this is recorded
  as a finding for the joystick owner instead.
- **The last recorded touch pass disagrees with today.** `2026-09-12T02-54-26Z-touch.md`
  found the hub SHOWING on `/model-book`, `/desk`, `/journal` and `/community` (sixteen
  routes, all showing). Today it is absent on exactly the six routes that are not
  registered hub sections (`hubRoutes.routeToModeId` is an exact match over the registry's
  `route`s, and none of the six has one; `/portfolio-heat` and `/formulas/reference` were not
  in the 9/12 roster). Whether W4 or a later change deliberately made the hub section-only
  — in which case the TOOL's "MOUNTS on every route" expectation is the stale half — or the
  hub lost its home-mode fallback on non-section routes, is the joystick programme's call.
  The two cannot both be right, and this record does not decide which.
- **A second, unrelated finding from the console lines.** `GET /api/barspack/manifest`
  answers **401** on every route (5+ times per page, and 401 anonymously by curl).
  `api/routers/barspack_router.py` has been gated by `require_bars_access` (session cookie
  or push secret) since `2d121371f` (2026-09-13, "gate the chart-data origins"), while
  `app/src/lib/barsPackClient.js` still fetches every barspack URL with `credentials:
  'omit'` — by its original design (2026-08-14) for Cloudflare cacheability, which the
  router's own docstring still asserts ("Public by design … must NOT be auth-gated"). Every
  browser therefore 401s, the client's `if (!r.ok) return` swallows it, and the Universe
  Bars Pack (instant D/W/M from IndexedDB) has been silently dead for every member for
  eleven days; charts fall back to per-request `/api/bars`. Not a crash, a lost fast path
  plus wasted requests on every page load. Fixing it is a design decision (gate vs.
  edge-cache) for whoever owns the bars pack, not a one-line change.

Raw log: kept by the running session; the per-route lines above are verbatim.
