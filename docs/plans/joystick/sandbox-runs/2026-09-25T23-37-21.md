# Sandbox run — shared-data-root integrity log

Every checkpoint below hashes the main `.db` files under the shared root.
`-wal` / `-shm` are excluded: opening a WAL database read-only rewrites its
`-shm` index, so mtime there is noise. See `scripts/data_root_snapshot.py`.

- `2026-09-25 23:37:26`  **pre-boot (baseline)** — C:\data, 61 db files — CLEAN
    - sandbox = C:\data-193walk
- `2026-09-25 23:38:02`  **post-boot (+15s)** — C:\data, 61 db files — CLEAN
- `2026-09-25 23:39:54`  **post-prewarm (+120s)** — C:\data, 61 db files — CLEAN
- `2026-09-25 23:42:22`  **shutdown** — C:\data, 61 db files — **1 FILE(S) CHANGED**

| | file | detail |
|---|---|---|
| ADDED | `flow_card_stats.db` | 12288 bytes |


## Attribution of the shutdown ADDED row (controller, 2026-09-25 ~23:55 CT) -- NOT this sandbox

The shutdown checkpoint reports one change: `C:\data\flow_card_stats.db` ADDED (12,288 bytes).
It was created by ANOTHER session's test run, not by this sandbox. Evidence, all read-only
(`?mode=ro&immutable=1`):

- The file holds one table, `flow_card_outcomes`, with 5 rows, all NVDA, all written between
  23:41:12.177 and 23:41:12.195 (18 ms): outcomes `rollup` / `empty` / `rollup` / `failed flow_error`
  x2 -- a test fixture's shape, not a request's.
- The table's owning code first appears in commit `15ae7fdd2` ("feat(discord-render): render ops --
  ... /flow outcome ledger ..."), committed 23:47:48 on branch `feat/discord-render-ops`, in
  `api/services/flow_card_ops.py`:
  `_DB_PATH = os.environ.get("FLOW_CARD_STATS_DB_PATH", "/data/flow_card_stats.db")`.
- `git merge-base --is-ancestor 15ae7fdd2 856369244` is FALSE, and `git grep flow_card_stats` over
  this sandbox's tree (`856369244`) returns nothing -- the sandbox had no code that names the file,
  and the census pins cannot have missed a literal that is not in the tree.
- The rows predate that commit by 6 minutes: a pre-commit test run in the other workstream wrote
  into the live `C:\data` on this box.

Not deleted (not this session's file). Reported to the owner as the other workstream's leak.
