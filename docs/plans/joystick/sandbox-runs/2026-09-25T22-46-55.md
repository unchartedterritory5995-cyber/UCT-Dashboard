# Sandbox run — shared-data-root integrity log

Every checkpoint below hashes the main `.db` files under the shared root.
`-wal` / `-shm` are excluded: opening a WAL database read-only rewrites its
`-shm` index, so mtime there is noise. See `scripts/data_root_snapshot.py`.

- `2026-09-25 22:47:03`  **pre-boot (baseline)** — C:\data, 61 db files — CLEAN
    - sandbox = C:\data-193walk
- `2026-09-25 22:48:07`  **post-boot (+15s)** — C:\data, 61 db files — CLEAN
- `2026-09-25 22:50:00`  **post-prewarm (+120s)** — C:\data, 61 db files — CLEAN
- `2026-09-25 23:02`  **shutdown** -- the launcher's own checkpoint was NOT written: it was started DETACHED (no console), so neither Ctrl+C nor Ctrl+Break can reach it (`os.kill(pid, CTRL_BREAK_EVENT)` -> WinError 87), and `Stop-Process` ends it before `finally` runs. Replacement check, weaker and labelled as such: `Get-ChildItem C:\data -Recurse -Filter *.db` -> 61 main `.db` files, **0 with LastWriteTime after the 22:47:03 baseline**, read both before and after the stop; launcher PID 42520 gone; port 8093 no listener. (mtime of a MAIN `.db` file, never a `-wal`/`-shm` sidecar.)
