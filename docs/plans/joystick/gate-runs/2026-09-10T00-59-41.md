# Gate run — 2026-09-10T00:59:41 — THE MANIFEST OF RECORD

> ⭐ **This is the gate the merge commit cites.** Tree `6891ac335…` (start == end), which is
> byte-identical to the tree of merge commit `0fcefb649` — master was the merge-base, so
> `--no-ff` carried the branch tip's tree through unchanged.
>
> ⛔ **ONE NEW NAME, AND IT IS NOT A REGRESSION.**
> `src/pages/desk/ArticlesSection.native.test.jsx > clearing the query brings the full
> archive back` is a load-sensitive settle timeout: it passes ALONE 12/12 (535 ms; file
> 1661 ms) against 5426 ms in-shard, the failure is a Testing Library async-settle timeout
> rather than a wrong assertion, the branch and master's three new commits touch zero
> `desk/` files, and it is absent from all four prior manifests at 1188 and 1190 files.
> Master's new `offlineOptInEvent.test.jsx` resliced shard 3 from 198 to 199 files, which
> is the mechanism. It is recorded in `gate-baseline.json`'s `load_sensitive` list and
> **deliberately NOT banked as a baseline failure** — banking a timeout leaves a slot a
> real failure can occupy unnoticed. Re-run it alone before classifying it.
>
> ⚠️ **This file describes the tree of its PARENT commit, not its own.** Committing a
> manifest necessarily changes the tree the manifest certifies, so it is banked one
> docs-only commit above the tree it measured.

- tree: `6891ac335a026a098c1da3c33e73b027971ce0cd` (start) -> `6891ac335a026a098c1da3c33e73b027971ce0cd` (end)
- wrapper: `scripts/gate_shards.py` blob `9351c4ac42582d8cf20bbe1927b0b0008ffa579b`
- shards: 6

| shard | test files | tests |
|---|---|---|
| 1 | 3 failed / 199 | 4 failed / 2519 passed / 2523 |
| 2 | 2 failed / 199 | 2 failed / 2617 passed / 2624 |
| 3 | 2 failed / 199 | 2 failed / 3508 passed / 3514 |
| 4 | 1 failed / 198 | 1 failed / 2677 passed / 2678 |
| 5 | 1 failed / 198 | 1 failed / 2360 passed / 2361 |
| 6 | 0 failed / 198 | 0 failed / 3561 passed / 3561 |
| **Σ** | **9 failed / 1191** | **10 failed / 17242 passed / 17261** |

- test files on disk: **1191** — RECONCILES with the summed file total (1191).

## Failing set vs baseline (`fee08cf77bda2a9714f851ec0bb5d0ef6660491d`, measured 2026-09-09 (re-corroborated after rebase onto f58383e69))

- observed **10** failing tests, baseline has **9**
- **NEW failures (regressions): 1**
    - ⛔ src/pages/desk/ArticlesSection.native.test.jsx > clearing the query brings the full archive back

⛔ **The failing set DIFFERS from the baseline** — read the two lists above.
