# Gate run — 2026-09-12T17:57:39

- tree: `e05721c4e47e8b4635da1ac5ce8334dd8c195d5f` (start) -> `e05721c4e47e8b4635da1ac5ce8334dd8c195d5f` (end)
- wrapper: `scripts/gate_shards.py` blob `7164d491f265055efcdb6121f59d17592757cdae`
- shards: 6

| shard | test files | tests |
|---|---|---|
| 1 | 3 failed / 217 | 5 failed / 2733 passed / 2741 |
| 2 | 0 failed / 217 | 0 failed / 3001 passed / 3006 |
| 3 | 2 failed / 216 | 2 failed / 3930 passed / 3936 |
| 4 | 0 failed / 216 | 0 failed / 3016 passed / 3016 |
| 5 | 2 failed / 216 | 2 failed / 2520 passed / 2522 |
| 6 | 0 failed / 216 | 0 failed / 4012 passed / 4012 |
| **Σ** | **7 failed / 1298** | **9 failed / 19212 passed / 19233** |

- test files on disk: **1298** — RECONCILES with the summed file total (1298).

## Failing set vs baseline (`258c5609d529a1f247ae6ff2da266f8479742823`, measured 2026-09-10 (re-measured on origin/master 62a228e5d))

- observed **9** failing tests, baseline has **7**
- **NEW failures (regressions): 2**
    - ⛔ src/hooks/pollingSites.rail.test.js > polling sites — the opt-in helper gets a rail > the wrapper exempts itself by RESOLUTION, and there is exactly one of it
    - ⛔ src/pages/journal-2-0/lib/iteratorGlobalFloor.test.js > no shipped chunk evaluates an UNSHIMMED global below the floor (iOS >= 16) > every built asset is clear

⛔ **The failing set DIFFERS from the baseline** — read the two lists above.

## Classification of the 2 NEW — both ENVIRONMENT, zero regressions

Run as instructed by the manifest itself: re-run a load-sensitive name ALONE before
classifying, and judge direction before calling anything a regression.

| NEW failure | verdict | evidence |
|---|---|---|
| `pollingSites.rail > the wrapper exempts itself by RESOLUTION` | **load-sensitive, NOT banked** | Timed out at 30000ms in-shard; **passed ALONE in 4822ms** on the same tree, box idle, minutes later. Not an assertion — the body is a repo-wide `census()` sweep. Content constant, outcome flipped, so the variable was load. Recorded in `gate-baseline.json::load_sensitive`, never in the baseline: banking a timeout leaves a slot a real failure can occupy unnoticed. |
| `iteratorGlobalFloor > every built asset is clear` | **no build present** | This worktree was recreated after the 2026-09-12 OOM sweep, so `app/dist/assets` did not exist. The rail fails loudly on a missing build **by design** rather than reporting green over zero files. After `npm run build` (292 JS assets) all **9 tests pass**, bundle scan included. |

**NEW regressions: 0.**

### What this run does and does not certify

- ⭐ It is the RETROACTIVE GATE for the iOS-17 Notebook hotfix, which ships in this tree
  (merge `6ae358922` is an ancestor). `iteratorGlobalFloor` passing against REAL built
  output is the part that matters: no shipped chunk evaluates an unshimmed global below
  the iOS 16 floor.
- ⚠️ Master moved while this ran. The gated tree is `e05721c4e`; master has since added
  `c8b38058c` (Notebook Q1 fix 2/2) and `2b6f0725e` (its gate docs). Both are
  Notebook-only and touch **no** hub file and **none** of the hotfix's files
  (`pdfjs.js`, `iteratorGlobalShim.js`, `PdfViewerBoundary.jsx`). The run was deliberately
  NOT restarted to chase them — lapping a moving master is its own recorded failure.
- ⛔ The wrapper printed `exit 1`; the background task reported **exit 0**, because the
  command was piped through `tail`. The manifest is the authority, not the exit code.
