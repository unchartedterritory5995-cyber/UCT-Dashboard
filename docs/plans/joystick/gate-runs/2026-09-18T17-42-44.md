# Gate run — 2026-09-18T17:42:44

- tree: `b7ea6c00e9d7e5b75d26f5442b37e1a4ec37f0f3` (start) -> `b7ea6c00e9d7e5b75d26f5442b37e1a4ec37f0f3` (end)
- wrapper: `scripts/gate_shards.py` blob `1e121f4946c3e43a3af1014b803a673809ce57e3`
- shards: 6

| shard | test files | tests |
|---|---|---|
| 1 | 3 failed / 238 | 3 failed / 3064 passed / 3067 |
| 2 | 1 failed / 237 | 1 failed / 3227 passed / 3233 |
| 3 | 3 failed / 237 | 3 failed / 4337 passed / 4344 |
| 4 | 0 failed / 237 | 0 failed / 3262 passed / 3262 |
| 5 | 1 failed / 237 | 1 failed / 2803 passed / 2804 |
| 6 | 0 failed / 237 | 0 failed / 4363 passed / 4363 |
| **Σ** | **8 failed / 1423** | **8 failed / 21056 passed / 21073** |

- test files on disk: **1423** — RECONCILES with the summed file total (1423).

## Failing set vs baseline (`1216958ed29aa263a91f7b3224e237ca4ecb7e03`, measured 2026-09-14 (ADOPTED from the six-shard run on master 1216958ed; manifest gate-runs/2026-09-14T17-48-27.md))

- observed **8** failing tests, baseline has **10**
- **NEW failures (regressions): 1**
    - ⛔ src/pages/desk/ArticlesSection.native.test.jsx > clearing the query brings the full archive back
- no longer failing: 3 (fixed, or silently stopped running — check which)
    - src/lib/presentation/presentationSingleFormatter.test.js > ⚠️ formatPercent is DECLARED AND ADOPTED BY NOTHING — stated, not hidden > nothing outside lib/presentation imports formatPercent from S10
    - src/pages/ThemeTrackerPage.chartmount.test.jsx > passes stored=null with no onStore, and keeps symbol retargeting enabled
    - src/pages/ThemeTrackerPage.chartmount.test.jsx > selecting a holding mounts ChartPane with that symbol and the current timeframe
- expected-red entries naming **no reason**: **0** — none

⛔ **The failing set DIFFERS from the baseline** — read the two lists above.

## §8 DO-NOT-BUILD sweep

✅ No §8 item has gained code in `app/src`, `api`, `scripts`, `tools`.

## ⛔ The 1 NEW failure, classified — NOT a regression from this branch

`src/pages/desk/ArticlesSection.native.test.jsx > clearing the query brings the full archive back`

The wrapper's own instruction is to classify by DIRECTION before treating a red as a
regression. Three independent lines of evidence, and the third is structural:

1. **It passes ALONE.** Re-run in isolation on this exact tree: 12/12 passed, the named
   test in **533 ms**. That is the load-sensitive signature CLAUDE.md records for
   `enumerationSites.test.js` (15,000 ms in company, 1,461 ms alone).
2. **The failure is a Testing Library async find, not a wrong answer** —
   `TestingLibraryElementError: Unable to find an element with the text: "The Rest of the
   Week - March 31, 2026"`. Testing Library's `asyncUtilTimeout` is NOT raised by
   `testTimeout`, so under shard load the find expires before the element arrives.
3. **⛔ NO IMPORT PATH EXISTS.** Built the module graph with `tools/import_edges.mjs` and
   took the transitive closure from the failing test: **15 modules, and NEITHER file this
   branch changes is among them.** Control: the same closure DOES contain
   `ArticlesSection.jsx`, so the graph is not answering empty.

⭐ **And the mechanism is absent by construction.** This branch's only remaining changes
under `app/src` are two STYLESHEETS (`hub/hub.module.css`, `styles/tokens.css`). **jsdom
applies no CSS and performs no layout.** A stylesheet cannot change the text content a
`getByText` query resolves, in either direction, ever.

⛔ **NOT banked into the baseline.** "A timeout is never banked as permitted breakage" —
adding it would leave a slot a real failure could later occupy unnoticed. It is recorded
here as load-sensitive and unrelated, with the evidence, and the baseline is left alone.

⚠️ What would settle it beyond doubt is a full six-shard gate on the merge-base to show the
same red there. That costs ~60 minutes on a box another workstream is also queuing for, and
it cannot be more conclusive than "there is no code path and no mechanism". Recorded as the
reason it was not run, rather than left as a silent judgement call.
