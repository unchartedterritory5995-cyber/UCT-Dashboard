# Gate run — 2026-09-14T17:48:27

- tree: `1216958ed29aa263a91f7b3224e237ca4ecb7e03` (start) -> `1216958ed29aa263a91f7b3224e237ca4ecb7e03` (end)
- wrapper: `scripts/gate_shards.py` blob `2240e9c4945f9ad18047697a2845797d5612d443`
- shards: 6

| shard | test files | tests |
|---|---|---|
| 1 | 4 failed / 225 | 5 failed / 2851 passed / 2856 |
| 2 | 2 failed / 225 | 2 failed / 3093 passed / 3100 |
| 3 | 2 failed / 225 | 2 failed / 4052 passed / 4058 |
| 4 | 0 failed / 225 | 0 failed / 3117 passed / 3117 |
| 5 | 1 failed / 225 | 1 failed / 2632 passed / 2633 |
| 6 | 0 failed / 225 | 0 failed / 4174 passed / 4174 |
| **Σ** | **9 failed / 1350** | **10 failed / 19919 passed / 19938** |

- test files on disk: **1350** — RECONCILES with the summed file total (1350).

## Failing set vs baseline (`258c5609d529a1f247ae6ff2da266f8479742823`, measured 2026-09-10 (re-measured on origin/master 62a228e5d))

- observed **10** failing tests, baseline has **7**
- **NEW failures (regressions): 3**
    - ⛔ src/components/screener/reachable.test.js > 🔴 every module under app/src is REACHABLE from an entry point > and nothing committed is connected to nothing
    - ⛔ src/lib/presentation/presentationSingleFormatter.test.js > ⚠️ formatPercent is DECLARED AND ADOPTED BY NOTHING — stated, not hidden > nothing outside lib/presentation imports formatPercent from S10
    - ⛔ src/surfaces/manifest.test.js > S1 CP1 — the surface manifest is derived, not typed > every Layout-hosted route has a declaration

⛔ **The failing set DIFFERS from the baseline** — read the two lists above.

## §8 DO-NOT-BUILD sweep

✅ No §8 item has gained code in `app/src`, `api`, `scripts`, `tools`.

---

## Resource log — appended by the run's memory monitor, not by `gate_shards.py`

⛔ Owner rule, 2026-09-14: record free memory at each shard boundary; at **4.5 GB**, stop cleanly
at the next boundary and record INCONCLUSIVE-RESOURCE with shards completed. **The floor was never
reached — this is a complete six-shard run, not a partial one.**

| boundary | time | free |
|---|---|---|
| pre-gate | 17:13:25 | 12.2 GB |
| shard 1 in flight | 17:17:14 | 10.6 GB |
| shard 2 in flight | 17:24:09 | **6.5 GB** ← lowest reading |
| shard 3/6 complete | 17:33:52 | 10.6 GB |
| shard 4/6 complete | 17:36:38 | 8.7 GB |
| shard 5/6 complete | 17:42:38 | 8.6 GB |
| shard 6/6 complete | 17:48:33 | 9.5 GB |
| manifest written | 17:51:08 | 12.1 GB |

Lowest: **6.5 GB**, against a 4.5 GB stop condition. ⚠️ The dip was transient and recovered
without intervention; the box also carried six unrelated python jobs throughout.

## Verdict block

**Baseline: REVISED — 7 → 10.** The failing set on master is a strict **superset** of the
baseline: all 7 baseline entries still fail, and 3 more now fail **on master itself**.

| # | failure | status |
|---|---|---|
| 1 | `ChartDrawingOverlay.surfaces.test.jsx > … > ⛔ ENTERING EDIT MODE IS NOT A RESIZE` | baseline |
| 2 | `manifestProse.test.js > … > ⛔⛔ every key the product READS survives the strip` | baseline |
| 3 | `pine.blindCorpus.test.js > … > ⏳ the accepted floor moves one way too` | baseline |
| 4 | `pollingSites.rail.test.js > … > no NEW bare polling site, and no allow-listed one that has since moved` | baseline |
| 5 | `ThemeTrackerPage.chartmount.test.jsx > passes stored=null with no onStore, and keeps symbol retargeting enabled` | baseline |
| 6 | `ThemeTrackerPage.chartmount.test.jsx > selecting a holding mounts ChartPane with that symbol and the current timeframe` | baseline |
| 7 | `tapFloor.test.js > … > no stylesheet declares a finger target on the PHONE only` | baseline |
| 8 | `reachable.test.js > 🔴 every module under app/src is REACHABLE from an entry point > and nothing committed is connected to nothing` | **NEW on master — R-29** |
| 9 | `presentationSingleFormatter.test.js > ⚠️ formatPercent is DECLARED AND ADOPTED BY NOTHING — stated, not hidden > nothing outside lib/presentation imports formatPercent from S10` | **NEW on master** |
| 10 | `surfaces/manifest.test.js > S1 CP1 — the surface manifest is derived, not typed > every Layout-hosted route has a declaration` | **NEW on master** |

⭐ **All three NEW failures are master's own**, measured on master with nothing of any feature
branch in the tree. None is on the baseline's `load_sensitive` list, so no alone-run is mandated
before classifying — though none has been re-run alone either, and that is stated rather than
implied.

⚰️ **R-29 is a RECURRENCE, not a new defect.** `gate-baseline.json`'s own `removals` records that
this exact row was **removed from the baseline on 2026-09-11** — *"It stopped failing in the
2026-09-11T07-36-04 gate."* It has since started failing again.

### Does the `2ae7e98aa` stage-2 gate verdict still stand?

**Its CONCLUSION HOLDS. Its NUMBERS no longer describe a merge performed today.**

- **HOLDS —** the stage-2 gate's claim was *"zero attributable NEW"*: the single NEW failure it saw
  was R-29, master's, not the branch's. **Re-verified here, not inherited**: R-29 fails on master
  `1216958ed` with no feature branch in the tree. And neither of the two other new failures is in
  the branch's 26 paths, so the branch still introduces nothing.
- **But it does not describe the merge result.** That gate measured a tree containing master at
  `4fb4f9daf`. Master is now 375 commits further on and carries **2 additional failures** that did
  not exist when it ran. A gate on a merge performed today would read **10+**, not 8 — every extra
  one master's.

**What would settle it:** a gate on the actual merge result — `git merge-tree` already proves it is
conflict-free at tree `aa9fbff5b` — compared against a baseline **revised to 10**. That run belongs
immediately after Patrick merges, not before: re-gating a frozen branch against a moving master is
a treadmill, and the branch's own contribution is already measured.

⛔ **`gate-baseline.json` is NOT amended by this run.** The standing ruling is that R-29 enters the
baseline only in the first docs/tool commit **after** the stage-2 PR merges; entries 9 and 10 are
new information for whoever does that, and they are recorded here rather than acted on.
