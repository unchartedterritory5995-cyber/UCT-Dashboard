# UCT Indicator Platform — Design Specification

**Date:** 2026-07-31 · **Status:** Approved roadmap; spec pending owner review
**Inputs:** 4 research reports (TradingView/Pine ecosystem, Lux Algo & premium vendors, competitor creation systems, technical feasibility) + 5-seat panel review (swing trader, UX, visual design, architecture, CEO/product). All panel findings adjudicated and folded in.

---

## 0. One-page summary

Build ONE indicator engine where every indicator is a versioned, declarative **definition** (meta + typed inputs + declared plots + named events + swappable compute slot). Two creation doors ride on it — a **curated UCT library** and, later, a **no-code builder** — plus an **AI concierge** (NL → definition → chart → honesty audit) that subsumes a scripting tier. A standalone user-facing scripting language is **killed** as a product; its sandbox survives as plumbing for AI-generated definitions.

**Positioning:** *"The chart TradingView can't be."* Not a TV replacement — the indispensable second screen: proprietary data fused into indicators (dark pool, options flow, GEX), signals that provably don't repaint, and an append-only signal ledger. **"The first indicator platform that shows its receipts."**

**Execution model:** all lanes running, merges gated. Every phase's spec/design work starts immediately; shipping order follows dependencies and verification gates, not calendar rest.

---

## 1. Strategy (research-derived, non-negotiable principles)

1. **One grammar, many surfaces.** The definition consumed by chart, alerts, screener, and builder is the same object. Never ship asymmetric capability across surfaces (TrendSpider's core failure).
2. **Data moat first.** Flagship indicators are views over proprietary data. TV vendors are tenants; UCT is a landlord.
3. **Receipts as brand.** Closed-bar evaluation by default; forming values visually ghosted; machine-or-audit-assigned repaint badges (never self-disclosed); append-only signal ledger recording from launch day (private-first; public per-toolkit only after it proves out).
4. **Sell toolkits, not indicators** (Phase E). Gate breadth (symbols, history), never mechanics.
5. **Don't innovate on chrome.** Users are TV-pre-trained: legend chips, three-tab settings, pane grammar copied exactly. Innovate on substance: error states, honesty features, data fusion.
6. **Traps to never step in** (vendor research): repainting scandals, backtest inflation, naked buy/sell-arrow marketing, unmeasured accuracy claims, billing dark patterns, countdown-timer pricing theater, metered credits for core features, auto-trading liability.

---

## 2. Roadmap — dependency pipeline

| Phase | Ships | Gate to ship |
|---|---|---|
| **A — Signature Launch** | 3 UCT Signature Indicators (server-computed, premium-gated) on the EXISTING chart path + signal ledger (write-only) + receipts landing section | Sep 5 launch; closed-bar semantics verified across live sessions |
| **B — Foundation** | Engine, binding layer, instance-list settings, two-flip migration of the 14 series-expressible natives (15 settings keys; `volumeProfile` is a canvas overlay and is carved out — see §11 and the B3 plan's adjudication A4), auto-generated settings UI, library dialog | Post-launch freeze lift (Sep 19+); per-indicator parity gates |
| **C — Alerts & depth** | ✅ **SHIPPED DARK 2026-08-06** (`b97c75b6`, 25 commits, then the daily-VWAP unit fix). Closed-bar alert engine **built and running in SHADOW** · every-plot-alertable + `sar` events as columns · `compute.rev` force-migration · fire-once / re-arm / snooze / fired log · price alerts (`close` as a LEFT operand, a third `PRICE_FUNCS` partition) · AVWAP + ATR bands + RS line · per-chart sets + templates · Signature genericized onto a `compute.kind: 'server'` lane · the ledger door built and **deliberately unwired**. ⛔ **THE CUTOVER ITSELF IS NOT SHIPPED**: `ALERT_EVAL_MODE` is still `"forming"`, so no live alert's behaviour changed. Task 8 flips it, alone, after three shadow sessions on real armed rows and an explicit owner decision. | B complete |
| **D — Builder + AI door** | ✅ **SHIPPED 2026-08-07** (`3c52e402`, **16 tasks — the plan contained 15**). `jsep` **pinned exact 1.4.0**, ONE parser, and the AST is the persisted artifact · a **closed table** (`engine/ast/closedTable.json`: 11 functions, 15 operators, 5 series = **31 names**) read by TWO interpreters that agree at **`REL_TOL = 1e-9`** (`tools/ast_conformance.py`), with the tolerance MEASURED by a perturbation ladder rather than chosen: a planted disagreement of 1e-6 is red, 1e-9 is red, 1e-11 is green. 🔴 **This row said "agree at 1e-11, never 1e-9" until 2026-08-07 and it was wrong in the dangerous direction** — it read the perturbation ladder as if it were the tolerance, so it claimed the gate was 100× tighter than it is AND that 1e-9 had been REJECTED as too loose. `1e-11` appears zero times in `app/src`, `api/` or `tools/`; §11 D-A2 below has said `1e-9` all along, so the file contradicted itself and the summary row is the half people read. "Fixing" `REL_TOL` to 1e-11 to match would red the whole conformance gate on float noise · a **budget** (`maxNodes 128`, `maxLookback 500`, `maxSeriesRefs 8`) that makes the escape census attributable · a **machine repaint linter** that contradicted a shipped badge on its first run · a **sentence read-back generated from the tree**, never written by a model · a **user-definition store** (append-only, `compute.rev` force-migration) · **alert admission through a fourth partition** `USER_FUNCS` that `build_alert_grid` does not read · an **NL→AST concierge** whose sentence the model cannot author · everything **paid**, with the gate derived from `router.routes`. ⛔ **AND A SIXTEENTH TASK THE PLAN NEVER CONTAINED**: Task 11 measured its parity case at **0 px with AND without the perturbation**, refused to report that as a pass, and exposed that `registerUserDefinitions` **validated but never installed** — `_byId` was built once at import from a frozen empty `AST_DEFS`, so a saved formula could never draw while ~6,100 frontend tests stayed green. Task 16 wired the install path; the case now reads **140,925 px** and its perturbation **141,551**, the +626 entirely inside the pane the user's own line lives in | B complete (C parallelizable) |
| **E — Screener & toolkits** | Definitions across full universe server-side; named ™ toolkits; tiering | C + D; ledger has public-worthy history |

**Parallel-from-today lanes:** Phase A implementation · Phase B build in isolated worktree (ship gated) · C/D spec + schema freeze + design tokens (this document) · ledger accruing history.

**Hard sequencing reasons (recorded so they're not re-litigated):** C consumes B's definitions; D emits B's definitions; E runs them. Verification gates are real only if they can fail. Launch window = no surgery on the flagship premium surface with deploy-restricted hours.

---

## 3. Definition schema v1

```jsonc
{
  "schemaVersion": 1,
  "id": "uct-rsi",                 // stable handle; plots addressable as uct-rsi.rsi
  "version": 4,                    // PRESENTATION version: meta, input UI, styling. Pin-safe.
  "compute": {
    "kind": "native",              // native | server | ast (D) | script (reserved, AI plumbing)
    "fn": "rsi",
    "rev": 2,                      // MATH revision. Bumped ONLY on output-changing logic.
    "budget": null                 // ⭐ NO LONGER RESERVED — Phase D Task 6 gave it a consumer.
                                   // `null` = take `budget.DEFAULT_BUDGET`; an object overrides
                                   // per key. THREE caps, each DERIVED from a number already in
                                   // this spec rather than chosen by taste:
                                   //   maxNodes      128 — larger than the biggest hand-written
                                   //                       native in the registry
                                   //   maxLookback   500 — §5's 5,000-bar frame; a window that
                                   //                       eats a tenth of it is a refusal
                                   //   maxSeriesRefs   8 — §5's ≤8 panes / ≤60 series budget
                                   // Enforced at REGISTRATION and again before any compute, in
                                   // both lanes, with the guard that fired NAMED (`budget:nodes`,
                                   // `budget:lookback`, `budget:series`). ⚠️ The JS side RETURNS
                                   // `{ok, error}` and the Python side RAISES — deliberately
                                   // different: one feeds a form that must render an error chip,
                                   // the other feeds an admission that must not be ignorable.
  },
  "meta": {
    "name": "RSI", "shortName": "RSI", "category": "Momentum",
    "description": "...", "tags": ["oscillator"],
    "author": "uct",
    "tier": "free",                // billing tier — ORTHOGONAL to compute.kind
    "repaint": "non-repainting"    // non-repainting | preview-repaints | repaints
                                   // 🔴 THIS COMMENT USED TO SAY "Phase A/B: audited metadata".
                                   // IT WAS NOT AN AUDIT — it was a shared DEFAULT. Measured at
                                   // Phase D Task 1: `nativeRegistry.js` sets
                                   // `repaint: 'non-repainting'` inside ONE helper and no native
                                   // definition overrides it; the whole file declares the field
                                   // exactly TWICE (that default and `rsLine`, which does not
                                   // pass through the helper). A uniform column is
                                   // indistinguishable from an unset one, and it was an unset one.
                                   // Phase D: machine-assigned by AST linter — but ONLY on the
                                   // `ast` lane, where it is a registration GATE refused in BOTH
                                   // directions (under-claiming is as false as over-claiming).
                                   // The 17 hand-written natives are `undecidable-hand-written`
                                   // by §11's own rule, so the linter read ONE plot of forty-two
                                   // — and disagreed with it. See
                                   // `docs/decisions/2026-08-06-machine-repaint-linter.md`.
  },
  "placement": {
    "target": "pane",              // price | pane | volume  (volume = shipped left-axis overlay feature)
    "scale": { "min": 0, "max": 100 }
  },
  "inputs": [
    { "key": "period", "type": "int", "label": "Length", "default": 14,
      "min": 2, "max": 200, "step": 1, "group": "Core", "tooltip": "..." },
    { "key": "lineColor", "type": "color", "default": "token:info" }
  ],
  "plots": [
    { "key": "rsi", "label": "RSI", "style": "line",
      "color": "$lineColor", "width": 1, "lineStyle": "solid",
      "opacity": "solid", "precision": 2, "role": "primary" },
    { "key": "levels", "style": "hlines", "levels": [70, 50, 30] }
  ],
  "events": [
    { "key": "overbought", "label": "Crossed above 70" }   // MUST match a returned column of {0,1,NaN}
  ]
}
```

### 3.1 Field rules (panel-adjudicated, field-level)

- **`version` vs `compute.rev`:** alerts/screens/ledger pin against `defId@version` freely (presentation pins are free). On a `compute.rev` bump there is NO eternal pinning: all bindings force-migrate with user notification, evaluator `last_value` reset, first post-migration cycle suppressed (prevents v_old-prev vs v_new-current false crossings). Contract: *"you will never be silently switched"* — not *"old math runs forever."*
- **Colors are tokens.** `color` fields accept `token:*` semantic references (see §7) resolved per-theme at render time; raw hex is the escape hatch. This MUST land before pilot indicator #1 — persisted user styleOverrides make day-1 defaults permanent.
- **`$<inputKey>` substitution grammar:** valid in plot fields `color`, `width`, `levels`, **`lineStyle`**. Resolution failure = definition invalid at registration. Never silent defaults. *(`lineStyle` was added in B3 Task 8 and a migration is why: VWAP is the first migrated native with a user-facing style picker — `indicators.vwap.lineStyle` → solid/dashed/dotted — and without the reference its definition could only carry an author's literal, so an engine-drawn VWAP rendered **solid** for every user who had chosen dashed. Measured at **2,966 changed pixels (0.398656%), 5/5 runs** on `engine_vwap_dashed_vs_legacy`, build `9c7b7e62e647` — on which `engine_vwap_vs_legacy` (lineStyle solid) was **0**, which is why no pre-existing case could see it. The grammar is unchanged; the resolved value is still checked against `PLOT_LINE_STYLES`, so an enum whose options wander outside the vocabulary still fails at registration.)*
- **Dynamic color channel:** `plots[].colorMode: "fixed" | "sign" | "column:<key>"`. Compute NEVER returns color strings; the data plane stays numeric. (`sign` covers MACD-histogram/volume up-down.)
- **Events are columns.** `events[].key` must match a returned column valued `{0, 1, NaN}`. Alerts, screener, and builder AST all consume this one shape.
- **Unknown-field policy (asymmetric):** document fields = ignore-and-preserve within a schemaVersion major. Behavioral fields = fail closed — unknown `inputs[].type` or `plots[].style` ⇒ instance refuses to render with error chip; never silent coercion. 🔴 **"Catalog fetch filters by client `supportedKinds`" WAS FALSE WHEN THIS LINE WAS WRITTEN AND STAYED FALSE FOR TWO PHASES.** Measured at Phase D Task 3 and re-measured at Task 8: repo-wide, `supportedKinds` appeared **twice, both in prose** — `defSchema.js`'s own comment and this sentence — and **zero identifiers** in `app/src` or `api/`. It describes a mechanism nobody had written, sitting in the file whose job is to fail closed. **Task 8 built it**, because D is the first phase in which a client can meet a kind it cannot run. ⭐ **What it filters now:** `defSchema.SUPPORTED_KINDS = ['native','server','ast']`, a **strict subset** of `COMPUTE_KINDS = ['native','server','ast','script']`, with exactly one consumer — `nativeRegistry.validateUserDefinitions` (the sole `SUPPORTED_KINDS.includes` call in the tree). ⚰️ **This line named `registerUserDefinitions` until 2026-08-07, which is the name Task 16 RETIRED for being misleading** — *"'Register' reads as 'put it in the registry'… It never did"* (`nativeRegistry.js`, verbatim), and the misreading kept the feature dead for six tasks. A reader greps the retired name, gets zero hits, and either concludes the filter was never built (the Task-3-era defect this sentence exists to close) or re-adds a `register` alias that re-opens the validate/install conflation. `installUserDefinitions` is the separate install door. ⛔ **Declared and supported are different claims**: a `script` definition is still **well-formed** (`validateDefinition` accepts it, asserted directly) and is refused at the **registry** door with *"a DECLARED kind and an UNSUPPORTED one"* — **listed, not deleted**, so a client one bundle behind lists an entry it cannot run instead of pretending it does not exist. Conflating the two lists would drop the whole server lane from any stale client.
- **Plot styles v1 (build):** `line, stepline, histogram, area, baseline, hlines, markers, band` (band = upper/lower + fill, covers BB/Donchian/cloud cases). **Schema-reserved, renderer later:** `zones` (typed `{from,to,state,label?}`, states per §7), `bgband`, `barcolor` (ownership crosses into live-tick candle path — Phase C+), `fill` (arbitrary pair).
- **Marker vocabulary (locked enum):** triangle-up/down = entry/exit · circle = info · diamond = named event · square = level interaction · cross = invalidation. Sizes `s|m|l` fixed px. Shape = event class, color = direction; neither free-form.
- **Input types v1 (build):** `int, float, bool, enum, string, color, source`. **Schema-reserved, deferred:** `timeframe` (MTF is a repaint minefield — reserved with confirmed-bars-only semantics so the schema never needs a v2 for it), `price`, `time`, `session`, `symbol`, `confirm`. Modifiers `group/inline/tooltip/activeWhen` ship v1.
- **Per-plot `role`:** `primary | secondary | context | signal` — drives legend order, default widths, and gives the visual-budget linter (§7) something to enforce.

---

## 4. Compute contract

```
compute({ bars, inputs, prevState?, barstate }) → { columns, state? }
```

- `bars`: columnar Float64Arrays `{t,o,h,l,c,v}` — **raw bars, never display-transformed** (no Heikin-Ashi input; JS and Python lanes identical on this).
- `columns`: one Float64Array per plot/event key, **aligned to bar count, NaN-padded** (server convention wins over the current trimmed-array client convention).
- **Wire format (server kind):** JSON arrays with `null` ⇄ NaN mapped at the client boundary. Binding layer converts NaN → LWC whitespace items; compute never emits point objects. (Base64 Float64 buffers are a later optimization, not v1.)
- **No rounding inside compute — ever.** JS `toFixed` (half-away) vs Python `round` (banker's) differ; precision is display config (`plots[].precision`). Fixtures compare at rel-tol 1e-9. Existing natives' internal rounding is removed at migration.
- **Streamability rule:** an indicator is streamable **iff** its last confirmed-bar compute returned `state`; then ticks call `compute({bars: tail, prevState, barstate})`. No `state` ⇒ throttled (rAF/250ms) full recompute — acceptable at ≤5000 bars. No hand-written per-indicator incremental paths.
- **Rollback discipline (Pine's model):** `prevState` snapshots at the last CONFIRMED bar only; forming-bar recomputes never advance it. Bar-close outputs must be reproducible from history alone; anything that can't be is labeled `repaints`.
- **Bars identity:** an epoch/generation token from the bars layer (history amendments: delta re-aggregation, reconciliation deletes) — engine never diffs tails to guess. Full `setData` only on epoch change; last-point `update()` otherwise.

---

## 5. Engine runtime & migration (Phase B)

- **Registry:** bundled natives + fetched catalog (server/premium entries listed for merchandising even when locked).
- **Instance model:** `[{instanceId, defId, defVersion, inputs, styleOverrides, placement, hidden, scope?}]`. `scope` (chartId) present from day one as data; **global default at cutover**, per-chart + templates flips on in Phase C.
- **Settings migration — ordered safeguards (architect R4):**
  1. FIRST commit: `mergeChartSettings` passes through `settingsVersion` + `indicatorInstances` (today's whitelist silently destroys unknown keys).
  2. Read-time migrator old→instance-list is a pure function golden-tested against REAL prod `chart_settings` blobs.
  3. Write path merges by `instanceId` (or CAS on `settingsVersion`) — multiple chart widgets are concurrent writers; last-writer-wins loses instance adds.
- **Binding layer:** generic `Map<instanceId, boundSeries[]>` owning ALL series/pane lifecycle; reuse-first; no create/remove during ticker flips or ticks; pane HANDLES (`series.getPane()`), never indices; lives OUTSIDE the monolithic settings effect (that isolation is the actual perf deliverable).
- **Two-flip migration (architect R1 — replaces naive strangler):**
  - **Flip A:** engine indicators render into the LEGACY stacked-margin bands via the placement adapter — zero geometry change, so per-indicator screenshot-parity gates are tight and honest. Order: LWC 5.1→5.2 bump + regression pass + baseline freeze → settings passthrough → BB + RSI pilots → **MACD third** (colorMode + multi-plot + histogram stress test) → remaining 11 (VWAP late; session semantics + `vwapOverride` special cases).
  - **Flip B:** after all 14 are engine-owned, ONE atomic, feature-flagged cutover from margin-bands to real LWC panes, with dedicated visual QA on all four theme surfaces and a rollback flag. `paneMargins`/`chartRegion` contracts stay unit-tested until Flip B completes. `volumeProfile`'s legacy canvas effect is NOT in that deletion — it is carved out (§11) and no flip touches it.
- **The enumeration ledger — the count is a TEST, not a comment.** "An indicator is enumerated in N places" is the problem this phase exists to end. `indicatorRegistry.js` said **seven** in July 2026. The B3 plan counted **sixteen**; walking it produced **twenty**, then **twenty-one**, then **twenty-two** (a frozen chart-settings capture in a PAGE component no chart-module walk had opened), and B3 Task 12's walk produced **thirty-two** — one entry per *(file, contiguous region)* a new or migrating indicator must be edited into, which is a stricter convention than the plan's and accounts for most of the delta. Five of the thirty-two were genuinely new finds: `readout.LEGACY_SLOTS` (`legChips`' twin), `ChartSettingsModal`'s hardcoded section list, the `toggle:` switch and the Alt-key block in `StockChart` (where the keyboard commands are *consumed* — the plan's site #13 named two shortcuts; there are **four**, across four regions in two files), and `indicator_alert_evaluator.INDICATOR_FUNCS`, the alert dropdown's backend twin. **B3 retired six regions per flipped indicator** (the refs, the compute branch, the render block, the crosshair read, the hide-all entry, the Flip-A guard), **one globally** via the pane-margin projection (`paneMargins.js` stays *consumed*, not owned), and **two outright** — the settings tab's hardcoded section list and `indicatorRegistry`'s `VWAP_FIELDS`. **Thirty-one remained at B3's handover**, and `app/src/components/chart/engine/__tests__/enumerationSites.test.js` holds them (that file's `SITE_COUNT` is the live number; this sentence is the dated handover figure and is not tracking it): every site is anchored (a marker that moves or duplicates fails BY NAME), the count is asserted, a discovery scan refuses a shipped module that hand-lists four or more indicators and is not on the ledger, and a settings section added without an engine definition fails. **B4 inherited eighteen regions and RETIRED EVERY ONE:** ~~`chartRegion.INDICATOR_LABELS`~~ · ~~`ChartToolbar`'s `OSC`~~ + ~~its 15 rows~~ · ~~`keyboardShortcuts`' two regions + `StockChart`'s two consumer regions~~ · ~~`IndicatorAlertPopover`'s `INDICATORS` + `CONDITIONS`~~ · `StockChart`'s ~~`IND_OPTS`~~, ~~`OSC_OPTS`~~, ~~right-click Hide~~, ~~`handleCopyShareUrl`~~, ~~`legChips` + `readout.LEGACY_SLOTS`~~, ~~the crosshair reads~~ · ~~`chartBus.ALLOWED_INDICATORS`~~ · ~~`indicatorRegistry.ENGINE_ROW_DEF_IDS`~~. (~~Struck~~ = retired, and by the end of the phase all eighteen are: **Task 3** the five name lists, **Task 4** the four keyboard regions, **Task 6** the settings-row list, the **alert-catalog** task the popover's five-part twin, **Task 8** the toolbar's fifteen rows, **Task 11** the voice bus, **Task 5** the share link, **Task 10** the legend's three parts. ⚠️ **A RETIREMENT CAN ADD A ROW**: Task 4's four regions retired *into* `INDICATOR_CHORDS`, which hand-names four ids and is therefore itself a ledger row (fate `keep`) — so `B4` fell by four while the total fell by three. The ledger's `B4` bucket is now **EMPTY**, and its partition assertion carries **no `B4` key at all** (`reduce` emits nothing for a fate with no members, so `B4: 0` would never match). Read the live count and partition in `enumerationSites.test.js`, never here.) That last one carried its own retirement rail — *the test fails the day B4's generated dialog covers VWAP's opacity / line style / line width* — and **B4 Task 6 fired it**: every definition now gets a generated row, the constant is deleted, and the rail is REPLACED by its successor (*every declared input of every definition is reachable from the generated dialog*) rather than removed. **Two of the twenty B3 handed over were re-adjudicated by the B4 plan and are NOT B4's:** `paneMargins.PANES` → **B5** (a layout table that retires when bands become panes at Flip C, and one that B4's own Global Constraints forbid it from modifying at all — a site a phase may not touch cannot carry that phase's fate), and `indicator_alert_evaluator.INDICATOR_FUNCS` → **C** (eight Python closures cannot be derived from a JS definition, and §8 rebuilds the evaluator in C; B4 collapses its FRONTEND twin — `INDICATORS` + `CONDITIONS` — into it, so the dropdown can no longer offer an alert that cannot fire). ✅ **PHASE C TASK 10 (2026-08-06) HONOURED THAT FATE AND THE ROW DID NOT LEAVE THE LEDGER.** The 28-row literal is DELETED and derived from `alert_series.SERIES_FUNCS` (a value is the last non-`None` element of a column), proven gone BY IDENTITY — the old anchor re-run under the regex that used to demand exactly one match now demands zero. ⚠️ But the *file* still hand-lists addresses (`ALERT_CONDITIONS` — which conditions an address offers is a product decision nothing derives), so the row was **RE-POINTED, not deleted**: the Python discovery scan still names the file, and a deleted row beside a named file is the unledgered-site failure the ledger exists to produce. **And one row was ADDED rather than retired:** `voice_client_action_tools.py`'s `_INDICATOR_ALIASES` → **C**, the voice add-indicator phrase map — a *sixth* site the JS discovery scan structurally cannot see (it walks `app/src/**/*.jsx?`, so a Python enumeration is invisible to it however many ids it names, which is the shape that turned seven into thirty-two). Found by the wave that fixed the voice bus, and recorded because a ledger's number is only worth something if it moves with the CODE, including for a site nobody retired. ✅ **PHASE C TASK 15 (2026-08-06) ADJUDICATED IT `keep`, ON A MEASUREMENT** (§11, C-A5): the map's only resolver is `_INDICATOR_ALIASES.get(raw, raw.replace(" ", ""))`, and deleting the map changes the answer for nine of its eleven phrases — a mishearing, two truncations, four colloquial MA phrases whose targets are not registry ids at all, and two multi-word names the fallback de-spaces into something else. The two it does not change are identity rows. **So the `C` bucket is now EMPTY**, which is what a phase ending looks like: every fate the letter promised was either done or answered, and none was left to rot. The ledger's asserted partition lives in `enumerationSites.test.js` → *"every B4 region is retired"* and is **deliberately not restated here**: every B4 task decremented its `B4` bucket, so a copy of it in this spec is a control that rots green. ⚠️ **The partition is a HISTOGRAM** — moving one site between fates fails it, but SWAPPING two fates preserves every count and passes. ✅ **THAT BLIND SPOT IS CLOSED, AND THIS SENTENCE USED TO END BY SENDING THE READER TO THE COMMENTS FOR IT.** Beside the histogram there is now a sorted `file::region → fate` literal, **regenerated from `LEDGER` and never hand-edited**, which refuses a permutation by NAME. The two assertions catch different failures and both are kept: a fate TYPO makes a sixth bucket, which only the histogram's whole-object `toEqual` refuses, and a mapping would accept it happily. **B4 shipped ZERO migrations:** `ENGINE_FLIPPED_DEF_IDS === ENGINE_MIGRATED_DEF_IDS` at every commit of the phase, both sets sealed against a runtime `.add()`, and the `engineEnabled` settings-migration record stays **OPEN** for B5. Finally, the ledger counts ENUMERATIONS; the other census — how many places can change an indicator's enable state — is `controlDoorCensus.test.js`, which pins every `setIndicatorEnabled` / `setIndicatorInput` call site to a named door, refuses a raw `cs.indicators.<id>.enabled` write outside `instanceControls` with five named exceptions, and counts the whole-blob writers that door seven is made of. It found door seven's **third** site (`ChartSettingsModal`'s reset) on its first run — a site no ledger walk and no discovery scan can see, because a reset names no indicator.
- **Perf budget:** ≤60 series AND ≤8 panes per chart; columnar→object mapping reused, never re-allocated per update; per-indicator compute time logged in dev with a >2× regression alarm.
- **Mount-site scoping:** full management UX on Charts workspace + TickerPopup; read-only rendering at the other mount sites (18 total; `hideLegend` surfaces get no chips → error states fall back to a console+telemetry path).

---

## 6. UX contract (Phase B addendum — REQUIRED before pilot implementation)

**Copy TV exactly:** legend chip anatomy, three-tab settings dialog (Inputs/Style/Visibility†), add-flow (search-first, add-and-stay-open, checkmarks), pane grammar (drag divider, move-to submenu, auto-remove empty panes). †Visibility tab deferred to C.

**Entry points:** labeled "Indicators" toolbar button (not icon-only in v1) · right-click/long-press → "Add indicator…" (pane-aware) · Ctrl/Cmd+I only — bare keys collide with type-to-search-symbol. Gear-panel checkbox rows become a "Manage indicators →" launcher at cutover, deleted at Flip B; both surfaces render the SAME instance list during migration.

**Touch mapping (uses shipped primitives):** tap chip → `ContextPopover` bottom sheet (name + values, then Settings/Hide/Move/Alerts/About/Remove as 44px rows) · settings in `Sheet variant="auto"` (fullscreen on phone) · long-press pane → context menu · chips collapse to dots below phone breakpoint · chip live values render only while crosshair active, rAF-throttled.

**Settings form spec:** one input per row, label left/control right, fixed control widths per type; `inline` packs ≤3 same-type; live-apply + Cancel-rollback (snapshot on open); 250ms debounced recompute; numeric commit on blur/Enter with visible clamping; `activeWhen` dims — never removes; dialog max-height 70vh with collapsible groups; full tab order, stepper arrows (Shift=×10), focus trap, aria from label+tooltip; ONE formatting pipeline drives Style-tab precision, chip values, and crosshair readout.

**Instance state inventory (10 states, strict color language — red broken / amber degraded / gray intentional):**
1 Loading (skeleton, never flash error) · 2 Refreshing (serve-stale: keep stale plot + subtle activity) · 3 **Warmup/insufficient history — gray "needs N more bars," NEVER error-styled** · 4 Compute error (red dot on chip, message in tooltip/click + Retry + copy-diagnostic; plots removed, chip stays) · 5 Server unavailable (amber, backoff retry, last-good ghosted with as-of; failed sole-occupant pane shows inline message, never blank) · 6 Premium locked (lock + preview) · 7 Hidden · 8 Hidden-on-this-TF (grayed + tooltip, not absent) · 9 Repaint badge (informational, never error-colored) · 10 Version-migrated notice (toast/inbox, never chip state).

**Deletion guard:** removing an instance that alerts reference prompts — "N alerts use this instance: keep evaluating / delete alerts." Never silent.

**Novice layer:** per-indicator About (plain language + what the badge means) · library rows show name, one-line "what it tells you," category, repaint badge, tier badge, star · sparkline thumbnails from canned SPY data · one-click Starter Set.

---

## 7. Visual design system (lock BEFORE pilot #1; keyed to existing tokens.css)

**Semantic tokens:** `ind.bull`→`--gain` · `ind.bear`→`--loss` · `ind.neutral` #8a8574 · `ind.warn` = NEW amber, distinct from gold · `ind.info`→`--info` · `ind.premium`→`--ut-gold` (rationed: ≤1 gold element/chart; gold ≠ warning).
**Opacity ramp (named steps, never raw alphas):** `fill-faint` 8% · `fill` 12% · `band` 16% · `emphasis` 24% · `glow` 35% · `solid` 100%. OLED theme resolves one step higher.
**Gradient standard:** two-stop `color@emphasis → color@0`, vertical from anchor. One shape everywhere.
**Multi-series ramp:** fixed-order 6-slot categorical, CVD-validated on all four theme surfaces.
**On-chart type:** Instrument Sans only, `tabular-nums`, never mono (owner-locked); values 11px/600, labels 10px/600, zone labels 10px/600 uppercase; text on chips/halo, never raw over candles; text never wears series color.
**Zone states:** forming = ghost `fill-faint` + dotted · active = `fill` + solid origin-edge border · mitigated = `fill-faint` + dashed + 40% desat · invalidated = border-only 50%.
**Institutional rules (machine-checkable):** ≤2 hues + 1 neutral per indicator, ≤4 per chart · line colors in OKLCH L 0.55–0.70 band (pure #0f0/#f00 rejected with snap-to-token) · steady fills ≤16% · glow fires on state change and decays over N bars — never permanent · shape first, color second · no pulsing/blinking; transitions 150–250ms ease-out and settle.

🔴 **THIS PARAGRAPH USED TO END "…become the visual linter in D". IT DID NOT, AND THAT IS A SCOPE DISAGREEMENT THIS SPEC HELD WITH ITSELF FOR A WHOLE PHASE.** §2's D row lists four deliverables and a *visual-budget* linter is not among them; this sentence promised a **second, unrelated linter** with its own corpus and its own gate. Phase D declined it, deliberately and in writing (plan Contradiction 6): it shares no mechanism with the AST work, and folding an unscoped second linter into the phase that first admits user input to a compute path would have put two unrelated risks inside one frame. **The repaint linter that D DID ship is a different tool** — it reads a tree's forward reach, not a palette. ⏳ **This is the ONE question that leaves Phase D open, and it is an owner call about scope, not a task's to take** (see §11).
**Legend chips:** one line 20px, `shortName + params`, max-width ~200px middle-truncate, controls on hover only, stack below OHLCV readout, >4 chips/pane collapses to "+N", error = 6px dot + border tint only.
**Branded screenshot export (Phase A/B feature, not afterthought):** opaque `--bg`, 1px gold hairline + bottom bar (compass mark, wordmark @60%, ticker·TF·date), **repaint badge composited into the frame**, 2× DPR render (never upscale), corner-anchored.

---

## 8. Alerts & signal ledger

- **Closed-bar rule: nothing enters the ledger unless it is closed-bar evaluated** (architect R2). The current evaluator fires on the forming bar with cycle-granularity crossings — publishing those fire-times would be an audit trail of our own repainting. Phase A satisfies this by construction (Signature signals are server-computed on confirmed bars only; the legacy evaluator keeps running but its fires do NOT enter the ledger). The evaluator itself is rebuilt in C: evaluate at the last CONFIRMED bar per TF boundary; derive `prev` from the computed series at the prior confirmed bar, not persisted cycle state; `last_value` demoted to delivery-dedup. One fixture feeds a forming bar and FAILS if a cross fires.
- **Ledger (Phase A, write-only):** append-only table recording every Signature signal from launch day (fire-time, defId@version+rev, symbol, TF, bar-close values). Alert-lane events join only after the closed-bar fix. Private-first surfaces (R-multiples, MFE/MAE per signal type) before any public scoreboard.
- **Phase C alert engine:** every plot auto-alertable (crossing/GT/LT/channel) + named events pinned to top with human labels; instance named in alert rows ("RSI(7) crossed 70" vs "RSI(14)"); threshold prefilled from current value; price alerts + push/Discord delivery + fired-alert history (same table family as ledger) + re-arm/snooze; per-TF worst-case latency stated in UI; orphaned bindings go to a visible needs-attention state — never silently dead.
- ✅ **BUILT, 2026-08-06 — and the closed-bar rule above is the ONE clause still switched off.** The rebuilt evaluator, the shadow lane, the declared per-address diff, fire-once/re-arm/snooze, the fired log, `close` as a LEFT operand, the `instance_id` producer and the ledger door all ship. `ALERT_EVAL_MODE` remains `"forming"`, so **the ledger door has zero call sites and forming-bar fires still cannot enter the ledger** — by a refusal that RAISES rather than returning `False`. Two things a reader of this section must not infer from "built": (a) *"one fixture feeds a forming bar and FAILS if a cross fires"* is satisfied in BOTH directions, and the refused fire is proven to EXIST before it is refused (bar 53, sample 1 of 4, value 74.31, read off the frozen fixture through the shipped forming lane); (b) `ichimoku.chikou` **can never fire closed-bar** — its 26-bar trailing pad makes the confirmed bar's value `None` — so a user's Chikou alert stops permanently at the cutover. That is the one item in this section a member could read as a bug, `ichimoku` is live in the catalog today, and it needs an explicit owner decision before Task 8.

---

## 9. Diagnostics & quality gates (standing, all phases)

1. **Shared golden fixtures** (bars→expected columns JSON) run by vitest AND pytest (later the AST interpreter); one written semantic spec (alignment, warmup, NaN, seeding); rel-tol 1e-9; mandatory session fixtures: extended-hours day crossing UTC midnight + a DST transition (the JS VWAP UTC-day bucketing bug class). `indicator_compute.py` stays plain loops (numpy changes summation order).
   ✅ **THERE IS NO EXCEPTION TO THE REL-TOL RULE. There was exactly one, and it is CLOSED.** `MACD_HEAD_MASK` (§11) held the engine's `macd` COLUMN and the array `StockChart` draws from at NaN on the first `signalPeriod - 1` bars of the MACD line where Python has values (8 bars at 12/26/9). The owner **dropped the mask on 2026-08-02** — measured at **88 changed pixels**, applied in its own commit — so both lanes and the render now agree at 1e-9 everywhere, with nothing carved out. Record: `docs/decisions/2026-08-02-macd-head-mask.md`. **A lane divergence found from here on is a bug and has no precedent to point at** — the one that existed was measured in pixels, signed off by the owner and then removed, which is the bar any future request for an exception is held to.
2. **Nightly end-to-end parity check:** server-computed vs client-path-computed for a hot symbol — catches input-path drift fixtures can't (house real-fetch rule applied to parity).
3. **Visual parity gate:** per-indicator screenshot diff during Flip A (tight, honest); Flip B gets its own four-surface QA. One renderer version under all baselines.
4. **Error isolation:** one indicator throwing never breaks the chart; server failure never blanks a pane.
5. **Lazy Python porting:** port natives to the server registry on demand (alert/premium need), each landing with its fixture. No eager 15-indicator port.
6. **Perf:** compute-time logging + >2× regression alarm; series/pane caps enforced.

---

## 10. Phase A — Signature Launch (immediate; own implementation plan)

**Ships by Sep 5, on the EXISTING chart path (no engine dependency):**
1. **Dark Pool Levels** — DP volume shelves as horizontal levels/zones on price.
2. **Flow-Confirmed Breakout** — breakout markers printing only on options-flow confirmation, closed-bar only.
3. **GEX Walls / Institutional Footprint** — dark pool + blocks + GEX fused at price.

Each: server-computed (hardcoded endpoints acceptable; genericize in C), wrapped serve-stale + single-flight, premium-gated by handler identity, wire format already spec-compliant (§4 null-for-NaN) so Phase B inherits them transport-ready. Hand-audited `non-repainting` badge + one-paragraph "how it's computed" honesty blurb + owner workshop video each. Signal ledger recording from day one. Landing section: receipts positioning aimed at burned-vendor customers. Stretch: branded screenshot export (extends existing `chartScreenshot.js`).

**Explicitly NOT before Sep 5:** LWC bump, engine/registry/binding/panes, settings migration, settings-dialog rework, generic alerts, migration of any existing indicator. The 15 natives ship untouched — untouched code is launch-stable code. (Phase B may BUILD in an isolated worktree during this window; it may not SHIP.)

**Prod note:** `serve_stale.py` (`174359fd`) is not yet deployed — Phase A endpoints depend on it landing first.

---

## 11. Adjudicated decisions log

| Question | Decision | Basis |
|---|---|---|
| Scripting tier (P3) | **Killed as product**; sandbox = AI plumbing only; revisit 2027 on demand | Trader + CEO; TV marketplace war settled; solo-owner security/support tax |
| Pilot pair | **BB + RSI**, MACD third, VWAP late | 4 of 5 seats; VWAP = session semantics + special-cased props |
| `events` in schema v1 | **Yes** (events-as-columns); consumption lands with alert rework | Unanimous |
| Server lane | Signature slice ships AT launch (hardcoded); generic lane in C; wire contract frozen now | CEO inversion + architect/UX P1.5 |
| Per-chart vs global | Global at cutover; `scope` key as data day one; per-chart + templates in C | Launch stability + R4 concurrency |
| LWC 5.2 | Before engine work, after Sep 5, never parallel | Unanimous; one renderer under all baselines |
| Repaint labeling | Audited metadata (A/B) → machine linter with AST (D) | No static analysis of hand-written JS; don't build throwaway introspection |
| Scoreboard | Ledger writes from day one; private surfaces first; public per-toolkit after proof | Trader ("loaded gun") + CEO (time-accrued value) |
| Pricing | No new SKU at launch; Signature strengthens existing premium; endpoints tier-gated from day one; toolkit names seeded in content early | CEO |
| Positioning | "The chart TradingView can't be" — alongside TV, not against it | Trader |
| `volumeProfile` (B3 A4, 2026-08-02) | **Permanently carved out** of the `plots[]` grammar. 15 indicator SETTINGS keys, **14 series-expressible** indicators. It stays a legacy canvas overlay and no B3 flip deletes it. ⚠️ **THE "when one exists (C/D)" CLAUSE IS STRUCK, BECAUSE A FATE WHOSE CONDITION KEEPS ARRIVING AND WHICH NOBODY ACTS ON IS A CONTROL THAT ROTS GREEN.** C explicitly declined it and handed it to D; D's §2 row does not list it and D declined it too; a parenthetical assigned it to two phases and no task in either. **It is assigned to NOBODY and needs an owner call, not another phase letter.** Measured at Phase D close: `nativeRegistry.CARVED_OUT_INDICATOR_KEYS` still holds exactly `volumeProfile` | It renders to a 2D canvas, not through `addSeries`: no compute function in `indicators.js`, absent from `indicatorData` and `paneMargins`, and no v1 plot style expresses horizontal volume bins. A definition for it could be neither computed nor bound. Written down + railed at `nativeRegistry.CARVED_OUT_INDICATOR_KEYS` |
| **`MACD_HEAD_MASK`** — the MACD head-mask (B3 A5, 2026-08-02) | ✅ **ACCEPTED 2026-08-02 — the owner DROPPED the mask; `MACD_HEAD_MASK = false` ships.** The chart no longer holds the MACD line's head back to the signal's first bar: bars 25-32 (8 at 12/26/9) draw at the values Python and the golden fixture publish. **Cost, measured before the decision and re-confirmed at the flip: 88 changed pixels (0.011828%)**, 20/20 runs, zero variance — one contiguous 44×4 px region at `x ∈ [136,179]`, `y ∈ [394,397]`. Builds `9f566cd22874`→`9045bb69fc56` (measurement) and `f141618f95e6`→`54443afee3e3` (at the flip), `--cases macd_headmask`. **§9.1's exception is CLOSED** — no lane divergence remains, at compute or at the render boundary | The maths was Python's and the fixtures'; the picture was what shipped, and moving it was visible at the start of every MACD chart — so it was priced first and applied second. Parked behind ONE constant that both lanes read (`nativeRegistry.MACD_HEAD_MASK`), which is why the flip was one edit in its own commit and why it measured 88 rather than 0: `macd` is not migrated, so a flip reaching only the engine would have moved nothing a user sees. The constant is KEPT (reversal is one edit, priced the same). Record + both measurements: `docs/decisions/2026-08-02-macd-head-mask.md`; pinned — in the new direction — by `nativeRegistry.test.js` and `engine/__tests__/macdHeadMaskRendered.test.jsx`, both of which go red the moment it moves again |
| **`VWAP_SESSION_ANCHOR`** — session VWAP bucketed by UTC DAY (B3 A7, 2026-08-02) | ✅ **ACCEPTED 2026-08-02 — the owner CORRECTED it; VWAP now anchors on the ET session and `compute.rev` is bumped to 2.** Before the fix, `computeVWAP` restarted its accumulator on a UTC calendar day, not an ET session. Regular hours never notice (09:30-16:00 ET is one UTC day), which is why nothing caught it; on EXTENDED hours — the only hours VWAP renders on — it wipes mid-session at 20:00 ET under EDT and **19:00 ET under EST**, and because Monday evening has already opened the next UTC day, **Tuesday's 04:00 ET open never resets at all**. **Correctness cost that was ELIMINATED: the chart used to open that session at 93.9178 where a session anchor reads 108.3633 — $14.45 wrong — and stayed >$0.50 wrong for 120 of its 193 bars; 207 of 579 bars differed by >$0.01.** **Pixel cost of the correction, measured before the decision and re-confirmed at the flip: 2,590 changed pixels (0.348118%)**, 20/20 runs, zero variance, `--cases vwap_only`. Measurement pair `d64c84c2ebf7` (UTC-day) → `8bbbb44e1110` (ET-session); **applied at `f5afd6db`, re-measured `89f73b36ae29` → `35ec82560ea5`, same 2,590**; independently re-confirmed a third time by review (same pair, 5/5). The SAME 2,590 with `--instances-side both`, because both lanes read one function. Each build independently deterministic (`--same-build --repeat 5`: 0 px). **Blast radius verified ZERO for alerts and backtests** — `vwap` is not in `INDICATOR_FUNCS` (8 keys) so a VWAP alert can be created but can never fire, ⚠️ **[DATED ADDENDUM, 2026-08-06 — THAT PREMISE EXPIRED AT B5 AND THE CONCLUSION DID NOT SURVIVE IT.** `INDICATOR_FUNCS` grew past eight during B5's cutover and `vwap` IS an alert address today: it fires, it is in the declared closed-bar diff, and the frozen replay log records its fires. Worse, the alert lane read it through a DIFFERENT call site than the chart, and that call site handed `compute_vwap_raw` the bars store's `YYYYMMDD` integer as `t` — so 400 daily bars landed one *second* apart and the whole window accumulated into a single 1970-anchored session, silently, in both lanes. Fixed 2026-08-06 (`29569946`): the compute now validates its own unit and refuses the WHOLE column rather than answering from one bucket. It is the only change ever permitted to move the frozen fire log, and it moved it once, with the delta predicted before it was measured.]** no backtester rule reads it, and `avwap_reclaim` is pivot-anchored with no time bucketing. Two RENDER surfaces do move and are intended to: `IntradayDayPopover` (forces VWAP on at `tf="5"`, mounted by Model Book) and the headless `ChartRender` newsletter path | Unlike the head-mask this is the MATHS, so applying it is a **`compute.rev` bump** (not `version`) and force-migrates every binding pinned to `vwap@rev 1` — which is why it is a trading decision and not a migration's. Deliberately NOT bundled into the VWAP Flip A commit: both lanes read `computeVWAP`, so correcting it moves A and B together and `engine_vwap_vs_legacy` would still report 0 — the migration would look verified while the picture changed underneath it. Record: `docs/decisions/2026-08-02-vwap-utc-day-bucketing.md`; pinned — **in the new direction** — by `engine/__tests__/vwapUtcBucketing.test.js` and by `goldenFixtures.test.js`'s three session cases, which go red the moment the anchor moves again. The golden fixture already carried BOTH series, so not one fixture byte was reseeded and neither lane could re-baseline underneath the other |
| **`ENGINE_ENABLED_MIGRATION`** — `engineEnabled` is false in every stored blob (B3 A13, 2026-08-03) | ✅ **RESOLVED 2026-08-04 by B5 Task 4 — the flag is DELETED at all SEVEN sites** (§8.2's default recommendation, adopted in full; record §12, plus `docs/decisions/2026-08-04-engine-enabled-deleted.md`). The history below is kept because the TRAP is the reusable lesson and the resolution only makes sense against it. ⚠️ Two sentences in it are now HISTORY, not status: *"the record stays OPEN and the flag is deleted at B5"* — both halves happened in ONE commit, because `enumerationSites.test.js`'s biconditional (*the header says OPEN ⇔ `mergeChartSettings` still reads the flag*) does not admit a halfway state; and *"asserts the record still says OPEN"* — it reads the header and requires it to AGREE with the code. ⚠️ The MIRROR (`cs.indicators` + its allow-list) is **B5 Task 9's** and is NOT resolved here. — **B3 shipped with it unresolved, deliberately, because nothing is broken and the obvious fix is a no-op.** `mergeChartSettings` computes `engineEnabled: parsed.engineEnabled === true` (`chartDefaults.js:404`) — a read of the **stored blob**, never of `CHART_DEFAULTS`. An absent key and an explicit `false` are the same answer, so **flipping the default cannot heal one**, and nothing in shipped source ever writes it `true` (the single occurrence is `ChartRender.jsx:236`, the headless `/r/chart` route, gated on `?instances=`). **Nothing breaks on ship day:** a FLIPPED definition runs the engine regardless of the flag (`engineActive = engineOn \|\| ENGINE_FLIPPED_DEF_IDS.size > 0`) because it has no hand-written block left, and `FLIPPED === MIGRATED`, so all four pilots draw for every existing user — evidenced by `flipBStoredBlobs.test.jsx`'s 25 real blob strings and by the `flipb_*` parity cases (legacy-shaped settings both sides, no instances) reading **0 px**. **What is missing is a decision**, and three things still ride on the flag: a MIGRATED-but-UN-FLIPPED definition needs it (an empty category B4 creates the first time it migrates without flipping — that definition would render for nobody); `engineDrawnInputs` returns EMPTY flag-off, so the toolbar shows the legacy mirror; and `engineInert` is identically false. **⭐ A SEVENTH WRITER was found closing this:** `applyPreset` (in `ChartToolbar.jsx` AND `Settings.jsx`) and `resetToDefaults` write a whole blob spread from `CHART_DEFAULTS`, stamping both engine keys over the user's — the `uctDefaultChartSettings()` hazard class, not an enumeration site, invisible to the discovery scan | **"engineEnabled must be on by ship" is a SETTINGS MIGRATION, not a default flip, and no phase plan has one.** Recommendation, unimplemented on purpose: require *migrate and flip in the same change* (now the runbook's per-indicator checklist rule) so the broken category never exists, and **delete the flag at B5** with the rest of `cs.indicators` — its only remaining job is a phase-internal state no user has an opinion about. If B4 needs a migrated-but-un-flipped definition, ship a **versioned read-time migration first** (`settingsVersion` 1→2; answer from `CHART_DEFAULTS` for a below-version blob with no explicit key; an explicit stored `false` must still win), in its own commit, and re-run the 24-case parity set — `mergeChartSettings` is on every chart's path. ⛔ **Do not flip `CHART_DEFAULTS.engineEnabled` alone**: it changes nothing for any existing user and moves the branch from "the flag decides nothing" to "the flag decides nothing and the tests say it does". Record: `docs/decisions/2026-08-03-engine-enabled-settings-migration.md`; pinned by `engine/__tests__/engineEnabledMigration.test.js`, which asserts the record still says OPEN, asserts the default-flip is a no-op **against the real default flipped in place**, and holds the seventh writer down by object identity plus a bounded source probe. **B4 (2026-08-03): adopted §8.2 — migrate-and-flip-together; no settings migration ships in B4; the record stays OPEN and the flag is deleted at B5.** B4 needs no migrated-but-un-flipped definition because its inherited regions are retired by DERIVING lists from definitions that already exist — derivation needs a definition, only rendering needs a migration. Railed by `enumerationSites.test.js`'s no-stranding assertion, which reads this row's decision from the record's own **Status:** HEADER line (isolated and counted — a whole-file match let a second `**Status:** … OPEN` line keep it green after the header was resolved) and asserts `FLIPPED === MIGRATED` in **both** directions, so it fires on any half moving: migrating without flipping while the record is OPEN, flipping without migrating (the legacy block deleted with nothing authorised to replace it), **or** resolving the record without re-reading the rail. It also probes both flip sets for **mutability** — `Object.freeze(new Set([…]))` does not stop `.add()`, and a runtime add is invisible to every static rail — which is the one clause no other rail carries. Record §10 |

| **C-A1 — `sar` alerting** (2026-08-06) | ✅ **The refusal SURVIVES, NARROWED.** `_SAR_IS_NOT_OFFERED` became `_SAR_IS_NOT_A_THRESHOLD`: `sar` still has **no fixed-threshold address**, and it gained **two EVENT addresses** (`sar.priceCrossedSar`, `sar.trendFlipped`) offered BY EVENT AND RELATION | The reason for the deferral was that the relational primitive belonged to C — so the deferral EXPIRED when C built it, and a refusal that outlives its own reason is the rot this spec keeps finding. The narrower refusal is also the truer one: SAR's value jumps sides at every trend flip, so a fixed threshold on it is meaningless, and that has nothing to do with which phase we are in. `compute_sar_raw` already returned a ±1 `trend` column pinned by an existing fixture, so **zero fixtures were reseeded**; the two event columns are re-derived from that pinned column in BOTH lanes, which makes the oracle older than either implementation. The prose test moved DOWN a level (`test_sar_has_no_fixed_threshold_address_and_says_why`) with the two event addresses as its POSITIVE control. ⚠️ Events went into a SEPARATE `EVENT_FUNCS` for a forcing reason: `build_alert_grid` generates the frozen replay grid from `INDICATOR_FUNCS`, so growing that dict would have destroyed the instrument |
| **C-A2 — the enumeration scan is PYTHON-AWARE** (2026-08-06) | ✅ **A second discovery scan, over `api/**/*.py`, runs beside the JS one** and refuses a shipped Python module that hand-lists four or more indicator ids and is not on the ledger | The JS scan walks `app/src/**/*.jsx?` only, so a Python enumeration was invisible to it however many ids it named — the exact shape that turned seven sites into thirty-two. It earned itself immediately: it flagged `indicator_compute._CASE_COLUMNS` on its FIRST run (unledgered), and then caught `alert_series.SERIES_FUNCS` **on the commit that created it**. ⛔ Its non-vacuity floor is derived from the `keep` fate and **never from `C`**, because every `C` row was scheduled to retire in this phase and a floor built on them would collapse to `[]` — which satisfies the unledgered-site assertion while checking nothing at all |
| **C-A3 — the `time` and `symbol` input types stay RESERVED** (2026-08-06) | ✅ **No schema v2.** AVWAP's anchor is an `enum`; the RS line's benchmark is an `enum` and the definition is `compute.kind: 'server'` | §3.1 fails closed on both reserved types, and §2 ships two indicators that *appear* to need them. They do not: an anchor is a named calendar boundary (seven of them, one frozen list read by the maths AND the definition), and a benchmark is a short curated list, not free-text symbology. Unreserving two input types inside an alerts phase would have put a schema change with no UI spec and no consumer inside the frame of a cutover. `defSchema.js` was not touched |
| **C-A4 — the delivery-rounding wrappers STAY** (2026-08-06) | ✅ **`compute_*_raw` cores stay unrounded; the `compute_*` delivery wrappers keep rounding.** New this phase: `compute_avwap` 4dp, `compute_atr_bands` 4dp, **`compute_rs_line` 6dp** | Retiring the wrappers would change what every armed alert computes, in the phase whose whole job is that alerts stop changing under people. The RS line's 6dp is the one place the convention was re-derived rather than copied: it delivers a RATIO near 0.10, where a 1e-4 quantum is ~1e-3 of the value against ~1e-6 for a price near 100 — measured, not chosen by taste, and pinned by `test_the_rs_line_wrapper_rounds_at_SIX_because_it_delivers_a_RATIO` |
| **C-A5 — `_INDICATOR_ALIASES` is `keep`, not `C`** (2026-08-06) | ✅ **Re-fated on a measurement, which EMPTIES the `C` bucket** | The brief for the adjudication argued the row had become derivable because Phase C ships an `avwap` DEFINITION. **That premise is false and it is false by measurement**: the tool's only resolver is `_INDICATOR_ALIASES.get(raw, raw.replace(" ", ""))`, under which `"anchored vwap"` resolves to `anchoredvwap`, so deleting the supposedly-redundant row BREAKS the phrase. Deleting the whole map changes the answer for **nine of eleven** phrases; the two it does not change are identity rows, and two of the seven targets (`ma50`, `ma200`) are not registry ids at all. What Task 14 actually changed is one door further out — `'avwap'` left `chartBusIndicators.NON_DEFINITION_ALIASES`, so the CLIENT now honours an action it used to refuse. ⚠️ **A fate whose condition has ARRIVED is a control that rots green** (B5 Task 13 deleted two `phase` rows for exactly this), so `C` could not simply be left in place at the end of Phase C |

| **D-A1 — ONE parser, and the AST is the persisted artifact** (2026-08-07) | ✅ **`jsep` pinned EXACT at `1.4.0`; `parse.js` is the only thing in the tree that turns text into a tree, and what is STORED is the tree, never the source.** `compute.ast` and `compute.source` must both exist and **agree by HASH** (a reformat is not a disagreement; different maths is), and `compute.fn` **IS** `astHash` — the tree is the implementation, so "the handle changed" and "the maths changed" become one event | A second parser is a second grammar and a second grammar drifts silently, which is the failure this whole platform's ledger exists to catch. Proved rather than promised: `canonicalise(jsep(source))` deep-equals the hand-written tree for **all 17** corpus cases — the only place source and tree are ever compared — closing the `parse(source) != ast` fork Task 2 had declared itself blind to. ⚠️ A jsep trap that makes the obvious control unfalsifiable was found and pinned: **`jsep.binary_ops` is a ONE-TIME COPY** made at init, so the default export answers with all 23 defaults forever while the live table holds the declared 12; the case reads `jsep.Jsep.*` and carries a negative control pinning the stale copy, so a future jsep that fixes the aliasing goes red instead of quietly measuring the right thing for the wrong reason |
| **D-A2 — the closed table is the unit the 1e-9 equality is asserted ON** (2026-08-07) | ✅ **`engine/ast/closedTable.json` — 11 functions + 15 operators + 5 series = 31 names — has ONE writer and TWO readers** (`engine/ast/interpret.js`, `api/services/ast_interpret.py`), and `tools/ast_conformance.py --check` compares them per case, per bar, at **`REL_TOL = 1e-9`** (measured in the tool, not quoted from a plan): **`CONFORMANCE LOG MATCHES, 17 asts × 579 bars`** = 9,843 rows, zero differences | A hand-copy of the table in either lane is a second grammar. The totality rail is DERIVED and anti-copy-proofed twice: one `totality()` checks `functions`/`operators`/`series` **both directions**, a planted entry per section must be reported **BY NAME with no edit to the rail**, and an AST walk over `ast_table.py`'s OWN SOURCE asserts no declared name appears as a string constant — full-equality, **never a grep**, because the module's docstring says `close` and `sma` in prose on purpose. ⛔ The refusal side is the twin of the number side: `--escapes` requires both lanes to agree on WHICH GUARD FIRED, and a wrong-door row or a lane disagreement prints `NOT ATTRIBUTABLE` and exits 1. **The wrong-door defect appeared FOUR times on this branch** — every occurrence a correct NUMBER produced by the wrong MECHANISM |
| **D-A3 — a user definition lives in an APPEND-ONLY store** (2026-08-07) | ✅ **`/data/user_definitions.db`, `UNIQUE(user_id, def_id, version)`, no UPDATE and no DELETE anywhere** — asserted by AST over the store's own source, with a control proving the same walk finds its INSERTs — and `soft_delete` **appends a tombstone version** so a `defId@version` pin still resolves after a delete. Growth is bounded by REFUSAL (64 KiB/row; **50 live definitions per user, checked on a create AND on a RESURRECT**), and a byte-identical re-save appends nothing | An edit to a formula is a `compute.rev` bump, and §3.1's contract is *"you will never be silently switched"* — which needs the old maths to still be **resolvable**, not still **running**. ⚠️ **Versions per definition are UNBOUNDED, deliberately** — *a version cap is not a retention policy, it is a way to break a `defId@version` pin* — with `stats()` making it observable and a test asserting `versions_are_pruned is False` so nobody reads it as an oversight. 🔴 **Named, not fixed: `save()` is a TRANSITION, not a RECONCILIATION.** If the migration lands and the append then fails, a subsequent *different* edit computes the same `new_rev`, the migration skips it as already-migrated, and that edit reaches the bindings unmigrated. The real fix reconciles against the definition's current rev, which needs a per-binding rev the alert lane only writes when a migration runs |
| **D-A4 — user formulas alert through a FOURTH partition, and the ledger door stays shut** (2026-08-07) | ✅ **`alert_user_series.USER_FUNCS`, keyed `<user_id>\x1f<address>`** — the one property no other partition has — and it is **NOT** in `ADDRESS_PARTITIONS` and **NOT** read by `build_alert_grid`. Measured at Phase D close, by AST over `build_alert_grid`'s own source: it reads `INDICATOR_FUNCS`, it does **not** read `USER_FUNCS`; `len(INDICATOR_FUNCS)` is **28**, `all_addresses()` is **31** across **16** catalog groups, all unchanged | This is the **third** time the same argument has been made (`EVENT_FUNCS` at C Task 3, `PRICE_FUNCS` at C Task 10, `USER_FUNCS` here) and it is made for one measured reason: `build_alert_grid` generates the frozen replay grid by iterating `INDICATOR_FUNCS` in order, so **growing that dict changes the shape of the instrument every other gate in Phases B, C and D is measured with**. ⛔ **The ledger door refuses an `ast` fire FIRST, before the mode gate**, because *whether an account wrote the arithmetic* is true in every mode and on every bar, while every other gate there is conditional and moves at the cutover. `LedgerAdmissionRefused` subclasses `RuntimeError` so every existing `except RuntimeError` rail still catches it. Non-vacuous both ways: the refused fire is a real closed-bar fire, and the *identical* fire from a builtin lands a row |
| **D-A5 — the concierge emits TREES, and the sentence is the tree's** (2026-08-07) | ✅ **The model can propose an AST and can never author the read-back.** Proved by `propose`'s own AST: every `ast.Assign` targeting `sentence` unparses to exactly `["sentence_for(ast_obj)"]` — with a control that the same walk over a synthetic module assigning `sentence = answer['summary']` reports that expression BY NAME. `MAX_MODEL_CALLS = 2` (one generate, one repair as a real `tool_result` turn), measured by CALL COUNT; the tool schema's three `enum`s ARE the manifest's own sorted key sets, so the model is never told a smaller vocabulary than the schema enforces | ⛔ **What it deliberately does NOT claim:** `_assert_within_schema` enforces exactly what a JSON Schema can express and **not** arity, window or resolution — claiming those would move refusals off `resolve:arity`/`resolve:window` and make the door that fired a lie. Attribution was proved by removing one gate at a time and the answers are not what a reader expects: removing the schema boundary moves the refusal ONE DOOR LATER (`schema:name` → `resolve:function`); removing the linter makes it DISAPPEAR (`lint:repaint` → `ok: True`); removing the budget gives the right answer at the WRONG DOOR. Order is load-bearing — `lint_repaint` is fail-closed, so a lint-first pipeline says *"this repaints"* about a formula whose real problem is that the function does not exist |
| **D-A6 — `ichimoku.chikou`: the linter's reading is ACCEPTED; the badge has NOT moved** (2026-08-07) | ✅ **DECIDED — `preview-repaints`, not `repaints`, and not `non-repainting`.** The owner delegated it explicitly (*"about the ichimoku, i have no idea. you decide."*). `compute_ichimoku_raw` writes bar `i`'s close to index `i − kijun_period` (pinned by `TRAILING_PAD = {("ichimoku_9_26_52","chikou"): 26}`), so a plotted point at a historical index moves while the newest bar forms — the shipped `non-repainting` badge is **a false claim to a trader about whether that line can be trusted**, on the exact axis the brand is sold on, and honest beats flattering. `preview-repaints` rather than `repaints` because the shift is **known-late, not revised-after-final**: *final the moment bar `i+26` closes*. ⏳ **The badge did not move in the closing commit and that is not a dodge** — see the basis | ⛔ **§11's own rule forbids static analysis of hand-written JS**, so the linter emitted `decided 1 · undecidable-hand-written 41` over 42 plots and *"the linter agreed with every shipped badge"* became an **unwriteable sentence**. It read one plot and disagreed with it. ⛔ **The blocker is that the ruling is PER-PLOT** (record §4: a per-definition badge *"cannot express this without lying in one direction or the other"* — badge all of `ichimoku` and four honest plots are slandered; leave it and the fifth is false) **and nothing in shipped source renders a per-plot badge.** Measured at Phase D close: the only two consumers are `indicatorCatalog.js` → `IndicatorLibraryDialog` and `StockChart`'s chip-about popover, and **both read `meta.repaint`, per definition**; a `plots[].repaint` would be accepted-and-ignored — a badge no trader ever sees. **The scheduled work is the render surface, and it is named rather than assumed.** ⚠️ **The ALERT half is Phase C Task 8's, at the cutover, NOT D's**: `chikou` can never fire closed-bar, so its rows must stay armed and surface as `needs_attention` with a `state_detail`. Record: `docs/decisions/2026-08-06-machine-repaint-linter.md` §4.1 |
| **D-A7 — a sixteenth task, because a gate refused to report its own zero** (2026-08-07) | ✅ **`registerUserDefinitions` VALIDATED AND NEVER INSTALLED, and ~6,100 green frontend tests never saw it.** `nativeRegistry.js` built `_byId` once at import from `[...NATIVE_DEFS, ...SERVER_DEFS, ...AST_DEFS]` with `AST_DEFS = Object.freeze([])`, and the only operation on `_byId` in the whole file was `.get` — there was no install door. A user could author a formula and have it parse, lint, budget, validate and persist, and `normalizeInstances` would then drop the instance | 🔴 **It was found because Task 11 refused to report `0 px` as a pass.** The case read **0 with AND without the perturbation** — a case that cannot report a difference has measured nothing — so it was reported as REFUSING, and the attribution was made a measurement (swapping only the `defId` to `rsi` read 143,540 px). **Fifth contract bug on this project, and every one of them lived in a contract BETWEEN components while every component was correct alone** ([[lesson_contract_bugs_need_a_live_surface]]). Task 16's deliverable is the fail-proof **MOVING**, not a green test: **140,925 px, 5/5, zero variance**, perturbation **141,551** with all **+626 inside `osc_strip`** and every other region byte-unchanged — and **both zeros stay reachable on demand** (delete `userDefs` → 0 px and an empty manifest diff; flip the stored badge to a stale `repaints` → 0 px, `installed: []`, and the linter's own sentence), which is what makes the number attributable to the definition and to nothing else |
| **D-A8 — `/confluence` + `/confluence-scan` are a DELIBERATE PROTOTYPE, not a chart indicator** (2026-08-08) | ✅ **NOT wired to a surface, and it is not going on the server lane.** Dark-Pool Reclaim Confluence (`dpc-v1`) is complete and paid-gated — `api/services/signature/confluence.py`, `tests/test_confluence.py`, a bounded scan with a background warmer, and **two later commits of performance work** (`53b5700a`, *"stops being a ten-minute request"*) spent on a request **no member can make**: the string `confluence` occurs nowhere under `app/`. The lane was measured before the call was taken, and it does not fit | ⛔ **`confluence.evaluate` scores `bars[-1]` — ONE current-state verdict, and its signal carries no `barTime`.** The server lane's wire is COLUMNS positionally joined to `times`, and the only route from a Signature signal to one is `registry_defs.event_columns`, which is handed `(iso_date, direction)` HITS; `flow_breakout.fcb_signals` can supply them because it iterates `detect_breakouts(bars)` and stamps every signal, and confluence has no bar to key on. ⛔ **Synthesising the history is CHEAP AND DISHONEST**: re-running the pure `evaluate` over `bars[:i+1]` costs no extra I/O, but `fetch_dp_levels(sym)` takes no as-of parameter, so bar `i` would be scored against print clusters that formed AFTER it — a `repaints` badge on signals that were never knowable, which is the exact claim the per-plot repaint work exists to stop a trader acting on. ⛔ **The non-positional shape is available and does not help**: `_DPL`/`_GXW` declare `columns: []` and draw through `signatureData.js`, and neither is in the frontend registry at all (`SHIPPED_DEF_IDS.server` is `['rsLine']`), so riding it would touch four files, still not reach the indicator library, and still not draw. ⛔ **And `/confluence-scan` answers about a LIST** (up to `_DPC_SCAN_MAX_SYMS` tickers, with `pending`/`complete`) — a SCREENER contract; no chart surface asks about 40 symbols, and the endpoint's own docstring already said *"PROTOTYPE … the auto-universe (DP-unusual ∩ notable-flow) scanner is the follow-up"*. **The follow-up is that scanner surface, and it is an owner call about scope, not a chart task.** Railed by `tests/test_confluence_prototype.py`, which makes the `barTime` measurement EXECUTABLE (with `fcb_signals` as its positive control) and holds a **biconditional**: a `dpc` row in `registry_defs.SERVER_DEFS` and this record both existing is a red test, so whoever wires it must retire this row in the same change |

⏳ **ONE question leaves Phase D open, and this is it — stated as a measurement, not as a mood.** §11 carried **two** at Task 7 (`ichimoku`'s badge and §7's visual-budget linter); **D-A6 closes the first**. What remains is **§7's visual-budget linter: is it in scope at all, and for which phase?** It is a genuinely separable deliverable with its own corpus and its own gate, §2's D row never listed it, and no task in this phase implemented it. It is an **owner call about scope**. ⚠️ Beside it, and deliberately NOT counted as an open question because it is a fate nobody owns rather than a decision nobody has taken: **`volumeProfile`'s `compute.kind: 'primitive'` lane is now assigned to NOBODY** (see the B3 A4 row) — a phase letter is not an owner, and it had two of them.

## 12. Out of scope (v1, affirmed)

Marketplace/user publishing (nothing until the ledger can hold publishers accountable) · auto-trading/execution · custom bar types beyond HA · metered credits for core features · Pine-dialect language · mobile-specific builder UI.

## 13. Handoff to phase plans

Each phase opens with its own implementation plan (writing-plans) against this spec and closes with its gates green. Phase A plan is next. Open items deliberately deferred to phase plans: Signature indicator exact math + thresholds (owner input on trading logic), Phase B worktree strategy during launch window, alert push-delivery transport (existing Discord infra vs web push), ledger table schema.
