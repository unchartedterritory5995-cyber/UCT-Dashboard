# Validation Coverage Map

Per addendum item 3: default UNVERIFIED absent executable evidence; never infer a green box. Ladder per
addendum item 2 (0 Exists · 1 Unit · 2 Integration · 3 Semantic · 4 End-to-End · 5 Adversarial ·
6 Regression · 7 Performance/Scale · 8 Staging/Prod-like · 9 Human · 10 Controlled Release). A cell lists
the highest level reached **with a citation**; a blank cell is UNVERIFIED, not assumed passing.

This is scoped to what's actually been touched so far (the translation-layer/BuilderSheet corner). Rows
for Custom Screens/Full-Market-Screener, data providers, telemetry, etc. are intentionally absent — they
don't exist in this map until a wave actually investigates them; adding an empty row would imply attention
that hasn't happened.

| Subsystem | Highest level reached | Evidence |
|---|---|---|
| Pine parser/translator (`pine.js`/`pine_table.py`, generally) | 1 — Unit | `BENCHMARK_REPRODUCTION.md`: 47/47 tests pass on the 21-file vendor corpus; corpus-wide numbers tracked live |
| Pine → canonical AST, specifically `07-rsi.pine` | **4 — End-to-End** | `CORE_GOLDEN_JOURNEY_01_PINE_RSI_IMPORT.md`: real browser, paste→translate→correct canonical `rsi(close,14)`→save→reload |
| thinkScript translator | 1 — Unit | `BENCHMARK_REPRODUCTION.md`: 50/50 tests pass on the corpus |
| thinkScript → canonical AST, specifically `03-adx-dmi-lower.ts` | **4 — End-to-End** | `CORE_GOLDEN_JOURNEY_02_THINKSCRIPT_ADX.md`: real browser, paste→translate→hand-verified DI+ formula→save→reload; DI-/ADX series not independently hand-verified (scope limit stated in the doc) |
| TC2000/PCF translator | 1 — Unit | `BENCHMARK_REPRODUCTION.md`: 57/57 on its own corpus; **no adversarial/blind corpus confirmed to exist** (RISK-009) — do not read this as stronger than it is |
| TC2000/PCF → canonical AST, specifically `long_term_uptrend` | **4 — End-to-End, plus exact-precision numeric check** | `CORE_GOLDEN_JOURNEY_03_TC2000_PCF_IMPORT.md`: real browser, translation matched the corpus's own declared expected native exactly; rendered value (1.00) independently provably correct (not just plausible) given SPY's own visible MA state; save→reload clean |
| Dual-kernel (JS/Python) conformance | 2 — Integration | `ast_conformance.py --check` passed live, 144 ASTs × 579 bars at 1e-9 tolerance (reproduced independently by two separate wave-one agents) |
| Vendor parity (translation layer vs. real TradingView/thinkorswim/TC2000 output) | **`rising`/`percentrank`/`median`(even-length): VENDOR-PARITY VERIFIED — MULTI-BAR. `bbw`: VENDOR-PARITY VERIFIED — SCOPED CONTRACT (integer multiplier). Up from 0, amended 2026-09-06 from an initial single-probe overclaim; ~5/64 functions at Unit via spec-probe only for the remainder** | `RISK_REGISTER.md` RISK-018b (amended): the FIRST pass compared exactly one probe row per function via `tools/vendor_parity_compare.py` and labeled all four broadly "VENDOR-PARITY VERIFIED" — correctly flagged by owner review as insufficient to discriminate semantics across state changes. A bounded audit (`tools/vendor_parity_lane_b_multibar_audit.py`, no new TradingView session) found the SAME preserved raw CSV already holds 300 rows across all 25 distinct states of the synthetic script's cycle (12 confirmed-identical recurrences per state, not 300 independent probes). Rebuilding the full series from that CSV and running it through the real production interpreter: `rising` 297/300 rows compared (3 cold-start-excluded), 0 mismatches, both boolean states + 24 transitions present, rejected running-max candidate disagrees on 24/297 real rows; `median` 297/300 compared, 0 mismatches, 22 distinct windows, rejected lower-middle candidate disagrees on ALL 297/297; `percentrank` 291/300 compared (9 excluded), 0 mismatches, all 4 real tie/rank values (0/25/75/100) present, rejected ÷(n+1) candidate disagrees on 60/291; `bbw` 281/300 compared (19 excluded), 0 mismatches, max rel delta `9.4e-16`, 25 distinct bandwidth values, rejected ratio candidate disagrees on ALL 281/281. Permanent regression: `tests/test_vendor_parity_lane_b_multibar.py` (6 tests) alongside the original `tests/test_vendor_parity_lane_b.py` (12 tests, single-probe + AST-mutation controls, still valid and kept). **Residual boundaries, precisely disposed:** `rising`'s NaN-anywhere-in-window convention is the closed table's own pre-existing, uniformly-applied rule (not unique to `rising`) but the artifact contains zero NA-gap rows, so Pine's real na-skip behavior remains **PARTIAL — NA BEHAVIOR UNVERIFIED**, narrower than the gap-free claim above. `bbw`'s `mult:int` is confirmed a translator/input-surface limitation (the compute formula itself applies no truncation and is verified float-capable by direct code read) — a pre-existing, whole-grammar constraint (no function anywhere has a float-literal type), not a new narrowing. `median`'s odd-length branch was never exercised by this artifact (every row used n=4) — disclosed as an untested coverage gap, though no known ambiguity exists for odd-length medians the way there was for even-length. **The other ~60 manifest functions are unaffected** — this closes the harness-minimum gap for exactly these four functions' tested state spaces, not generally. **Do not read dual-kernel conformance or golden-fixture pass rates as vendor-parity evidence for any function outside this row** — they are a different, weaker claim (self-consistency), and this exact conflation risk is the reason this row exists separately from the row above |
| Static analysis / repaint & budget linting | 3 — Semantic, for the RSI case | Journey #1: "3 nodes · 14-bar lookback" + explicit non-repainting badge, correct for the known input |
| Chart delivery (user definition → live pane) | **4 — End-to-End**, one fixture | Journey #1: real subplot rendered, semantically plausible value, survived reload |
| Save/persistence (create, list) | **4 — End-to-End** | Journey #1 |
| Save/persistence (delete) | **4 — End-to-End** | Journey #1: duplicate cleanly removed via UI |
| Save/persistence (edit) | **4 — End-to-End** | 2026-09-05, Phase One Track F follow-up: the "Your formulas" pencil (`BuilderSheet.jsx`'s `openForEdit`) driven live in a real browser (isolated sandbox) — reopened a saved definition, its formula and Track F parameter both correctly restored, edited again, saved (confirmed a PUT to a STABLE `def_id`, version 1→2, via direct database read), reloaded, re-verified. Permanently regression-tested, real UI, nothing mocked but `fetch`: `BuilderSheet.paramReopen.test.jsx`. Supersedes the prior "pencil icon observed, never exercised" entry — the door was found by reading `BuilderSheet.jsx`'s own `rows`/`openForEdit` wiring after several live-browser search paths (Indicators dialog row, per-instance settings, legend context menu, Screener's "My Scans") missed it; it renders at the bottom of the same "New formula" dialog, past the Save/Cancel buttons |
| Pine input-parameter fidelity (Track F, `input.int`/`input.float`) | **4 — End-to-End** | `TRACK_F_V1_IMPLEMENTATION_COMPLETION_REPORT.md`: live browser rerun of `07-rsi.pine` (isolated sandbox) — `length` adjustable 14→21, chart/formula/hash updated, saved, reloaded correctly; direct database read confirmed `compute.ast`/`paramManifest`/`paramState`. Reject-not-clamp confirmed live (a 500 against a declared `maxval=200` reverted, never clamped). Corpus-wide: 14/14 translating scripts (100%) in `tests/fixtures/pine/*.pine` gain ≥1 adjustable parameter, 29 total distinct parameter ids, zero change to which scripts translate — **reproduced via a standing regression test since 2026-09-05** (`pine.paramCorpusCount.test.js`; was a one-time manual count before, per `PROJECT_EVIDENCE_ASSUMPTION_AUDIT_01.md` §4). 60 passing tests across server (21), translator (13), edit-application (16), UI (10), +5 for the new corpus-count regression |
| Pine input-parameter reopen/re-tune (Track F follow-up) | **4 — End-to-End** | Live browser pass (2026-09-05, isolated sandbox): saved a parameterized definition, closed the builder, reopened via the real "Your formulas" edit door, confirmed the persisted value (14) and title ("Length", never a raw `__uct_param_1` id) restored, changed to 25, saved (stable `def_id`, version 1→2, immutable `default:14` preserved, confirmed via direct DB read), a second close/reopen confirmed the SECOND edit (25) persisted. Permanent regression: `BuilderSheet.paramReopen.test.jsx` |
| Save idempotency (double-submit) | **5 — Adversarial, and it failed** | Journey #1: double-click Save duplicated a chart instance (RISK-012). This is a case where adversarial testing found a real, if minor, defect — recorded as failed-at-5, not silently omitted |
| Numeric-vs-boolean screener gate (client+server stamp) | **4 — End-to-End**, confirmed door-agnostic AND confirmed to be an output-type rule, not a per-door rule | Journey #1: Pine numeric refused, Pine boolean accepted; Journey #2: thinkScript numeric hit the identical refusal; Journey #3: TC2000/PCF boolean (an AND-of-comparisons, no explicit threshold used) correctly ACCEPTED — same underlying rule (`<tree> != 0` on the last confirmed bar) now directly confirmed from the product's own explanatory text, not inferred |
| Screener artifact reachability (definition → filter chip) | **4 — End-to-End** | Journey #1 (Pine), Journey #2 (thinkScript), Journey #3 (TC2000/PCF) — three source languages into the same gate |
| Screener execution (actual scan results) | 0 — Exists (unverified), **partially superseded 2026-09-05** | Journey #1: architecturally nightly-only, enforced by a dedicated test forbidding request-path execution; not observed completing. See journey doc's "why ENVIRONMENT-BLOCKED" section. **Partially superseded (Track D, RISK-003):** the sweep is now confirmed to run to completion on schedule against the full 3,742-ticker universe with real, non-zero hits (`scan_coverage.MAX(as_of)` gap-free) — closing this row's original "sweep never observed completing" gap. **Still unverified: semantic correctness of the matched-ticker set.** These are two different claims, not a contradiction: Track D proves the sweep's own scheduler/completion/liveness health (it runs, on time, against the real universe, and produces real matches); this row asks whether a specific AST's resulting hit set is the *semantically correct* membership for real market data — no test or process anywhere independently confirms that. `scan_coverage` rows are written only on a definition's completion (`scan_evaluator.py` — a half-run leaves no receipt), so completion evidence is real, not merely a heartbeat, but it is not a correctness check. Phase Two's Vendor Parity Tranche 2 does not close this gap either (it targets indicator-function output, not scan-membership correctness) — this remains open, tracked in `PHASE_TWO_PLAN.md` |
| Negative path: unsupported property access | **4 — End-to-End** | Journey #1: `ta.cmf(20)` correctly refused, Save no-op, specific message |
| Negative path: missing-default-argument ambiguity (assisted-edit offer) | **4 — End-to-End** | Journey #2: thinkScript `SimpleMovingAvg(varhigh,20)` refused to guess `displace`, offered an inspectable "Put this in my script" fix, Save was a no-op until resolved |
| Compatibility harness Layer A (public-script translate/AST/screener-eligibility classification, 8-script `pine_community/` sample) | **2 — Integration** | `compatHarness.publicScript.test.js`: real per-script results via the same production doors `doorScorecard.test.js` measures with (5 SUPPORTED, 1 PARTIAL, 1 UNSUPPORTED, 1 CORRECTLY_REFUSED — cross-referenced, not duplicated, against `doorScorecard.test.js`'s own RULED/OFFERED rosters). Non-vacuity: more-than-one distinct classification asserted, plus a real mutation (corrupting a builtin call) proven to flip a SUPPORTED result. Render/persistence/vendor-comparison steps are Layer B/C's job, honestly marked `ENVIRONMENT_BLOCKED` here rather than fabricated |
| Compatibility harness Layer B (visual/persistence, Lane 2 Level 1 fixture) | **1 — Unit**, classifier only; live re-run hit a harness-side blocker | `tools/compat_harness_visual.py` + `tests/test_compat_harness_visual.py`: the classification LOGIC (SUPPORTED/HARNESS_DEFECT/VISUAL_BLOCKED/ENVIRONMENT_BLOCKED branching) is unit-tested with mocked `chart_parity.py` output and proven mutation-sensitive. A LIVE re-run against a real dev server in this session reproduced a deterministic `FontNotSettledError` on the existing, previously-proven `ast_user_formula_sma20` case (RISK-027) — root cause undiagnosed, the harness correctly refused to report a comparable pixel count rather than fabricate one. **Do not read this row as "Level 1 visual rendering is broken"** — the case's own prior, independently-verified evidence (140,925 px, 5/5 runs, zero variance) stands; only THIS session's live re-run attempt is blocked |
| Compatibility harness Layer C (real-script live browser capture against TradingView, 8-script-attempt sample across two sessions) | **4 — End-to-End**, for 6 of 8 attempts (3 of 4 original + all 3 tested this closeout); 2 harness-side (1 vendor-decay pair unaffected, 1 superseded harness-defect attempt now fixed and reproduced clean) | `tests/fixtures/compat_harness/results/public_script/{02-wavetrend-oscillator-lazybear,19-cm-macd-ult-mtf,03-cm-williams-vix-fix,29-zigzag-plus-plus}-layerc.json` (2026-09-06, first session), plus `{29-zigzag-plus-plus-layerc-retry,14-earnings-gap-ups-layerc,27-support-resistance-channels-layerc}.json` (2026-09-06, closeout session) against the same chart `jHASRSzx` (account TSDR_TRADING). **The headline finding (RISK-028): two scripts UCT's own translator classifies SUPPORTED at Layer A do NOT compile in TradingView's real, current engine when pasted verbatim** (WaveTrend fails on `hlc3` under implied Pine v1; CM MacD Ult MTF fails on `outSignal` under implied v1 AND explicit v4) — a Layer-A SUPPORTED classification is a claim about UCT's translator only, demonstrated here to NOT imply the script currently runs on its own native platform. Williams VIX Fix confirmed fully SUPPORTED end-to-end live (real visual chart render as a histogram pane, real server-side save/reopen persistence via the "Open my script" library listing, PARTIAL/ineligible-without-comparison screener finding matching Layer A's static prediction). ZigZag++'s ORIGINAL attempt was HARNESS_DEFECT, not a real finding either way — Monaco's editor auto-indent corrupted this script's nested if/else blocks when entered via simulated typing (the same shape RISK-018a's session already found); Layer A's own CORRECTLY_REFUSED classification for this script was unchanged and not contradicted. **RISK-030 (this closeout, 2026-09-06): the harness limitation is fixed** — a hardened OS-clipboard-paste source-entry mechanism (verified byte-exact pre-paste, line-count + zoomed-indentation-verified post-paste) reproduced no corruption across three scripts of increasing nesting depth, and the previously-open question is answered: ZigZag++'s cross-script `import` resolves cleanly in the real vendor (full parameter resolution). Two new/retested scripts add real evidence: `14-earnings-gap-ups.pine` (input-heavy: custom `enum`/UDT/`method`/`switch`) and `27-support-resistance-channels.pine` (visual-heavy, previously Layer A UNSUPPORTED) both compile and render cleanly in the vendor with strong non-placeholder evidence (a live-resolved custom `enum` display value; a live-resolved `string`-typed `input` with `options=[...]`), while UCT's Layer A rejects both — correctly, for `pine:no-output` and `pine:reassign` respectively. `pine:reassign` corrects an earlier, less precise assumption (missing array/box builtins) about Support Resistance Channels' real UCT-side blocker. Two reusable harness-methodology findings recorded for future runs, both confirmed again this closeout: a placeholder chart-object-tree entry is added by "Add to chart" even on compile failure (must be checked and removed every time), and the hardened clipboard-paste mechanism is now the standing recommended method for any nested-block Pine v5 script (simulated typing is confirmed unreliable twice; the hardened mechanism is confirmed reliable three times). **Do not read the 2 vendor-compile failures as UCT defects** — there is nothing to remediate on UCT's side; these are facts about vendor platform decay over ~11 years, not translator gaps. Full 17-item synthesis: `PUBLIC_SCRIPT_COMPATIBILITY_CHECKPOINT_01.md` |
| Negative path: missing-capability refusal (no assisted-edit offered) | **4 — End-to-End** | Journey #2: thinkScript `high(period=Period)` cross-timeframe daily-regardless-of-chart-tf aggregation correctly refused as a concept the engine doesn't have, with no false conventional-default offer |
| Negative path: unresolved/unknown name (TC2000/PCF) | **4 — End-to-End** | Journey #3: fabricated `FibExtension(...)` correctly refused, exact character position named, Save no-op |
| Negative paths, broadly (addendum's ~20 named cases) | 0 — Exists (4 of ~20 exercised) | The four cases above; the rest are UNVERIFIED, not assumed similar |
| Alert creation (`Indicator Alerts` dialog) | 0 — Exists (unverified) | Seen by accident during navigation, never exercised |
| Plain-language (AI concierge) door | **4 — End-to-End** (2026-09-05, corrected live run) | `GOLDEN_JOURNEY_04_05_LIVE_RESULTS.md`: real `claude-opus-5` round trip, `tools/track_e_run_golden_journey.py`, DEC-008 scoped credential. Real prompt → real model call → canonical AST (`close > sma(close,50)`) → save → reload, byte-identical `compute.ast`; saved definition accepted by the ordinary scan-delivery door with no AI-door-specific path. **Scoped, not blanket**: one positive phrasing, one ambiguous phrasing, one unsupported-named-function phrasing were exercised live (each passing); broader phrasing coverage is non-live only (`tests/test_definition_concierge.py::TestSemanticCoverageGate`). A first live attempt this same day (`6 failed, 1 passed`) found two real semantic-safety defects (silent named-function substitution, silent ambiguous-language guessing) — fixed (see RISK_REGISTER.md), then this corrected run passed clean, live, on both fixed cases |
| Screenshot (vision) door | **4 — End-to-End** (2026-09-05, corrected live run) | `GOLDEN_JOURNEY_04_05_LIVE_RESULTS.md`: real vision call to `claude-opus-5` (12,249 in / 870 out tokens, $0.082995) against the known-answer RSI-shaped fixture — returned 3 ranked, compiler-validated candidates (RSI(14) of close at confidence 88, correct; two lower-confidence alternatives at 8 and 4, honestly labeled as less likely rather than suppressed or overstated). Same `sentence_for`-derived read-back authority as the plain-language door, now live-confirmed rather than code-level only |
| Cross-browser | 0 — Exists (unverified) | Chromium only |
| Mobile/responsive | 0 — Exists (unverified) | Not attempted |
| Production/staging behavior | 0 — Exists (unverified) | Everything above is local + sandboxed; nothing checked against Railway |
| Screener/Scanner: nightly-snapshot query engine | 1 — Unit | `test_screener_wave4_query.py`, `test_screener_filters.py`: 101/101 passed live (wave-two #2 archaeology) |
| Screener/Scanner: AST-scan ↔ Finviz-snapshot join, "Honest-None" disclosure | **2 — Integration** | Same test run: `test_never_swept_hash_is_INERT_and_disclosed_not_a_silent_universe` and `test_empty_or_malformed_value_REFUSES_never_the_silent_noop` passed; independently corroborated by Journey #1's live "first sweep tonight" observation (two different evidence angles agreeing) |
| Base & Structure Library (`base_catalog.py`/`lift_ledger.py`) | 1 — Unit | `test_base_count.py`, `test_base_catalog.py` passed live; no browser rendering of the "Structure library" dialog attempted |
| Screener/Scanner: actual filter results in a real browser | 0 — Exists (unverified) | Journey #1 only observed the empty-sandbox state ("0 matches," "nothing in tonight's snapshot") — never a populated result set |

## What this map is not

It is not a capability matrix (that needs the multidimensional MP-033 schema across all doors, not just
Pine) and not a release-readiness verdict. It is a running record of what's actually been earned, updated
as new evidence lands — including recording where adversarial testing *found* something (the Save
double-click row), since a map that only ever moves up would itself be a paper-capability violation of
CL-009.
