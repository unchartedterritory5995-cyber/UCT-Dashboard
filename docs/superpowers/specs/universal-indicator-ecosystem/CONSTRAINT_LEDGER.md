# Constraint Ledger — Universal Custom Indicator + Screener Ecosystem

Non-negotiable constraints only. Each cites its exact authority (master-prompt section, addendum item,
a `DECISIONS.md` entry, or a repo house rule discovered during Phase Zero orientation) per the master
prompt's own instruction to use the exact source as authority, not paraphrase. All currently active.

| ID | Constraint | Authority | What would violate it |
|---|---|---|---|
| CL-001 | Do not replace an existing subsystem without proving: deficiency, why-incremental-fails, measurable improvement, migration cost, compatibility risk, rollback strategy, user impact, saved-artifact impact, API/downstream impact, and parity verification | MP §0 | Proposing to replace e.g. the screener engine or evaluator without producing all ten points first |
| CL-002 | Preserve the closed, bounded, deterministic execution model (no arbitrary remote user-code execution, statically discoverable history requirements, predictable resource use) | MP §9 | Adding unbounded loops, arbitrary code execution, or unpredictable resource use to chase broader syntax compatibility |
| CL-003 | Do not reopen arrays/collections support without new benchmark evidence, real telemetry, or a use case that cannot be represented safely otherwise | MP §11 | Adding array/collection support because a competitor language has it, absent one of the three named evidence types |
| CL-004 | A silent wrong answer always ranks worse than a correct refusal; never raise an acceptance/success rate by accepting more inputs if it increases false success | MP §34 | Loosening a translator's acceptance criteria to improve a headline pass-rate metric at the cost of more false-success cases |
| CL-005 | Release quality bar: zero known critical (S0/S1) defects and zero known silent semantic wrong-answer defects in core supported workflows before broad release | MP §72–73, ADD items 16–17 | Shipping a broad release with a known S0/S1 bug, or a known silent-wrong-answer defect that was not fixed or explicitly disabled/refused |
| CL-006 | Stop and request owner review before: destructive migrations, abandoning important existing architecture, changing fundamental IR guarantees, introducing significant new infrastructure, changing user-visible semantics, changing core data providers, incurring large recurring infrastructure cost, committing to a native DSL, broad production migration, or irreversible schema decisions | MP §82 | Unilaterally committing to any of the ten listed actions without routing through owner/ChatGPT review (Bucket C) |
| CL-007 | Never ask the owner a question repo inspection, tests, or code can answer | MP §83 | Asking "where is the parser," "what tests exist," or "what frontend framework is used" instead of searching |
| CL-008 | All capability and benchmark claims — including this program's own memory and prior session reports — are provisional until reproduced from executable evidence | MP §7, ADD "assume unverified" framing | Reporting a capability as supported on the strength of a prior Claude report, a doc, a compatibility matrix, or a memory entry alone |
| CL-009 | No "paper capability": a claim of support requires the full proof chain (parser → dialect semantic translation → canonical representation → validation → static analysis → data requirements → execution → chart/screener/alert delivery → verified expected output), not just a parser recognizing syntax or a unit test that only exercises the parser | MP §8 | Marking something "supported" because a function name appears in a registry or a parser accepts the syntax |
| CL-010 | No big-bang migration; prefer incremental coexistence (old artifacts keep old execution while new work uses a new path, or the new engine shadows the old one before selected artifacts migrate) | MP §57 | A single cutover that replaces an execution path for all existing artifacts at once, with no coexistence or shadow period |
| CL-011 | Every major phase's rollout design is incomplete unless it has a credible answer to "how do we return to the previous working state if this goes badly" | MP §58 | Proposing or executing a phase with no rollback plan |
| CL-012 | Preserve the user-observable contract even when internal implementation changes; a flawed behavior may be corrected only if demonstrably wrong, documented, compatibility-understood, migration-safe, and proven by tests | MP §5 | Silently changing what a user sees/gets while refactoring underneath, or "fixing" a behavior without documenting and testing the change |
| CL-013 | Never silently change an existing member's saved indicator or screen semantics across a version/upgrade boundary | MP §52 | An upgrade that changes a saved artifact's computed output without the member's knowledge |
| CL-014 | Extend and harden the existing 7/31 Indicator Platform program (approved architecture + shipped Phase A + in-flight Pine/thinkScript work) as the baseline; do not perform a full reset; do not reopen a previously-settled decision merely because the master prompt raises it as an option, absent new evidence | `DECISIONS.md` DEC-001 | Proposing a competing "second indicator platform," or reopening a 7/31 decision without documenting genuine new evidence and routing it through owner review |
| CL-015 | No standalone user-facing scripting language as a product; master-prompt §15's native-DSL evaluation is RESEARCH/HYPOTHESIS only, not a build directive; the internal canonical grammar/IR is a distinct concept from a user-facing authoring language and is not itself constrained by this rule | `DECISIONS.md` DEC-002 | Building or shipping a UCT-hosted general-purpose scripting language as a product surface, or allowing free-text editing of translated/canonical source in a way that functionally recreates an authoring language |
| CL-016 | Never modify the Ravi-adjacent files: `api/live_massive_router.py`, `api/schwab_router.py`, `api/massive_ws_worker.py`, `api/massive_processor.py`, `app/src/pages/OptionsFlow.jsx`, `api/liveflow_router.py` | Repo house rule (`docs/superpowers/plans/2026-07-31-phase-a-signature-launch.md` Global Constraints; `feedback` memory: partner collab branch) | Editing any of these six files without explicit acknowledgment from the collaborator who owns them |
| CL-017 | Never commit to the main `uct-dashboard` checkout directly; it is stale and carries active third-party WIP. Work happens in an isolated worktree | Repo house rule, same source; `lesson_uct_dashboard_shared_worktree` | Running `git add`/`git commit` against `C:\Users\Patrick\uct-dashboard` (not a worktree of it) |
| CL-018 | A benchmark's denominator may not be modified after results are observed merely to improve the score | MP §36 | Changing what counts as the test set, after seeing which cases pass or fail, to raise a reported number |
| CL-019 | Do not turn absence-of-discovered-defects into VERIFIED, passing unit tests into end-to-end-verified, translation success into product delivery, successful execution into semantic parity, or a plausible-looking result into a correct one | Owner's Phase Zero authorization message, 2026-09-04 ("VALIDATION STANDARD" section) | Reporting a subsystem as VERIFIED in the Validation Coverage Map on the strength of a weaker evidence class than the level being claimed |
