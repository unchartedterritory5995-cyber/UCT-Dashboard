# Journal 2.0 — P0 Launch-Blockers Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Spec = `scratchpad/journal-research/MASTER-REPORT.md` §5 P0 (the 15-agent research synthesis). Steps use checkbox syntax.

**Goal:** Close the P0 launch-blockers so UCT Journal 2.0 can credibly launch as a top-tier journaling brand: the Risk Block, First-Insight onboarding, broker-trust surfaces, pricing/trial/honest-copy, and infra hardening for launch load.

**Architecture:** Additive on the shipped P1–P6 stack (FastAPI+SQLite, React+Vite, featureFlags.js, Insights hub, overview.py 1-payload, SnapTrade broker rail, Sync Trust Center, chartScreenshot canvas infra). OPS half of P0 (env flags + backfills) runs separately via Railway — NOT in this plan.

## Global Constraints
- Coverage-gated honesty everywhere (aggregate None + counts when thin; ConfidenceStat n<10). Never a fake number.
- Design doctrine LAWS 1-11 (MASTER-REPORT §6) are binding: no new surfaces/bands; sentence→number→math; empty-state-is-the-feature; five-fields-then-expansion; no emoji (UIcon).
- winRate = wins/(wins+losses), net P&L = gross − fees (app conventions).
- Additive only; `grep -c broker_sync api/main.py` ≥ 7 pre-push; `python -c "import api.main"` + `npm run build` green per task.
- Baseline: ~20 pre-existing backend failures (test_options + test_coach_chat_tools) + 3 test_interventions wall-clock flaps — never attribute to P0.
- The web pod is ONE uvicorn process (524 law): nothing new on the per-request hot path; schedulers stay web-side (auth.db is web-local — the report's "evict schedulers to worker" is REJECTED as architecturally impossible; they already run off-peak).

### Task B1 — The Risk Block (report P0.6, effort S)
Backend `analytics.py::_risk_section` (SQL over closed trades): R-multiple histogram buckets + expectancy line value; max + current drawdown computed off the closed-trade equity sequence; longest/current win + loss streaks; ONE losing-streak-probability strip (P(≥k-loss streak in N trades at win rate p) — the only Monte Carlo/probability math we ship, closed-form or simple simulation, deterministic seed). Add `risk` to the get_analytics return. FE `RiskBlock` inside the existing Risk & Exits (exit-quality) section — NOT a new Insights section (LAW 1): R histogram (bars), drawdown stat pair, streaks, the probability strip sentence ("At your 52% win rate, a 5-loss streak is expected ~9×/year — variance, not failure."). Coverage-gated (n<10 → honest empty). Tests both sides.

### Task B2 — First-Insight onboarding → "Your First Edge Report" (report P0.5, effort M)
One linear first-run flow for a NEW journal user (no closed trades yet): a designed entry card on Today (replacing/augmenting the existing TodayZeroData checklist) with 3 paths — Connect broker / Import CSV (competitor presets named: TradeZella, Tradervue, TraderSync) / Log first trade. After an import/sync lands ≥10 closed trades → route to **"Your First Edge Report"**: a one-screen generated insight — best setup (playbook), worst hour (hour×DOW if computable), cost of top mistake tag (if tagged) or top-loss pattern, Edge Score, one Compass-written sentence (reuse coach infra if cheap; else a deterministic sentence). Coverage-gated for thin history ("Log N more trades to unlock X"). Reuse existing analytics — ZERO new analytics. A `uct.j2.firstReport.seen` once-per (calendar_seen 'first_report' or localStorage). Flag `firstInsight` in featureFlags.
NOTE: hour-of-day analytics — `hour_et` is stamped; if no hour aggregate exists yet, compute inline for this screen (simple SQL) and note it feeds the later P1 heatmap.

### Task B3 — Broker Trust surfaces (report P0.3 code half, effort M)
(a) **Verified Sync page**: a public (logged-out OK) page `/brokers` listing brokers in three honest groups — "Verified by us" (from a code-config list `VERIFIED_BROKERS = [{name, verifiedDate, notes}]` — starts with Robinhood + date; easy to extend as the owner verifies more), "Supported via SnapTrade (30+)", and a "Request verification — we verify within 48h" mailto/support affordance. Honest copy per the brand voice. (b) **Sync freshness chip**: "Last synced Xm ago · Sync now" on the Today surface (the broker hero may have freshness — extend/ensure it's ALSO visible on Trades closed view header). Reuse the existing sync-status/freshness data (BrokerSyncStatus / trust endpoint). (c) **Broken-token proactive nudge**: when the Sync Trust Center's tokenState reads broken/expired, surface a CoachStrip banner row (sev warn) "Broker connection needs re-auth — trades may be stale · Reconnect" linking to accounts. No new band (LAW 7).

### Task B4 — Pricing page + trial + honest copy (report P0.4 + P0.7 code half, effort M)
(a) **Pricing page** `/pricing` (public): Free forever column (unlimited manual + CSV trades incl. competitor presets, core analytics, calendar, notebook, PNG cards) vs **UCT Intelligence — $19/mo billed annually ($228/yr) or $29 monthly**: everything — broker auto-sync, full unmetered Compass (verdicts, chat, interventions, EOD/weekly), excursions/Exit Quality, regime, exports, The Floor, live charts/flow/breadth ("TradeZella sells a journal for $399/yr; this price includes the journal, live charts, options flow, breadth, and the community"). State out loud: **14-day full-access trial, no credit card · one-click cancel · published refund policy · "your data leaves with you — always" (full export)**. No AI credits, ever. Wire the CTA to the existing Stripe checkout (env `STRIPE_PRICE_ID_PRO` exists; support optional `STRIPE_PRICE_ID_ANNUAL` env for the annual price — graceful when unset: show both prices, checkout uses what's configured). (b) **Trial mechanics**: implement a 14-day no-card trial — investigate the existing plan/paid gating (`useIsPaid`, AuthGuard, subscriptions table): cleanest additive path = treat users whose account age < 14 days as paid-equivalent for the paid-gated J2/Compass features via ONE server-side helper (e.g. `is_paid_or_trial(user)`) used by the existing gates + a "Trial — N days left · Keep everything for $19/mo" banner chip. Careful: do NOT open admin or billing surfaces; trial covers feature access only. Flag `trial14` (env `J2_TRIAL_ENABLED`, default ON in code but respect env=0). (c) **Copy scope sweep**: audit user-facing journal marketing copy for multi-asset/prop/replay over-claims → scope to US equities + options ("built for US stock + options traders"). (d) **Export promise**: ensure a full-account export exists (trades CSV/JSON export shipped in P3 — add account-level "Export everything" affordance in Settings if missing).

### Task B5 — Infra hardening for launch load (report P0.2, effort M — SCOPED)
(a) **Poller collapse**: inventory Today's recurring pollers (positions/options 15s, discipline 20s, overview/nudges/interventions/broker-unreviewed 60s). Fold what overview.py already carries (interventions count, week/today aggregates) — have CoachStrip/Today consume the overview payload instead of separate nudge/intervention fetches where the data is already in it; lengthen non-critical intervals (60s→120s where UX-neutral); ensure useMobileSWR-style pause-on-hidden applies. Target: ≤4 recurring requests/user on Today without UX regression. MEASURE first (list the actual hooks), then cut. (b) **Continuous auth.db backup**: a web-side scheduled job (off-peak cadence, e.g. every 6h + 2:55am daily) that snapshots auth.db (sqlite3 .backup API → temp file → upload via the existing data_sync R2 rail creds under `authdb/backup/<ts>.db.gz`, keep newest ~14) — env-gated `AUTHDB_BACKUP_ENABLED` default OFF in code (owner flips with the other flags); MUST be cheap + off the request path + WAL-safe (use the sqlite backup API, never file-copy a hot WAL db). (c) **WAL confirm**: verify auth.db WAL + busy_timeout as documented; no change unless missing. (d) **Postgres trigger doc**: write `docs/runbooks/postgres-migration-trigger.md` — the metrics that trigger migration (sustained write-lock contention, users > threshold, p95 targets) — doc only. REJECTED from the report: evicting schedulers to the worker pod (auth.db is web-local; impossible) — note in the doc.

**SHIP GATE:** full FE journal suites + backend slice green, build, broker_sync≥7, import, whole-branch adversarial review + fix pass, rebase, push, deploy-verify (health + `/pricing` + `/brokers` probes). Update memory.
