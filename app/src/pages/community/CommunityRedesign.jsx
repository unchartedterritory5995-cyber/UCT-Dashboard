import Floor2 from '../../floor2/Floor2'

// Redesign of the Community section ("The Floor"), mounted inside the app shell.
// Prototype: renders the Reddit/Discord/Twitter-style Floor from mock data so it
// can be evaluated in the real layout before the backend is extended.
export default function CommunityRedesign() {
  return <Floor2 embedded />
}
