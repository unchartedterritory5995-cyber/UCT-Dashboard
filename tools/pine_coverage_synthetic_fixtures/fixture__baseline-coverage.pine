//@version=6
// UCT rendering-parity fixture — exercises `plot.style_baseline` only.
// Added 2026-09-19 to close a zero-coverage gap found by the primitive coverage matrix
// (controller ruling: baseline was flagged "unchecked, not confirmed zero" in this plan's
// own deviations section and never closed out — the real corpus regex confirmed a true
// zero, so Task 5's fixture scope was amended from 4 to 5 fixtures to cover it).
indicator(title = "Fixture: baseline coverage", overlay = false)
mom = ta.mom(close, 10)
plot(mom, title = "Momentum", style = plot.style_baseline, color = color.new(color.teal, 0))
